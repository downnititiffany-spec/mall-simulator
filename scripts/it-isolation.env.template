# ───────────────────────────────────────────────────────────────────────────
# 隔离档环境变量契约（模板；**只含占位符，不含任何真值**）
#
# 背景（V25-S02 未闭环项，2026-09-14 用户裁决 A）：
#   `scripts/it-prepare-isolation.ps1` 原先只把两段档案片段**打印**出来，由人手工
#   抄写成模块工作目录下的 `*.local.properties`，且口令写成 `credref:<id>`。
#   本模板 + `scripts/run-isolated-tests.ps1` 把它改成：
#     ① 档案**模板化落库**（本文件，占位符）；
#     ② 真值**只走环境变量**（由运行者当次注入，不写盘、不入库）；
#     ③ 隔离档唯一入口是 `scripts/run-isolated-tests.ps1`，它先过门禁再跑。
#
# 用法（在 PowerShell 里当次注入，别写进任何文件）：
#   $env:IT_GUARD_PASSWORD      = '<本次 runId 的受限账号口令>'
#   pwsh -NoProfile -File scripts/run-isolated-tests.ps1 -RunId <runId> -Module both -Confirm
#   先看它打算干什么（不连库、不跑 Maven、不落盘）：
#   pwsh -NoProfile -File scripts/run-isolated-tests.ps1 -RunId <runId> -Module both -DryRun
#
# ── 取值顺序（源码事实）────────────────────────────────────────────────────
#   `IsolationGuard`（mall-simulator / synthetic-data-generator 两侧同源）：
#     系统属性 `-Dit.guard.<key>` → 环境变量 `IT_GUARD_<KEY>` → 档案 `it-guard.local.properties`
#     ⇒ 环境变量**优先于档案**，所以残留的旧档案不会"意外生效"。
#   Spring 侧（mall-simulator）：`src/test/resources/application-test.yml` 用
#     `${MALL_ISOLATION_URL}` / `${MALL_ISOLATION_USER}` / `${MALL_ISOLATION_PASSWORD}`
#     （无默认值 ⇒ 不提供就上下文创建失败，绝不回退宿主 3306）。
#
# ── 变量清单（值一律为空，运行时注入）─────────────────────────────────────
# [两侧共用 · 门禁]
# IT_GUARD_ENABLED=true                      # 必须显式开启，默认关闭
# IT_GUARD_INSTANCEPORTS=3307                # 允许的隔离实例端口（注意键名无下划线：INSTANCEPORTS）
# IT_GUARD_RUNID=                            # 本次 runId；目标库名必须以此前缀开头
# IT_GUARD_SERVERFINGERPRINT=                # 登记实例指纹＝hostname:port（唯一权威形态，例 dahaishui:3307）
#                                            #   规范化后必须与实连实例 @@hostname:@@port 完全相等；
#                                            #   裸 hostname／裸端口／127.0.0.1:port／localhost:port 一律拒绝；
#                                            #   @@server_uuid 是门禁6 的独立漂移事实，**不是** fingerprint 替代值
# IT_GUARD_URL=                              # jdbc:mysql://127.0.0.1:3307/<runId>_mall
# IT_GUARD_USER=                             # <runId>_app（禁用 root / mall_app）
# IT_GUARD_PASSWORD=                         # 只走环境变量；不写盘、不写 credref
#
# [mall-simulator · Spring 数据源]
# MALL_ISOLATION_URL=                        # jdbc:mysql://127.0.0.1:3307/<runId>_mall?...
# MALL_ISOLATION_USER=                       # <runId>_app
# MALL_ISOLATION_PASSWORD=                   # 同上，只走环境变量
# MALL_ISOLATION_FLYWAY_ENABLED=true         # 隔离门禁通过后才显式打开
#
# [synthetic-data-generator]
# 该模块测试只读 `IT_GUARD_*`（见其 `src/test/resources/it-guard.local.properties` 注释），
# 不额外需要 Spring 变量。
#
# ── 硬约束（与 V25-S02/K-04 同源）──────────────────────────────────────────
#   * 端口 3306 一律拒绝：宿主正式实例，一次误跑可能就是不可逆前向迁移。
#   * 账号禁用 root / mall_app；库名禁用正式库名；库名必须带本次 runId 前缀。
#   * 口令不得出现在命令行参数、不得落盘、不得进 git；只经环境变量传递。
#   * 任何一道门禁不过 ⇒ 不执行任何语句、不跑 Maven。
# ───────────────────────────────────────────────────────────────────────────
