<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.graduation.analytics</groupId>
    <artifactId>analytics-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>ai-decision</artifactId>
  <name>ai-decision</name>
  <description>AI、证据包、建议、决策闭环（整改书 §17/§18；待迁移）</description>
  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
      <version>${mybatis-plus.version}</version>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.github.jsqlparser</groupId>
      <artifactId>jsqlparser</artifactId>
      <version>4.9</version>
    </dependency>
    <dependency>
      <groupId>com.graduation.analytics</groupId>
      <artifactId>platform-common</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!--
      R8-1（§19.2/§25）：证据包的数值**只从已发布数据读取**，读取所有者是 metric-analysis 的
      AnalysisService/MetricAdsReader/MySqlMetricStore。这里直接复用同一所有者，避免在 ai-decision
      里再造一个快照钉住 + ADS 读取的第二实现（依赖方向 ai-decision → metric-analysis，无环）。
    -->
    <dependency>
      <groupId>com.graduation.analytics</groupId>
      <artifactId>metric-analysis</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
