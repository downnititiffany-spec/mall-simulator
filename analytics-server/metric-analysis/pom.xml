<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.graduation.analytics</groupId>
    <artifactId>analytics-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>metric-analysis</artifactId>
  <name>metric-analysis</name>
  <description>MetricStore、固定分析、报表 DTO（整改书 §15/§16；待迁移）</description>
  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
      <version>${mybatis-plus.version}</version>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.github.jsqlparser</groupId>
      <artifactId>jsqlparser</artifactId>
      <version>4.9</version>
    </dependency>
    <dependency>
      <groupId>com.graduation.analytics</groupId>
      <artifactId>platform-common</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-crypto</artifactId>
    </dependency>
    <!-- R7-2：ADS 读写 DAO 的 L0 单测（不连库，验证白名单/参数绑定/SQL 形状） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- DEF-16：复用 platform-common 的测试工具（RepoRoot 单一所有者），
         本模块测试不得再写「工作目录＝模块 basedir」的相对路径 -->
    <dependency>
      <groupId>com.graduation.analytics</groupId>
      <artifactId>platform-common</artifactId>
      <version>${project.version}</version>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <properties>
    <!-- V25-S02 / K-02 分类口径：默认纯测试套件排除 @Tag("it")（真实 MySQL 的集成测试）。
         刻意写成**用户属性**再引用：把字面量写死在 <configuration> 里就无法用
         -DexcludedGroups= 清掉，隔离档会得到 "Tests run: 0 / BUILD SUCCESS" 的假绿。 -->
    <v25.it.excluded.groups>it</v25.it.excluded.groups>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- 默认档 = 普通单元测试（不连真实 DB）。只加 excludedGroups，刻意**不写** <includes>：
               默认包含式（Test*/*Test/*Tests/*TestCase）保持有效，故本模块默认档的用例选中集合
               与本改动前逐一致（回归口径见 DEV-003b 报告）。 -->
          <excludedGroups>${v25.it.excluded.groups}</excludedGroups>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- DEV-003b：隔离档。把本模块的真库 IT 接进项目统一入口
         （scripts/run-isolated-tests.ps1 -Module analytics|all），不再要求人工 -Dtest= 点名。 -->
    <profile>
      <id>isolated-tests</id>
      <properties>
        <!-- 清空默认排除（写死 excludedGroups 时清不掉，故上面用属性引用） -->
        <v25.it.excluded.groups></v25.it.excluded.groups>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <!-- 只选 @Tag("it")：未打标的 *IT（AnalysisGoldenMySqlIT / MetricAdsMySqlIT /
                   MetricPublisherMySqlIT 等）即使被 <includes> 捞到也会被 tag 过滤掉，不会误跑 -->
              <groups>it</groups>
              <!-- 必须显式写 includes：本类的类名是 *IT，不在 surefire 默认包含式里，
                   缺了它就会 "Tests run: 0"——那正是 DEV-003b 要消灭的假绿形态之一 -->
              <includes>
                <include>**/*IT.java</include>
              </includes>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
