# 脱敏说明（E4 证据目录，2026-09-12 追加）

## 1. 本次掩码的范围

| 项 | 内容 |
|---|---|
| 触发原因 | 本轮新增的集群证据里含**真实内网地址**：`raw-fix/` 的 10 作业 `*-submit.log` / `*-yarn-logs.txt`（YARN 容器启动行、RPC 目标等），以及 run-1 的 `raw/` 同批文件 |
| 匹配式 | `192\.168\.18\.10[0-9]`（**只匹配末位为数字的真地址**，不会误伤既有占位符 `192.168.18.10x`） |
| 替换为 | `192.168.18.10x`（沿用本仓库既有口径：**集群节点地址一律写 `192.168.18.10x`，完整地址不入库**） |
| 命中量 | **41 个文件 / 136 处** |
| 掩码后复核 | 严格正则 `192\.168\.18\.10[0-9]` 残留 **= 0**；占位符 `192.168.18.10x` **= 140 处** |
| 未改动 | **除被替换的地址字符串外，其余字节一律未改动**；文件编码统一为 UTF-8 无 BOM；行数与换行符剖面不变（掩码是等长/定长替换同一行的 ASCII 片段） |
| 保留项 | 集群主机名 `node01` / `node02` / `node03`、`hdfs://node01:8020`、端口号（如 `:8020` / `:9083` / `:8088`）**保留**——它们是复现与排障所必需，且不构成新的暴露面 |
| 本地口令 | 本机 `root/123456` 一类测试口令按用户既有裁决**不处理** |

## 2. 误报备案（不是地址，不要当泄露）

`(?<![\d.])10\.\d{1,3}\.\d{1,3}\.\d{1,3}` 在本目录命中 **180 处**，**全部**是 Derby 的版本号字面量
`10.14.2.0`（出现在 `derby-10.14.2.0.jar` 之类的文件名/日志中），**不是**私有网段地址。

## 3. 残留（按既定裁决**不处置**，诚实登记）

看板 `docs/项目实施进度与任务看板 V2.2.md` 的 **15:46 行正文**里含两个内网地址字面量（末位分别为 `101` 与 `103`），
该文本**已随提交 `f26be26` 公开推送**。本轮**不重写已公开历史**——与 `p207_meta_pw` 明文残留
（公开远端 `a81fde6`，账号已删除、已成为无对应主体的死字面量）适用**同一处理原则**：
用户 2026-09-12 已裁决「不再重写历史」，故此处只登记、不改写。
（过程留痕：本轮掩码脚本曾连带改了该行 1 处，随后**已还原为推送时的原文**，
看板本轮 diff 复核实测为 **净增 1 行 / 删除 0** ⇒ 历史正文逐字未动。）

## 4. 如何自行复核

```powershell
# 目录内不应再有真地址（期望输出 0）
Get-ChildItem docs\acceptance\e4-cluster-1000-20260912 -Recurse -File |
  Select-String -Pattern '192\.168\.18\.10[0-9]' -AllMatches | Measure-Object | Select-Object -ExpandProperty Count

# 占位符仍在（期望 > 0）
Get-ChildItem docs\acceptance\e4-cluster-1000-20260912 -Recurse -File |
  Select-String -Pattern '192\.168\.18\.10x' -AllMatches | Measure-Object | Select-Object -ExpandProperty Count
```
