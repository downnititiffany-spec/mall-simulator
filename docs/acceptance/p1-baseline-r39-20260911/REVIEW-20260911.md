# P1-01 只读基线 · 总控独立复核（2026-09-11 20:18–20:21）

> 复核人：总控（本会话）。复核对象：`docs/acceptance/p1-baseline-r39-20260911/`（作者 2026-09-11 17:46 交付，看板状态 `REVIEW`）。
> 复核方式：**不复用作者脚本**，用独立手段复测（`mysql` CLI 聚合查询 + 文件系统聚合 + 逐叶子 JSON 比对 + 逐文件 sha256 比对）。
> 复核结论：**通过（PASS）**。基线可继续作为 P1–P5 的冻结边界依据；本轮未发现口径问题。

## 1. 复测结果：只读指纹 26/26 全等，0 处漂移

对 `verify/baseline.json` 的 `readonlyProof.fingerprintAfter` 逐项独立复测（复测时点 2026-09-11 20:20，现场读时钟）：

| 指纹项 | 基线值 | 今日复测 | 判定 |
|---|---|---|---|
| `file_checkpoint` 行数 / 去重路径 / Σ next_offset | 102 / 102 / 807930925 | 同 | 相同 |
| `ingestion_batch` 行数 / maxId / Σ record_count | 39 / 39 / 1739544 | 同 | 相同 |
| `ingestion_batch_file` 行数 / maxId | 109 / 109 | 同 | 相同 |
| `pipeline_run` 行数 / maxId | 38 / 39 | 同 | 相同 |
| `pipeline_stage_run` 行数 / maxId | 241 / 256 | 同 | 相同 |
| `analytics_metric.metric_snapshot` 行数 / maxId / ACTIVE 数 | 8 / 23 / 1 | 同 | 相同 |
| `analytics_metric.metric_value` 行数 / maxId | 70 / 110 | 同 | 相同 |
| 8 张 ADS 镜像表行数（active_trend / behavior_funnel / data_quality / hot_product / operation_overview / product_conversion / sale_trend / user_profile） | 7 / 28 / 28 / 33 / 7 / 33 / 7 / 19 | 同 | 相同 |
| `landing/events` 文件数 / 字节 | 52 / 404139515 | 同 | 相同 |
| `spark-warehouse` parquet 文件数 / 字节 / 库数 | 972 / 4977445 / 6 | 同 | 相同 |

**26 项全部相同，不一致项 = 0**（含 run 39 仍为 `SUCCESS`、`target_snapshot_id=S20260901_39`；`analytics_metric` 唯一 ACTIVE 快照仍是 `S20260901_39` v8；`runtime_profile` 仍为 id=1 `local-dev` v3 `ACTIVE` `source_id=1`、`hive_database_prefix=NULL`）。

## 2. 元数据库 21 张表：19 相同，2 处差异且均已解释

| 表 | 基线(17:45) | 复测(20:20) | 解释 |
|---|---|---|---|
| `flyway_schema_history` | 14 | 15 | **合规变更**：`V16__source_registry.sql` 于 19:56:28 应用（P1-02 交付，提交 `cedd356`）。最高版本仍为 V16；真实库**未**执行 V17（`file_checkpoint` 无 `source_id` 列，已复测确认） |
| `user_session` | 156 | 159 | **基线已声明的自身副作用**：新增 id 157/158/159（19:11:24 · 19:58:28 · 20:00:08，均 `user_id=1`、`login_ip=127.0.0.1`）= 后续轮次的登录换 token。该表已被作者写入 `readonlyProof.excludedFromFingerprint` 与 `volatilePaths.normalizedByTable`，`selfInflicted` 亦记录「每次运行登录一次，新增 1 行」 |

其余 19 张表（含 `pipeline_run` 38、`pipeline_stage_run` 241、`metric_snapshot` 9、`metric_value` 132、`quarantine_record` 104、`data_quality_result` 379、`spark_job_run` 205、`ingestion_batch` 39、`file_checkpoint` 102、`t_ckpt_test` 0 等）**逐表相等**。

## 3. 证据内部自洽性核验（作者主张 vs 独立比对）

1. **`raw/` 与 `verify/raw/` 19 个原始响应文件**：sha256 比对 9 个不同、10 个相同。对 9 个不同文件做**递归逐叶子 JSON 比对**，结果显示**每个文件的差异叶子恰好只有 1 个：`.traceId`**（每请求新 UUID，如 `.traceId: '4ee05dbd…' vs 'f21f8834…'`），业务字段零差异 —— 包括 232 KB 的 `api-pipeline-run.json`（run 39 全阶段证据）。
2. **`baseline.json` 与 `verify/baseline.json` 顶层键**：仅 `baselineId`、`generatedAt`、`durationSec`、`rawFiles`、`metadataDb` 五项不同，前四项正是作者 `volatilePaths.topLevel` 声明项，`metadataDb` 的差异即 §2 的 `user_session` 归一化 ⇒ 与 `compare-report.json`（`differences: []`、`identical: true`、`ignoredTopLevelKeys`/`ignoredNestedPaths`/`normalizedTables` 三项声明）**自洽**。
3. **只读性主张**：`readonlyProof.sqlStatementCount=120`、`nonSelectStatements=[]`（全部 SELECT）、采集前后指纹逐项相同、`changedSections=[]`、`mutationDetected=false` ⇒ 采集过程确为只读。本次复核同样全为 SELECT / 文件读取（复核脚本 `.verify/p101-review-counts.sql`）。

## 4. 反熵核查：`analytics_meta` 侧 `metric_snapshot`/`metric_value` 重复所有者疑点——已闭合，无需新任务

复测中发现 `analytics_meta.metric_snapshot`(9 行，ACTIVE=`S20260907_11` run 11) 与 `analytics_metric.metric_snapshot`(8 行，ACTIVE=`S20260901_39` v8) **同名并存且各自声称一个 ACTIVE**，属"重复所有者"形态。核查结论：**该疑点在 V13 迁移中已被显式裁定，且其可证伪预期今日仍然成立**：

- `analytics-server/platform-app/src/main/resources/db/meta/V13__metric_definition_r7.sql` §3（第 18–26 行）：`metric_snapshot / metric_value` 的**唯一所有者是 `analytics_metric`**；`analytics_meta` 侧为 **R7 起的弃用副本**，「平台代码不再读写（`MySqlMetricStore` 走 `metricPublish`/`metricRead` JdbcTemplate，`metric.mapper` 扫描已移除）」，保留数据待单独清理迁移，**禁止直接 drop 丢数据**。
- V13 写下可证伪预期：「两个数应在切换后保持不变，若继续增长说明仍有旧写入路径」，并记录编写时实测值 2026-09-10 = 9 行 / 132 行。
- **今日复测：9 行 / 132 行，与 2026-09-10 完全相同（基线 17:45 亦为 9/132）** ⇒ 无残留旧写入路径，预期成立。
- 生产读取侧独立佐证：`MetricPublishRepository`、`MySqlMetricStore`、`MetricAdsReader` 均使用 metric 库 JdbcTemplate；`AnalysisGoldenMySqlIT:46` 注释亦写明「真库当前 ACTIVE 快照（`analytics_metric.metric_snapshot`）」。

结论：**不是缺陷，是已归档的口径**。复核只确认其数值未增长；清理动作（DROP 弃用副本）**不在本轮**，仍须按 V13 的「数据迁移与读写切换完成后单独清理迁移」执行，且需用户按范围确认。

## 5. 未取证与限制（本轮**没有**做的部分，不得当成已完成）

1. **Hive 物理全表行数（ODS/DWD/DIM/DWS/ADS）未采集** —— 作者已声明：P1-01 明令不启 Spark、不重跑大数据；归 `P1-06` 的 T2（同脚本扩展 Hive 计数开关）。
2. **多源登记状态（`source_registry` / `runtime_profile.source_id`）未纳入本基线** —— 作者已声明：P1-02 建表后才有该结构；归 `P1-06`。
3. **历史 `\.\` 断点行的合并删除未做** —— 属 D-024 Data Destruction Guard，须用户按范围确认后单独立项。
4. **本次复核是"事后复测"，不是"采集时点同时性"证明**：20:20 的复测只能证明"17:45 记录的值在 20:20 依然成立"，不能替代作者在采集瞬间的前后指纹自证（后者已由 `readonlyProof` 提供）。
5. `analytics_meta` 侧弃用副本（9/132 行）**仍在库中**，其清理时点未定（见 §4）。

## 6. 复跑命令（可独立验证）

```powershell
# 表行数（21 张元数据表）
& 'C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe' -uroot -p123456 --default-character-set=utf8mb4 -N -B analytics_meta -e "<.verify/p101-review-counts.sql 内容>"
# 只读指纹 26 项 vs 基线 verify/baseline.json 的 readonlyProof.fingerprintAfter（脚本见本文件同目录 README 的采集口径）
# 原始响应文件差异：对 raw/ 与 verify/raw/ 同名文件做 ConvertFrom-Json 后递归比对，预期差异叶子仅 .traceId
```

## 7. 看板登记

- `P1-01` 由 `REVIEW` → **`DONE`**：E1（脚本编译/运行）+ E3（本地真实链只读取证：真库 + landing + warehouse）+ 总控独立复核（2026-09-11 20:20，26/26 指纹全等，2 处差异已解释）。
- 变更日志新增一行（见看板）。
