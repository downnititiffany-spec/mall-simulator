# P1-01 冻结基线口径复核（2026-09-12，只读取证）

> **性质：补记（append-only）。** 本文件不改动 `baseline.json`、`README.md`、`REVIEW-20260911.md` 的任何字节，只在 2026-09-12（E4 集群闭环、V18 部署、CT 契约批次施工中）这一时点追加一份**只读**复核记录。
> **归属**：M1 边界冻结收尾第二项「P1-01 基线口径复核」。
> **取证方式**：真库 `SELECT`（无 DDL/DML）＋ 文件系统测量 ＋ 与 `baseline.json` 逐字段对照。**未**跑任何 Spark 作业、**未**动 `spark-warehouse`、**未**提交任何写入。
> **时点**：2026-09-12 16:38（现场读时钟）。

---

## 0. 本次复核要回答的三个问题

| # | 问题 | 结论落点 |
|---|---|---|
| Q1 | 冻结基线**声明的口径**在后续（已授权）工作之后是否仍成立？哪些是不变式、哪些是预期漂移？ | §2 / §3 |
| Q2 | 基线 `notCollected` 的两条是否已被后续任务补齐？补齐到什么程度（可否说"与基线一致"）？ | §5 |
| Q3 | 目录内两份 `baseline.json`（根 / `verify/`）的差异是否与 `REVIEW-20260911.md` 的陈述相符？ | §6 |

判定原则沿用既有纪律：**未实测不写结论**；漂移必须**逐项归属**到已授权任务，归属不到的一律记"未归因"；物理量对比必须先对齐口径（本轮踩到一次，见 §3.2）。

---

## 1. 方法与可复现命令

```powershell
# 1) 指标面 + 元数据面（全 SELECT）
& 'C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe' -uroot -p123456 --default-character-set=utf8mb4 -B -e @'
SELECT id,snapshot_id,status,version,pipeline_run_id,business_time,source,active_flag FROM analytics_metric.metric_snapshot ORDER BY id;
SELECT COUNT(*) FROM analytics_metric.metric_snapshot WHERE status='ACTIVE';
SELECT metric_code,metric_value,unit,period,definition_version FROM analytics_metric.metric_value WHERE snapshot_id='S20260901_39' ORDER BY metric_code;
SELECT COUNT(*) FROM analytics_metric.metric_value;
SELECT id,source_code,display_name,ingest_mode,status,profile_version,warehouse_prefix,profile_path FROM analytics_meta.source_registry ORDER BY id;
SELECT id,profile_code,type,status,source_id,hive_database_prefix,spark_master,spark_job_jar_uri,metric_store_type,version,updated_at FROM analytics_meta.runtime_profile ORDER BY id;
SELECT COUNT(*) FROM analytics_meta.ingestion_batch; SELECT COUNT(*) FROM analytics_meta.file_checkpoint;
SELECT COUNT(*) FROM analytics_meta.pipeline_stage_run; SELECT COUNT(*) FROM analytics_meta.data_quality_result;
SELECT MAX(CAST(version AS UNSIGNED)) FROM analytics_meta.flyway_schema_history WHERE success=1;
'@

# 2) T2 判据的独立再证：39 与 41 的逐值差集（预期 0 行）
& $mysql ... -e "SELECT a.metric_code,a.metric_value AS v39,b.metric_value AS v41 FROM analytics_metric.metric_value a JOIN analytics_metric.metric_value b ON a.metric_code=b.metric_code AND b.snapshot_id='S20260901_41' WHERE a.snapshot_id='S20260901_39' AND (a.metric_value<>b.metric_value OR a.unit<>b.unit OR a.definition_version<>b.definition_version);"

# 3) 物理量（各按基线同一口径分别统计）
(Get-ChildItem 'spark-warehouse' -Recurse -File -Filter '*.parquet').Count           # 基线口径：parquet 文件数/字节
(Get-ChildItem 'landing\events'  -Recurse -File).Count                                # 基线口径：landing/events
(Get-ChildItem 'landing'         -Recurse -File).Count                                # ⚠ 不同口径，仅作旁证（§3.2）
```

---

## 2. 判定一：不变式（后续工作之后逐项仍成立）

| # | 不变式 | 基线值（2026-09-11 17:40:30） | 本轮实测（2026-09-12 16:38） | 结论 |
|---|---|---|---|---|
| 1 | `S20260901_39` 的 10 个指标**逐值** | 88.16 / 0.2 / 10 / 0 / 88.16 / 88.16 / 1 / 5 / 0(v2) / 5 | **完全相同**（含 `unit` / `period` / `definition_version`） | **不变** ✅ |
| 2 | 基线时点的 8 条 `metric_snapshot` 行仍在 | 8 行（`raw/api-metrics-snapshots.json`：id 4,5,14,15,16,17,22,23） | 8 行**全在**；其中**仅** `S20260901_39`（id 23）由 `ACTIVE`→`ARCHIVED`（`version` 仍为 v8），其余 7 行的 `snapshotId`/`status`/`version` **逐项不变** | **只在尾部演进** ✅ |
| 3 | `metric_snapshot` 中 `ACTIVE` 条数 | 1 | **1** | **不变** ✅ |
| 4 | `quarantine_record` | 104 | **104** | **不变** ✅ |
| 5 | 旧快照的 ADS 镜像行**未被覆盖**（MySQL 侧，按 `snapshot_id` 分组） | 基线未按快照分组记录（总行 7/28/28/33/7/33/7/19） | `S20260901_39` = **1/4/4/9/1/9/1/1（30）**，`S20260901_41` = 同序列（30），两者**并存不覆盖** | **追加式，旧行未改** ✅ |
| 6 | 本地 `spark-warehouse` | 972 parquet / 4,977,445 B | **972 / 4,977,445** | **不变** ✅ |
| 7 | `runtime_profile#1` 关键字段 | `version=3`、`status=ACTIVE`、`hive_database_prefix=NULL`、`updated_at=2026-09-10 16:48:17.303` | **完全相同** | **不变** ✅ |
| 8 | 两份 `baseline.json` 的 `metricSnapshot`/`activeProfile`/`warehouse` 大对象 | — | **逐字段相同** | **不变** ✅ |

> 说明：#8 的"两份"指 `baseline.json` 与 `verify/baseline.json`（作者采集 vs 独立复核采集），详见 §6。

---

## 3. 判定二：已解释漂移（逐项归属到已授权任务）

| # | 项 | 基线 → 本轮 | 漂移量 | 归属依据 |
|---|---|---|---|---|
| 1 | `metric_snapshot` 新增 `S20260901_41`（v9，run 41，`ACTIVE`） | 8 行 → 9 行 | +1 行 | P1-06 的 T2 运行（`docs/acceptance/p1-06-golden55-20260912/`：`stage-evidence-run41-PUBLISH_METRIC.json` 存在） |
| 2 | `S20260901_39` 状态 | `ACTIVE` → `ARCHIVED` | 状态迁移 | 同上：新快照发布时的**设计内**生命周期切换（`active_flag` 仍全局唯一=1） |
| 3 | `metric_value` 行数 | 70 → 80 | +10 | = run 41 的 10 个指标值（逐值见 §4） |
| 4 | `ingestion_batch` | 39 行 / maxId 39 → 40 行 / maxId 40 | +1 | T2 的采集批次 |
| 5 | `ingestion_batch_file` | 109 → 112 | +3 | T2 的清单文件 |
| 6 | `file_checkpoint` | 102 → 105 | +3 | T2 的检查点推进（D-024 结构不变量：行数=不同路径数） |
| 7 | `pipeline_run` | 38 行 / maxId 39 → 40 行 / maxId 41 | +2 | run 40 `FAILED`（09:23:15→09:23:26，11 s，其 `stage-evidence-run40-LOAD_ODS.json` 为 **0 B**）+ run 41 `SUCCESS`（09:26:27→09:31:13）；与 T2 证据自述"首跑失败、复跑成功"一致 |
| 8 | `pipeline_stage_run` | 241 行 / maxId 256 → 252 行 / maxId 267 | +11 | 同上两次运行 |
| 9 | `data_quality_result` | 379 → 401 | +22 | run 41 的质量校验结果 |
| 10 | Flyway 已应用最高版本 | 15 → **18**（`success=1` 共 17 条） | +3 | V16/V17/V18；V18 部署为用户 2026-09-12 明确授权并已实测完成（看板 15:09 行） |
| 11 | `landing/events` | 52 文件 / 404,139,515 B → 59 文件 / 406,114,133 B | +7 文件 / +1,974,618 B | 见 §3.1（**数量与字节完全归因**） |
| 12 | ADS 镜像 8 表**真实** `COUNT(*)` | 7 / 28 / 28 / 33 / 7 / 33 / 7 / 19 → 8 / 32 / 32 / 42 / 8 / 42 / 8 / 20 | **+1/+4/+4/+9/+1/+9/+1/+1 = +30** | = `S20260901_41` 新增的那 30 行；该增量与 T2 记录的 **Hive `dw_ads` 8 正式表行数序列 `1/4/4/9/1/9/1/1`（=30）逐个相同**（T2：`raw/hive-counts-20260912-093352.log` L205–L233），即 MySQL 镜像与 Hive ADS 在**同一快照**上跨系统一致 |

> **⚠ 本行是本轮自查更正的结果**：第一版把该项误判为"不变式"，依据是 `information_schema.tables.table_rows`（InnoDB **估算列**）恰好等于基线记录值。真因见 §3.3 陷阱 #29 与 §3.4。
>
> **该口径并非新发现（补记 16:47）**：T2 `docs/acceptance/p1-06-golden55-20260912/README.md` **§8.2（L120–L121）已登记**原文「`analytics_metric.ads_*_m` 是"每快照一组"的历史存储（`_22/_23/_24/_25/_29/_30/_39/_41` 各一组，`_38` 因 FAILED 无行），新快照追加自己那组（8 表 30 行），`_39` 的组保留」⇒ 本条与 T2 结论**一致且互补**（T2 按快照判 8/8 一致；本轮补测出 `S20260901_39` 与 `S20260901_41` **各 30 行且逐表序列相同**），**不构成新发现**。本轮第一版仍复犯了同类口径错误（机理不同：T2 是"用总行数"，本轮是"用估算值"），过程教训见 §3.4。

**未归因项：无。**上表 12 项均可归属到 T2 或 V18 部署这两件已授权工作；§2 的 8 项不变式未发生任何漂移。

### 3.1 `landing/events` 新增 7 个文件（清单，可与基线逐名比对）

| 文件 | 字节 | 最后写入 |
|---|---|---|
| `2026091120.jsonl` | 460,026 | 09-11 20:55 |
| `2026091121.jsonl` | 165,616 | 09-11 21:06 |
| `2026091122.jsonl` | 130,261 | 09-11 22:02 |
| `2026091209.jsonl` | 1,131 | 09-12 09:53 |
| `2026091210.jsonl` | 584,644 | 09-12 10:46 |
| `2026091211.jsonl` | 631,946 | 09-12 11:01 |
| `2026091215.jsonl` | 994 | 09-12 15:05 |

> 注：7 个文件的字节合计 = `460026+165616+130261+1131+584644+631946+994` = **`1,974,618`**，**恰好等于**基线→本轮的字节差 ⇒ 无未列明的文件被并入统计。
> **未实测**：这 7 个文件"分别由哪一次运行写入"未逐项取证（仅完成数量与字节的完全归因）。不做归因表述。

### 3.2 ⚠ 陷阱 #28：与基线对比前必须对齐基线的统计口径

我第一次测量时把 **`landing/` 递归全量**（584 文件 / 1,262,018,276 B）与基线的 `landing_events`（52 文件 / 404,139,515 B）直接相减，得到"+532 文件、+858 MB"的**假漂移**。真因是两者**口径不同**：基线只统计 `landing/events`（另有 `accepted/`、`logs/`、`manifests/`、`quarantine/` 未计入该字段）。

- 陷阱表述：**与基线对比的任何物理量，必须先对齐基线的统计口径（目录范围、扩展名、是否递归），否则噪声会被读成漂移。**
- 纠正后同口径差 = +7 文件 / +1,974,618 B，且可逐文件列全（§3.1）。

### 3.3 陷阱 #29：`information_schema.tables.table_rows` 是**估算值**，不得当行数用

本轮实测（同一时刻、同一 8 张 ADS 镜像表）：

| 取数方式 | 读数 |
|---|---|
| `information_schema.tables.table_rows`（InnoDB **估算**） | 7 / 28 / 28 / 33 / 7 / 33 / 7 / 19 |
| `SELECT COUNT(*)`（**真值**） | **8 / 32 / 32 / 42 / 8 / 42 / 8 / 20** |

**为什么危险**：估算值**恰好等于**基线记录值（基线脚本用的是 `select count(*)`，见 `scripts/accept-p1-baseline.ps1:210-211`），于是"估算 vs 真值"的错配会给出**"零漂移"的假结论**——本轮第一版正是这样把 +30 行的真实增量写成了"不变式"。已由自检门禁（`tools/p101-recheck-selfcheck.ps1` 断言 A11/A15）捕获并更正。

- 陷阱表述：**行数一律用 `COUNT(*)`；`table_rows`/`TABLE_ROWS` 只是优化器估算，且其"恰好正确"会让错误对比看起来是对的。**

### 3.4 本轮自查更正记录（保留痕迹，不改历史）

| 时点 | 事项 |
|---|---|
| 16:38 第一版记录 | 把「ADS 镜像 8 表行数」列为**不变式**，依据是 `information_schema.table_rows` 与基线记录值逐表相同 |
| 16:41 自检门禁首跑 | 断言 A11（真实 `COUNT(*)` 对比）**FAIL**：实际 `8/32/32/42/8/42/8/20` ≠ 基线 `7/28/28/33/7/33/7/19` |
| 16:41 定位真因 | ①基线脚本用 `select count(*)`（**真值**口径）；②我用的是 InnoDB 估算列 ⇒ 属"错配对比"，我的"不变"结论不成立 |
| 16:42 更正 | 该项从 §2（不变式）移入 §3 第 12 行（已解释漂移 +30 行，归属 run 41 发布）；补陷阱 #29；补本 §3.3 |
| 16:42 第二处更正 | §6 第一版写「顶层键集合 **27** 键」（两份），实测为 **25** 键（`PSObject.Properties.Name` 计数）。已改为 25 并注明；自检门禁新增断言 **D8**：直接把 §6 表格里写的数字与实测计数比对，防同类笔误复发 |
| 16:41 门禁首跑 | `tools/p101-recheck-selfcheck.ps1` 首跑 **30 条断言 / 23 PASS / 7 FAIL**。逐条定位：**6 条是我脚本自身的缺陷**（`Rows` 返回单元素时被 PowerShell 解包成标量，`[0]` 取到首字符 ⇒ `'80'[0]='8'`、`'18'[0]='1'`；§3.1 表格行的正则只认数字开头），**1 条是真实发现**（即上面 A11 的镜像 +30 行）。⇒ 修脚本（`return ,@(...)` + 正则放宽）、订正文档、并把 §3.1 之外的口径写成断言 |
| 16:42 门禁复跑 | 修完并补 A14/A15/A16/D8/D9/D10 后复跑：**36 条断言 / 36 PASS / 0 FAIL / exit 0**（全文见 `.verify/p101-recheck-selfcheck.log`） |
| 16:47 同类错误复犯登记 | 上面 A11 涉及的**口径本身并非新发现**：T2 `p1-06-golden55-20260912/README.md` §8.2 早已登记「`ads_*_m` 是每快照一组的历史存储，**不得用总行数作判据**」⇒ 本轮属**同口径错误复犯**（T2 用"总行数"、本轮用"估算值"，机理不同、错误同类）。**教训**：动笔前必须先查既有口径登记（T2 README §8、看板陷阱清单 #18–#29、`p1-baseline-r39-20260911/REVIEW-20260911.md`），不得自行另立判据；本条已同步进 §10 复现清单 |

> 教训与既有纪律一致：**未实测不写结论**——"用估算列代替实测"就是没实测。自检门禁的价值正在于此：它把"看起来对"的结论拦下来了。

---

## 4. T2 判据的独立再证（不依赖 T2 自己的脚本）

T2 的验收原文是「只跑一次 golden-55 本地链；**源 A 指标与 P1-01 基线逐值一致**」。本轮用一条**自连接差集**SQL 独立再证（比逐行目视更强）：

```sql
SELECT a.metric_code, a.metric_value AS v39, b.metric_value AS v41
FROM analytics_metric.metric_value a
JOIN analytics_metric.metric_value b
  ON a.metric_code = b.metric_code AND b.snapshot_id = 'S20260901_41'
WHERE a.snapshot_id = 'S20260901_39'
  AND (a.metric_value <> b.metric_value
    OR a.unit <> b.unit
    OR a.definition_version <> b.definition_version);
```

**结果：0 行（差集为空）。** 即新 `ACTIVE` 快照 `S20260901_41`（run 41）与冻结基线 `S20260901_39`（run 39）的 10 个指标在**值、单位、定义版本**三个维度上完全一致。

结论：**「源 A 指标与 P1-01 基线逐值一致」在 2026-09-12 16:38 时点仍成立**，且该结论来自基线侧与发布侧的对撞查询，不依赖 T2 运行脚本的自述。

---

## 5. 判定三：基线 `notCollected` 三条的补齐状态

| 基线 `notCollected` 条目 | 基线声明 | 本轮实测状态 |
|---|---|---|
| ① Hive 物理全表行数（ODS/DWD/DIM/DWS/ADS） | 未采集（延后到 P1-06 的 T2） | **已由 T2 补齐"首测"**：ODS 365/271/281/83（合计 1,000 = 批 31 记录数）、DWD 138/1/27、DIM 4/11、DWS 7 表、ADS 8 正式表 1/4/4/9/1/9/1/1（=30）。**T2 自述「P1-01 从未采集过 Hive 物理行数，故这是首测而非'与基线一致'」** ⇒ 只能表述为"测量已补齐"，**不得**表述为"与基线一致"。本轮**未复测**该组数字（未跑 Spark，原因见 §8）。<br>**口径提醒**：T2 自述"不动 `spark-warehouse`"，且本轮实测该目录 972 parquet 逐值不变 ⇒ T2 的这组物理行数**不属于**基线的 `spark-warehouse`（T2 用的是自己的仓库），两者**不可直接比较**（同类口径陷阱，见 §3.2 #28）。 |
| ② 多源登记状态（`source_registry` / `runtime_profile.source_id`） | 未采集 | **本轮真库实测成立**：`source_registry` 1 行（`mock-mall` /「参考商城（源 A）」/ `FILE` / `ACTIVE` / profile_version `1.0` / `warehouse_prefix='dw'`）；`runtime_profile#1.source_id=1`。与 T2 记录（含 `file_checkpoint` 全 105 行 `source_id=1`）**互不矛盾且互补**（本轮未复测 `file_checkpoint` 列值，仅复测了其行数=105）。 |
| ③ 历史 `\.\` 写法 checkpoint 行的删除 | 独立项，需用户确认 | **保持未动**（基线自检项 `legacy-spelling-residual-untouched: 50 行` 明示"D-024 未执行删除"）。本轮**未复测**该 50 行。 |

---

## 6. 判定四：两份 `baseline.json` 的差异（复核补细）

| 项 | 根 `baseline.json` | `verify/baseline.json` | 判定 |
|---|---|---|---|
| 字节 / sha256 前缀 | 392,281 B / `A28F69650FE0D42ADCDF8FAB…` | 392,397 B / `210E82885111DFC10CC02D24…` | 不同文件 |
| 顶层键集合 | 25 键 | 25 键 | **完全相同**（本轮实测计数；第一版误记 27，见 §3.3） |
| `baselineId` | `p1-baseline-r39-20260911` | `verify` | 预期（作者 / 复核者） |
| `generatedAt` / `durationSec` | 17:40:30 / 147.1 | 17:43:02 / 157.9 | 预期（漂移字段） |
| `rawFiles` | `…\raw\…` | `…\verify\raw\…` | 预期（路径前缀） |
| `metadataDb` | — | — | **差异唯一定位到 `user_session` 行数 155 → 156**（两次采集相隔 2.5 分钟，商城侧新增 1 条会话） |
| `metricSnapshot` / `activeProfile` / `warehouse` | — | — | **逐字段相同** |
| `checks` / `notCollected` | 19 / 3 | 19 / 3 | 数量相同 |
| `repo.head` | `076f5d211c9c22883281d5746707d3a77d4fda66` | 同左 | **相同** |

**与 `REVIEW-20260911.md` §2 的陈述一致性**：该文件称两份差异仅 `baselineId`、`generatedAt`、`durationSec`、`rawFiles`、`metadataDb` **五项**，前四项属作者声明的漂移字段。本轮独立复算**结论一致**，并把它那句"`metadataDb` 不同"**细化到唯一叶子**（`user_session` 155→156）。

⇒ 两份文件不是"两个互相冲突的基线"，而是**同一次运行的两个时点采集**，结论一致，**无需择一为准**；本轮不改 `REVIEW-20260911.md`。

---

## 7. 本轮新发现（登记，不改历史证据）

### F-81（新增，`OPEN`）运行时可追溯性缺口：`runtime_profile` 只记 jar **路径**、不记指纹；且现存唯一 `ACTIVE` profile 未描述集群运行形态

**证据（本轮真库实读）**：

- `runtime_profile` 全表 21 列中**没有任何指纹/摘要列**；jar 只用路径引用：
  `spark_job_jar_uri = D:/Develop_code/GraduationProject/spark-jobs/target/spark-jobs-0.1.0-SNAPSHOT.jar`
- 该路径下的**文件内容**已在 2026-09-12 因 F-80 修复被重新构建（E4 证据登记：285,256 B / sha256 `4F29735547D6DDEFDE4BF25C8E3157BF316D8F9EE6DBA10436E611A02421AA3A`）——**路径不变、字节已变**。
- E4 集群 10 作业链的提交形态（`docs/acceptance/e4-cluster-1000-20260912/tools/e4-run-chain-fix.ps1`）：
  L40 `'--master','yarn','--deploy-mode','cluster'`、L53 `'--runtimeProfileId=1'`、L35 `$NS = '--hiveDatabasePrefix=dw'`、L54 `'--shufflePartitions=4'`、L74 `--class com.graduation.analytics.job.JobRunner` + **jar 显式传入**；
  而 `runtime_profile#1` 仍是 `type=LOCAL`、`spark_master=local[2]`、`hive_database_prefix=NULL`。

**影响**：

1. 库内**无法证明**"某个快照是用哪一版作业 jar 发布的"；F-80 事故（集群跑的是旧 jar）只能靠外部 sha256 台账发现，平台自身无痕。
2. profile 行与真实运行形态之间**没有可验证的一致性约束**：同一次集群运行既声称 `LOCAL`/`local[2]`，又由命令行的 `--master yarn --deploy-mode cluster` 决定实际形态 ⇒「环境档案 = 运行事实」的等价性**不成立**（M3 集群前置应新增 `cluster` 档 profile + jar 指纹登记）。

**本轮不做**：不改代码、不改库（属 M3 集群前置范围）。**不得**据此宣称"平台已支持多环境档案"。

### P3-01（升级：由"疑似缺失"→ **真库 + 负向搜索双证据确认**）

**证据**：

- 注册表实读：`source_registry.profile_path = analytics-server/source-profiles/mock-mall.v1.json`（该源 `status='ACTIVE'`）。
- `Test-Path analytics-server/source-profiles/mock-mall.v1.json` ⇒ **False**。
- 同目录**阳性对照**：实际存在 `p1-03-probe-1.v1.json`(1,123 B)、`p1-03-probe-2.v1.json`(987 B)、`README.md`(2,122 B) ⇒ 目录可枚举、同类 profile 制品格式已知，**不是**"目录不存在"或"搜索失效"造成的假阴性。
- 全仓 `Get-ChildItem -Recurse -Filter 'mock-mall*.json'` ⇒ **0 命中**。

**影响**：唯一 `ACTIVE` 源指向**不存在的**配置文件，且注册表**无完整性校验**（可把不存在路径标为 `ACTIVE`）⇒ B-06「文件模式配置/清单」无法落地。属 **M2（源实例 / 连接器 / 映射）前置阻塞项**。

---

## 8. 未实测 / 不得表述（本轮边界）

1. **未跑 Spark**（与 CT 施工泳道的 Maven 内存互斥，见陷阱 #26）⇒ Hive 物理行数（§5①）、历史 `\.\` 行数（§5③）、本地 warehouse **逻辑**行数**均未复测**。
2. **未逐项归因** `landing` 内每个文件由哪次运行写入（只做数量与字节的完全归因，§3.1）。
3. **未重跑集群**：§9 关于 P1-04 集群档的判定**引用** E4 证据（`docs/acceptance/e4-cluster-1000-20260912/`），本文件**未**在集群上重新取证。
4. **未复测** E4 的任何读数（本文件全部为本地 MySQL / 文件系统只读读数）。
5. 因此**不得**表述：Hive 物理行数"与基线一致"、`landing` 漂移已逐文件归因、P1-04 已在集群上"重跑验证"。

---

## 9. 状态建议（提出，不擅自改状态）

| 项 | 现状态 | 建议 | 依据 |
|---|---|---|---|
| **P1-01** | `DONE` | **维持 `DONE`** | 8 项不变式全部仍成立（§2）；12 项漂移全部归属已授权工作、无未归因项（§3）；T2 判据独立再证成立（§4）；两份 baseline 差异已细化且与复核记录一致（§6）。本复核**未发现**任何未经授权的基线改写。 |
| **P1-04** | `DONE_LIMITED`（限制理由："集群档留 T4"） | **建议升 `DONE`**，范围限定为：*"按源数仓库名空间（前缀参数化）在**本地探针**与**集群全链**两档均已实证"* | 集群档证据（引用 E4，非本轮重跑）：① 作业自身日志 `[spark-jobs] 数仓库名空间: dw_ods, dw_dwd, dw_dim, dw_dws, dw_ads`（`raw-fix/bdw-yarn-logs.txt:908`）；② 实际 HDFS 读路径 `hdfs://node01:8020/graduation/warehouse/dw_ods.db/ods_behavior_event/dt=20260901/hour=NN/part-0…`；③ 8/8 正式 ADS 分区 `LOCATION` 落在 `…/dw_ads.db/<table>__staging/snapshot_id=S20260901E4/dt=20260901`（E4 README §10 + 提交前门禁 53/53 断言 H 段）。**不含**"注册表 `warehouse_prefix` → 命名空间自动接线"——E4 的 `dw` 是命令行 `--hiveDatabasePrefix=dw` 显式传入，注册表取值**未参与**，该接线属 P1-06/P4。 |
| **F-81** | — | 新增 `OPEN`（M3 集群前置） | §7 |
| **P3-01** | 待办 | 升级为**已证实** `OPEN`（M2 前置阻塞） | §7 |
| 陷阱清单 | #18–#27 | **追加 #28**（与基线对比前必须对齐基线统计口径）与 **#29**（`information_schema.table_rows` 是估算值，不得当行数；其"恰好等于基线"会制造假无漂移） | §3.2、§3.3 |
| 本文件的可验证性 | — | 新增**自检门禁** `tools/p101-recheck-selfcheck.ps1`：把本文件全部量化主张回实测，逐条 PASS/FAIL + 条数自检 | §10、§3.4 |

---

## 10. 复现清单（本文件全部读数的来源）

| 读数 | 来源 |
|---|---|
| 9 条快照 / 1 条 ACTIVE / `S20260901_39` 逐值 / `metric_value` 80 行 | 真库 `analytics_metric`（§1 命令 1） |
| 39 与 41 的差集 = 0 行 | 真库自连接查询（§1 命令 2） |
| 元数据 9 项计数、Flyway 18、`source_registry` 1 行、`runtime_profile#1` 11 字段 | 真库 `analytics_meta`（§1 命令 1） |
| run 38/39/40/41 的状态与时刻 | `analytics_meta.pipeline_run`（`id>=38`） |
| 972 parquet / 4,977,445 B | `spark-warehouse` 递归（`-Filter '*.parquet'`） |
| 59 文件 / 406,114,133 B 与 7 个新增文件名 | `landing\events` 递归 |
| 两份 baseline 的键/值差异 | `docs/acceptance/p1-baseline-r39-20260911/baseline.json` 与 `verify/baseline.json` 的 `ConvertFrom-Json` 逐键比对 |
| E4 集群命名空间证据 | `docs/acceptance/e4-cluster-1000-20260912/`（README §10、`raw-fix/`、`tools/e4-run-chain-fix.ps1`） |
| P3-01 负向搜索 | `Test-Path` + `Get-ChildItem -Recurse -Filter 'mock-mall*.json'`（同目录阳性对照） |
| ADS 镜像 8 表真实 `COUNT(*)`、按 `snapshot_id` 分组计数 | 真库 `analytics_metric.ads_*_m`（`COUNT(*)` / `SUM(snapshot_id=…)`；**不用** `information_schema.table_rows`，见 §3.3 #29） |
| 「按快照行数」这一判据的既有出处（非本轮新立） | T2 `docs/acceptance/p1-06-golden55-20260912/README.md` §8.2 L120–L121（"每快照一组"）；§3.4 16:47 行 |
| T2 的 Hive `dw_ads` 逐表物理行数 `1/4/4/9/1/9/1/1` | `docs/acceptance/p1-06-golden55-20260912/raw/hive-counts-20260912-093352.log` L205–L235 |
| 基线脚本的取数口径（`select count(*)`，非估算列） | `scripts/accept-p1-baseline.ps1` L174–L213、L346–L360（`baseline.json` 内 `script.sha256 = E8A9C69F…`） |
| 本文件全部量化主张的自动复核 | `tools/p101-recheck-selfcheck.ps1`（随本目录入库）：**36 条断言**，`36/36 PASS`、exit 0；含两道防漂移关卡——「条数自检」（断言总数须等于脚本声明的应然值）与「`§6` 写的数字须等于实测」（D8/D9/D10，其 D10 为自指断言：文档自报条数 = 脚本实际条数）。日志：`.verify/p101-recheck-selfcheck.log` |
