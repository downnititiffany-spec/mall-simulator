# M1-8 三程序独立启停验收记录

- 验收对象：指导书 **V2.1 §3.4** 独立验收第 **1** 条（独立构建产物/配置/端口/日志/启动命令）与第 **5** 条（分别停止三者；其余程序不崩；停数据源时平台明确显示采集不可用、历史指标仍可读），并按现状标注第 2/3/4/6 条的归属任务。
- 时间：2026-09-11 14:25–14:33（本地，Windows + JDK17 `D:\Develop\JAVA17` + Spark 3.5.1）
- 分支：`remediation/r1-boundary`；改动面：`scripts/accept-three-programs.ps1`（新增）、`scripts/start-all.ps1`（补第三程序与 `-GeneratorOnly`）
- 原始观测（逐条 `step/what/value`，可复核）：
  - `docs/acceptance/m1-8-20260911-1425.md` + `.json`（含**独立构建**证据）
  - `docs/acceptance/m1-8-20260911-1430.md` + `.json`（含**隔离启停 / 联合启停 / 无数据源**证据）

## 1. §3.4-1 三个程序有独立构建产物、配置、端口、日志和启动命令 —— **通过（E1+E5）**

| 维度 | analytics-platform | reference-mall | synthetic-data-generator |
|---|---|---|---|
| 端口 | 8091 | 8090 | 8092 |
| 配置 | `analytics-server/platform-app/src/main/resources/application.yml`（库 `analytics_meta` + `analytics_metric`） | `mall-simulator/src/main/resources/application.yml`（库 `mall_simulator`） | `synthetic-data-generator/src/main/resources/application.yml`（库 `generator_meta`） |
| 独立构建命令 | `mvn -o -q -f analytics-server/pom.xml -pl platform-app -am clean package -DskipTests` | `mvn -o -q -f mall-simulator/pom.xml clean package -DskipTests` | `mvn -o -q -f synthetic-data-generator/pom.xml clean package -DskipTests` |
| 构建结果（14:25 实测） | exit **0**，20.6 s，`platform-app-0.1.0-SNAPSHOT.jar` **31.54 MB** @14:25:42 | exit **0**，8.6 s，`mall-simulator-0.1.0-SNAPSHOT.jar` **28.52 MB** @14:25:50 | exit **0**，7.1 s，`synthetic-data-generator-0.1.0-SNAPSHOT.jar` **24.92 MB** @14:25:57 |
| 启动命令 | `java -Dfile.encoding=UTF-8 -jar analytics-server/platform-app/target/platform-app-0.1.0-SNAPSHOT.jar -Dplatform.metric.publish.export-dir=<root>/metric-staging` | `java -Dfile.encoding=UTF-8 -jar mall-simulator/target/mall-simulator-0.1.0-SNAPSHOT.jar` | `java -Dfile.encoding=UTF-8 -jar synthetic-data-generator/target/synthetic-data-generator-0.1.0-SNAPSHOT.jar`（另有 CLI 文件模式） |
| 日志 | `.verify\start-platform.log`（启动命令 stdout/stderr 重定向；该程序未配 `logging.file.name`） | `.verify\start-mall.log`（同上） | 程序自管 `./logs/generator.log`（`logging.file.name=${GENERATOR_LOG_FILE:./logs/generator.log}`）+ 重定向 `.verify\start-generator.log` |

- 三个构建命令**只依赖各自的 pom 与仓库根**，互不引用；三个端口实测互不相同（`8091,8090,8092`）。
- 说明（不掩饰）：验收用的独立构建带 `-DskipTests`，即"编译+打包"独立；三个程序的测试套件在各自批次单独执行（商城 54 条、生成器 52 条、平台 321 条的历史结果见 §3.2 与 §6）。
- 一键脚本已补齐第三程序：`scripts/start-all.ps1` 现支持 `-PlatformOnly` / `-MallOnly` / `-GeneratorOnly` 与三程序同时启动；**实测** `pwsh -File scripts/start-all.ps1 -GeneratorOnly` → 仅 `8092` 监听（PID 46700）、`8090/8091` 均无监听、`GET /api/v1/scenarios` = 200（14:33），随后停止、端口释放。

## 2. §3.4-5 停止任一程序不影响其余 —— **通过（E5）**

同一时刻三程序全起后，逐个停止并观测另外两个的健康端点：

| 动作 | 另两个程序观测 |
|---|---|
| 停 `analytics-platform`(8091) | `reference-mall` HTTP **200**、`synthetic-data-generator` HTTP **200** |
| 停 `reference-mall`(8090) | `analytics-platform` HTTP **200**、`synthetic-data-generator` HTTP **200** |
| 停 `synthetic-data-generator`(8092) | `analytics-platform` HTTP **200**、`reference-mall` HTTP **200** |

每轮停止后均把该程序重新拉起并确认健康（三轮 `restart = True`），结束后三程序全部停止、`8090/8091/8092` 均无监听（`cleanup.*.port-closed = True`）。

**单程序隔离启动**（起一个 → 另两个端口必须关闭）三组全部成立：

| 只启动 | 其端口 | 另两个端口 |
|---|---|---|
| `analytics-platform` | 8091 = 200 | 8090 关闭、8092 关闭 |
| `reference-mall` | 8090 = 200 | 8091 关闭、8092 关闭 |
| `synthetic-data-generator` | 8092 = 200 | 8090 关闭、8091 关闭 |

## 3. §3.4-5 停数据源时平台明示"采集不可用" + 历史指标仍可读 —— **部分通过（历史可读 ✅ / 明示不可用 ❌ → DEF-07）**

场景：商城与生成器**均已停止**，平台存活并已登录（`POST /api/v1/auth/login` admin 成功）。

| 观测项 | 结果 |
|---|---|
| 平台健康 | `GET /api/v1/metrics/health` = 200 |
| 历史批次 | `GET /api/v1/ingestion/batches?limit=3` = 200（batch 32/31/30，含 `recordCount`、`status`） |
| 历史运行 | `GET /api/v1/pipeline-runs?limit=3` = 200（run 36 全字段：`sourceDataVersion`、`targetSnapshotId=S20260901_36`、`status=FAILED`） |
| 环境档案 | `GET /api/v1/runtime-profiles/active` = 200（profile 1 `local-dev` `ACTIVE`，`sparkSubmitPath=D:\Develop\spark-3.5.1-bin-hadoop3\bin\...`） |
| 历史快照 | `GET /api/v1/metrics/snapshots?limit=5` = 200（`S20260901_30` **ACTIVE**，`publishedAt=2026-09-11T11:09:19.753`） |
| **历史指标值** | `GET /api/v1/metrics/overview` = 200 且返回真实值：`avg_order_value=408.4 元`、`buy_rate=1.0`、`dau`…（快照 `S20260901_30`） |
| 质量结果 | `GET /api/v1/metrics/quality?limit=3` = 200（run 36 的三条规则行；中文 `detail` 为乱码，即 DEF-06 实测现场） |
| **采集动作** | `POST /api/v1/ingestion/runs` = 200 `{"batchId":33,"status":"SUCCESS","recordCount":0,"fileCount":0,"acceptedBytes":0}` |
| 采集状态 | `GET /api/v1/ingestion/status` = 200，`profileState=ACTIVE`、`landingError=null`、`pendingFiles=49 / pendingBytes=404118978`——**没有任何字段表达"数据源未产出/采集不可用"** |

结论：**"历史指标仍可读"通过**（含真实指标值 408.4/1.0）；**"明确显示采集不可用"未通过**——平台在数据源停机时给出的是"成功采集 0 条"的沉默结果，缺口登记为 **DEF-07**（决策项 **B-08**，三个候选口径见《开发过程事实与决策记录》DEF-07）。因此 M1-8 判 **`DONE_LIMITED`**。

## 4. 其余 §3.4 条目归属（本次不冒充通过）

| 条目 | 现状 | 归属 |
|---|---|---|
| 2. 商城工程中不存在 `generator` 包、`GeneratorController`、生成器页面 | **已通过（M1-7 补验，2026-09-11 15:3x）**：源码删除 23 文件（`mall/generator/**` 整包 16 类 + `GeneratorController` + 6 个测试文件 17 条用例 + `mall-frontend/src/views/Generator.vue` + 陈旧产物 `static/assets/Generator-B3ZZkWuU.js`）；重建后商城 jar 静态资源仅 7 个文件（`index.html` + `Mall/Login/AdminProducts` + 3 CSS），jar 内无 `mall/generator/**` 类；**真实 8090 带 admin token 探测**：`GET /api/v1/generator/scenarios` → **404**、`GET /api/v1/generator/runs` → **404**（无 token 时 401，说明鉴权拦截先于路由，探针必须带 token）；`mvn -o test -f mall-simulator/pom.xml` → 38/38 GREEN。名字含 `Generator` 但**保留**的仅 `outbox/EventIdGenerator`、`outbox/UuidEventIdGenerator`（Outbox 事件 id 生成，与演示生成器无关） | 已完成（M1-7 `DONE`） |
| 3. 生成器不引用商城实体/Mapper/Service/数据源配置 | 已有源码级守卫测试 `synthetic-data-generator/.../boundary/GeneratorBoundarySourcePolicyTest`（E2，0 违规） | 已完成 |
| 4. 平台不引用商城/生成器 Java 包、商城表或 `mall.*` 配置键 | 已有源码级守卫测试 `analytics-server/platform-app/.../boundary/PlatformMallBoundarySourcePolicyTest`（E2） | 已完成 |
| 6. 第二个 `MallTargetAdapter` / 异构字段夹具 | **未做** | M1-9（需 B-06 契约严格度口径） |

## 5. 复现命令

```powershell
# 完整验收（含三个程序的独立构建，约 1 分钟）
pwsh -File scripts/accept-three-programs.ps1
# 只做配置/隔离启停/无数据源可读性（跳过构建，约 2 分钟）
pwsh -File scripts/accept-three-programs.ps1 -SkipBuild
# 三程序一键启停（含单选开关）
pwsh -File scripts/start-all.ps1                # 三个都起
pwsh -File scripts/start-all.ps1 -GeneratorOnly # 只起生成器
```

## 6. 两次运行说明（诚实记录）

第一次运行（14:25，含构建）的脚本在"停止某个程序"时用 `-like '*.jar'` **后缀**匹配命令行，导致：平台（命令行尾部还有 `-Dplatform.metric.publish.export-dir=...`）永远匹配不上、而商城/生成器被误杀——第 3/4 步的隔离与联合结论因此**无效**，已改为"精确 jar 名 + 端口占用者兜底"并复跑（14:30，`-SkipBuild`）。第 1 步的**独立构建**结论不受影响，故仍以 14:25 那次为构建证据。两次原始记录都保留在 `docs/acceptance/`。

## 7. 遗留

1. **DEF-07 / B-08**：数据源停机时"采集不可用"需显式化（推荐候选 1：`ingestion/status` 增 `dataSourceState`/`lastArrivalAt`，`RunResult` 增 `noNewData` + 中文说明）。
2. §3.4-2（M1-7 商城侧删除生成器）与 §3.4-6（M1-9 第二适配器）未做，本记录不覆盖。
3. 平台/商城未配 `logging.file.name`，日志只有启动命令的 stdout/stderr 重定向文件；如需按 V2.1 更严格的"日志"口径，可后续给两程序补 `logging.file.name`。
