# V25-E3 交付的总控独立核验（2026-09-14 15:2x）

> 本文件由**总控**撰写，**不改动**本目录 `README.md` 与 `raw/` 内任何字节。
> 目的：①对 L4 的关键结论做**独立复算**；②对 L4 §11.2 的 7 项待裁逐一给出裁决；③登记一条对 L4 表述的**精确化勘误**。

## 1. 独立复算：3306 零写入的「内容面」判据

不复用 L4 的输出，总控在同一宿主实例（`127.0.0.1:3306`）上**重跑 L4 自己的只读脚本** `scripts/fingerprint-3306.sql`（`MYSQL_PWD` 注入，`-t` 表格输出），与 L4 的 `raw/baseline-3306-after.txt` 逐键比对：

| KEY | L4 after（14:16:13） | 总控复算（15:2x） | 判定 |
|---|---|---|---|
| `metric_snapshot_rows` | 12 | 12 | 一致 |
| `metric_value_rows` | 110 | 110 | 一致 |
| `metric_snapshot_fp` | `c224e4e9d44c98f5389607f7beb9d6d2` | 同 | 一致 |
| `metric_value_fp` | `a1a073114d8232c5fcb11c56157466c2` | 同 | 一致 |
| `active_pointer` | `id=27 / S20260901_47 / version=12 / active_flag=1` | 同 | 一致 |
| `flyway_3306_count` | 3 | 3 | 一致 |
| `flyway_3306_latest` | `3 / 3@2026-09-10 20:04:15` | 同 | 一致 |
| `runtime_profile_v25it_rows` | 0 | 0 | 一致 |

**结论**：8/8 一致，`Compare-Object` 差异 0 ⇒ ①L4 的指纹**可复现**（脚本自包含、算法显式声明）；②至总控复算时点，宿主 3306 相对 L4 窗口**仍无任何漂移**。
**局限（与 L4 自述一致，总控不缩小）**：`analytics_meta` 侧没有窗口前全表指纹，"只改旧行不改计数"型写入本判据发现不了；本轮不存在此类写入者（应用连接面只有 3307）。

## 2. 独立复核：旁证指纹 `452b7223…` 到底能不能复现

### 2.1 全盘检索（含 `target/`、gitignored 归档）

按 `452b7223` 做**全盘**文件内容检索（`Get-ChildItem -Recurse -Force` ＋ `Select-String -List`），命中**仅 4 处、全为文档**：

```
docs/项目实施进度与任务看板 V2.5.md
docs/acceptance/v25-e3-isolated-chain-20260914/README.md
docs/acceptance/v25-s01-it-safety-20260914/README.md:173
docs/backups/v25-board-20260914-1229/项目实施进度与任务看板 V2.5.md
```

⇒ **全仓（含归档）不存在任何计算该指纹的脚本或 SQL**。

### 2.2 勘误（精确化，非推翻）：L4 README `:216`「没有留下任何 SQL 或算法说明」

`docs/acceptance/v25-s01-it-safety-20260914/README.md:172-173` **确有一句文字描述**：

```
-- 全表行内容指纹（排序后拼接再 MD5）
452b7223a4a2b9dd0df7f3c883cdb74b
```

⇒ 准确表述应为：**「无 SQL、无列集、无分隔符、无 NULL 处理、无作用域（哪几张表），仅有一句『排序后拼接再 MD5』的自然语言描述 ⇒ 不可复现」**。
L4「不可比／未取证」的**结论不变**，只是理由的第一句需按上式精确化（原文不改写，以本节为准）。

### 2.3 受控穷举（总控尝试，失败并留证）

以 `information_schema` 取 `analytics_metric.metric_snapshot`（15 列）/`metric_value`（10 列）**按 `ORDINAL_POSITION` 全列**，穷举 **90 个候选**：

- 行哈希：`MD5(CONCAT_WS(sep, cols))`、`MD5(CONCAT(cols))`
- 分隔符：`|`、`,`、空串
- 聚合：`GROUP_CONCAT(h ORDER BY h)`、`GROUP_CONCAT(h ORDER BY h SEPARATOR '')`、`GROUP_CONCAT(h)`
- 组合：仅 snapshot／仅 value／`MD5(CONCAT(S,':',V))`／`MD5(CONCAT(S,V))`／`CONCAT(S,':',V)`
- `group_concat_max_len=1073741824`

**结果：90/90 未命中 `452b7223a4a2b9dd0df7f3c883cdb74b`** ⇒ 维持「**跨泳道互证不可得**」；今后互证的唯一路径是**算法与 SQL 一并入库**（L4 的 `scripts/fingerprint-3306.sql` 已入库，即为**新基准**：`metric_snapshot_fp=c224e4e9…`、`metric_value_fp=a1a07311…`）。

## 3. 总控对 L4 §11.2「待裁决」七项的裁决

| # | L4 提出 | 总控裁决 |
|---|---|---|
| 1 | F-88 是否复测（需含 V19/V20 的工件重跑） | **接受，立为下一任务**。前置：重新构建（含 `V19`/`V20`）的 `platform-app` 工件并**在 3307 隔离库**重跑链路，验证写侧是否真写 4 列、`rule_fingerprint` 是否与 `QualityRuleDefinition.checksum()` 一致。**不得在 3306 上验证** |
| 2 | V19/V20 的归属与批准状态 | **总控已批准**（2026-09-14 13:5x「待裁-8/9/10」，迁移号 V19＋V20、允许 NULL 且**严禁回填**）。L4 无交付义务，其观察与批准内容一致 |
| 3 | 是否加 D-5「非隔离启动即写 3306」闸门 | **批准做**，落 **V25-S04**（R4 授权的"启动前检查"）：**脚本层＋应用层双门**，`platform.meta.url`/`platform.metric.url` 端口 ∉ 白名单时**拒绝启动**；`V19/V20` 已在树内 ⇒ 一次误启动即对 3306 施加**不可逆前向迁移**，风险等级为高 |
| 4 | D-4 未登记规则码是否改类型化异常 | **登记进 V25-D04/Q02 域**（规则矩阵与拒绝路径），**不单独开工**；L4 的实测「15/15 已登记、路径未触发」即为该项的现状基线 |
| 5 | 是否要 L4 补跑 `mvn test` | **不在本泳道补跑**。理由：部分 IT 历史上直连 3306（见 `docs/acceptance/v25-s01-it-safety-20260914/README.md` §R-1/R-3），在启动前门禁（S04）落地前跑全量测试**有写正式库风险**；改由 S04 落地门禁后在隔离条件重跑。⇒「测试通过」在本通道**保持 ❌（未运行）** |
| 6 | `D:\p103-e3` / `D:\p105-e3` 处置 | **已完成**（L4 因纪律未读取内容，故不知情）：用户已逐项批准，二者与 `graduation-lane-backup` 等**先归并入仓、逐字节校验 BYTE-IDENTICAL 后删除原件**。归档位置 `docs/backups/strays-20260914/{p103-e3,p105-e3}`（**gitignored**，含正式库数据与明文口令，禁止提交外发）；依据与证据见 `docs/acceptance/v25-stray-consolidation-20260914/EXECUTION-20260914.md`。总控已按 L4 的提示检查归档内脚本是否留有 `452b7223` 算法：`e3-p105*.ps1`/`e3-evidence-extra.ps1` 的 3 处命中系 `MD5(` 的**子串假阳性**，与旧指纹无关 |
| 7 | 旁证指纹不可比 | **维持**（见 §2.3：90 变体穷举未命中），改以 L4 入库脚本为新基准 |

## 4. 总控对四级状态的核定（不代 L4 加码）

| 级别 | 总控核定 | 说明 |
|---|---|---|
| ① 提交完成 | ✅ | 本目录 53 文件自包含；总控已独立复算其核心判据（§1） |
| ② 测试通过 | ❌ 未运行 | 与 L4 自述一致；被实测 jar 是 `-DskipTests` 产物 |
| ③ 限定验收 | ✅（限定集内） | 总控认可其限定集表述明确（3307／工件 SHA256／仅 env 覆盖／3 步链路／local[2]）；**限定外未验收** |
| ④ 完整验收 | ❌ | 同意 L4 列的 5 条缺项；另总控补一条：**F-88 写侧未复测**（本文件 §3-1 已立任务） |

## 5. 遗留与未取证（总控登记）

- `%LOCALAPPDATA%\Temp\spark-<uuid>\…`（L4 §10 披露，186 个）＝`spark-submit`/Derby **自建**的 OS 临时目录，**在允许范围内**（用户授权：临时文件可用 `$env:TEMP`），登记**不清理**（清理属破坏性操作，且与作业日志中的自删告警一致）。
- 本核验自身未取证：未核对 L4 的 `raw/35-3306-window-absence.txt` 内 140 条 SQL 是否**逐条**只读（仅抽查脚本生成逻辑为全 `SELECT`）；未复核 8090/8092 窗口前状态（L4 亦未取证）。
