================================================================================
叙述改正 · 回读（read-back）
任务：总控 21:3x 工单 A（叙述改正，只改叙述、不改业务代码）
执行泳道：专职执行泳道 / 分支 remediation/r1-boundary
时刻：2026-09-12 21:5x–22:0x
依据：RULINGS-M3-STEP8-BATCH3-20260912.md §2「撤回一 / 撤回二」
================================================================================

【本文件性质】回读证据。以下每处均给出**文件 + 行号 + 原文逐行照抄**，
且原文由 `Get-Content` 直接读出后粘贴，非复述。

--------------------------------------------------------------------------------
§0 改正清单（三处）
--------------------------------------------------------------------------------
  #  位置                                          状态
  1  raw/post/p2-03-m2-rootcause-evidence.txt §2.1   已改正 ✔
  2  IMPL-REPORT.md §3.5                            已改正 ✔
  3  raw/post/p2-03-regression-evidence.txt §6 更新块 已改正 ✔（本轮额外发现并一并改正）
  另：TradeDwdJob.scala 方法级文档块由父侧已改；其**行内 SQL 注释**由本泳道改正（见 §4）

--------------------------------------------------------------------------------
§1 回读一：raw/post/p2-03-m2-rootcause-evidence.txt
--------------------------------------------------------------------------------

--- L64 ----------------------------------------------------------------------
因此本 SQL 的取值列表必须凑满 **23 项**（22 个普通列值 + `dt`）。

--- L66–L69 ------------------------------------------------------------------
### 2.1 旧表（20 列）——`t.dt` 是**第 20 项**，位置**正确**（无错位）
旧表结构 = **19 个普通列 ＋ 动态分区列 `dt`**（共 20 列）。
旧 SELECT = **19 个普通值 ＋ `t.dt`**（共 20 项）。
⇒ `t.dt` 前面已有 **19 个普通字段**，它自己落在**第 20 项**，正好对上分区位。

--- L71–L74（旧表对照表：全对） ----------------------------------------------
| 序 | 旧表列 | 旧 SELECT 项 | 类型 | 结果 |
|---|---|---|---|---|
| 1…19 | 19 个普通列 | 19 个普通值（逐列对齐） | 同名同型 | ✅ 正确 |
| **20** | `dt`（动态分区列） | **`t.dt`**（STRING） | STRING→分区 STRING | ✅ **正确落入分区位** |

--- 撤回块（旧「静默写错」说法已删除并显式撤回） ---------------------------------
> **撤回**：早先版本的本文件写「旧表第 19 列 `final_refunded_flag` 落入 `t.dt` ⇒ 静默写错」，
> **该说法不成立，已删除**。旧 SELECT 的第 19 项是 `t.final_paid_flag`/`t.final_refunded_flag` 本身，
> 第 20 项才是 `t.dt`；旧表第 19 列也恰是 `final_refunded_flag` ⇒ 逐列正确对齐。

--- §2.2 标题与结论（新表：第 20 个普通列＝user_key） ---------------------------
### 2.2 新表（23 列）——`t.dt` 仍在第 20 项，但**第 20 个普通列已变成 `user_key`**

| 序 | 新表列 | 实际落在该位的 SELECT 项 | 类型 | 结果 |
|---|---|---|---|---|
| **20** | `user_key` **BIGINT** | **`t.dt`（STRING）** | STRING→BIGINT | ❌ **不可安全转换 ⇒ 阶段失败** |
| 21 | `product_key` BIGINT | `$userSurrogate`（BIGINT） | BIGINT→BIGINT | ⚠️ 类型侥幸安全，但**语义错列** |
| 22 | `category_key` BIGINT | `$productSurrogate`（BIGINT） | BIGINT→BIGINT | ⚠️ 同上 |
| 23 | `dt`（分区） | `p.category_key`（BIGINT） | BIGINT→分区 STRING | ⚠️ 未及诊断即被第 20 列拦下 |

--- §2.2 末 口径说明（撤回二） --------------------------------------------------
**口径说明（撤回二）**：run 43（旧 jar，batch 42 / 1,000 行）与 run 47（新 jar，batch 43 / 55 行）
的**输入批次与行数不同** ⇒ 其**行数/分区数差异不得**用于论证旧版数据损坏。
本缺陷在旧 jar 上**根本不触发**（位置正确），故两者之间不存在「旧版静默写错」这回事。

--- §2.3 位置对照表结论（旧形态错位起点＝第 20 项） ------------------------------
**结论**：
- **旧 SELECT 从第 20 项起**整体右移一位（第 20–23 项共 4 项错位，第 1–19 项**全部正确**）。
- 新 SELECT **23/23 逐列对齐**。
- 关键点：`t.dt` 在旧 SELECT 里是**第 20 项**（前面 19 个普通字段），
  而第 20 个**普通列**在 P2-03 后已变成 `user_key` ⇒ 该位冲突即根因。

--- §6.2 末 口径说明（撤回二落地） ----------------------------------------------
**口径说明（撤回二，必读）**：run 43（旧 jar，batch 42 / 1,000 行，30 分区 / 138 行）与
run 47（新 jar，batch 43 / 55 行，29 分区 / 139 行）的**输入批次与行数不同**
⇒ 其**行数/分区数差异不得**作为「旧版静默写错 / 数据损坏」的证据，**也不作为可比基准**。
（根本原因：本缺陷在旧 jar 上**不触发**——旧 SELECT 位置本就正确。）

--- 假说一标注（hypothesis-1 / FALSIFIED） --------------------------------------
| 假设一 | `SurrogateKey.nullSafe` 裸 `THEN NULL` ⇒ `CASE` 推断 STRING | **已证伪** | 修复已落包（296,334 B）＋run 46 仍**逐字相同**报错 |

--------------------------------------------------------------------------------
§2 回读二：IMPL-REPORT.md §3.5（起始行 L135）
--------------------------------------------------------------------------------

--- L135 起 真根因块 ------------------------------------------------------------
### 3.5 真根因（**已由双实验钉死**，不再是假设）
`TradeDwdJob.orderDetailInsertSql`：INSERT **不写列名**，与 DDL 全靠**位置**对齐。
而 `INSERT OVERWRITE TABLE … PARTITION (dt)` 未给分区值时，Spark **仍要求 SELECT 提供该分区列，
且它必须落在 SELECT 列表的最末位**。本 SQL 因此必须凑满 23 项（19 个数据字段 + 3 个代理键 + `dt`）。

**关键事实：`ccc1dff` 之前没有错位。**
旧表 = **19 个普通列 ＋ 动态分区列 `dt`**（20 列）；旧 SELECT = **19 个普通值 ＋ `t.dt`**（20 项）
⇒ `t.dt` 前面已有 **19 个普通字段**，它是**第 20 项**，正好对上分区位 ⇒ **位置正确**。

`ccc1dff` 给表**追加 3 个 BIGINT 列**（普通列 19→22，总列数 20→23），
并把 3 个代理键 SELECT 项**追加在 `t.dt` 之后**，而 `t.dt` 的 SELECT 位置**没动**（仍是第 20 项）
⇒ 按位置匹配时，第 20 个**普通列**已变成 `user_key`：

| 序 | 新表列 | 实际落在该位的 SELECT 项 | 类型 | 结果 |
|---|---|---|---|---|
| **20** | `user_key` **BIGINT** | **`t.dt`（STRING）** | STRING→BIGINT **不安全** | ❌ **阶段失败** |
| 21 | `product_key` BIGINT | `$userSurrogate`（BIGINT） | BIGINT→BIGINT | ⚠️ 类型侥幸安全，但**语义错列** |
| 22 | `category_key` BIGINT | `$productSurrogate`（BIGINT） | BIGINT→BIGINT | ⚠️ 同上 |
| 23 | `dt`（分区） | `p.category_key`（BIGINT） | BIGINT→分区 STRING | ⚠️ 未及诊断即被第 20 列拦下 |

**正确顺序**应为：19 个数据字段 → `user_key` → `product_key` → `category_key` → `t.dt`（分区列最末位），
即**与 DDL 顺序一致**。原代码把代理键插到 `t.dt` 之后，**恰好把 DDL 顺序写反了**。

⇒ 报错里的 STRING **就是 `t.dt`**；「位置错位」与「代理键」恰在第 20 列相撞，
故错误信息把两者显示在一起——这正是最初把注意力误导向 `user_key` 表达式的原因。
⇒ 也解释了「旧 jar run 43 SUCCESS / 新 jar run 44+ FAILED」而变量只有 jar：
**本缺陷在旧 jar 上根本不触发**（旧 SELECT 位置本就正确）。

--- L163–L170 两条撤回 + 假说一标注 ---------------------------------------------
> **撤回（父侧授权改正）**：
> 1. 早先本报告写「旧表 20 列时 `t.dt` 落在 `final_refunded_flag`(INT) ⇒ 静默写错」——**不成立，已删除**。
>    旧 SELECT 的第 19 项是 `final_refunded_flag` 本身，第 20 项才是 `t.dt`；旧表第 19 列也正是它 ⇒ 逐列对齐。
> 2. run 43（batch 42 / 1,000 行）与 run 47（batch 43 / 55 行）**输入批次不同**
>    ⇒ 其**行数/分区数差异不得**用作「旧版数据损坏」的证据。

> **假说一（`nullSafe` 裸 `NULL`）状态 = FALSIFIED（已证伪）**：标为 **hypothesis-1 / FALSIFIED**，
> **必要但不充分**；修复保留的**唯一**理由是「改善类型确定性」，**不得**与真因混写、**不得**作为根因。

--- 另两处牵连同改（同义措辞） --------------------------------------------------
L173: - 实验 1（复刻错位形态：19 个普通值后紧跟 STRING 的 `t.dt`，使 `t.dt` 为**第 20 项**）
        ⇒ **逐字复现**同一错误：
L62 : | 对应 run | run 43 SUCCESS（位置正确、无错位） | run 44/45/46 FAILED | **run 47 SUCCESS** |
L122: | 17:37:56 | run 43 `tdw` **SUCCESS**（138 行 / 30 分区）← 旧 jar，**位置正确、无错位**（见 §3.5） |

--------------------------------------------------------------------------------
§3 回读三：raw/post/p2-03-regression-evidence.txt §6 更新块
--------------------------------------------------------------------------------
（该块原为本泳道 21:26 所写、沿用了旧措辞，本轮一并改正）

> **【2026-09-12 21:26 更新 · 状态：假设 → 已证实】**
> 本节原登记的「假设，未经实测」**已被实测钉死为真根因**，并据此完成 P2-03-m2 修复：
> - **机制**：`INSERT OVERWRITE TABLE … PARTITION (dt)` 未给分区值时，Spark 仍要求 SELECT 提供该列，
>   且它**必须落在 SELECT 最末位**。**`ccc1dff` 之前没有错位**：旧表 = 19 个普通列 ＋ 动态分区列 `dt`，
>   旧 SELECT = 19 个普通值 ＋ `t.dt` ⇒ `t.dt` 前面已有 19 个普通字段，它是**第 20 项**，位置**正确**。
> - **`ccc1dff`(P2-03) 才引入缺陷**：新增 3 个 `BIGINT` 代理键并**追加在 `t.dt` 之后**（`t.dt` 位置未动）
>   ⇒ 按位置匹配时，第 20 个**普通列**已变成 `user_key`(BIGINT) ⇒ STRING 的 `t.dt` 落入其中。
>   STRING→BIGINT **不安全** ⇒ `CANNOT_SAFELY_CAST` ⇒ 阶段失败。
>   报错里的 STRING 就是 **`t.dt`**，不是 `user_key` 表达式。
> - **正确顺序**：19 个数据字段 → `user_key` → `product_key` → `category_key` → `t.dt`（与 DDL 列序一致）。
> - **撤回**：早先本块写的「旧表第 19 列 `final_refunded_flag`(INT) ⇒ STRING→INT 安全 ⇒ run 43 静默写错」
>   **不成立，已删除**：旧形态 `t.dt` 恰好落在分区位，写入是正确的；也**不得**用 run 43 与 run 47 的
>   行数/分区数差异论证数据损坏（两者输入批次不同）。
> - **实验 1**（复刻错位形态：19 个普通值后紧跟 STRING 的 `t.dt`，即 `t.dt` 为**第 20 项**，一次性表）
>   ⇒ **逐字复现**同一报错。
> - **实验 2**（同一 SELECT，**仅** `t.dt` 移到末位）⇒ 写入成功，读回 `EXPERIMENT2_OK 111 222 333 20260901 0`。
> - **m2 修复**后 **run 47 八阶段全绿**，`tdw` in18/out139/rej0，本地快照 `S20260901_47` 已产出。
> - **假设一（`nullSafe` 裸 NULL）＝ 已证伪（hypothesis-1 / FALSIFIED）**：必要但不充分；
>   修复保留的理由**仅限**「改善类型确定性」，**不得**作为根因。
> - 实验库/表已彻底删除并二次确认。

（该文件表头另加状态提示：本书「未改代码 / 已停手 / 假设未经实测」等表述均为 **21:20 之前**的状态，
已过时但保留备查 ⇒ 本书保留为「假设一被证伪」的原始证据链。）

--------------------------------------------------------------------------------
§4 生产代码：行内 SQL 注释改正（**非业务改动**）
--------------------------------------------------------------------------------
改动仅 1 行、仅注释文本，SQL 语义零变化：

  -  |  t.dt                                  -- 静态分区列**必须在最后一个位置**（见下）
  +  |  t.dt                                  -- 必须与 DDL 列序一致：分区列落在最末位（见方法上方说明）

改正理由：旧注释是**本泳道 P2-03-m2 时写的错措辞**，它把「与 DDL 列序一致」这一真规则
误述成孤立口号；而这条注释所配的方法级文档块（父侧已改，L131–L141）讲的正是列序对齐。

`git diff -U0` 该文件的**全部**工作区差异（相对 HEAD `caf9c9d`）恰为上面这一行：
  @@ -176 +176 @@ object TradeDwdJob {
  -       |  t.dt                                  -- 静态分区列**必须在最后一个位置**（见下）
  +       |  t.dt                                  -- 必须与 DDL 列序一致：分区列落在最末位（见方法上方说明）

--------------------------------------------------------------------------------
§5 因上述注释而重编 jar：指纹与行为等价性证据（重要，须总控裁定）
--------------------------------------------------------------------------------
【事实】为改正该注释，jar 被重建；**重建前的 run 47 jar 已不存在**
（`mvn clean package` 的 `clean` 删除了旧产物，未事先备份 ⇒ 本泳道操作失误，如实登记）。

【当前 jar 实测】
  path   spark-jobs/target/spark-jobs-0.1.0-SNAPSHOT.jar
  bytes  296359
  mtime  2026-09-12 21:58:08
  sha256 71C2BCCB54066996E7197DEA004E88F16B4DF912481CB723B4D84CC33CE0C966
  （**不是**工单里假定的 296,340 B / F9882E6A…9432A0B；以实测为准，故在此显式登记）

【行为等价性证据】
 1. 该文件的全部工作区差异 = 1 行注释（§4）⇒ **无任何可执行语句变化**。
 2. 编译产物 `TradeDwdJob$.class`：bytes 6316 / sha256
    CD979288A93A40C7FFDB8A1176C9C11AE905E449FB78158FAD7F173C090C34DD / class major **52**
 3. `javap -p` 公共/私有成员签名逐一未变，特别：
      public java.lang.String orderDetailInsertSql(
        com.graduation.analytics.warehouse.WarehouseNamespace, java.lang.String, java.lang.String);
      public org.apache.spark.sql.types.StructType OUT_SCHEMA();
 4. 编译后 class 内**确实含**改正后的注释文本、**不再含**旧文本：
      含「必须与 DDL 列序一致」= True ；含「静态分区列」= False
 5. E2 测试（JDK8 / JAVA_HOME=D:\Develop\JDK1.8）在**当前源码**上重跑：
      Total number of tests run: 111 ; Suites: completed 15, aborted 0 ; BUILD SUCCESS (exit 0)

【**新发现：jar 级构建不可复现**（影响指纹口径，须总控裁定）】
同一源码状态连续 3 次 `mvn -o -f spark-jobs/pom.xml clean package -DskipTests` 得到**3 个不同 jar 哈希**：
  78FB3101DC480C546202B68FD398AAF5AA0D2FEC5600E5A86A88BB288E61634A  296359 B
  EA6147E4AC9A3F314E4D50F76F6B60EE0C7C98423BE88FC48152B35EF870ED84  296359 B
  71C2BCCB54066996E7197DEA004E88F16B4DF912481CB723B4D84CC33CE0C966  296359 B
原因（实测）：jar 是普通 ZIP，**每个 entry 内嵌写入时刻**。当前 jar 内 122 个 entry：
  115 个时间戳 = 2026/9/12 21:57:56；6 个 = 21:58:08；1 个 = 16:14:28（资源文件，保留原 mtime）
⇒ **jar 整包 sha256 随构建时刻变化，不能作为「同一构建物」的稳定标识**；
  可复现的稳定标识是**逐 entry 内容哈希**（class 载荷在多次构建间恒定，如 §5.2）。
⇒ 建议口径：集群作业的「构建物指纹」以 **class entry 哈希 + class major** 为准，
  jar 整包 sha256 仅作「本次落包记录」。

【需要总控裁定的一件事】
工单 B 原假定「用当前 jar、不要再重建」。但叙述改正本身改动了被编译的源文本（注释），
故 **jar 已不可避免地被重建**，原 296,340 B / F9882E6A 指纹已无法沿用（该构建物已被 `clean` 删除）。
本泳道**未**再提交任何集群作业、**未**改其它生产代码。
是否接受以 §5 的「行为等价性证据 + 当前 jar 指纹」作为集群前置基准，**请总控裁定**。

--------------------------------------------------------------------------------
§6 边界合规
--------------------------------------------------------------------------------
- 未 `git add/commit/push`（留总控入库）
- 未写 MySQL 业务数据（未执行任何写语句）
- 未提交集群作业、未触碰 `e4c1000` / `/graduation/export/e4c1000` / `/graduation/export/m3s8-55`
- 未重启平台（PID 61104 仍在）
- 未重跑本地链、未跑 run 48
- 未修改 `MAPPING-20260912.md`（冻结件）
- 数字仅落在 `raw/post/` 与 `IMPL-REPORT.md`

--------------------------------------------------------------------------------
§7 一处遗留（如实登记）
--------------------------------------------------------------------------------
本轮对 `dw_exp`（一次性实验库）之外的 Hive 元数据**未**做任何变更。
但**HDFS 只读核实（`hdfs dfs -ls /graduation/export` 等）本轮两次超时失败**
（180 s / 600 s 上限均超时，exit 1），**未取得读数** ⇒ 该核实项状态为**未完成**，
已列入 `raw/post/cluster-gate-prep.txt` 的门禁待办（需先确认 HDFS/NameNode 可达）。
================================================================================
回读结束
================================================================================
