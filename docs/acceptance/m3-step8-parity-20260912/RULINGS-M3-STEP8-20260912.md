# M3 §6.4 第 6+8 步 总控裁决书（D-137）（2026-09-12 20:4x）

- 裁决人：总控（父会话）　对象：`docs/acceptance/m3-step8-parity-20260912/PLAN.md`（69 行，E0 级计划）
- 授权来源：用户 2026-09-12 20:2x 选择「**合并执行 §6.4 第 6 步 ＋ 第 8 步**」（同 55 条输入，本地与集群各一次真跑，然后逐值比较）
- 本条**只补计划缺口**，不改变授权范围；**不**授权任何 DDL／删除／迁移／重命名

---

## 0. 为什么必须补这一条：计划里有一个**未被验证的隐含假设**

`PLAN.md` §4 的 A3/A4 写的是「集群侧…取得 `runId`、快照 ID、**10 个核心指标值**」「对 10 个核心指标**逐指标逐值**比较本地 vs 集群」——**但没有写集群侧的指标从哪条通道读**。

父侧 2026-09-12 20:45 实测（范围 `spark-jobs/src/**`，`*.scala`/`*.java`/`*.xml`）：

| 检索串 | 命中 | 含义 |
| --- | --- | --- |
| `jdbc:mysql` | **0** | 无 MySQL 连接串 |
| `DriverManager` | **0** | 无 JDBC 入口 |
| `getConnection` | **0** | 无连接获取 |
| `analytics_metric` | 5（`job/MetricExportJob.scala:16`、`metric/MetricAdsSpec.scala:6,16`） | 均为**名字引用**；配合前三项 0 命中 ⇒ **不是写入面** |
| 阳性对照 `spark.sql.warehouse.dir` | **4** | 检索有效 |

**结论（实测，非推断）**：`spark-jobs` **没有任何 MySQL/JDBC 写入路径** ⇒ 集群链的指标值**不可能**出现在 `analytics_metric`／`metric_value` 里 ⇒ 「集群侧指标」唯一可取证落点是 **`mxp` 作业的 HDFS 导出物**（`--exportDir=hdfs://node01:8020/graduation/export/<新目录>`）。

**若不先裁决这一点**：条件满足后会跑出「本地有 10 个指标值、集群侧 MySQL 里什么都没有」的局面 ⇒ 要么误判失败，要么被迫**事后**发明比较口径（事后发明口径 = 结果导向，禁止）。

## 1. D-137 比较通道（**运行前冻结**）

1. **本地侧口径**：以平台自身发布结果为准 —— MySQL `analytics_metric` 的 `metric_snapshot`（本次运行的快照）＋ `metric_value`（10 个核心指标），并同时留存该快照指向的 ADS 镜像行数。
2. **集群侧口径**：以 `mxp` 导出物为准 —— **独立回读**（不信 `mxp` 自报），方法沿用既有工具 `docs/acceptance/e4-cluster-1000-20260912/tools/e4-mxp-readback.ps1` 的口径（清单文件以 `_` 开头的识别方式、清单 `rowCount` 与导出文件实际行数逐表对账、对照 SQL 用**真实的 8 张正式 ADS 表**，不得用 v1 表名）。
3. **逐指标映射表必须在运行前冻结并落盘**（写入本目录 `MAPPING-20260912.md`）：10 个核心指标 ⇄ 集群导出物中的表/列 ⇄ 本地 `metric_value.metric_code`。**禁止**在拿到两侧数字之后再调整映射（那是结果导向）。
4. **比较粒度**：逐指标逐值；值相等的判据是**数值相等**（整数比整数、小数按导出物原始精度），**禁止**引入「四舍五入/容忍度」自行抹平（与 `PLAN.md` §5 一致）。
5. **不得混用**：不得用 1,000 行那次运行（E4）的任何数字冒充本次同输入比较；不得用 Mock/夹具替代真实集群运行；不得把本地档写成集群档。

## 2. 输入与写入边界（把「同一份输入」钉死）

1. **同一份输入** ＝ `tests/golden-dataset/events/golden-20260901.jsonl`（55 行 / 18,430 B / sha256 `2351BCC35E04CCD2…`）；两侧运行前、后各记录一次 sha256，**前后必须一致**（防第三方写入）。
2. 集群侧需要一个新 landing 目录：**只允许新建** `hdfs://node01:8020/graduation/landing/m3s8-55/`（本任务新增目录），并把上述 55 行**原样上传**（上传后回读并核对 sha256 ＝ 本地同一值）。
3. **禁止**写入/覆盖 `…/landing/e4c1000`（E4 的既有输入）、**禁止**改动既有快照与既有分区、**禁止**复用 E4 的 `--outputSnapshotId`（本次用**新的**快照 ID，且必须与本地侧快照可区分）。
4. 本地侧运行仍走平台 API（`POST /api/v1/ingestion/runs` ＋ `POST /api/v1/pipeline-runs`），`runtimeProfileId=1`、`businessTime=2026-09-01T00:00:00`；**不得**手工改库制造输入。

## 3. 门槛（与 `PLAN.md` §3 一致，补两条可执行判据）

1. **P2-03 已落地**：泳道代码提交入库、`git status --porcelain` 中**代码类条目 = 0**（文档/验收目录条目可另计，须逐条说明）、其 E1/E2 全绿、报告交付 ✓（当前：代码 11 文件**未提交** ⇒ **不满足**）。
2. **两侧用同一份 jar，且指纹登记**：① 集群侧 **`spark-jobs` jar**（提交用）＋ ② 本地侧 **平台 fat jar**（`analytics-server` 打包产物，本地链由平台执行）**都必须在冻结工作区重建**，登记「路径＋字节数＋sha256＋构建时点」；并遵守 **F-81**：指纹**只证明文件是这个**，**不证明**由当前 HEAD 构建 ⇒ 报告中不得写成「由当前 HEAD 构建」。
3. **本机内存纪律（陷阱 #26）**：分页文件上限实测 **4,196 MB** ⇒ 本地链与集群提交**串行**，运行期间不并发任何 Maven/测试/其他 JVM 重活（连只读 `pwsh` 都可能 `Allocation error`）。
4. **数据面冻结**：比较期间无第三方写入 landing/数仓；两侧输入 sha256 一致（§2.1）。
5. **用户授权** ✓（已具备）；本裁决**不扩大**授权。

## 4. 停下条件（任一触发即停，不得自行绕过）

1. 集群不可达／队列或资源拒绝；2. 两侧输入 sha256 不一致；3. 同侧两次运行结果不同（非确定性 ⇒ 先解决确定性，指导书要求排行/NTILE 增稳定次序键）；4. 需要任何 DDL/迁移变更；5. 触发本机内存上限；6. **映射表无法在运行前冻结**（说明 10 个指标与导出物之间缺少可判定的对应关系 ⇒ 停下报告，不得边跑边定）。

## 5. 本裁决未做／未测

- **未**跑任何本地链、**未**提交任何集群作业、**未**上传任何文件到 HDFS、**未**连集群、**未**改任何代码/DDL/契约；本轮只做**只读检索与文件读取**。
- 集群侧导出物的**真实列结构**本轮**未读取** ⇒ 映射表（§1.3）能否冻结**尚未验证**；若不可冻结，按 §4.6 停下报告。
- 本文件**不构成** M3 出口判据的达成证据；M3 在真正跑完并逐值比较之前不得记 `DONE`。
