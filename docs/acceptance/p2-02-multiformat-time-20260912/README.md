# P2-02「多格式时间与 quarantine 原因码」起草泳道

- 泳道职责：**只起草规格与施工单**，不实现代码、不改既有文件、不做 git 写操作。
- 时点：2026-09-12 20:04 ～ 20:20（+08:00）
- 开工 HEAD `3dfe94e`；取数时 HEAD **`ce36065c3db4572115ff09945ef6b041fcfdc5e2`**（总控期间提交 4 次）
- 结论一句话：**规格草案与施工单草案已出**，但**均不可据以直接开工** —— 施工单 §2.1 有一条硬阻塞（P2-03 正在写 `spark-jobs/**`，D-091），且规格含 **10 条待裁决**。

## 文件索引

| 文件 | 性质 | 给谁看 |
|------|------|--------|
| `draft/p2-02-spec-draft.md` | **规格草案**（DRAFT）：目标与"不变什么"、时间语义、原因码枚举与稳定性、可度量出口判据、相容性核对表、非目标、未测项 | 评审者 / 总控 |
| `WORKORDER-P2-02-IMPL.md` | **施工单草案**：A1–A9 施工项（改动面／验收命令／证据名／停止条件）、并行约束、E1–E5 证据计划、回滚、**不得声称清单**、开工检查表 | 实施者 / 总控 |
| `SUGGESTED-RULINGS-20260912.md` | **待裁决清单（建议 D-101…D-110）**。**本泳道未自行认定任何一条** | 总控 |
| `RAW-READINGS.md` | 只读取数台账：命令原文、原样输出、sha256、抽样范围、阳性／阴性对照、未测项 | 评审者 |
| `raw/` | 原始读数与扫描脚本（含 sha256） | 复算者 |

## 三条最该先看的结论

1. **需求定义不在看板里。** 「多格式时间」全仓库只在看板出现 3 处、每处只有「格式、未知版本、坏时间负例」9 个字。真正可施工的需求在 `docs/superpowers/plans/2026-09-11-mall-agnostic-platform-implementation.md:153-157`（`event_time` 依 source profile 的格式列表**依次解析**；最终**统一 UTC**；未知版本／无法解析时间／缺 eventId **进 quarantine**，保留**原因码**与**原位置**）。看板没有链接到它 ⇒ 只读看板则 P2-02 不可施工。

2. **「多格式」在真实数据上零发生。** 349 个 `.jsonl` / 1,215,989,873 B 全量扫描 ⇒ **2,622,616** 个 `event_time` 取值 **100% 为 `yyyy-MM-ddTHH:mm:ss+08:00`**，其余 10 类形态**全 0**（阳性对照 54/54、阴性对照 0）。⇒ 出口判据**必须**含构造夹具；任何「真实数据已支持多格式」的说法都不成立。

3. **字段名更正：真库没有 `reason_code`。** 实际列是 **`reason VARCHAR(255) NOT NULL`（自由文本）**，108 行 / 9 类原因 / **0 条时间相关**；`data_quality_result` 445 行 / 15+ `rule_code` / **0 条时间相关**。另外 `warehouse/ddl/01-dwd.sql:90` 的 `reject_reason` 注释已声明 `FUTURE_TIME` 但**代码从不写**（token 扫描命中 1 处，仅注释）。

## 边界声明

- 本目录是**新建**目录；未修改任何既有文件（含看板、契约、源码、`warehouse/ddl/**`）。
- `spark-jobs/**` 全程**只读**（P2-03 正在写）；引用行号处均附**读时 sha256**，见 `raw/source-fingerprints-20260912.txt`。
- **未运行**任何测试、未构建、未提交、未做 DB DDL/DML。
- 所有「0 命中」结论均附**阳性对照**；一次由 `Tee-Object` 未 flush 造成的「0 字节输出」已作为**采样窗口伪影**留档（`RAW-READINGS.md` §0.1），未当作读数。
