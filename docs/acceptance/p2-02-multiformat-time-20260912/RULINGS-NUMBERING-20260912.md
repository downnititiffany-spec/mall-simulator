# P2-02 号段分配通知（2026-09-12 20:2x）

- 出具人：总控（父会话）
- 性质：**号段分配 ＋ 一条施工闸门**；**不是**对 10 条待裁决的实体裁定（实体裁定下一轮出）。

## 1. 为什么必须重分配

泳道在 `SUGGESTED-RULINGS-20260912.md` 中**建议号段 D-101 起**。父侧本轮独立实测（`docs/**/*.md`，不含 `backups/`，逐文件 `Select-String 'D-\d{3}' -AllMatches`）：

- 全仓出现过 **111** 个 `D-xxx` 形态编号，其中 `D-001` … `D-110` **全部已被占用**（第 111 个是 `d-202`，落在长数字串内，属正则假阳性）；
- 因此 **`D-101` … `D-110` 已被占用** ⇒ 泳道建议的号段**全部撞号**，不可使用。

**下一个可用号 = `D-111`。**

## 2. 分配结果

| 泳道 | 分配号段 | 条数 | 状态 |
| --- | --- | --- | --- |
| **P2-02 多格式时间** | **`D-111` … `D-120`** | 10 | 号段**已保留**；实体裁定**待出**（下一轮） |
| P2-04 DWD 源投影 | `D-121` … `D-126` | 6 | **已裁定**（见 `p2-04-…/RULINGS-P2-04-20260912.md`） |
| P2-05 ODS 重建守卫 | `D-127` … `D-134` | 8 | **已裁定**（见 `p2-05-…/RULINGS-P2-05-20260912.md`） |

## 3. 对 P2-02 施工单的即时效力（闸门不变）

1. `WORKORDER-P2-02-IMPL.md` 中所有 `[需裁决 D-xxx]` 条目的正式号位，请按上表把 `D-101…D-110` **机械替换**为 `D-111…D-120`（**保持原有先后顺序**，即建议清单第 1 条 → `D-111`，依此类推）；替换后各条的**结论栏仍为空**，**不得**把「号段已分配」写成「已裁定」。
2. **A1／A2 以及所有标 `[需裁决]` 的条目在实体裁定下达前一律不得实施**（含夹具落点、`timePolicy.formats` 取值语法、原因码、DQ 规则、`OdsLoadSql.scala:181` 静默丢弃的处置）。
3. 其中 **D-101（G1「统一 UTC」 vs D-053 `event_time` 保持 `STRING`）** 是本行最大未决点，父侧已收悉泳道给出的 P-A/P-B 两案；实体裁定将连同其余 9 条一并出具，**不会**以「暂缓」代替裁定。

## 4. 纪律提醒（沿用）

- 规格与施工单仍是**草案**（E0 级）⇒ 不得据以声称「P2-02 已开工」或「多格式已支持」。真库实测事实不变：2,622,616 个 `event_time` **100% 为 `yyyy-MM-ddTHH:mm:ss+08:00`**，其余形态 0 ⇒ 多格式在真实数据中**零出现**，出口判据必须依赖**构造夹具**，且**不得**声称「已支持多格式」。
- 泳道提出的「`OdsLoadSql.scala:181` 静默丢弃必须消除」属 `spark-jobs/**` 改动 ⇒ 受 P2-03 写面占用与 D-091 顺序约束，实施时机由总控另行安排。
