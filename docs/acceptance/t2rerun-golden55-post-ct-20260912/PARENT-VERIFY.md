# R2 主跑 · 父级独立复核（不采信泳道自述）

- **复核对象**：R2 主跑 —— 发布快照 `S20260901_42` 的那次运行（run **42**）
- **复核工具**：`.verify\r2\r2-parent-verify.ps1`（父级自有；44 条断言；参数 `-Repo -Mysql -LogPath`；`.verify` 不入库）
- **复核日志**：`.verify\r2\post-run-verify.log`（172 行；sha256 前 16 位 `7C510C81AC1F9CCB`）
- **复核时点**：2026-09-12 17:30:01（8090/8092 在跑 ⇒ 属**声明时点**，非冻结窗口，已在 `raw/gates-pre-run.txt` G5 记录）
- **结果**：**44 条断言 = 38 PASS / 6 FAIL**，退出码 1。6 条 FAIL 逐条归因见 §2，**全部是预测前提不成立，不是链路故障**
- **自检（外部核对）**：脚本末尾**重读自身日志**，比较 `[PASS]`/`[FAIL]` 标记数与脚本内部计数 ⇒ `[PASS] SELF 日志标记数与脚本计数一致`（不采信同一次输出的内存计数）
- **适用范围**：本文件只覆盖**主跑**。同输入对照（R2-b）与其余事项由泳道证据与 §5 的补记覆盖；两者结论冲突时以**晚出的实测**为准，且以补记形式追加，不修改本文件正文

## 1 判为「绿」的读数（逐值）

| 断言 | 应然 | 实测 |
| --- | --- | --- |
| A0a/A0b 跑前锚点 | run>41、批>40 | run **42**、批 **41** |
| A1/A1b ACTIVE 唯一 + 发布者带 R2 标记 | 1 / `r2` 前缀 | 1 / `r2-post-ct-20260912-172230` |
| A2/A3 版本迁移 | ACTIVE\|10、前快照 ARCHIVED\|9 | `S20260901_42` ACTIVE `v10`；`S20260901_41` ARCHIVED `v9`（版本保留） |
| A6 新快照指标行数 | 10 | 10 |
| A8/A8b/A9 发布者终态与阶段 | SUCCESS；`target_snapshot_id`=新 ACTIVE；8/8 | run 42 **SUCCESS**；`S20260901_42`；`WAIT_LANDING…PUBLISH_METRIC` **8/8 SUCCESS** |
| A10a–A10e **P1（golden55 逐文件归因）** | 51 接受 + 4 隔离 = 55 | 51 / 4 / 55 ✔ |
| A10f/A10g 归因可信度 | golden55 隔离 4 行；**其余文件贡献 0** | 4 / **0** |
| A10b/A10b2/A10h 接受数口径三处互证 | `record_count` = 接受行 = WAIT_LANDING `acceptedRecords` | **2740** = 2740 = 2740（文件侧总和 2744 = `record_count+quarantine_count`） |
| A11a–A11e jar 与进程 | 现算 sha16+字节 ∈ 登记文件；8091 有新 pid 且命令行指向 platform-app | `33,135,937 B` / sha16 `C099F307D80135E2`；pid **59304**（跑前 39220）✔ |
| A12a/A12b 契约守卫 | 退出码 0 + **正向对照 PASS** | 0 / PASS（台账 113 行 / 199 处） |
| A13a/A13b 数仓结构 | 8 张 `__staging` 目录齐备且非空 | 0 / 0（见 §3 口径更正） |
| A14a/A14c/A14d/A14e/A14f **导出通道** | 快照一致；8 表 rowCount 序列 = DB 镜像；jsonl 行数 = 声明；声明路径存在且有 parquet | 全绿（`1,4,4,4,1,4,1,3`；导出目录时点 17:28:10） |
| A15a–A15d **读路径 API** | 登录成功；10 项；快照 = ACTIVE；逐值与库相同 | 全绿（值按**数值**比：`[decimal]`；单位/口径/期间按字符串比） |

**三通道互证**：DB 镜像 `analytics_metric` = 导出 `metric-staging\S20260901_42\_export.json` + 8 个 `*.jsonl` = `GET /api/v1/metrics/overview`，逐值相同（A14c/A14d/A15d 同时绿）。

**落地区（E3-g，时点 17:30:01）**：`landing\events` **60 文件 / 406,132,563 B**；`landing\manifests` **41 文件 / 38,098 B**。跑前为 59 文件 / 406,114,133 B ⇒ 差 **+1 文件 / +18,430 B**，正是 golden55 源件大小（见 §3）。

**数仓（E3-f，跑前 972 parquet / 4,977,445 B @17:16:05 → 跑后 137 parquet / 1,234,592 B @17:30:01）**：

| 层 | parquet 跑前→跑后 | 字节 跑前→跑后 |
| --- | --- | --- |
| `dw_ods.db` | 866 → **29** | 4,567,412 → 799,164 |
| `dw_dwd.db` | 57 → 58 | 317,273 → 338,827 |
| `dw_dws.db` | 14 → 14 | 28,547 → 28,761 |
| `dw_ads.db` | 30 → 30 | 54,737 → 54,998 |
| `dw_dim.db` | 3 → 4 | 8,198 → 11,564 |
| `probe_r613.db` | 2 → 2 | 1,278 → 1,278 |

⇒ 全仓 parquet 数**下降**（ODS 分区被本次运行重建，非"输入相同"的复跑）。这与 ORDER-1 §4 E3-f 的前提「输入相同 ⇒ 前后逐值相同」**只对 T2 复跑成立，对 R2 不成立**；本判据据此改为「**逐层跑前→跑后报告 + 结构不变量（`__staging` 无空表）**」，属**口径更正**（沿用 G1 的处理方式，不是放宽判据）。

## 2 判为「红」的 6 条：全部为「输入不同 ⇒ 差异不可归因」

| 断言 | 应然（P5/P3 冻结预测） | 实测 | 归因 |
| --- | --- | --- | --- |
| A4 | `1,4,4,9,1,9,1,1` | `1,4,4,4,1,4,1,3` | 基线 `S20260901_41` 输入 = **批 31（1000 行）**；主跑输入 = **批 41（2740 行 / 6 文件）** |
| A4b | 合计 30 | 22 | 同上 |
| A5 | 与前一快照逐表序列相同 | 不同（同 A4） | 同上 |
| A14b | 导出 totalRows 30 | 22 | 同上（导出通道内部仍自洽） |
| A6b | 两快照十指标并集 = 10（逐值相同） | 20 | 同上 |
| A7 | 与 P1-01 基线逐值相同（10 项） | 10 项全不同（`avg_order_value 88.16→408.4`、`buy_rate 0.2→1.0`、`dau 10→3` …） | 同上 |

**两个方向的结论都必须克制**：

- **不得**据此判「CT 破坏了指标面」——输入不同，差异不可归因；
- **也不得**判「CT 未破坏指标面」——那需要**同输入对照**（§5）。

**反证「不是通道解耦被破坏」**：若镜像/导出/接口三条通道解耦失败，应出现三通道互相不一致；实测三通道**逐值相同**（§1）⇒ 解耦本身在主跑中成立。

## 3 口径更正（两处，均有实测支撑）

**(1) A13b 断言目标：正式表 → `__staging`。** 设计上 ADS 只写 `{table}__staging/snapshot_id=S/dt=D`（`PipelineService.java` §14.4；`…Job.scala:13`「不碰正式分区」）。实测 8 张**正式**表最后写入时间停在 **09-10 18:07:25–18:07:31（另 1 张 09-10 18:40:10）**，主跑起点 17:22:48 ⇒ 本 run **未写正式表**。原断言「正式表有 parquet」把判据挂在**运行期不使用的表**上 ⇒ 改为「8 张 `__staging` 目录齐备且非空」，并保留正式表的时点观察（另立 **F-84**）。

**(2) 采集语义：复制 + 按结果切分，不是搬移。** 实测 `landing\events\r2-golden55-20260912-172221.jsonl` **仍在**（18,430 B，写入 16:52:06），而 `landing\accepted\41\` 内是**按结果切分后的副本**：接受件 17,419 B（51 非空行）/ 隔离件（4 行）；同批其余 5 件同样「源件仍在 events、accepted 内是子集副本」（如 `2026091209.jsonl` 1,131 B → 1,128 B）。父级此前把「accepted 里出现了文件」**推断**为「源文件被移走」，该表述**作废**，以本节实测为准。`file_checkpoint` 实测按**路径**去重（`file_path` 归一化；`next_offset` = 已消费字节数；`file_identity` 为秒级时间戳，非内容哈希）。

## 4 本轮新测出的两处「声明 ≠ 实物」

**F-83（新增）**：`analytics_meta.pipeline_run.input_batch_id` 在 **41/41 行全为 NULL** ⇒ 该列**声明但从不写入**（假取证）。run→批次的**可信链接只有** `pipeline_stage_run.evidence` 里 WAIT_LANDING 的 JSON（主跑 run 42 = `{"batchId":41,"acceptedUri":"accepted/41","checksum":"4aa9fbd3","acceptedRecords":2740,"schemaVersions":["1.0"]}`）。全库无其它 run↔批次结构化列。

**F-84（新增）**：ADS **正式表与 `__staging` 表双所有者**，正式表自 09-10 18:07 起未再写入，而全链路（导出/镜像/接口）一律读 `__staging`；同时导出的 `hiveTable` 字段写**正式表名**、`hivePath` 却指 `__staging` ⇒ 同一 JSON 内声明与实物不一致（8/8 表，逐字证据）：

```json
{"hiveTable":"dw_ads.ads_operation_overview","mysqlTable":"ads_operation_overview_m","rowCount":1,
 "hivePath":"file:/D:/.../spark-warehouse/dw_ads.db/ads_operation_overview__staging/snapshot_id=S20260901_42/dt=20260901", ...}
```

## 5 未测 / 不得声称（本轮）

- **未测**：CT 对指标面的影响（需 R2-b 同输入对照 —— 已具备实测手段：`findReadyManifest` 取 READY 且 `accepted+quarantined>0` 的**最大 batchId**（`PipelineService.java:935-970`）、重试按本 run 证据钉住原批（`:895-913`）、`landing\events` 60 文件**全部已有断点**（差集 0）⇒ 换名副本可成为唯一输入）
- **未测**：E3-d（`status()` 迁移序列 + 一次受控 `cancel()`），由泳道在对照之后执行
- **不得声称**：R2 复现了 T2 的 55 条重跑（本轮输入是批 41，且 golden55 只是 6 个文件之一）；CT 未破坏指标面；正式 ADS 表已发布；`pipeline_run.input_batch_id` 可用于追溯；L20 已满足（仍为 `DONE_LIMITED`）
- **可复现性口径**：本脚本按「当前 ACTIVE 快照 → 其 `pipeline_run_id`」锚定 ⇒ 本日志覆盖的是**发布 `S20260901_42` 的那次运行**；此后若又有新 run，重跑脚本复核的是**新 run**（设计如此，不是回归）

---

## 补记 2026-09-12 17:40 · E3-e 三条判据的显式读数（父级独立实跑）

守卫 `scripts/check-bare-anchors.ps1` 实跑（17:35，耗时 0.5 s，退出码 0）：

| E3-e 判据 | 应然 | 实测 |
| --- | --- | --- |
| 守卫集合 ⊆ 台账 | 成立 | `结果 = PASS（裸锚点 199 处 / 112 行，**全部在台账内**；台账 113 ≤ 基线 113）` |
| 正向对照（防"判据无鉴别力"） | PASS | `对照样本锚点=4 裸锚点=1 → §2.5 L92`；带文件名/版本前缀的 3 个**未计入** ✔ |
| 台账只减不增 | 台账行数不增 | 台账 **113 行** ＝ 基线 **113 行**；且 `scripts/contract-bare-anchors.allowlist.txt` **受版本控制、工作区与 HEAD 完全相同、mtime `13:10:18`（在 R2 窗口 17:16 之前）** ⇒ 本轮**未改动台账** |

- 扫描实况：`contract-specs` 9 个文件、锚点总数（含合规）**313**、裸锚点 **199 处 / 去重 112 行**。
- **两项待收敛（非 R2 回归，属既有记账滞后）**：① 台账内有 **1 行**已不再是裸锚点（可删减，须在看板登记后跑 `-WriteLedger`）；② `contract-specs\schemas\canonical-event.v1.schema.json :: §2.4 L84` **台账=2 / 实测=1**（出现次数列陈旧）。②是本轮能测到的 **CT 对契约文档的痕迹**（该文件的一处裸锚点随 CT 改动消失），与运行期指标面无关。
- **脚本演化**：§2 的 44 条断言为本日志（17:30）的计数；此后新增 A12c/A12d（把 E3-e 从"退出码 0"升为显式判据）⇒ 现为 **46 条**。旧日志不改写，新判据生效于后续重跑。

---

## 补记 2026-09-12 17:41 · R2-b「同输入对照」父级四通道实测 ⇒ **CT 未破坏指标面（通过）**

**对照设计**（先落预测后开跑，泳道 `raw/predictions-control-run.txt` mtime `17:33:43`；注入件 `17:34:18`）：把 CT 前批 31 的 1000 行接受副本（`landing\accepted\31\gen-s3b-1000-20260911.jsonl`，363,848 B）以新名放入落地区，因 `file_checkpoint` 按**路径**去重而重新采集 ⇒ 得到与 CT 前**同一输入**的一次新运行。前置已实测：`landing\events` 60 文件**全部**已有断点（差集空）＋ 假文件名**正向对照**（判无断点）⇒ 输入纯净。

| 通道 | 应然（CT 前 `S20260901_41`，批 31 输入） | 实测（CT 后 `S20260901_43`） | 判定 |
| --- | --- | --- | --- |
| 采集（批 42） | 1000 / 0 隔离 | `record_count=1000`、`quarantine_count=0`、`status=SUCCESS` | ✔ |
| WAIT_LANDING 证据 | 内容校验和 `89b82028`（run 41/批 31） | `{"batchId":42,"acceptedUri":"accepted/42","checksum":"89b82028","acceptedRecords":1000,…}` | ✔ **内容级同一性** |
| DB 指标 | 10 行 | `_43` 与 `_41` 五元组并集 = **10**（逐值全等，含 `unit`/`period`/`definition_version`） | ✔ |
| DB 镜像 | `1,4,4,9,1,9,1,1` = 30 | 逐表 = `1,4,4,9,1,9,1,1` = **30** | ✔ |
| 导出 | totalRows 30 / 8 jsonl = 声明 | `totalRows=30`；8 表 rowCount 多重集合相同；8 jsonl 行数 = 声明；8 条 hivePath 存在且有 parquet（0 不符） | ✔ |
| 接口 | 10 项同基线 | `88.16 元 / 0.2 / 10 人 / 0 / 88.16 / 88.16 / 1 单 / 5 次 / 0（v2）/ 5 人`，period 全 `day:2026-09-01` | ✔ |

- run **43** = `SUCCESS`，**8/8 阶段 SUCCESS**，339 s，`target_snapshot_id=S20260901_43`，`source_data_version=r2ctl-b31replay-post-ct-20260912-173418`
- 快照迁移无跳号：`S20260901_43` ACTIVE **v11**（run 43）/ `_42` ARCHIVED v10 / `_41` ARCHIVED v9

**结论（按先写死的判定规则）**：**CT 未破坏指标面 —— 同输入对照通过**。据此可把主跑 P3/P5 由「未命中（前提不成立）」改写为「**同输入对照通过**」；§2 的 6 条红因此**全部归因于输入不同**（批 41 的 2740 行 ≠ 批 31 的 1000 行），可归因性由此闭合。**仍不得**声称原字面预测命中。

**方法学登记（本轮新踩两处，父级自身）**：① 导出 `_export.json` 的表**顺序**与库查询序不同 ⇒ 两序列必须**按表名对齐**再比，按位置比会制造**假红**（已核 A14c 是按表名对齐，故此前 `_42` 的绿非巧合）；② PowerShell 把 `if` 表达式当命令实参是**解析错误**（本轮复犯两次，须先算变量）。

---

## 补记 2026-09-12 17:45 · 同一套 46 条断言对「对照跑」的复跑 ＋ A4 前提判据改为内容级

**① 复跑结果（锚点 = ACTIVE 快照 `S20260901_43`）**：**40 PASS / 6 FAIL**，日志 `.verify\r2\post-run-verify-r2b.log`，退出码 1（`.verify` 不入库）。
**② 基线锚定的指标面断言全部转绿**（这是对 R2-b 结论的独立佐证）：A4/A4b（8 表 = `1,4,4,9,1,9,1,1`=30）、A6/A7/A7b（逐值不符项 **0**）、A14c/A14d（导出序列 = 库序列 = 声明）。同一脚本在 17:30 对**主跑**这些断言是红的 —— 差别只来自输入不同。
**③ 6 条红的成因（全部是判据隐含前提，非回归）**：A5「新快照 = 前一快照」隐含"输入相同"；A6b 比的是**前一快照** `_42`（同输入应对 `_41`）；A10c–A10f 在**批 42** 里找 golden55 的行数（结构上必为 0）。
**④ 判据措辞修正（防下一位读者误读）**：A5/A6b 的**隐含前提已写进断言名**（"输入不同则本条不适用 —— 口径注记，非回归"）。
**⑤ A4 的前提自述改为内容级判据（重要自纠）**：原注解写死"本轮输入 = 批 41（2740 行）"（换 run 即失真），随后改成按**批次号**判"是否同输入" —— 但对照跑的批 42 与基线批 31 **行数同为 1000、`checksum` 同为 `89b82028`**，按批次号会误判为"输入不同" ⇒ 最终改为**按 WAIT_LANDING 证据的 `checksum` 与基线 run 41 比对**（基线侧经 `metric_snapshot.pipeline_run_id` 取）。重跑后 A4 注解自动变为：`本轮输入 = 批 42（1000 行）；基线 = 批 31（1000 行）⇒ 前提成立（内容级同输入：checksum 89b82028 = 基线 run 41 的 89b82028）`。**教训：同一性判据必须落在内容上，不能落在身份（批次号/文件名/行数）上。**



---

## 补记 2026-09-12 19:50 · E3-d 与 E3 收口由父侧补齐 ＋ 两处**口径更正（以此为准）** ＋ 新缺陷 F-85/F-86

**① 接管缘由（留痕）**：原执行泳道 `313a4f8f-833d-4b99-bf72-7d02f661dd22` 于 **17:4x 失败退出**（状态 `ready`、收尾消息为空），留下的原始读数可用，但**未写 README、未执行 E3-d、未完成 E3-b/c/f/g 的逐条判定**。父侧 17:17–19:45 补齐：E3-a 事实、E3-b、E3-c、E3-d（19:19–19:28 实测）、E3-f、E3-g、E3-h 引用、`README.md`、`tools/e3-close-verify.ps1`。

**② 口径更正 1（时长，以此为准）**：本文件 17:41 补记与泳道 R2-b 行写的「run 43 = **339 s**」是**外部墙钟**口径（17:33:43 落预测 → 17:34:18 注入 → 提交等待 → 终态轮询）。DB 权威口径 `pipeline_run.started_at/finished_at` = **293 s**（17:34:58.008 → 17:39:51.973）。**两值都真，但不得混用**；主跑 run 42 = **324 s**（17:22:48.200 → 17:28:12.823）。历史正文按 append-only 保留，本行起以 DB 口径为准。

**③ 口径澄清（判据适用面，非放宽）**：ORDER-1 §4 的 **E3-c** 字面要求「镜像按快照计数与 Hive **正式** `dw_ads` 8 表比对」，但设计上 ADS 只写 `{table}__staging/snapshot_id=…`（`PipelineService` §14.4、`…Job.scala:13`「不碰正式分区」），实测 8 张正式表本轮**未被写入**（`dw_ads.db` 跑前跑后均 **30 文件/54,737 B 逐值相同**，文件 mtime 停在 09-10 18:07）⇒ **对正式表的逐行比对判 N/A（有据）**，可判定部分改为与**实际发布面**（`__staging` → 导出 jsonl / 镜像表 / 接口）逐表按**表名**对齐比对，全部相等。**这不是放宽**：正式表 stale 本身已登记为 F-84。

**④ 口径澄清 2（读数可信度）**：`raw/state-post-20260912-172840.txt` 那帧 `spark-warehouse` = **137 文件 / 1,234,592 B**，与跑前（972/4,977,445）和现静默窗口（972/8,177,073）**互斥**，内部却自洽 ⇒ 判为**运行中枚举的部分读数**，**不采信为「跑后」**；该脚本无逐文件清单、无静默窗口声明（新陷阱 #41）。**E3-f 的跑前/跑后一律用 17:19:15（静默）与 19:25:50（静默）两点。**

**⑤ E3-d（父侧独立实测，不采信自述）**：模块测试 `Tests run: 4, Failures: 0, Errors: 0`（`RUNNING→CANCELLED`、退出码 → SUCCESS/FAILED、未知作业 → SUBMITTED、UTF-8 注入）；父侧长窗口探针（`cmd /c ping -n 60`）`status()` 连续 13 次 `RUNNING` → `cancel()` 即时 `CANCELLED`（+2 s 仍 `CANCELLED`，不回退）；**外部** `Get-Process` 捕获 `cmd/50684` ＋ 子 `PING/5384`，于 cancel 时刻（比代码自称晚 ~0.2 s）**双双消失** ⇒ 真实进程树被回收。**诚实边界**：模块测试自身的 cancel 窗口仅 **20 ms**（19:22:03.433→.453）⇒ 外部轮询**不可能**捕获；父侧 19:18:55–19:21:45 那轮**早于**测试开跑，**不得**读作「进程未创建」。**P4 命中**（真实链 `run.status=RUNNING currentStage=INIT_SCHEMA`）。**F-85**：pipeline run **无取消端点**（`PipelineController` 仅 POST/GET/retry/GET；`/{id}/cancel` 属 `DecisionController`）⇒ 提交器层是唯一取证面。

**⑥ E3 收口读数（30 条判据，脚本自检，exit 1 如实）**：E3-b —— 配对 **10** 行／四元组**差集 0** 行／正向对照（+1）**10** 行；接口 10 项 vs P1-01 冻结基线 **0 处不符**。E3-c —— 镜像 `_43` = `1,4,4,9,1,9,1,1` = **30**、`_41` 同 = 30；导出 `totalRows=30`、8 jsonl 与库侧**按表名对齐 8/8 全等**。E3-g —— events **59/406,114,133 → 61/406,496,411（+2 文件/+382,278 B = 18,430 + 363,848 逐值闭合）**；manifests 40/36,733 → **42/38,861**。输出：`raw/e3-close-readings-20260912-192734.txt`，**PASS 25 / FAIL 2 / DIFF-登记 3**（FAIL = E3-f 字节差 ＋ 17:28:40 帧不采信；DIFF = 三层逐项列举），**红项不隐藏不改写**。

**⑦ E3-f 判据不成立（红）＋ 归因边界**：文件数 **972 全等**；字节 **4,977,445 → 8,177,073（+3,199,628）**：`dw_ads.db` 30/54,737、`dw_dim.db` 3/8,198、`probe_r613.db` 2/1,278 **逐值相同**；`dw_ods.db` 866/**4,567,412 → 7,766,992**、`dw_dwd.db` 57/317,273→317,297（+24）、`dw_dws.db` 14/28,547→28,571（+24）。**归因只到这一步**：ODS **866 文件 mtime 全部落在 17:35:18–17:35:39（20 s 单窗口）** ⇒ 该层为**覆盖写**（否则必残留 run 41/42 旧文件，实测无残留）；对照跑与 CT 前 run 41 **checksum 相同**、分区集相同 ⇒ 字节差**可归因于 CT 之后的 ODS 写路径**。**未逐列证实**（未比 parquet schema/列集合）⇒ **不得**写成「CT 导致 ODS 变宽已证实」。

**⑧ 新缺陷 F-86**：`metric-staging/S20260901_21/_export.json` **不是合法 JSON**（`tables[0].exportFile` 的 Windows 路径反斜杠未转义；严格解析器报 `Bad JSON escape sequence: \D`）。范围已实测界定：**12 个导出目录中仅此 1 个非法**，`_41`/`_42`/`_43` 等 11 个合法 ⇒ 属**历史**缺陷，非本轮引入。

**⑨ 未扰动不变量（E3-d 后复测）**：`analytics_metric` 链 `_43` ACTIVE v11 / `_42` ARCHIVED v10 / `_41` ARCHIVED v9 无跳号；十指标与 R2-b 判定逐值相同；`pipeline_run` 42 行/max 43、`ingestion_batch` 42 行/max 42；8091 pid 61104（19:17:49 起）health `{"ok":true,…}`、jar `33,135,937 B`/sha16 `C099F307D80135E2` 与跑前登记一致。**17:46–19:17 三服务全停**（用户指令），19:17:49 仅重启 8091 ⇒ **19:1x 后不是冻结窗口**，E3-d/收口读数按「后续时点」登记。

**⑩ 父级自身失误（登记，不改写）**：① surefire 属性名写错（`-DfailIfNoSpecifiedTests`）⇒「无匹配测试即 FAILURE、后续模块 SKIPPED」，**不是**测试失败（陷阱 #42）；② 探针 `-Dfile.encoding=UTF-8` **未加引号** ⇒ JVM 收到 `-Dfile` + `.encoding=UTF-8`（陷阱 #1 **第 3 次**复犯）；③ 在收口自检命令里把 `$board.FullName` 用在字符串上、把 `@($a-2,$a-1)` 当数组 ⇒ 两处报错后仍打印出 **LF/CR = 0/0**，即**工具失败退化为读数 0**（陷阱 #39 自我复犯），已用绝对路径重测为 **LF=480 / CR=5（CR 属既有头部，新增内容为纯 LF）**。

**⑪ 未测/不得声称（本轮新增）**：E3-f「跑前跑后逐值相同」；「CT 造成 ODS 字节差」已被证实；「用 API 取消 pipeline run」可行；P2（离线 schema 校验）已复现；E3-c 已对**正式表**逐行比对；`CANCELLED` 在**真实 Spark 阶段执行器**中的消费路径已验证（探针用 ping）；jar 指纹可证明「由当前 HEAD 构建」（F-81）。
