R2 可证伪预测（**跑之前**落盘）— 2026-09-12 17:2x
=====================================================
依据：docs/acceptance/t2rerun-golden55-post-ct-20260912/ORDER-1.md §3
落盘人：总控（父侧）；落盘时机：**在任何 POST / 任何 Spark 之前**
落盘时仓库状态：HEAD = 095118afc7cc1a6a2a6287e54422e43cbdb7e08a（CT 批已集成：5690ffb 落地 + e9a9bdf 登记 + 095118a 归档）
                git status = 干净（`git status --porcelain` 0 行）

-----------------------------------------------------
P1 采集层接受/隔离数与改前一致：该 55 行文件 = **51 接受 / 4 隔离**
   依据：平台侧运行期无 JSON-Schema 校验器（CT PLAN §5 逐字检索 JsonSchema|SchemaValidator|
         json-schema|networknt|everit ⇒ 0 命中）；CT-3 只改 schema 文本/形状与文档；
         CT-1 退休的 EventContract.SOURCE_SYSTEM 唯一读者是对账测试
   若不成立的含义：**重大发现** —— schema 在运行期被某处使用 ⇒ CT PLAN §5 的「0 命中」结论被证伪，
         记新缺陷；**不得**改写成「通过」

P2 离线 schema 校验（Python Draft202012Validator）失败行集合 = 改前集合 **减去** items 相关失败行，
   其余逐行不变（依据：CT PLAN §3 的 E1-c 判据）
   若「改后 0 失败」⇒ 判据被写宽，**不合格**

P3 源 A `day:2026-09-01` 十指标与 P1-01 基线**逐值一致**（值/单位/口径/期间四项）
   依据：T2 首跑已成立（BAD=0）
   若变化 ⇒ CT 改动破坏了指标面，属回归，**必须停并报告**

P4 M1-11：真实链上至少观测到 **RUNNING** 与 **SUCCESS** 两态，且 `cancel()` 后进程树**不再存在**
   （有 pid/退出码证据）。依据：M1-11 的 E2 已完（有界 LRU 256 句柄登记）
   若仍全程 SUBMITTED ⇒ M1-11 修复无效，`REVIEW` 不得收口

P5 ADS 镜像**按快照**计数成立：同一快照 8 表各为 1/4/4/9/1/9/1/1（=30）
   依据：T2 §8.2 已登记「每快照一组」；P1-01 复核补测出 _39 与 _41 各 30 行且逐表序列相同
   若按快照不一致 ⇒ 镜像与 Hive 脱钩（新缺陷）

-----------------------------------------------------
口径红线（ORDER-1 §3 尾 ＋ 陷阱 #29 ＋ T2 §8.2）
- 镜像行数**只用** `COUNT(*)` 且**必须按 `snapshot_id` 分组**；
  **禁止** `information_schema.table_rows`（估算值）；**禁止**用「总行数不变/变化」作判据。
- 任何「零命中/零差异」判据必须带**正向对照**（F-38 ①）。

父侧对 G1 的一处口径更正（**不是放宽判据，而是基线事实**）
- ORDER-1 §1 G1 写「E1-a `Tests run: N, Failures: 0, Errors: 0` 且 N ≥ 改动前基线」。
  该字面判据**在 CT 动工前即不成立**：主检出基线两次独立测量均 **537 tests / 1 failure / BUILD FAILURE**
  （既有红 `IngestionManifestSourceSchemaTest.allOnDiskManifestsStillValidate`，:166-168）。
- 故 G1 的 E1-a 子项以**不劣化**取值：改动后复跑 **537 / 1 / BUILD FAILURE**、
  对比门禁 8/8 PASS（总数不减、无新增失败、6 模块逐名相同、逐模块数不降、既有红逐条保留）。
  证据：`docs/acceptance/ct-integration-20260912/README.md` §10.1/§10.3 与 `raw/ct-e1a-*.log`。
- G1 其余子项**已实测满足**：`git status` 干净、CT 文件清单逐条对得上、
  `contract-specs/specs/warehouse-namespace.v1.json` LF 规范化 sha256 = `463D9DC3503D563D…`（冻结指纹未变）。

未实测声明（写进 R2 README）
- 本单跑的是**重建后**的 platform-app 制品（含 CT 代码面改动）；F-81 未修 ⇒ 库内**无从证明**哪个 jar
  发布了该快照，`raw/jar-fingerprint.txt` 只是**人工旁证**，不等于环境档案。
- 8090/8092 未停机 ⇒ 本单**不是**冻结窗口 ⇒ 所有「文件数/字节」必须带时点，
  **不得**与 T2 首跑的冻结窗口数值直接比较（陷阱 #28）。
- 本单不动集群、不构成 CT-4 裁决、不声称 `items` 字符串形态已归一（属 DWD P2-04/P2-05）。
