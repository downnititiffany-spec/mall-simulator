R2-b「同输入对照」预注册预测（**先落盘，后开跑；禁事后改写**）
=====================================================================
落盘时点：2026-09-12 17:31（在本对照任何一步执行**之前**）
落盘人  ：R2 执行方（一次性泳道）
依据    ：父侧 ORDER 补充指令（R2-b），及 `docs/acceptance/t2rerun-golden55-post-ct-20260912/raw/predictions-before-run.txt`
          （R2 主跑 P1–P5 预注册文件，本文件**不改写**它，只新增一份对照预测）

---------------------------------------------------------------------
一、为什么需要本对照（可归因性论证）
---------------------------------------------------------------------
  R2 主跑（run 42）的采集输入 = 批 41（2740 行 / 6 文件，见 raw/p1-batch41-breakdown.txt）。
  而 P3/P5 的基数是 **批 31（1000 行）**发布的 `S20260901_41`。
  ⇒ 输入**不同** ⇒ 主跑指标与基线不一致**不可归因**于 CT 改动（既不能判"CT 破坏"，
    也不能判"CT 未破坏"）。**CT 是否破坏指标面 = 主跑未测**。
  ⇒ 判它的唯一实测手段 = **同输入复跑**：用批 31 的那 1000 行，在 CT 后重跑一遍，
    与 CT 前的 `S20260901_41` 逐值比。

---------------------------------------------------------------------
二、预测（**写死，跑完不得修改本文件**）
---------------------------------------------------------------------
  D1 【指标值】用批 31 的 1000 行同输入复跑，新快照的 10 个 `day:2026-09-01` 指标
      与 `S20260901_41` **逐值完全相同**（五元组 metric_code / metric_value / unit /
      period / definition_version 全等；差集 = 0 行）。
      预期逐值（照抄 `S20260901_41` 实测，见 raw/metric-verdict-run42-*.txt 的 C0b 段）：
        avg_order_value  88.1600  元   day:2026-09-01  v1
        buy_rate          0.2000  (空) day:2026-09-01  v1
        dau              10.0000  人   day:2026-09-01  v1
        full_refund_rate  0.0000  (空) day:2026-09-01  v1
        gmv              88.1600  元   day:2026-09-01  v1
        net_sale         88.1600  元   day:2026-09-01  v1
        paid_order_cnt    1.0000  单   day:2026-09-01  v1
        pv                5.0000  次   day:2026-09-01  v1
        refund_rate       0.0000  (空) day:2026-09-01  v2   ← 注意 definition_version = v2
        uv                5.0000  人   day:2026-09-01  v1
  D2 【镜像行数】8 张 ADS 镜像表按 `snapshot_id` 分组 `COUNT(*)` = `1,4,4,9,1,9,1,1`，合计 **30** 行，
      与 `S20260901_41` 逐表相同。
  D3 【采集】`POST /api/v1/ingestion/runs` 吃下本对照文件 ⇒ `recordCount = 1000`、`quarantineCount = 0`。
  D4 【阶段】run 到终态 SUCCESS，8/8 阶段 SUCCESS。
  D5 【三通道一致】库（analytics_metric）/ 导出（`metric-staging/<快照>/_export.json` + 8 个
      `ads_*_m.jsonl`）/ 接口（`GET /api/v1/metrics/overview`）三者在指标值与 8 表行数上彼此一致。

---------------------------------------------------------------------
三、判定规则（**先写死**，避免事后挪动门槛）
---------------------------------------------------------------------
  D1 且 D2 逐值全等
    ⇒ 判「**CT 未破坏指标面（同输入对照通过）**」，
      并把主跑 P3/P5 由「未命中（前提不成立）」**升级为**「同输入对照通过 ✓」。
  D1 或 D2 有不符
    ⇒ 属**真回归**（CT 改动确实改变了指标面）⇒ **立刻停止并报告父侧**，
      不得自行改判据、不得重跑掩盖、不得把不符解释成"输入差异"。
  若 D3 采集读数 ≠ 1000/0 ⇒ 输入不纯 ⇒ **停止并报告**（不得带病跑 pipeline）。

---------------------------------------------------------------------
四、已知前提与风险（如实登记）
---------------------------------------------------------------------
  1. 采集是**整目录全量扫描**（非"只采我放的文件"）：`landing/events` 里任何**无断点**的
     文件都会被一起吃掉 ⇒ 必须**先证明无其它待处理文件**，否则输入不纯（步骤见 raw/control-input-purity.txt）。
  2. 采集去重键是**路径**（`file_checkpoint.file_path`）⇒ 换名副本会被重新采集（这是本对照可行的原因）。
  3. 采集**不搬移**源文件：`landing/accepted/31/gen-s3b-1000-20260911.jsonl` 是 1000 行的
     **已过滤副本**（363,848 B），源件仍在 `landing/events`。
     （**口径更正**：父侧此前把"accepted 里出现文件"表述为"源文件被移走"，该表述作废；
       按实测写「复制 + 按结果切分」。）
  4. 8090/8092 **未停** ⇒ 非冻结窗口：跑本对照期间生成器可能继续往 `landing/events` 落新文件，
     从而污染输入。⇒ 必须在采集**紧前**做一次纯净度复测；若采集响应读数 ≠ 1000/0，按 §三 停止。
  5. `businessTime` 必须与主跑一致 = `2026-09-01T00:00:00`。
  6. 本对照产生的 run/快照**晚于**主跑，命名与证据里必须显式标注
     「**本次为 R2-b 对照，非 R2 主跑**」（主跑 = run 42 / 批 41 / S20260901_42 / ACTIVE v10）。

=====================================================================
五、父级冻结应然表（2026-09-12 17:3x 补充；**本段只追加，不修改前文**）
=====================================================================
【A】前置纯净度已由父级独立排除（R2 亦独立复测，见 raw/control-input-purity.txt）：
  landing/events 文件**全部**能在 ile_checkpoint 找到（按叶名去重差集 = 0；断点共 110 行）
  ⇒ 换名副本将成为该批**唯一**输入 ✓
  另记两条机制事实（父级实测，R2 复核一致）：
    - ile_checkpoint.next_offset = **文件字节数**（18430 / 994 / 631946 …）
    - ile_identity 是**秒级时间戳**（非内容哈希）⇒ 采集去重**按路径**

【B】应然表 = S20260901_41（CT 前、run 41、输入批 31）实测值，逐值冻结：
    avg_order_value   88.1600  元    day:2026-09-01  v1
    buy_rate           0.2000  (空)  day:2026-09-01  v1
    dau               10.0000  人    day:2026-09-01  v1
    full_refund_rate   0.0000  (空)  day:2026-09-01  v1
    gmv               88.1600  元    day:2026-09-01  v1
    net_sale          88.1600  元    day:2026-09-01  v1
    paid_order_cnt     1.0000  单    day:2026-09-01  v1
    pv                 5.0000  次    day:2026-09-01  v1
    refund_rate        0.0000  (空)  day:2026-09-01  v2
    uv                 5.0000  人    day:2026-09-01  v1

【C】8 表按快照应然 = 1,4,4,9,1,9,1,1（合计 30），逐表对应：
    ads_active_trend_m 1 / ads_behavior_funnel_m 4 / ads_data_quality_m 4 /
    ads_hot_product_m 9 / ads_operation_overview_m 1 / ads_product_conversion_m 9 /
    ads_sale_trend_m 1 / ads_user_profile_m 1
    （本段与 §二 D2 的 8 个数一致，此处补上**表名对应**，避免顺序歧义。）

【D】比较口径（**判据实现必须照此**）：
  - **值**：按**数值**比（DECIMAL 的 88.1600 与基线 JSON 的 88.16 **等价**）——
    禁止按字符串比，否则会因小数位不同误判。
  - **单位 / 口径(definition_version) / 期间(period)**：按**字符串**比（空单位就是空串）。
  - 对照必须**同输入**才成立 ⇒ 本预测的输入 = **批 31 的 1000 行副本**，**不是批 41**。

【E】判定与措辞（父级指定，照此写）：
  逐值全等 ⇒ 结论写「**CT 未破坏指标面（同输入对照通过）**」，
    依据 = 输入相同 + 四通道读数逐值全等；
    并把 P3/P5 判定从「未命中（前提不成立）」改写为
    「**同输入对照通过；原字面预测因输入不同不适用**」。
    **不得**因此声称原字面预测命中（原字面预测的对象是"与基线一致"，
      而基线的输入是批 31 —— 主跑输入是批 41，两者不是同一个命题）。
  有不符 ⇒ 属**真回归** ⇒ 立刻停并报父侧（不改判据、不重跑掩盖）。
=====================================================================
六、落盘时点补记（父级实测校正；**只追加，不改写正文**）
=====================================================================
  父级实测本文件 mtime = $(D:\Develop_code\GraduationProject\docs\acceptance\t2rerun-golden55-post-ct-20260912\raw\predictions-control-run.txt.LastWriteTime.ToString('yyyy-MM-dd HH:mm:ss.fff'))
  （Get-Item 读数：LastWriteTime = 2026-09-12 17:33:43.245，
    Length = 8181 B，CreationTime = 2026-09-12 17:32:25.568）

  **落盘时点以文件 mtime $(D:\Develop_code\GraduationProject\docs\acceptance\t2rerun-golden55-post-ct-20260912\raw\predictions-control-run.txt.LastWriteTime.ToString('yyyy-MM-dd HH:mm:ss.fff')) 为准**；
  正文第 3 行所写「17:31」系**起草时点**，特此更正（该更正由父级独立实测触发）。
  它与注入件落盘时点 2026-09-12 17:34:18.631 的关系：
    mtime 17:33:43  <  注入 17:34:18.631
  ⇒ **先落预测、后开跑的次序不受影响**，预测仍然是"跑前写死"的。