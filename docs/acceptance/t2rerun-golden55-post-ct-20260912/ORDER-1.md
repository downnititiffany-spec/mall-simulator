# R2 施工单：CT 集成后的真实本地链复跑（golden-55）— 兼收口 M1-11 的 E3 与 M1-5 的守卫

- 日期：2026-09-12　编制：总控　状态：**已登记，未开工**（等 CT 批次集成 + E1-a 复核通过）
- 上游依据：`docs/acceptance/ct-batch-20260912/PLAN.md`（CT-1/2/3 ＋ 一次性升版，L81「T2 重跑排在本批之后」）；看板 M1-5（L81，`REVIEW`）、M1-11（L87，`REVIEW`）；T2 首跑证据 `docs/acceptance/p1-06-golden55-20260912/`
- 纪律：**真实链、先写预测再跑、未实测不写结论**；本单不含集群（E4 已另账，见 `docs/acceptance/e4-cluster-1000-20260912/`）

---

## 0. 命名消歧（防"一个动作挂三个名"）

| 名称 | 是什么 | 现在的状态 | 证据所有者 |
|---|---|---|---|
| **T2 首跑** | 2026-09-12 09:23–09:31 的 golden-55 本地链（run 40 失败 + run 41 成功） | `DONE` | `docs/acceptance/p1-06-golden55-20260912/` |
| **R2（本单）** | CT 集成后**再跑一次**同一类真实本地链；并用**同一次**运行收口 M1-5 / M1-11 的 `REVIEW` 残留 | 本单 | `docs/acceptance/t2rerun-golden55-post-ct-20260912/` |
| **M1-11** | `LocalProcessSparkSubmitter.status()` 恒返回 `SUBMITTED`、`cancel()` 无句柄 ⇒ **存活检测缺失** | `REVIEW`（代码+E2 已完，残留＝**真实 Spark 链路上的 E3 观测**） | 代码面 `analytics-server/connection-ingestion/.../runtime/submit/`；**E3 面 = 本单** |
| **M1-5** | 中立契约目录 `contract-specs` | `REVIEW`（残留＝**CT 批次落地** ＋ **裸锚点守卫**可跑） | `contract-specs/**`、`scripts/check-bare-anchors.ps1`；**守卫实跑 = 本单** |

> **措辞订正（不追改在飞施工单）**：`ct-batch-20260912/PLAN.md:81` 写「T2 重跑 = **M1-11**」，该等式不精确——M1-11 的本体是提交器存活检测，T2 重跑只是**它的 E3 取证手段**。**以本表为准**；待 CT 批复核时在 `PLAN.md` 追加一行回填，不改原句。

---

## 1. 开工门禁（全满足才开工）

| # | 门禁 | 判据（可复算） |
|---|---|---|
| G1 | CT 批已集成进 main | `git apply` 后 `git status` 干净、CT 补丁文件清单逐条对得上；E1-a `Tests run: N, Failures: 0, Errors: 0` 且 **N ≥ 改动前基线**；`contract-specs/specs/warehouse-namespace.v1.json` sha256 仍为 `463D9DC3…`（**冻结指纹不得变**） |
| G2 | 无并发 Maven / 无并发 Spark | 泳道已收工；**陷阱 #26**：集群提交链、本地 Spark、Maven **三者不得重叠**（本机页面文件上限 ~4.2 GB） |
| G3 | 8091 可启动且迁移幂等 | **已实测依据**：`analytics_meta` Flyway 最高版本 = **18**、`success=1` 共 17 条 ⇒ V18 已应用，重启**不再**触发新迁移（首跑时"重启＝对真库执行 V18"的顾虑已消失） |
| G4 | jar 指纹**人工**登记（F-81 未修期间的补偿控制） | 跑前把 `<jar 路径>\|<sha256 前 16>\|<字节>\|<mtime>` 写入本单 `raw/jar-fingerprint.txt`；README 必须写明**库内无从证明**该 jar（F-81） |
| G5 | 落地区时点声明 | T2 §9.7 教训：引用"落地区 N 文件"必须带**时点**。8090/8092 是否为本单短暂停机**需用户裁决**；默认不停 ⇒ 必须记录跑前/跑后时点与文件数 |

---

## 2. 范围

**做**：
1. 起 8091（如未起），走平台真实链：采集 → 8 阶段 → 发布，落在**真实 MySQL + 本地 Spark + Hive** 上。
2. 复算三面判据：① 采集层接受/隔离数；② 源 A `day:2026-09-01` 十指标 vs P1-01 冻结基线逐值；③ ADS 镜像**按快照**计数（口径见 §3 P5）。
3. M1-11 的 E3 观测：真实链上 `status()` 的**状态迁移**（RUNNING/SUCCESS）与一次受控 `cancel()` 的进程树回收。
4. M1-5 的守卫实跑：`scripts/check-bare-anchors.ps1`（集合 ⊆ 台账 ＋ **正向对照**）。
5. 数仓前后一致性（T2 同口径：`spark-warehouse` parquet 文件数/字节）。

**不做**：集群/`yarn` 提交（E4 另账）；`spark-jobs/**` 代码改动；`tests/golden-dataset/**` 与任何证据数据改动；mall-simulator 代码改动（8090 仅作为在跑进程存在）；E5 页面级验收（8091 恢复后可另单）；CT-4 裁决（B-06/Q6 仍待 CT-4 自决）。

---

## 3. 可证伪预测（**跑之前**先落盘到 `raw/predictions-before-run.txt`）

| # | 预测 | 依据（已实测） | 若不成立的含义 |
|---|---|---|---|
| P1 | 采集层**接受/隔离数与改前一致**（该 55 行文件：**51 接受 / 4 隔离**） | 平台侧**运行期无 JSON-Schema 校验器**（CT `PLAN.md` §5 逐字检索式：`JsonSchema|SchemaValidator|json-schema|networknt|everit` ⇒ **0 命中**），CT-3 只改 **schema 文本/形状**与文档；CT-1 退休的 `EventContract.SOURCE_SYSTEM` 唯一读者是对账测试 | **重大发现**：说明 schema 在运行期被某处使用 ⇒ CT PLAN §5 的"0 命中"结论被证伪，记新缺陷，**不得**改写成"通过" |
| P2 | 离线 schema 校验（Python `Draft202012Validator`）失败行集合 = 改前集合 **减去** `items` 相关失败行，其余逐行不变 | CT `PLAN.md` §3 的 E1-c 判据 | 若"改后 0 失败"⇒ 判据被写宽，**不合格** |
| P3 | 源 A `day:2026-09-01` 十指标与 P1-01 基线**逐值一致**（值/单位/口径/期间四项） | T2 首跑已成立（`BAD=0`） | 若变化 ⇒ CT 改动**破坏**了指标面，属回归，必须停并报告 |
| P4 | M1-11：真实链上至少观测到 **RUNNING** 与 **SUCCESS** 两态，且 `cancel()` 后进程树**不再存在**（有 pid/退出码证据） | M1-11 的 E2 已完（有界 LRU 256 句柄登记） | 若仍全程 `SUBMITTED` ⇒ M1-11 修复无效，`REVIEW` 不得收口 |
| P5 | ADS 镜像**按快照**计数成立（同一快照 8 表各为 `1/4/4/9/1/9/1/1`） | T2 §8.2 已登记"每快照一组"；本轮 P1-01 复核补测出 `_39` 与 `_41` 各 30 行且逐表序列相同 | 若按快照不一致 ⇒ 镜像与 Hive 脱钩（新缺陷） |

> **口径红线（陷阱 #29 ＋ T2 §8.2）**：镜像行数**只用** `COUNT(*)` 且**必须按 `snapshot_id` 分组**；**禁止**用 `information_schema.table_rows`（估算值）、**禁止**用"总行数不变/变化"作判据。任何"零命中/零差异"判据必须带**正向对照**（F-38 ①）。

---

## 4. 出口证据（每条都要落盘，禁止只写结论）

| # | 证据 | 通过判据 |
|---|---|---|
| E3-a | 真实链 8/8 阶段 | 每阶段 `SUCCESS`；落 `runId` / `ingestion_batch` id / 快照 ID / 起止时刻 |
| E3-b | 指标逐值 | 自连接差集 **0 行**（不依赖被验脚本自述） |
| E3-c | 镜像按快照 | 8 表 `COUNT(*)` 与 `SUM(snapshot_id=…)`，与 Hive `dw_ads` 8 正式表序列逐个比对 |
| E3-d | M1-11 观测 | `status()` 迁移序列 ＋ 一次 `cancel()` 前后 `Get-Process` 对照（含子进程/退出码） |
| E3-e | 裸锚点守卫 | 守卫集合 ⊆ 台账；**正向对照**（一条带文件名的锚点必须**不**被计入）；台账"只减不增" |
| E3-f | 数仓前后一致 | `spark-warehouse` parquet 文件数与字节跑前/跑后逐值相同（若不同必须逐项解释） |
| E3-g | 落地区时点 | 跑前/跑后时点 ＋ 文件数 ＋ 字节（带时点表述） |
| E3-h | jar 指纹 | `raw/jar-fingerprint.txt`（G4） |

**证据目录**：`docs/acceptance/t2rerun-golden55-post-ct-20260912/{README.md, raw/, tools/}`；脚本一律**入库**并**自检**（陷阱 #27：脚本须自带"应然值 vs 实际"断言与条数自检）。

---

## 5. 禁改面

- 不改 `spark-jobs/**`、`mall-simulator/**`、`tests/golden-dataset/**`、任何**历史证据**（append-only：新读数写新文件）。
- 不改 `contract-specs/specs/warehouse-namespace.v1.json`（指纹 `463D9DC3…`）。
- 不做破坏性数据操作（不删分区、不改历史快照状态、不清理 `landing/`）。
- 不启停 8090/8092 除非用户裁决（§1 G5）。

## 6. 未实测 / 不得声称（写进 README）

1. **F-81 未修前**：库内无法证明"哪个 jar 发布了该快照"；本单的 jar 指纹只是**人工旁证**，不等于环境档案。
2. **集群档**不属本单（E4 已另账）：**不得**据本单声称集群行为。
3. 本单**不构成** CT-4 裁决，也不重开 B-06/Q6。
4. `items` 字符串形态的**归一化**属 DWD（P2-04/P2-05）；本单只观测"契约加性接受"的后果，**不得**声称已归一。
5. 若 8090/8092 未停机：本单**不是**冻结窗口，所有"文件数/字节"必须带时点；**不得**与 T2 首跑的冻结窗口数值直接比较（陷阱 #28：先对齐口径）。

## 7. 回滚与留痕

- 本单**不改代码**（除必要的跑前进程启停）⇒ 回滚 = 停 8091、保留全部证据文件。
- 若 G1 复核发现 CT 集成有问题：**先回滚 CT 提交**（`git revert`），本单不开工。
- 开工与收工各在看板追加一行（开工登记 / 出口结论，含 `file:line`）。
