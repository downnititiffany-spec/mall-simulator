# 本目录证据的脱敏说明（2026-09-12）

- 本目录下 `namenode-jmx.json`、`hdfs-dfsadmin-report.txt` 中的集群内网地址已在落库前掩码：完整地址 → `192.168.18.10x`（末位通配）。
- 主机名 `node01` / `node02` / `node03` 保留原样：它们是通用名，且与掩码后的地址一一对应（原始映射见本机 `hosts` 文件，不入库）。
- 掩码只影响地址字面量，**其余字节未改动**（JSON 仍可被 `ConvertFrom-Json` 解析；`dfsadmin` 报告的数字与主机名未变）。
- 依据：用户 2026-09-12 指示「涉及隐私的都脱敏处理」；`root/123456` 一类本地测试口令按同一指示**不处理**。
- 补记（2026-09-12 15:46）：`yarn-logs-0004.log`、`yarn-probe-submit.log` 按同口径掩码 —— 实证命中 `192.168.18.101`、`192.168.18.103`，掩码为 `192.168.18.10x` 后严格正则 `192\.168\.18\.\d{3}` 残留 **= 0**；掩码只替换地址字面量，其余字节未改动。
- **误报备案（避免后续复查重复报警）**：全目录私有地址复检会命中 `10.14.2.0` —— 经上下文核对为 Apache Derby 的**版本号**（`derby-10.14.2.0.jar`，出现于 `__spark_libs__` 清单与 HDFS jar 路径），**不是内网地址**；同类误报还有版本串截断产生的 `0.10.9.7`、`1.1.10.3`。凡私有地址复检命中，**必须先看上下文再定性**。