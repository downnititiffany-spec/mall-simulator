# P2-03 代理键统一 总控裁决书（D-138）（2026-09-12 20:4x）

- 裁决人：总控（父会话）　对象：P2-03 泳道 B 交付（报告 `IMPL-REPORT.md` 44,958 B / `84ECD77E…9718`；证据 `impl/` 13 件）
- 已入库：代码提交 **`ccc1dff`**（白名单 **11** 文件 / 1,261 insertions / 32 deletions）；证据提交 **`f51d96b`**（13 文件 / 1,822 insertions）；均已推送，`HEAD == origin/main`

## 0. 裁决结论：状态 = **`REVIEW`**（**不是** `DONE`，**不是** `DONE_LIMITED`）

理由：任务的**出口判据之一（A4「记一条 DQ」）零实现** ⇒ 出口判据未达 ⇒ 上限被卡在 `REVIEW`（交付完整、待处置），连 `DONE_LIMITED` 都不成立。泳道自评与此一致，总控**采纳泳道的不声称声明**。

E1–E5 逐层（本题适用面）：

| 层 | 判据 | 实测 | 裁决 |
| --- | --- | --- | --- |
| E1 编译 | 编译通过 | `e2` 日志 `BUILD SUCCESS` / `exit=0`（与 E1 **同一条命令**，见 §2） | ✅ 达成 |
| E2 模块自测 | 模块测试绿 | `Tests: succeeded 111, failed 0, canceled 0, ignored 0, pending 0` / `Suites: completed 15, aborted 0` / `All tests passed.` | ✅ 达成（111→111、0 跳过） |
| E3 本地真实链 | 本地真实链取证 | A5 JOIN 15/15、16/16；A7 `dim_product` 4 行、`product_key`/`category_key`/`brand_key` 各 4 非 NULL、`parent_category_key` **0**（阳性对照 `product_key > 0` 已断言）；`parent_category`/`coupon` **无数据未取证** | 🟡 **部分** |
| E4 集群 1,000 行 | 集群链 | **未跑**（A8 T2 未跑） | ⬜ 未达 |
| E5 页面可用 | 员工可用页 | 未做 | ⬜ 未达 |

## 1. 父侧独立复核（提交前已跑，非采信泳道自报）

| 判据 | 实测 |
| --- | --- |
| 报告指纹 | **44,958 B / `84ECD77E4124F682…`**（与泳道**更正后**指纹逐位一致；更正前 `35242F05…` 已作废） |
| `e2-scala-test.log` | 16,815 B / `C41D6295…` / mtime **20:39:49** |
| `e1-scala-test.log` | 20,574 B / `8B6B8EAA…` / mtime **20:10:02**（原样留存，**未**被覆盖） |
| `git status --porcelain` | 提交前 15 项 = 我的 11 代码 + 我的 2 交付目录 + 兄弟泳道 1（`?? analytics-server/source-profiles/mock-mall.v1.json`）+ 父侧 1（看板，已入库）⇒ **泳道越界条目 = 0** |
| `OdsV2SchemaOwnerSpec.scala` 相对 HEAD 差异 | **0** 行（撤回自证成立） |
| `IdCodec.scala` / `OdsLoadSql.scala` / `contract-specs` 差异 | 各 **0** 项（未动契约） |
| 越权探针是否复活 | `P2CountProbeSpec.scala` **不存在**、`P2SqlLayerProbeSpec.scala` **不存在**、嵌套 `spark-jobs/spark-jobs` **不存在** |
| 撤回表口径 | `dwd_surrogate_key_quality` 命中 **1** = `SurrogateKey.scala:343` **注释**；阳性对照 `dwd_reject_record` **13** ⇒ 检索有效 |
| 段落计数断言 | `statements.size should be(37)` 命中 **1**（已回位） |
| 提交后工作区 | **2** 项，均为兄弟泳道 ⇒ **代码类未提交条目 = 0** |

## 2. 五项待裁决 —— 逐条裁决

1. **A4「记一条 DQ」走哪条路 ⇒ 采纳泳道方案 ①**：**本轮不新增表**；空 id ⇒ NULL 键 + 不丢行（D-087 前两条**已实现并已断言**）；「记一条」**登记为已知缺口**（`openItems`）。
   - 另**单开任务 `P2-03-k`**（不属本行）：在**平台侧**用既有 `data_quality_result`（`DataQualityResult.java:15` `@TableName`，属 `analytics_meta`）承接空 id 记录 —— 该表由平台侧写入方（`QualityChecker`/`DataQualityGate`/`PipelineService`）拥有，`spark-jobs` 侧引用实测 **0** ⇒ 只能由平台侧实现。
   - **不采纳**方案 ③（扩 `dwd_reject_record` 枚举 ＋ 改 `INSERT INTO`）：与 D-087 L53「不进 quarantine、不丢行」及 L99 对照表双重冲突。
   - **不得声称**：D-087 已完全满足（当前只满足前两条）。
2. **E2 能否作验收证据 ⇒ 能**。E1 与 E2 是**同一条命令**（`spark-jobs` 模块测试）在两个时点的两次运行；以**后一次**（E2，20:39:49）为验收证据，E1（20:10:02，`succeeded 104, failed 7` / `BUILD FAILURE`）**原样保留为历史红**、不删不美化；报告中两者读数与时间戳**必须并列**。
   - 泳道「E1 七例归属 = **0 例**由 D-096 撤回引发」**采纳**（依据为时间戳 20:10:02 早于 D-096 的 20:12/20:13、七例无一例提及 `statements`/DQ 表名、用例总数 111→111 未减 ⇒ 属**测量**而非推断）。
3. **`SurrogateKey.scala:343` 残留（含已撤回表名的注释）⇒ 保留**。操作口径改为双口径并列：**可执行语句口径 = 0**（阳性对照 13 ⇒ 检索有效）、**字面口径 = 1（仅注释）**。
   - **父侧勘误**：我此前「全仓归零」的表述**不准确**，以此为准 —— 归零的是**可执行语句**；**治理留档注释保留**（删掉它反而提高复发风险）。若某工具要求字面归零，须**逐条**说明理由后另裁。
4. **`IdCodecSpec` 计数断言 7/7 ⇒ 采纳**（结构性推导：新键每列引用原始 id 3 次 = 取值 1 ＋ 判空 2；旧列 2 ＋ JOIN 1 ＋ `IS NOT NULL` 1 = 3；3+3+1 = 7）。
5. **P2-03-b（删 `IdCodec`）⇒ 现在不开**。开关前置：① A8 T2（55 条真实链 ＋ **新 jar**）通过；② 新键真链回读取证。
   - **合并口径**：M3 §6.4 第 6+8 步（同一份 55 行输入、本地与集群各一次、**重建后的 jar**）**同时充当 A8 T2** —— 但必须在回读中额外取证 `dim_product` 的 `product_key`/`category_key`/`brand_key` 非 NULL 计数（否则该次运行不构成 A8 T2 证据）。此合并**合法**：同一棵树、同一 jar、同一输入。

## 3. 由本泳道实测**新增的一条跨任务硬约束**（写进 P2-02 的实施口径）

泳道实测 `DwdSql.scala:62-65` 既有写入模式为 `INSERT OVERWRITE TABLE …dwd_reject_record PARTITION(dt='$dt')` ⇒ **同 `dt` 先清空**。因此：

- **P2-02（D-114）向 `dwd_reject_record` 写拒绝行时，禁止沿用 `INSERT OVERWRITE … PARTITION(dt)`**（会把同分区既有 `DUPLICATE_EVENT` 记录清掉）；必须用**追加语义**（`INSERT INTO`）或等价的不破坏既有分区的机制。
- 该约束是 D-114 的**补充**，不改变 D-114 的通道选择与枚举冻结（D-116）。

## 4. 采纳泳道的两处自我更正

1. 「施工单把两个测试文件写在 `main` 侧、需裁决」⇒ **泳道读错**：施工单 **L59** 写的就是 `test`，与实测一致 ⇒ **无需裁决**（父侧 20:20 审计与实测逐项吻合）。
2. 「category/brand 在 `main` 侧零命中」⇒ **泳道说错**：实测 `DimSql.scala:76/78` **有**生产调用点，`[A7]` 实测 `category_key` 4/4、`brand_key` 4/4 非 NULL ⇒ 准确口径是 **user/product/category/brand 已取证；`parent_category`、`coupon` 未取证（夹具 55/55 行不含该字段）**。
3. 泳道承认**误读 A4 的「既有」二字**、新增 DQ 表并改了白名单外文件、且在自己注释里把推断写成「施工单允许」——**父侧记录在案**：撤回已自证（§1），报告未删未美化；该类偏差**不得**以「已撤回」为由从报告中消失。

## 5. 不得声称（承接报告 §12 的 14 条，父侧追加）

不得声称 P2-03 `DONE`／`DONE_LIMITED`；不得声称 A4 已实现或 D-087 已满足；不得声称 `IdCodec` 已替换/退役；不得声称集群复跑过；不得声称 Java/Scala 已同值；不得声称 `parent_category`/`coupon` 已取证（也不得反向声称 category/brand 无生产调用点）；不得声称契约已冻结或升版；不得声称「D-096 撤回引发 7 例失败」；**新增**：不得声称「字面口径全仓归零」（真实是**可执行口径 0 / 注释 1**）；不得声称 M3 §6.4 第 6+8 步已完成（未跑）。

## 6. 本裁决未做／未测

未改任何代码/DDL/契约；未跑 Maven/真实链/集群；未执行 `selfcheck.ps1`（泳道自报 §0–§5 全 PASS，父侧**未复跑**）；未复核 `IMPL-REPORT.md` 全部 44,958 B 内容（§1 的表为**抽查级**独立复核 ＋ `file:line` 与 `git` 判据）。
