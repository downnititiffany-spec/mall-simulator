# P2 语义注册表与字段映射层 —— 总控裁决（D-095…D-103）

- **裁决时间**：2026-09-12 14:0x（墙钟以 `Get-Date` 为准；本文件落盘时 HEAD = `143a0d3`）
- **裁决人**：总控（session 父级）
- **依据顺序**：① 冻结契约（`contract-specs/**`） ② 指导书 **V2.4**（sha256 `580AD7BE…`，57,442 B） ③ 看板 V2.2 ④ 专项设计书（`docs/superpowers/specs/2026-09-11-mall-agnostic-platform-design.md`，sha256 `BDC62679…`，25,299 B） ⑤ V2.1 及更早
- **证据基座**：`docs/acceptance/p2-semantic-registry-20260912/RECON.md`（75,748 B / 973 行 / sha256 `4CE6A21A…`，已由总控独立复核指纹并入库）及其 `raw/` 8 个原始读数
- **效力**：本文件为总控裁决。任何泳道实施本层时**不得**与本裁决冲突；若发现本裁决与冻结契约冲突，**停下来上报**，不得自行取舍。

---

## 0. 勘误（不影响任何结论）

- **E-R1**：`raw/cited-file-fingerprints.tsv` 实际 **23 行**（其中 1 行为 `MISSING` 对照项，真实存在文件 22 个）。RECON §13.2 与泳道口信称"**24 项**"，**以此为准 = 23 行**。该勘误不改变 §13.2 的用途（行号有效性以指纹表为准）。

## 0.1 总控已补答 RECON 的 U-10（活表判定），并据此扩展一条规则

- **活表 = `analytics_metric.metric_snapshot`**。三条互证：① 平台三数据源配置（`PLATFORM_META_URL`→`analytics_meta`；`PLATFORM_METRIC_PUBLISH_URL`/`PLATFORM_METRIC_READ_URL`→`analytics_metric`）；② `analytics_metric` 侧 `max(published_at)=2026-09-12 09:31:13.388`，而 `analytics_meta` 侧停在 **2026-09-07 19:33:50.123**；③ 已入库日志 `8091-stdout.log` 末行 `09:31:13.399 metric publish: 快照 S20260901_41 发布成功` 与该 `max(published_at)` **同批同一秒**。
- **规则（并入 F-40）**：凡涉及同名表，**必须先声明目标 schema 与判定依据**，再执行任何读/写；`analytics_meta.metric_snapshot` 为陈旧同名表，**不得**作为任何活表使用。

---

## D-095（TQ-1，最高优先级）—— 映射载体 = **③ 两者并存，但所有权严格不重叠**

- **裁定**：**采纳 ③**：
  1. **文件（源画像）拥有**「源词汇 → canonical」这一段：每个源一份版本化 JSON，随 git 变更 ⇒ 保住设计 `:149` 的**接入证据**（"接入新商城 = 新增一个 JSON"）。
  2. **DB 拥有**「canonical → 分析含义」的注册表 + **生效状态/版本/生效区间**（可查询、可审核）⇒ 满足 V2.4 L344–L350「版本化配置**和审核记录**」与 L324 `schema_mapping`、L348「没有 ACTIVE mapping ⇒ 隔离」所需的**可查询 ACTIVE 状态**。
  3. **不得双向**：DB 中**不得**出现"源词汇→canonical"的第二份权威副本；文件里**不得**定义 canonical 字段集（见 D-099）。任何一侧出现对方职责的副本 ⇒ 视为**重复所有者**，按反熵纪律**先立新所有者、再删旧副本**，且删除需总控确认。
- **理由**：设计 `:213` 已给出边界原则（"profile 管源词汇→canonical；DB 管 canonical→分析含义"）；模式 B（配置驱动映射）与之同向；③ 唯一代价"双所有者风险"由上述不重叠切分与"禁双向"条款消解。
- **代价（明示）**：契约工作量最大（源画像契约 + 语义注册表契约两份），且必须同批落锚点与版本号（见 D-102/D-101）。

## D-096（TQ-5）—— 映射目标 = **按契约取齐**（不得为保现状降口径）

- **裁定**：**采纳 ①**。映射层的正确性判据是 **`canonical-event.v1` 契约**，不是今天的 `EventContractValidator` 行为。
- **明示后果（必须记账，不得模糊）**：按契约口径，真实夹具 55 行中**仅 26 行合规、25 行违约**（RECON §8.4 独立复算，且与真实 manifest 的 51 接受/4 隔离逐字一致）⇒ 现行"51 行成功"的**验收基线将在新口径下改变**。**该基线的改变是裁决接受的代价，不是缺陷**。
- **顺序**：**先冻结契约口径（CT 批）再落"映射执行器"**；但**载体与契约制品**（D-095/D-102）部分**不因此阻塞**，可先行。
- **禁止**：不得把 25 行违约数据固化为"映射成功"；不得为让存量数据"通过"而放宽契约。
- **与 TQ-6 的关系**：是否在本层一并收紧校验器（TQ-6）**本轮不裁**（见 D-103），但**无论怎么裁，映射层的目标都是契约**。

## D-097（TQ-7 ＋ F-41 合并）—— `source_system` 必须改为**由源提供**，纳入契约变更批

- **裁定**：`canonical-event.v1.schema.json` 把 `source_system` 钉成 `const: "mock-mall"`（契约 `:39`）与既有 **F-41**（`EventContract.java:17` 字面量 `"mock-mall"` ＋ `CanonicalEventSchemaParityTest:65-73` 把 schema `const` 锁死）**是同一处冲突**：与「分析系统不得与商城写死」「数据类型/种类不得写死」的既定方针**直接冲突**，且**接第二个源（P5 异种源 B）时必然撞上**。
- **裁定内容**：目标形态 = 选项 ② 方向（**由源注册表/画像提供**，`source_registry.source_code` 为运行时真相）。
- **但**：① 冻结契约是最高权威 ⇒ **不得**由本层或任何实现泳道私自做破坏性变更；该变更**纳入契约变更批（CT）**，走 VERSION minor 递增 + 变更记录。
- **过渡期硬约束**：契约变更落地之前，**任何新代码不得依赖 `const "mock-mall"`、不得依赖字面量做分支**；`--sourceSystem=<source_registry.source_code>`（P2-07 泳道 A12）为唯一注入通道。

## D-098（TQ-12）—— 真实画像**求实**，另立**测试夹具画像**取得映射能力证据

- **裁定**：**采纳"分流"**（不是 TQ-12 的两个原选项之一）：
  1. `mock-mall.v1.json`（P3-01 交付物）**照实**写参考商城的**同名映射**。**禁止**把它写成"故意错位"的词汇表来制造证据 —— 那是**把 Mock 当真实**，违反铁律。
  2. 为了取得"映射真的生效"的证据，**另立一份测试夹具画像**（非真实源；文件名与目录须能一眼区分，如 `fixtures/vocab-shifted.v1.json`，并在文件内 `x-contract-status` 标注"测试夹具、非真实源"），用它做 E2/E3 的映射能力证明。
  3. 正式的异源映射证据**待 P5 异种源 B 接入后**取得。
- **理由**：直击看板已登记的 **F-25**（参考商城状态词与规范 enum 恰好同名，**掩盖**了"缺少映射"，第二适配器会立刻暴露）⇒ 若只有同名画像，"没有映射"与"映射正确"在证据上不可分。

## D-099（TQ-11）—— canonical 骨架的**唯一所有者**是契约 schema

- **裁定**：`contract-specs/schemas/canonical-event.v1.schema.json` 的 properties/`$defs` 是 canonical 字段集的**唯一所有者**；源画像与语义注册表**只做映射**，**不得**重新定义 canonical 字段集、**不得**新增 canonical 字段。
- **冲突口径**：两个源映射到同一 canonical 字段时**不冲突**（多对一是映射的常态）；真正需要唯一性的是 **canonical 骨架**，已由本条钉住。
- **设计 `:147` 的落实**：「缺失的映射项 = 该源没有这个语义（不是默认值）」⇒ 实现侧**禁止**任何"缺映射就给默认值"的兜底路径。

## D-100（TQ-13）—— 映射层**不新增物理落层**；namespace 解析**单一所有者**

- **裁定**：
  1. 映射层**不新增**物理落库层：canonical 事件的物理形态就是 **landing 的规范事件 JSON**（既有物），ODS 侧由 P2-01 的 `payload_json` **原样保真**承载。
  2. 映射层的产物 = **源画像（文件）＋ 语义注册表（DB）**，供分析侧**按源解释** canonical 字段。
  3. **namespace 解析的单一所有者 = 既有 `ActiveProfileWarehouseNamespaceProvider`**（由快照 `sourceId` 解析一次，P2-07 已在其上工作）；映射层**不得**自建第二条解析链，`raw_path`/"源级 ODS"/命名空间三处**共用同一 `sourceId` 链**。
- **理由**：RECON 已警示三处会互相踩；单一所有者是防踩的唯一低成本办法。

## D-101（TQ-14）—— 新增本层契约前，`contract-specs` 的 V2.4 复读**必须**先完成

- **裁定**：**是**。`contract-specs/README.md:38` 自述"本目录尚未按 V2.1/V2.2 复读"、`:3` 仍称依据 V2.1 §3.1，而权威已到 **V2.4** ⇒ 新增本层契约制品前必须先完成该复读（含 §5.2/§5.4 引用点）。
- **归属**：**并入契约变更批（CT）**，**不**归 M1-5（M1-5 是锚点审计，职责不同）。

## D-102（TQ-4）—— 新契约制品命名与初始状态

- **裁定**：命名 **`semantic-mapping.v1.json`**（对齐 L324 表名，便于与 `source-profile` 区分）；`status` 初值 **`DRAFT`**（比照 `surrogate-key.v1.json` 的 `DRAFT-2026-09-12` 先例）；制品**必须**满足 D-065 的裸锚点台账规则（新增锚点即入台账，只减不增）。
- **版本号规则**：沿用既有口径「按落盘时的当前值 minor 递增」（现 `contract-specs 2.1.0`）。

## D-103 —— **本轮不裁**的 TQ 项（明确记录"未裁 ≠ 遗漏"）

以下各项与 **D-095 载体形态**强耦合，**留待载体落地时一次性裁**，此前任何人不得据其中某一选项先行实施：

| 未裁项 | 内容 | 前置 |
|---|---|---|
| **TQ-2** | "审核记录"落在 `operation_audit_log` 复用还是新表 | D-095 载体落地（表形态确定） |
| **TQ-6** | 校验器是否在本层一并收紧、存量 25 行 accepted 数据如何处理 | D-096 已定目标为契约；**处理存量数据的方式**待裁（涉数据处置，须总控确认） |
| **TQ-8** | quarantine 四要素落地方式（加列 / 新表双写 / 最小加两列） | 涉存量 104 行，需变更方案与备份 |
| **TQ-9** | `reason_code` 闭集取计划 6 类还是 L348 四类 | 与 TQ-8 同批 |
| **TQ-10** | ACTIVE 状态的表达与查询 | 已由 D-095 定方向（DB 侧承载）；**具体表形态**待载体落地 |

---

## 未裁项之外：本轮裁决**不**触碰的东西

- 不改任何冻结契约文本（D-097/D-101 的契约侧变更**只能在 CT 批**做，且必须先 VERSION 递增）。
- 不改任何实现代码；不新增/删除任何表；不动真实库（本轮无任何库写）。
- 不推翻 RECON 的任一读数；本裁决只做**方向与边界**的判定，具体形态（表结构、列名、迁移号）待载体落地时另出施工单。

## 停止条件（任一触发即停并上报）

1. 发现本裁决与**冻结契约**冲突（以契约为准，停止实施并上报）；
2. 实施中需要**破坏性变更**（改契约 const、删列、改存量 104/25 行数据语义）而未获总控确认；
3. 发现同一职责出现**第二个所有者**（DB 里出现源词汇映射副本、或文件里定义 canonical 字段集）；
4. 需要跨出既有泳道边界（`spark-jobs/**`、`analytics-server/**`、`synthetic-data-generator/**` 各有在写泳道）时。

---

## 勘误与归属复核（2026-09-12，总控；**append-only，上文任何字未改**）

> 本节为**总控自纠**：本轮 D-095…D-103 落盘后，总控按反熵纪律回查既有裁决台账，发现其中 **4 条与既存裁决重复或部分重复** ⇒ 按"同一职责不得有两个所有者"处理，逐条降级为交叉引用并写明增量边界。
> **本文件 L3 的"裁决时间 2026-09-12 14:0x"为估算值、不可用**；真实落盘时刻以 commit `4124d00` 的 `%ci` = **2026-09-12 13:38:03 +0800** 为准（见 E-R3）。

### E-R2 归属复核表（防重复所有者）

| 本轮条 | 归属判定 | 既存所有者（以之为准） | 本条**真正新增**的部分 |
|---|---|---|---|
| **D-095** | 新增 | 无（设计 `:213` 是边界**原则**，非裁决） | 载体三选一**落 ③**；源词汇/分析含义的不重叠切分；**禁双向副本** |
| **D-096** | 新增 | 无 | 目标 = **契约**（非今日校验器）；**明示"51 行成功"基线将改变且被接受** |
| **D-097** | **重复（零新增）** | **D-061（CT-1，`docs/acceptance/ct-batch-20260912/RULINGS.md` §1）** | **无** |
| **D-098** | 新增 | 无 | TQ-12 **分流**：真实画像求实 ＋ 另立测试夹具取证据 |
| **D-099** | 新增 | 无 | canonical 骨架**唯一所有者 = 契约 schema**；禁"缺映射给默认值" |
| **D-100** | **部分重复** | **D-072/D-073/D-074（P2-07 `RULINGS-20260912.md` L30–40）** | namespace **所有者归属不得重裁**；新增仅两条：**①** 映射层**不得自建第二条解析链**；**②** 映射层**不新增物理落层** |
| **D-101** | **部分重复** | **D-091（P2-03 `RULINGS-20260912.md` L74，V2.4 复读的一般义务）** | 新增仅两条：**①** `contract-specs/README.md` **属**该义务范围（其 `:38` 自述未复读、`:3` 仍称 V2.1）；**②** 归属 = **CT 批**（非 M1-5） |
| **D-102** | **部分重复** | **D-092（P2-03 L79–81：版本号规则、D-065 裸锚点对新文档同样适用）** | 新增仅两条：**①** 命名 `semantic-mapping.v1.json`；**②** 初值 `status=DRAFT` |
| **D-103** | 新增 | 无 | 未裁项登记（TQ-2/6/8/9/10） |

- **「以此为准」**：上表判为"重复/部分重复"的部分，**一律以既存裁决为准**；本文件 D-097/D-100/D-101/D-102 的相应表述**降级为交叉引用**，**不得**被任何泳道当作独立授权或第二次裁决。

### E-R2.1 D-097 逐条对照（证明"零新增"）

D-061 已裁定的内容（逐条对应 D-097 的全部主张）：① `canonical-event.v1` 取消 `source_system` 的 `const`，改 `{ "type": "string", "minLength": 1 }` 且**刻意不加 `enum`/`pattern`**（值域单一所有者 = 注册表）；② 取值 = 该事件所属源的 `source_registry.source_code`，由平台经 `spark-submit --extra` 注入（**引 D-056**）；③ `EventContract.java:17` **退休该常量**（delete-first）；④ `CanonicalEventSchemaParityTest.java:65-73` 断言改为**结构化守卫**「`source_system` 不得带 `const`」＋**正向对照**（`schema_version` 仍带 `const`）；⑤ `contract-specs/VERSION` 随该批递增。
⇒ **D-097 无一条超出 D-061**。故：

- **F-41 的归属更正**：F-41（`EventContract.java:17` 字面量 ＋ `CanonicalEventSchemaParityTest` 锁定 schema `const`）**不是新风险**，而是 **D-061 第 ③④ 条"尚未落地"的现状快照**（13:36 实测代码仍在）。登记口径改为「**F-41 = D-061 未落地，随 CT-1 关闭**」，**不另立所有者、不新增处置动作**。
- **A12 的措辞更正**：A12 所要求的"平台注入 `--sourceSystem`"**不是新要求** —— 它由 **D-056**（经 D-061 §1 L33 复述）早已裁定。前一轮看板行（L445）把它写成"抓到一处跨组件接口缺口"**失真**（隐含"新发现"）；**正确表述**：该要求**早已裁定但从未实现**，A12 的增量**仅为实施指令**（落到 `JobCommandBuilder` ＋ fail-closed ＋ 两条逐参数断言），属**施工**而非**裁决**。缺口仍然真实且会打断真实链（spark-jobs 侧 fail-closed 已落地），故 A12 继续有效。

### E-R3 时间戳勘误（**扩展**看板 L444 的勘误；含总控自身 5 行）

- **机械扫描口径**：正则匹配看板中**任意位置**的 `2026-09-12 HH:MM`（含粗粒度 `H:xx` 与单元格内），与 `Get-Date`（真钟 **2026-09-12 13:39:04**）比较；`MM` 为 `x` 时按 `:00` 保守比较。
- **实测结果**：未来时间**共 14 处**，位于 **L53、L434、L435、L436、L437、L440、L441、L442、L443、L446、L447、L448、L449、L450**。
  - 已知并在册：**L441/L442/L443**（已由 L444 勘误作废）。
  - **本轮新发现（粗粒度族）**：**L434（14:0x）、L435（14:0x）、L436（14:1x）、L437（14:2x）、L440（15:2x）、L53（14:2x，单元格内）** —— 偏移 **+21min ～ +1h45min**。
  - **本轮新发现（总控自己写的 5 行，精确到分）**：**L446（13:41）／L447（13:47）／L448（13:52）／L449（14:02）／L450（14:10）** —— 偏移 **+2min ～ +31min**，且**随写入顺序单调递增**。
- **根因（自认，不推给工具）**：并非"读了错误的钟"，而是**按上一行顺延/估算**；偏移随写入次数**累积**。L444 已立规则「时间戳必须读 `Get-Date` 或 `git log`，不得估算」，而**此后 5 行内被总控自己违反**（L446–L450）。本文件 L3 同病。
- **规则升级（取代 L444 的口径，仍不改 L444 原文）**：
  1. 看板行时间戳**必须**是**同一条命令内** `Get-Date`／`git log --format=%ci` 的**机器输出**并**当场回显**；**禁止**手写、**禁止**由上一行顺延、**禁止**"大致现在"。
  2. 已存在的未来时间一律按「**时刻不可用**」处理，**不得**反推真实时刻；引用时以 **git commit `%ci`** 为唯一权威时间轴，看板时间戳仅作人类可读提示。
  3. 自本行起，凡总控追加的看板行，其时间戳一律由生成该行的同一条命令写入（模板化，避免手抄）。

- **未做**：未修改上文任何已写时间戳（append-only）；未重写 L444；未删除任何行；本轮无任何库写、未改任何泳道代码、未停启 809x。
