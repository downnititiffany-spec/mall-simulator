# V25-R01 覆盖审计（首版）— 口径、方法与边界

- 交付目录：`docs/acceptance/v25-r01-coverage-20260914/`
- 交付物：`README.md`（本文件）、`MATRIX.md`（逐条覆盖矩阵）、`CORRECTION-229.md`、`VERIFY-T01-T02.md`、`ADS-FIRST-RELEASE.md`、`DECISIONS.md`、`raw/`（只读取证原始输出，**10 个文件**：`01`–`10`）
- 任务出处：`docs/项目实施进度与任务看板 V2.5.md` §3 L61（`V25-R01 修复覆盖矩阵`，IN_PROGRESS，Owner = 审计泳道）、§4 L104（登记行）
- 审计对象：`docs/项目完整实施指导书 V2.5.md`（769 行 / 74,100 B / sha256 `33A2C5B3234B1C724C6A9C9C0492B2480D443FA392D2DB72C3F4902805E55F6F`，mtime 2026-09-14 09:13:07）
- 唯一执行入口：`docs/项目实施进度与任务看板 V2.5.md`。首版引用修订版 `9E9CC76571A57E74647D8342F0EFA6168BD12B4977F53EDDD477B7D2702373AE`（122 行 / 16,593 B / mtime 12:06:05）；**12:5x 复核时该文件已被再次就地更新为 124 行 / 19,127 B / sha256 `71A99A60E687CB02D43784F84EC2D58B0B69A99982D2EB27E3844187E209CF6B`（mtime 12:11:51）** ⇒ 本目录所有「看板 §3/§4 行号」引用均指 **`9E9CC765` 修订版**，与 `71A99A60` 版行号可能错位
- 采集时刻：2026-09-14 12:08:47 – 12:12 +08:00（各 `raw/` 文件首行各记各的时刻）；**第三版增补（§9）采集于 12:4x–13:0x**，新增 `raw/10`（时点见其首行）
- 代码基线：**首版时点** `HEAD = 6062434792cf086600b67b38b562737ee6f65eb0`（`2026-09-14 12:06:44 +0800`，commit msg `V2.5 看板开工登记`）+ 38 行未提交工作树；**12:5x 复核时点** `HEAD = 3fcf90e30962ae528b2079dc5c40070f1f6b7257` + **61 行** dirty（中途经 `7296fda` 12:09:55、`09d7046` 12:12:36，均只改看板）；分支 `remediation/r1-boundary`；`git worktree list` = 3
- 本轮性质：**只读审计**。不写库、不启停服务、不跑真实链路、不跑 Maven 测试、不改任何既有证据目录、不改指导书/看板/契约/源码、不 `git add/commit/push`

---

## 1. 为什么有这个目录（对上一轮审计的正式纠正）

V2.5 L32 对上一轮交付的原文判定：

> 既有 `guideline-v24-coverage-20260912/COVERAGE.md` 的 **229 条不是 269 条重新验收，存在指针错位、缺 README 和证据覆盖不足，不可直接计算完成率**。扫描到类名、表名、HTTP 200、单次 SUCCESS 都不足以证明对应完整业务要求。

上述批评经本轮**独立复核成立**，并逐条落实为硬约束：

| 上轮问题 | 本轮对策 | 可核对处 |
|---|---|---|
| 229 条 ≠ 269 条 | 本目录**不产出**与 229 的对齐表；269 条在 `MATRIX.md` 文末列为**16 组 + 合计 = 269 条全部「未映射·待归并」** | `MATRIX.md` §「§269 交叉映射」 |
| 缺 README | 本文件即 README，含口径/方法/四级状态/证据强度/**未覆盖范围** | 本文件 §1–§6 |
| 指针错位 | 矩阵每行 `代码` 列必须给 `file:line`；指不出实现就写「无」，并同步把状态降为 `TODO`/`未取证` | `MATRIX.md` 全表 |
| 「扫到类名/表名即算覆盖」 | **本矩阵不采用该判据**：类名/表名命中只用于判断「载体存在与否」，覆盖结论一律要求**可证伪断言**；无断言即 `未取证` | `MATRIX.md`「判定纪律」第 1 条；样例见 §5-2/§5-3/§7-14 |
| 「HTTP 200」 | 当前 8090/8091/8092 **均无监听**（看板 §5 L120 只读实测），HTTP 相关旧结论在本时点**一律失效**；矩阵中零 HTTP 判据 | `MATRIX.md` §3-1、§4-10 |
| 「单次 SUCCESS」 | 历史成功只允许以「限定验收」登记，并必须引用看板 §2 的限定语 | `MATRIX.md` §3-2、§6-8、§5-33 |
| 计算完成率 | **本目录零完成率数字、零「X 完成／Y 部分／Z 未做」汇总**（V2.5 L15 明确不沿用 132/90/42） | 可对 `README.md` + `MATRIX.md` 全文检索 `完成率`/`%`/`132`/`90`/`42` 自证 |

历史件处理：`docs/acceptance/guideline-v24-coverage-20260912/`（README 缺失 / `COVERAGE.md` 229 行 / `raw/01`–`raw/07`）**保留不删**，仅作历史引用，**不作为本矩阵依据**。V2.4 指导书与 V2.2 看板同样降为历史证据、只读保留。

---

## 2. 口径与方法

1. **权威顺序**：按 V2.5 §2.1 L47–51 —— ① 已冻结契约文件 → ② V2.5 指导书 → ③ 看板 V2.5 已确认的任务状态与证据 → ④ 本版链接的专项设计与实施书 → ⑤ V2.1/V2.0/整改指导书/旧看板/历史计划/交接文档。
2. **不自行修文**：发现 V2.5 或看板与实测冲突时，矩阵写「冲突」并给出**两侧原值**，交总控裁决（`MATRIX.md` 文末「需要裁决」Q1–Q10）。本轮**未修改任何指导书/看板/契约/源码**。
3. **不沿用旧结论**：V2.5 L732 原文「禁止从旧覆盖表的 DONE 直接复制结论」。矩阵所有行的状态均按 `代码 file:line` + 断言 + 运行证据在当前基线重新推导；旧 269 条的历史计数仅在 `MATRIX.md` §「§269 交叉映射」里作为**历史引用**出现，并明确标注「非当前统计」。
4. **粒度**：以 V2.5 的「一条可判定要求」为行。V2.5 自身编号的子项**逐条点名、不抽样**：§4.1.1.2 十一条硬约束（拆 4 行，行内逐条列全 1–11）、§5.6 D1–D14（14 行）、§7.3 十二项质量规则（1 行内逐项列全并在缺口列逐项判定）、§9.2 T0–T4（4 行）、§11 九条实施顺序（9 行）+ L718 出口（1 行）、§13 AIW-001～020（20 行）、§13.3 六条完成定义（1 行）。
5. **证据引用纪律**：`运行证据` 列**只引用已存在且可复核的文件路径**。本轮不跑 Maven、不跑真实链路，因此凡「有测试文件但无本轮运行输出」的行一律写「未取证」，并注明「本轮不跑 Maven」，**不得**用 `target/` 中间产物充当永久证据（V2.5 L676）。
6. **计数口径**：本文件报告「要求条目数 / 已取证条数 / 未取证条数」时，分母 = `MATRIX.md` 的数据行数（含 §13-4～§13-24 的 20 条 AIW 行），并在报告中显式给出该分母数值，避免隐含统计。

---

## 3. 四级状态标记（四者独立，前者不得代替后者）

按 V2.5 L731 原文「四项状态分别登记『代码提交/测试结果/限定验收/完整验收』，不得以已推送文件数量推导完成度」，`MATRIX.md` 每行固定四列：

| 列 | 取值 | 判据（必须能指出证据） |
|---|---|---|
| `提交` | `已提交` / `未提交(dirty)` / `部分已提交` / `无代码` | `git log`/`git status --porcelain` 的实测结果；`raw/01-identity-git.txt` |
| `测试` | `通过` / `失败` / `未跑` / `不适用` | 必须有本轮或已归档的**运行输出**（含退出码/汇总数）。本轮不跑 Maven ⇒ 凡无归档输出的行写 `未跑` |
| `限定验收` | `有(<限定语>)` / `无` / `未取证` | 限定语必须来自看板 §2 或证据文件原文；**不得自行放宽或收紧** |
| `完整验收` | `无` / `未取证` | 无条件验收。本轮**没有任何一行**填 `通过` |

另有独立的状态列（V2.5 §2.2 L65 词表，不新增取值）：`TODO / READY / IN_PROGRESS / BLOCKED / REVIEW / DONE_LIMITED / DONE / REGRESSED / DEFERRED / N/A`，本矩阵另保留一个**非词表**的自有标记 `未取证`（含义：无法判定，需补运行证据），并在使用处同时给出 `TODO`/`IN_PROGRESS` 等真实任务态以免混淆。

**四条硬规则**：
- 「已提交」不蕴含「测试通过」；「测试通过」不蕴含「限定验收」；「限定验收」不蕴含「完整验收」。
- `DONE` 在本矩阵**只在四列同时满足时**才可使用；本轮矩阵**没有任何一行**使用 `DONE`。
- `DONE_LIMITED` 必须带限定语与出处（看板 §2 或证据文件行号）。
- 历史成果一律**不升级**：看板 §2（L17–L24）六行全为 `DONE_LIMITED`，矩阵逐行引用其限定语原文，包括「历史 run 47 链**不是**当前 WSL 验收」「集群 1000 条 10 作业**同输入对照与完整导入未闭环**」「P5 夹具实验**已发现 92/180 不符，不能算 P5 成功**」「P3-02 草案**规格盘点，没有生产代码**」。
- `F-88/V25-Q01` 一行按 V2.5 §7.3.1 写为**当前实现的版本边界缺口**，不写「已完成」（见 `MATRIX.md` §7-9）。

---

## 4. 证据强度分级（已证 / 强推断 / 未取证）

矩阵用 V2.5 §9.5 L676 的 E1–E5 标注**级别**，同时用下列三档标注**强度**；两套标记不互相替代：

| 强度 | 含义 | 本目录中的例子 |
|---|---|---|
| **已证** | 有本轮或已归档的**运行输出/文件哈希/只读实测**直接支持，且路径可复核 | `raw/01`–`raw/05` 的全部实测；`docs/acceptance/local-readiness-20260914.md` 的 09-14 测试汇总；看板 §5 L120 的端口只读实测 |
| **强推断** | 有 `file:line` 代码/契约/文档载体，但缺该要求的**运行证据**（未跑或本轮不允许跑） | `MATRIX.md` §5-26（schema 结构断言有文件，测试本轮未跑 ⇒ 强推断）；§7-15（历史断言 + 历史证据，非当前时点） |
| **未取证** | 既无运行证据也无可用载体，或载体存在但**无任何可证伪断言** | §5-2～§5-33 中表/类 0 命中的各行；§8-3～§8-7；§13-4～§13-24 |

E 级别定义（V2.5 §9.2/§9.5）：**E1** 编译/静态 · **E2** 模块自动化测试 · **E3** 本地真实链（T2 黄金 55 条/WSL） · **E4** 集群 · **E5** 员工可用页面路径。矩阵中 E 列为 `—` 表示该行**不产生运行证据**（治理/口径/文档类要求），此时强度只能是「已证（文档层）」或「未取证」，不得据此推导功能完成。

**证据保全纪律（V2.5 §9.5 L676）**：每次验收独立目录保存 README、命令与退出码、JDK/工具版本、git commit + dirty patch 指纹、jar hash、配置/夹具 hash、结果明细和未测范围；日志脱敏；`target/` 报告只作中间产物、不能作为唯一永久证据。本目录自身即按此执行（`raw/01` 记录 commit + 38 行 dirty + 文件哈希；`README.md` §6 记录未测范围）。

**已知未达标本条纪律的目录（实测，供总控登记）**：
- `docs/acceptance/guideline-v24-coverage-20260912/`：**无 README**（本目录外唯一一处；`v25-w01-env-inventory-20260914/` 已于 12:10:28 补上 README，见 `raw/07-baseline-drift.txt`）。
- `docs/acceptance/v25-s01-it-safety-20260914/`：看板 §4 L75 已声明为证据链接，但至 12:18 仍**目录不存在**（`raw/02-acceptance-inventory.txt` L24 首次实测；`raw/07` 复核仍 NOT-EXIST）。`v25-t01-t02-baseline-20260914/` 已于 12:15:29 出现（48 文件，含退出码与测试日志），该条前半已消解。

**审计期间环境变动（必读）**：本轮采集期间同一工作树被其他泳道持续改动 —— 新增 2 个提交（`7296fda` 12:09:55、`09d7046` 12:12:36，均只改看板）、dirty 由 38 行升至 **59 行**、看板 V2.5 就地更新为 124 行 / 19,127 B / sha256 `71A99A60E687CB02D43784F84EC2D58B0B69A99982D2EB27E3844187E209CF6B`。因此：

- 本目录所有「看板 §3/§4 的 L 行号」引用的是**修订版 `9E9CC765`（12:06:05，122 行）**，不是 12:11:51 之后的版本。
- **V2.5 指导书未被改动**（sha256 `33A2C5B3…05E55F6F` 与 `raw/01` 一致）⇒ 本目录所有 V2.5 行号引用仍然有效。
- 受影响的矩阵行已逐条登记在 `MATRIX.md` §「12:18 即时更正」，**不改写原行**，由读者按时点引用。
- 本目录是 **12:08–12:15 时点的快照**；「当前」二字在本目录内一律指该时点。

---

## 5. 本次未覆盖范围（明确不宣称）

本版**未做**以下任何一项，且**不得**由本目录的任何文字反推为已做：

1. **未重新验收 269 条**。`MATRIX.md` §「§269 交叉映射」把 269 条全部登记为「未映射·待归并」，承接 `V25-R02`（看板 §3 L62 = TODO）。旧 `docs/v2-completeness-audit.md` 的 132/90/42 是 commit `34f37a8` 的冻结快照，**不是当前统计**。
2. **未跑任何自动化测试**。本轮不执行 Maven/Vitest/`node --test`；09-14 的 566/111/74/6 数字来自 `docs/acceptance/local-readiness-20260914.md`，**引用而非复现**（该文件 L7 自述「本次不提交、不推送」，L23 记「首次遇错停止；第二次为收集其他模块结果使用 `maven.test.failure.ignore=true`；第二次命令的 BUILD SUCCESS 不表示测试全绿」）。
3. **未启停任何服务、未发任何 HTTP 请求**。8090/8091/8092 保持停止（看板 §5 L120）；本目录零 HTTP 判据。相关历史「HTTP 通过」证据仅对当时有效。
4. **未写库、未读库**。`SELECT` 与 `information_schema` 虽被允许，但本轮**未执行**任何数据库查询 ⇒ `MATRIX.md` §4-8（§4.2 六表）、§7-4、§7-15 的库内结构/约束状态为「未取证」。承接裁决问题 Q10。
5. **未跑真实链路、未跑 WSL 链、未跑集群**。T2/T3/T4 全部未跑；WSL 仅做了**只读**枚举（`docs/acceptance/v25-w01-env-inventory-20260914/raw/`），且该盘点**未进入 Ubuntu 核对 Java/Spark/MySQL 安装状态**（`local-readiness-20260914.md` L41）。
6. **未做 E5 页面验收**。无在跑服务 ⇒ 无页面可验收；不做登录、不下单、不采截图。`V25-E03`（TODO）承接。
7. **未鉴定 WSL 内运行时版本/兼容矩阵**（`V25-W03` TODO）、**未迁移源码到 Linux 路径**（`V25-W02`/`W04` TODO）。
8. **未修复任何缺陷**。本审计只登记；已知缺陷（`SINGLE_NODE` 缺省 master、两条基线测试失败、`platform-common` 不可编译、IT 防误写缺失、F-88 版本边界）全部保留原状，各归其任务行。
9. **未审计 AI 建模专项文件全文**。`MATRIX.md` §13-4～§13-24 的 20 条 AIW 行只核到「定义位置 + 是否有 Owner/证据位」，**未逐条核对专项文件的技术判据**。
10. **未核对论文素材完整性**。论文/答辩属 `V25-X04` = DEFERRED（V2.5 L716「日期与学校清单不得由 Agent 发明」）；素材缺口清单见 §7。

---

## 6. 条目数与已/未取证条数（自检实测，`raw/06-selfcheck.txt`）

**分母 = 139**（`MATRIX.md` 中 V2.5 要求的数据行数，不含文末 §269 归并桶的 6 行）。分布：§1 = 3、§2 = 4、§3 = 5、§4 = 11、§5 = 33、§6 = 8、§7 = 16、§8 = 9、§9 = 9、§10 = 1、§11 = 10、§12 = 5、§13 = 24（含 AIW-001～020 共 20 行）、§14 = 1。

| 维度 | 计数 | 说明 |
|---|---:|---|
| 要求条目数 | **139** | 上表分布之和 |
| `运行证据` 列 = `未取证` | **27** | 既无运行证据也无可用载体，或载体存在但无可证伪断言 |
| `运行证据` 列 有已存在证据指针（已取证/强推断） | **112** | 指针均为本轮新增 `raw/01`–`raw/05` 或既有证据文件路径；**不等于**该要求已通过 |
| `完整验收` 列 = `未取证` | **139（全部）** | 本轮**没有任何一行**通过完整验收 |
| `限定验收` 列 = `有（限定…）` | **45** | 限定语均引看板 §2 或证据文件原文 |
| `限定验收` 列 = `无` | 2 | |
| `限定验收` 列 = `未取证` | 92 | |
| `测试` 列 = `未跑`（含更具体说明） | **113** | 本轮不跑 Maven/Vitest；`不适用` 22 项为治理/口径类要求 |
| `测试` 列 = `失败` | **4** | 全部指向 09-14 已归档的 2 项基线失败（`WarehouseNameLiteralGateTest`、`IngestionManifestSourceSchemaTest`）及其影响面 |
| 状态 = `TODO` | **80** | |
| 状态 = `DONE_LIMITED` | **34** | 全部带限定语，无一升级 |
| 状态 = `IN_PROGRESS` | **17** | |
| 状态 = `DEFERRED` | **5** | |
| 状态 = `READY` | **3** | |
| 状态 = `DONE` | **0** | 四列同时满足才可用，本轮无一行满足 |

**不产出**：完成率百分比、`X 完成／Y 部分／Z 未做` 汇总、与旧 132/90/42 的任何比较（V2.5 L15）。

- 本目录的自证：`raw/06-selfcheck.txt`（交付物 sha256：`README.md` = `D30ED2EBD7CF964152F7B1056986679082C629312DFCC0326B96AB48C89C472A`，`MATRIX.md` = `F3A257DDE3F294C770CCE0FD6D0991F57864D6B2B403F975041C1AF0A88F0365`）、`raw/01-identity-git.txt`（commit/分支/dirty/文件哈希）、`raw/02-acceptance-inventory.txt`（证据目录盘点 + 看板 §4 链接存在性）、`raw/03-ruleseverity-version-boundary.txt`、`raw/04-v25-object-presence.txt`、`raw/05-269-baseline.txt`。全部为命令原始输出，未做删改；本轮**未修改**任何既有证据目录。

## 7. 论文素材缺口（供 §1.1 / `V25-X04` 使用，只列事实）

1. 技术栈不实风险：前端测试实为 `node --test`（74 项），全仓**无 vitest 依赖** ⇒ 论文若写 Vitest 即不实（旧 269 条 §4 行）。
2. 「金额/比例计算无 double」成立，但 §5.6 D7 新增的「从 JSON 原始文本解析、超精度 `UNNECESSARY` 拒绝」**未实现** ⇒ 论文不得写成已完成。
3. 「窗口一律左闭右开」不严谨：生成器小时环 `SimulationEngine.java:289` 用 `!cur.isAfter(endTime)` 含端点，且无 `[start,end)` 实现可查。
4. 商品丰富度：实测 **32** 个商品（指导书建议 100–300），库存全 100 无差异，无季节/促销/爆款/长尾标签字段。
5. ADS **实产 8/10** 张；`net_sale_amount` 未落 ADS；`ads_category_sale`/`ads_region_sale` 无产出作业 ⇒ 论文「10 张 ADS」不实。
6. DWS 漏斗粒度退化为单行「日」（分类/渠道硬编码 `-1`/`'all'`）。
7. 三个数据集 `medium-1k`/`perf-100k`/`perf-1m` **不存在**；L2 未做 ⇒ 论文不得写 10 万/100 万条实验。
8. 集群 1000 条 10 作业**保留但同输入对照与完整导入未闭环**（看板 §2 L21）⇒ 不得写成 E4 通过。
9. P5 异构源**92/180 不符**（看板 §2 L22）⇒ 不得写成成功。
10. WSL 单节点**从未跑通**完整采集→发布链（`local-readiness-20260914.md` L5 原文）⇒ 论文「主环境」为**目标**不是**事实**。

---

## 8. 第二版增补（12:5x 时点，总控第二版范围①②③④）

本版**只追加**，不改写 §1–§7 任何结论（双时点并列原则）。

### 8.1 本版新增交付物

| 文件 | 内容 | 对应总控范围 |
|---|---|---|
| `VERIFY-T01-T02.md` | 逐个 `final-*.log` 的 `EXIT` 与 `Tests run` 复核；T02 未在 Maven 下执行的方法学级证据；12:20:53 红属其他泳道；spark-jobs `Tests run: 0`；S01 仍不存在 | ① |
| `raw/08-old229-pointer-audit.txt` | 旧 229 行指针三级解析机械核对原始输出（A=104 无指针 / B=117 可解析 / C=8 假阳性，合计校验 229） | ③ |
| `CORRECTION-229.md` | 按 V2.5 L32 三条批评逐条订正（缺 README 成立 / 指针错位不能按路径复现 / 覆盖不足部分成立），**零完成率** | ③ |
| `raw/09-information-schema-readonly.txt` | 总控 Q10 授权的只读 `information_schema` 实测（含**权限边界更正**） | ② |
| `ADS-FIRST-RELEASE.md` | 9 场景 ADS 的三批落地顺序、输入表、逐条 oracle；超出集合 = 后续可选 | 总控裁决 ② |
| `DECISIONS.md` | 「必须用户裁决」3 条（E-01～E-03，逐条标注命中四类之一）vs「自行决定」12 条（D-01～D-12，决定＋依据）＋ Q1–Q10 裁决留档 | 用户裁决 ① |

### 8.2 本版实测关键结论（各行证据指针见 `MATRIX.md` §「第二版更正与新增」）

1. **T01 有 Maven 级红→绿双证据**（12:16:37 EXIT=1 红 → 12:19:11 EXIT=0 / 33 tests / BUILD SUCCESS）。
2. **T02 从未在 Maven 下执行**：`-pl platform-app -am` 使 `-Dtest=` 在上游模块报 `No tests matching pattern`，且所传 `-DfailIfNoSpecifiedTests=false` 不是 surefire 的键 ⇒ T02 证据仅到**探针级**，**不得**记为「测试通过」。
3. **E1 双时点**：12:08 不成立 → **12:19:17 起 platform-common 整模块绿（79 tests / 0 F / EXIT=0）**；但 12:20:53 又因其他泳道新建的 `TestIsolationGuard*`（不在 HEAD，`git cat-file -e` 退出码 128）转红 ⇒ 「E1 绿」必须带时点。
4. **DB 实测（只读）**：`meta_app` 可见范围内仅 `analytics_meta`（22 表）；§4.2 五个对象、`quality_rule_definition` **0 命中**；`metric_snapshot` 12 列**无 `source_id` 列**，唯一键仅 `PRIMARY(id)` 与 `uk_snapshot_id`（无发布作用域唯一）⇒ §7.4 断言未满足。**受权限限制的面记为未取证，不写「不存在」。**
5. **新缺口**：ACTIVE 快照权威库分叉（配置指 `analytics_metric`、实测约束在 `analytics_meta`）⇒ E-03 上升用户；ADS 表名 `_m` 后缀不一致；spark-jobs `EXIT=0` 但 `Tests run: 0` 与 09-14「111 通过」互斥。
6. **旧 229 三条批评**：缺 README **成立**（`COVERAGE.md` L3 指向同目录不存在的 README）；指针错位**不能按路径断链复现**；覆盖不足**部分成立**（104/229 行无路径指针）。

### 8.3 本版新增的未覆盖范围

11. **§7.4 约束在 `analytics_metric` 的实际结构未取证**（该库对 `meta_app` 不可见）。
12. **跨库同名表枚举未取证**（总控提示「同名表在 11 个库里」，`meta_app` 权限下无法枚举）。
13. **旧 229 行指针的语义正确性未核对**（只验可解析性，未验指向是否正确、行号是否仍对应）。
14. **T02 的 E2/E3 级证据、spark-jobs 真实测试数、S01 交付物**均未取证。
15. **本版未执行**：任何 Maven、任何服务启停、任何写库、对既有证据目录的任何修改、任何 `git add/commit/push`。

### 8.4 本版仍不产出

- 仍然**零完成率、零百分比、零「X 完成／Y 部分／Z 未做」汇总、零与旧 132/90/42 的比较**（V2.5 L15）。
- 「完整验收」列仍**全部未取证**，无一行升级；`DONE` 仍为 **0**。

---

## §9 第三版增补（12:4x–13:0x 时点，总控 v3 范围①–⑤）

> 全部只读：未跑 Maven、未启停服务、**未写库**（仅只读 `SELECT` 与 `information_schema`）、未改既有证据目录、无 git 操作。证据：`raw/10-crossdb-census-and-74-invariants.txt`。

### 9.1 新增交付

| 文件 | 内容 | 对应总控范围 |
|---|---|---|
| `raw/10-crossdb-census-and-74-invariants.txt` | ① 总控 12:4x 跨库普查**原文照抄**（含实例指纹、9 库表数、口令**脱敏**写法与「只读/零写/时点」声明）；② 本泳道 root 只读独立复核 R1–R13（实例指纹自证、跨库对象命中、唯一键分布、§7.4 不变量 `COUNT(*)`）；③ 复核结论与**两处更正** | ① ② |
| `MATRIX.md` §「第三版更正与新增」 | 更正 A/B、E-03 消解、§7.4 不变量实测表、D-10 落档、#12 处置、**风险 #16/#17**、销毁边界 | ③ ④ |
| `DECISIONS.md` | E-03 移出用户裁决（改「已由总控普查消解」）、D-02/D-03/D-09/D-10 按实测改写、§4 新增两条事实提醒 | ③ ④ |

### 9.2 本版实测关键结论（**含对本目录前两版与总控普查的更正**）

1. **更正 A（高）·§4.2 五个对象存在且已投产**：全部位于 schema **`generator_meta`**，DDL 为 `synthetic-data-generator/src/main/resources/db/generator/V1__generator_meta.sql:12,30,50,74,92`，行数 `generator_target`=**422** / `generation_plan`=**430** / `generation_run`=**184** / `generation_artifact`=**551** / `generation_event_stat`=**947**，代码侧 `GeneratorMetaStore.java` 等 7 处在用。
   ⇒ 本目录首版「5 张不存在」、第二版「可见范围内 0 命中」、总控普查「9 个 schema 都不存在」**三条均作废**（普查按 `analytics_*` 前缀过滤，漏掉生成器库）。
   ⇒ **纪律教训（同一错误第二次）**：**按前缀/按账号过滤的普查不能得出「不存在」结论**；存在性结论必须写明覆盖的 schema 集合与过滤条件。
   ⇒ 仍未取证：这 5 张表**列结构是否逐项符合 §4.2 规格**（行数只证「在用」，不证「合规」）。
2. **更正 B（中）·`metric_snapshot` 在 11 个 schema**：meta 族 5 个（各 12 列）/ metric 族 4 个（各 15 列）/ **`mall_simulator`、`mall_simulator_test`（各 11 列，普查未计入）**。
3. **E-03 消解**（不再上升用户）：权威库 = **`analytics_metric`**（与 `application.yml:15` 一致）；meta 族 12 列是**历史副本** ⇒ 性质为「历史重复表 ＋ schema 漂移」，**不是架构分叉**。独立复核支持：metric 族**独有** `uk_active_profile`、**独有** `definition_version NOT NULL` ⇒ 此前基于 12 列副本写的「无 `definitionVersion`」**按权威侧更正为存在且非空**。
4. **§7.4 不变量（实测，`analytics_metric`，零写）**：`active_flag` 语义 = ACTIVE 行 `1` / 非 ACTIVE 行 `NULL`（**1 = 1 行，NULL = 11 行**）；「每档案至多一行 ACTIVE」`multi_active=0` ✔；「无档案缺 ACTIVE」`without_active=0` ✔；规模 `total_rows=12` / `distinct_profiles=1` / `active_rows=1` ⇒ **§7.4 成立但仅在 1 个档案上成立**，**并发原子性仍未取证**，不得写成「已通过压力验证」。
5. **D-10 按总控裁决定案**：以现有 `uk_active_profile(runtime_profile_id, active_flag)` 满足「按发布作用域唯一」，**不新增 `source_id` 列**；措辞待 `V25-C01` 改写，**不开迁移**（实测两族均无 `source_id`，仅 `source varchar(32)`）。
6. **#12 处置**：库内 ADS 实名 **8 张全带 `_m`** ⇒ **代码与库一致**，待订正的是 **DDL 文字**（`warehouse/ddl/04-ads.sql` 无后缀）。
7. **新风险 #16/#17（登记，不实现）**：9 个 `analytics_*` 库并存 ＋ 2 个商城库含同名表 ⇒ 表级结论**必须写明 schema 名、禁止裸表名**；同表名**三种列结构**已实际存在（12/15/11 列），连接串若被改成 `_p105` 等库会**静默读到缺列表**。
8. **销毁边界**：历史重复表属 `persistent-state`，**本任务未执行、也未生成任何破坏性命令**；若确需清理须由用户对具体库名+表名给出作用域确认并附备份/回滚说明。

### 9.3 本版仍未取证（随更正收缩，旧条目保留不删）

11. **【已关闭】** ~~§7.4 在 `analytics_metric` 的实际结构未取证~~ → 本版已取证（但**并发原子性**仍未取证）。
12. **【已关闭】** ~~跨库同名表枚举未取证~~ → 本版已取证（**11 个 schema**）。
16. **§4.2 五表与 V2.5 §4.2 的字段级一致性未取证**（列结构/约束）。
13. 旧 229 行指针的**语义**正确性未核对（只验可解析性）。
14. T02 的 E2/E3、spark-jobs 真实测试数、S01 交付物 —— 未取证。
15. 本版未执行：任何 Maven、任何服务启停、任何写库、对既有证据目录的任何修改、任何 `git add/commit/push`。

### 9.4 本版仍不产出

- 仍然**零完成率、零百分比、零「X 完成／Y 部分／Z 未做」汇总、零与旧 132/90/42 的比较**（V2.5 L15）。
- 「完整验收」列仍**全部未取证（139/139）**，无一行升级；`DONE` 仍为 **0**。
- 上升用户的裁决**从 3 条减为 2 条**（E-01 漏斗维度粒度、E-02 是否含 AI 建仓章节）；E-03 已消解。**减少裁决数不等于任何完成度变化。**

