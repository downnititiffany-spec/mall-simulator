# 旧 229 行覆盖表的订正（V2.5 L32 口径）

- 依据：V2.5 L32 原文「既有 `guideline-v24-coverage-20260912/COVERAGE.md` 的 **229 条不是 269 条重新验收，存在指针错位、缺 README 和证据覆盖不足，不可直接计算完成率**」
- 被测对象：`docs/acceptance/guideline-v24-coverage-20260912/COVERAGE.md`（351 行 / **229** 数据行 / 目录内 **无 README**）
- 机械核对原始输出：`raw/08-old229-pointer-audit.txt`（复核时点 12:4x，HEAD `3fcf90e`）
- **本文件不产出任何完成率、百分比或「完成/部分/未做」汇总**（V2.5 L15、L731）。只给逐条可核对状态。

---

## 1. V2.5 L32 三条批评的逐条核对结论

| V2.5 L32 的批评 | 核对结论 | 证据 |
|---|---|---|
| **缺 README** | **成立（确定）**。`COVERAGE.md` **第 3 行**原文：「口径、方法、统计数字与『本审计不能证明什么』见同目录 `README.md`」——而 `docs/acceptance/guideline-v24-coverage-20260912/README.md` **不存在**（`Test-Path` = False）。即：该表**自己的口径声明就是一条断链**，读者无法得知其状态词表之外的方法与边界 | `raw/08` 首段；`Test-Path` 实测 |
| **指针错位** | **不能按「路径断链」复现**。对 229 行的反引号 token 做三级解析（S1 仓库根 / S2 相对该审计目录 / S3 全仓 basename），排除 HTTP 路由、状态机枚举、配置片段、表格碎片后：**无法解析的 token 只剩 8 行，且 8 行全部不是路径**（见 §2）。⇒ 该表述的真实所指应是**语义错位**（指针能解析但不支撑其结论），**该面本版未取证** | `raw/08` §解析规则与 C 类 8 行 |
| **证据覆盖不足** | **部分成立（结构性）**。229 行中 **104 行行内根本没有路径型指针**（结论只靠散文/grep 陈述支撑，无法按指针复核）；**117 行指针可解析**（其中若干为 `raw/NN` 缩写自引用，需回该目录内补全文件名） | `raw/08` A/B/C 计数 |

> 措辞纪律：本文件**不写**「229 条里 X 条已完成」。229 ≠ 269（V2.5 L714：269 条逐条重审由 `V25-R02` 独占），任何拿 229 推断 269 的做法都被 V2.5 明确禁止。

## 2. 逐条核对计数（可复算，合计校验 229）

| 分类 | 行数 | 含义 |
|---|---:|---|
| A. 行内无路径型指针 | **104** | 多为口径/禁止项/枚举/治理行，指针面不适用；其结论无法按指针复核 |
| B. 路径指针可解析 | **117** | 含少量 `raw/NN` 缩写自引用（该目录 `raw/` 下确有 `01`–`07` 系列） |
| C. 含无法解析 token | **8** | 逐条列于下；**8 条经人工判读全部不是路径**，属解析器假阳性 |
| **合计** | **229** | A+B+C = 104+117+8 = 229 ✔ |

C 类 8 行原文（供核对，均为值列表/扩展名列表，非文件指针）：
`V24-061 priceUnreadable|notes|catalog|gap`、`V24-070 ExternalOrder/Refund.status`、`V24-091 .vue/.html/.js/.css`、`V24-132 -1/unknown`、`V24-158 S20260901_44/45/46`、`V24-207 132/90/42`、`V24-210 M1-1/2/6/7/8/10`、`V24-211 LocalFileIngestor.checkpointKey/canonical`。

## 3. 处置（本任务只读，不改历史件）

1. **不删、不改** `guideline-v24-coverage-20260912/` 任何文件（含那条断链的第 3 行）——历史件按 V2.5 L32 只读保留；本目录用本文件**在矩阵内**完成订正。
2. 旧表**不得**作为完成率或结论来源；V2.5 L732 原文「禁止从旧覆盖表的 DONE 直接复制结论」。
3. 229 → 269 的归并**唯一承接方 = `V25-R02`**（总控 Q9 裁决）；本目录只交「未映射·待归并」桶（见 `MATRIX.md` §「§269 交叉映射」）。
4. 新增缺口（供总控登记）：`guideline-v24-coverage-20260912/` 是全仓**唯一**仍缺 README 的证据目录（`v25-w01-…` 已于 12:10:28 补上）。是否为其补一份**只读说明**（不复活 229 结论、仅声明历史件与断链），属编排性决定，见 `DECISIONS.md` 组二 D-09。

## 4. 本文件不能证明什么

1. **不能**证明 229 行里哪些结论仍成立——那是 `V25-R02` 的逐条重审工作。
2. **不能**证明「指针不错位」：本核对只验**可解析性**，未验**指向是否正确**（例如行号是否仍对应该内容、指针是否指向同名不同库对象）。语义面 = **未取证**。
3. **不能**证明 229 与 269 的对应关系（V2.5 已判定 229 不是 269 的重新验收）。
4. **不能**用本文件的 A/B/C 计数推导任何完成度。
