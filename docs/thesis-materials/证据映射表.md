# 论断 → 真实代码/证据 映射表

> **对应验收条目**：指导书 V2.0 §30「文档与论文」最后一条（L1580）
> ——「论文中的功能、指标、截图和实验都能映射到真实代码与验收证据」。
>
> **编制时点**：2026-09-11 ｜ 分支 `remediation/r1-boundary` ｜ 基线提交 `fabc6cb`（R8 真机验收）
> **R9 复核（2026-09-11，提交 `e272c8a`）**：唯一 ACTIVE 快照已更新为 **`S20260901_30`**（run 30，
> 业务日期 20260901，attempt 1 SUCCESS，指标值与 `S20260901_22` 逐项相同）；新增 6 条可靠性与真实缺陷
> 映射（见"四、可靠性与失败恢复"表末），并补齐平台全量测试 `303/303` 的落档
> （`docs/acceptance/r9-20260911-e272c8a-run30-S20260901_30/24-fulltest-analytics-server.log`）。
> **快照口径**：当前唯一 ACTIVE 快照 = **`S20260901_30`**（`S20260901_22`/`23`/`24`/`25`/`29` 均已 ARCHIVED）。
> **编制方式**：只读取代码/配置/迁移/脚本/`.verify/` 报告/`docs/acceptance/` 证据包与九章论文草稿；
> **本表不新增任何未实测数字**。凡仓库内没有当前真实证据的论断，一律标 `未实测` 并写明缺失条件；
> 凡结论成立但与论文表述有差异的，标 `部分` 并写明差异。
>
> **状态定义**
> - `已实测`：有可复现脚本或落盘报告，且关键字段本次逐条核验过；
> - `部分`：结论成立，但**口径/载体/覆盖范围**与论文表述不一致，或只有单测/IT 而无端到端留档；
> - `未实测`：仓库内无当前证据（含只有 Mock 基线、或证据文件已被删除/失效）。
>
> **唯一写入边界**：本表及同目录 `results-tables.md`、`screenshot-list.md` 为本次可写文件；
> 其余路径（源码、`docs/remediation-status.md`、`docs/acceptance-checklist.md`、`docs/thesis-draft/**`、`.verify/**`）**只读**，
> 本表中的全部修正建议均以「论文该改什么」的形式给出，不代改。

---

## 0. 速览

| 状态 | 条数 | 占比 |
|---|---:|---:|
| 已实测 | 57 | 77.0% |
| 部分（口径/载体/覆盖需修正） | 9 | 12.2% |
| 未实测（须标"待环境/待密钥"或删除） | 8 | 10.8% |
| **合计** | **74** | 100% |

（分布：架构 5、数据链 10、指标口径 7、可靠性 14、Web 产品 8、AI 与安全 12、决策闭环 5、性能与规模 6、文档与复现 7。
可靠性由 8 条增至 14 条：R9 补充 6 条真机证据——幂等并发、resume 只重跑失败阶段、质量失败不污染、D-R9-1/D-R9-2 两个真实缺陷、发布前校验负路径。）

**一句话结论**：论文的**主体功能与指标**（八阶段链、双产品硬边界、指标口径、只读与安全、五态页面、AI 证据包与六段解释、决策 12 态）全部能落到真实代码与落盘证据；
**不能落地的是 8 项**：性能"优化前"基线、决策 EFFECTIVE 效果示例、AI 100 题 Mock 评测、真实 LLM 对照、AI 数值事实一致率、集群规模分档、Flume→HDFS 断点恢复实录、Parquet Snappy 压缩。
另有 **9 项口径差异**必须改文（第八章黄金对账整表、七阶段→八阶段、7 页面→9 页面、6 作业→11 作业、Flyway V1~V5→V15 等）。

---

## 一、架构与硬边界（§30 架构 3 条）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 三/四章；图 4-1 | 分析平台与模拟商城是**两个独立应用**（不同进程、端口、库、前端、构建产物） | `analytics-server/pom.xml:16,28-35`；`analytics-server/platform-app/src/main/resources/application.yml:2`（`port: 8091`）；`mall-simulator/pom.xml:15`；`mall-simulator/src/main/resources/application.yml:2`（`port: 8090`）；前端物理拆分 `web/` 与 `mall-frontend/` | `.verify/r7-4-mall-dom.py` → `.verify/r7-4-mall-dom-report.json` **assertions=17 / failed=0**（8090 不提供 `/api/v1/dashboards/overview`、`/api/v1/metrics/health`，均 401）；`.verify/r8-accept.ps1` → `.verify/r8-accept-report.json` **PASS=53 FAIL=0**（base `http://127.0.0.1:8091`） | 已实测 | 可写「双进程双库」。**遗留未收敛**：商城应用配置库名 `mall_simulator` 与 `warehouse/migrations/init-three-dbs.sql:5` 的 `mall_business` 并存，论文不要把这个矛盾写成"三库已统一命名" |
| 四章；图 4-1 | 平台**不依赖**商城（无 Maven 依赖边、不含生成器代码） | 全仓 `<artifactId>mall-simulator</artifactId>` 仅命中其自身 pom:15；`SimulationEngine.java:47`/`GeneratorRunService.java:19`/`GeneratorController.java:28` 全在商城侧；`analytics-server` 下无 `generator|simulat` 目录 | 同上 17/17（平台可独立运行）；`mall-simulator` 测试 **54/54 GREEN**（`docs/remediation-status.md:235`） | 已实测 | 建议表述为「无编译期依赖 + 平台无需商城即可运行黄金文件分析」，不要写成"两系统零耦合"（数据面仍通过 Landing 文件衔接） |
| 二/四章 | `analytics-server` 为 6 模块聚合工程，可运行模块是 `platform-app` | `analytics-server/pom.xml:16`（packaging=pom，:28-35 六模块）；`platform-app/pom.xml:11,72`（spring-boot-maven-plugin） | `analytics-server` 全模块 `-DskipTests package` **BUILD SUCCESS**（`docs/remediation-status.md:131,141,169,180`） | 已实测 | 论文写"平台是 Spring Boot 应用"时须点明父 pom 只是聚合，产物在 `platform-app` |
| 三/四章 | 三库 + 四账号读写隔离（`mall_app`/`meta_app`/`metric_pub`/`metric_read`） | `warehouse/migrations/init-three-dbs.sql:5-7,11-27`；`application.yml:7-21`（三数据源） | R1 实测：`metric_read` 执行 CREATE 被拒 **1142**（`docs/remediation-status.md:31`）；`.verify/r8-accept-report.json` D 组 5 条 PASS | 已实测 | **素材里出现的 `metadata_app` 在仓库中不存在**（全库 0 命中），真实账号是 `meta_app`；另 `docs/acceptance-checklist.md:16` 写的 `mall_reader` 亦不存在，真实只读账号是 `metric_read` |
| 七章 7.1 | 分析平台前端只连平台 8091，商城页面已迁出 | `web/vite.config.js:12-14`（`/api` → `http://127.0.0.1:8091`，:11 端口 5173；注释 L4-6 明写"商城演示与商品后台页面已迁出本前端"）；`mall-frontend/package.json:5` 明写与 `web/` 严格分离 | `.verify/r7-4-dom-report.json` **22/22 PASS**（含"离线可运行：无非本机外网请求"） | 已实测 | **论文 07-系统实现.md:12 写「dev 代理 `/api`→8090」与代码相反**，必须改为 8091 |

---

## 二、数据链与对账（§30 数据链 3 条）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 三/五/七章；图 5-2 | 生产链 **8 阶段**：WAIT_LANDING → INIT_SCHEMA → LOAD_ODS → BUILD_DWD → BUILD_DWS → BUILD_ADS → QUALITY_CHECK → PUBLISH_METRIC | `analytics-server/warehouse-pipeline/.../pipeline/PipelineService.java:202-204`（`STAGE_ORDER`） | `docs/acceptance/r9-20260911-fabc6cb-run22-S20260901_22/02-pipeline-stage-run.tsv` **8 行全 SUCCESS**；`.verify/r8-evidence-truncation-proof.ps1` → `.verify/r8-evidence-truncation-proof.json` **PASS=12 FAIL=0** | 已实测 | 载体是 `List<String>` 常量，**不是 Java enum**，论文别写"八阶段枚举"；**草稿多处写 7 阶段**（见第三章修正表 J-3~J-6） |
| 五章 5.2 | 阶段→Spark 作业映射；11 个 job code（`sci/odl/bdw/dim/tdw/usw/fna/dqc/pub/mxp/ljp`） | `.../pipeline/spark/SparkStageExecutor.java:41-48`（7 键映射，WAIT_LANDING 由 Java 承载）；`spark-jobs/src/main/scala/.../job/*.scala` 11 个；`JobRegistry.scala:13-40` | spark-jobs ScalaTest **46/46 GREEN**（`docs/remediation-status.md:213,249`）；`.../04-spark-job-summary.tsv`（odl 51→51、bdw 15→14、dim 18→7、tdw 18→7、usw 14→3、fna 3→66、dqc 66→18、mxp 44→44、pub 66→66、sci 0→37） | 已实测 | **草稿写"6 个 Scala 作业"（02:11、05:3）要改成 11 个**；`run-spark-chain.ps1` 只覆盖部分作业，不能当作"11 作业一键脚本" |
| 五章 5.1；表 5-1 | 四层数仓 **29 张表**（ODS 4 / DWD 3 / DIM 5 / DWS 7 / ADS 10） | `warehouse/ddl/00-ods.sql`(105行) / `01-dwd.sql`(67) / `02-dims.sql`(73) / `03-dws.sql`(99) / `04-ads.sql`(133) | 逐文件计数 `CREATE TABLE` = **4/3/5/7/10，合计 29**（本次实测一致） | 部分 | **必须注明"设计 DDL 口径"**：本地真实链由 `sci` 作业（`LocalSchemaInitJob.scala:34-245` `STATEMENTS`）实建 **32 张**——DIM 仅 2（缺 `dim_date/dim_region/dim_metric`）、ADS 正式 8（缺 `ads_category_sale/ads_region_sale`）、另有 8 张 `__staging` 孪生表。且 DDL 的 `LOCATION` 全是 HDFS 风格 `/user/hive/warehouse/...`，与本地 `file:///D:/.../spark-warehouse` 不是同一路径体系 |
| 五章 5.4 | Spark 3.5.1 + Scala 2.12.x + JDK 17；Hive on Spark + 嵌入式 Derby metastore | `spark-jobs/pom.xml:15,16,18,19`（scala 2.12.19 / spark 3.5.1 / source,target 17）；`analytics-server/pom.xml:21-23`；`.../spark/SparkStageExecutorFactory.java:73-78`（`jdbc:derby:<abs>/derby-metastore;create=true` + `EmbeddedDriver` + `hive.metastore.jars=builtin`） | `experiments/spark-chain-local-*.json`、`experiments/spark-local-20260906.json`；`.../15-hive-layers.txt`（ODS 15/16/2/14/4、DWD 14/7/1、dws_trade_day 5 行、ads_formal 8 表） | 已实测 | **仓库无 `build.sbt`**（构建只用 Maven），论文若写 sbt 需删；`experiments/env-profile.json` 里 spark 字段仍是 `"3.5.1 (待本地验证)"`，与已完成验证不一致，引用时以 `spark-jobs/pom.xml` + `15-hive-layers.txt` 为准 |
| 五章 5.1 | 全部 Parquet 存储 + `dt` 分区（ODS 另含 `hour`） | `00-ods.sql:27,28,54,76,103`；`01-dwd.sql:25,26`；`02-dims.sql:17,18`；`03-dws.sql:17,18`；`04-ads.sql:20,21` | 逐条核验：全部 `STORED AS PARQUET` + `PARTITIONED BY (dt STRING)`，ODS 4 张另有 `hour STRING` | 已实测 | — |
| 四章 4.2；五章 5.1 | 「Parquet/**Snappy** 压缩」 | 无 | 全仓 grep `snappy` / `Snappy` / `compression` = **0 命中**；DDL 无 `TBLPROPERTIES`；代码/配置未设 `spark.sql.parquet.compression.codec` | **未实测** | **必须删掉"Snappy"或改为"压缩编码未在代码中固定"**。受影响：`04-总体设计.md:12`、`05-数仓与Spark实现.md:9` |
| 五章 5.5 | 仓位置 `file:///D:/Develop_code/GraduationProject/spark-warehouse` | 配置是相对路径：`application.yml:32-33`（`${PLATFORM_SPARK_WAREHOUSE_DIR:./spark-warehouse}`、`metastore-dir: ./derby-metastore`）；绝对 URI 在运行时拼：`SparkStageExecutorFactory.java:70,82-88` | 物理目录 `spark-warehouse/`、`derby-metastore/`（含 `seg0/`、`service.properties`）实际存在 | 部分 | 结果等价但**不是"配置里写死绝对路径"**；论文描述成"运行时以工作目录绝对化"才准确。另 `spark-jobs/metastore_db/` 与仓根 `metastore_db/` 是历史遗留，与平台的 `derby-metastore/` 不是同一份 |
| 七章；表 7-2 | 最近真实链 run 22：八阶段 SUCCESS、8 张 ADS 导出 `metric-staging/S20260901_22/*.jsonl`、ACTIVE=`S20260901_22` | `spark-jobs/.../job/MetricExportJob.scala:28,41,72,104`；`PipelineService.java:110-111,522-525`；`MetricPublisher.java:130-137` | `docs/acceptance/r9-.../01-pipeline-run.tsv`（run22 ODS_TO_ADS SUCCESS attempt=4 error NULL）、`02-pipeline-stage-run.tsv`（records 51/37/51/28/3/22/6/44）、`10-metric-snapshot.tsv`（22 ACTIVE version=3；23/24 ARCHIVED）、`12-ads-table-counts.tsv`；`.verify/r8-evidence-truncation-proof.json`（8 个 `.jsonl`） | 已实测 | 论文写"流水线 SUCCESS"必须带 `runId=22` + `snapshotId=S20260901_22`，否则与 R7 时点的 `S20260901_24` 混淆（§30 禁止"以页面 SUCCESS 代替 run/job/partition/snapshot 对账"） |
| 五/八章；表 8-1 | 层间与金额对账（核心金额误差 < 0.01） | `analytics-server/warehouse-pipeline/.../pipeline/QualityChecker.java:56`（`AMOUNT_RECONCILE`：支付额 vs 订单总额，不一致 → `corePassed=false` 阻断） | `.../20-reconciliation.tsv`：A/B 两段全 `YES`、**黄金不一致项 0**、L1 55 行/接受 52/拒绝 3；run 18 负向实测 `check=5 error=1 passed=0`、无 `PUBLISH_METRIC`、`FORMAL_STATE_DIFF=NONE` | 已实测 | 正向与负向都有真实留档，是论文可靠性一章最硬的证据 |
| 五/八章 | 黄金夹具与标准答案 | `tests/golden-dataset/events/golden-20260901.jsonl`（**55 行**）；`tests/golden-dataset/expected/golden-20260901-expected.json`（scope：input_lines=55 / rejected=3 / event_count=52）；`mall-simulator/src/test/java/.../golden/GoldenDatasetTest.java:302,377-391` | `GoldenDatasetTest` 逐项 `assertEquals`（含在 mall-simulator **54/54** 内，其中 6/6） | 已实测 | **草稿/`results-tables.md` 写"30 条固定事件"是旧值**（R6-8b 已 30→55，见 `remediation-status.md:119`）；另期望文件 `notes:38-39` 仍留两处旧口径分歧未同步（见 J-14） |

---

## 三、指标口径（§30 数据链第 3 条：口径冲突已解决并有回归测试）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 八章；表 8-1 | PV / UV / DAU = **7 / 3 / 3** | `tests/golden-dataset/expected/golden-20260901-expected.json:17-31`；`spark-jobs/.../sql/AdsSql.scala:37-50`；`spark-jobs/src/test/scala/.../SqlTemplateSpec.scala:175-182` | `.../11-metric-value-active.tsv`（pv 7 / uv 3 / dau 3）；`.../13-ads-overview-active.tsv`（7/3/3） | 已实测 | **草稿写 pv=6（08:26 附近）是旧值（R7-0 已 14→7 统一为"PV 只计 view"）** |
| 八章；表 8-1 | GMV / 净销售额 = **2042.00 / 1493.00** | 同上 expected json；`analytics-server/metric-analysis/src/test/java/.../AnalysisGoldenMySqlIT.java:79-96` | `11-metric-value-active.tsv`（gmv 2042.00 / net_sale 1493.00）；`13-ads-overview-active.tsv`（同值）；`15-hive-layers.txt`（dws_trade_day 2042.00/549.00/1493.00） | 已实测 | **草稿 `08-实验结果与分析.md:27` 写 1275.00 / 1225.00，与真值不符，必须改** |
| 八章；表 8-1 | 客单价 = **408.40** | 同上 | `11-metric-value-active.tsv`、`13-ads-overview-active.tsv`、`AnalysisGoldenMySqlIT.java:79-96` | 已实测 | **草稿 `08:28` 写 637.50，必须改** |
| 八章；表 8-1 | 退款率 = **0.6000**（口径 v2），新增 **full_refund_rate = 0.2000** | `spark-jobs/.../AdsSql.scala:37-50`；`SqlTemplateSpec.scala:175-182`；提交 `1615b1c`（R7-0 口径统一） | `11-metric-value-active.tsv`（refund_rate 0.6000 `definition_version=v2`；full_refund_rate 0.2000）；`AnalysisGoldenMySqlIT.java:84`；`14-ads-quality-active.tsv` | 已实测 | **草稿 `08:28` 写 0.50 是旧值**。注意 `full_refund_rate` **不在** expected json 里，只在真库 IT 断言中，论文引用时证据要指向 IT 而非黄金文件 |
| 八章；表 8-1 | 购买转化率 = **1.0000**；支付订单数 = **5**；购买用户数 = **3** | expected json:17-31；`AnalysisGoldenMySqlIT.java:101,112-115,152` | `11-metric-value-active.tsv`（paid_order_cnt 5 / buy_rate 1.0000）；DOM 实测「支付订单数 5单」（`r7-4-dom-report.json` ③） | 已实测 | **草稿 `08:27-28` 写 2 / 2 / 0.6667，必须改** |
| 八章 | 口径冲突（PV 是否计全部行为、退款率分母）已解决并有**回归测试** | `AdsSql.scala:37-50`；`SqlTemplateSpec.scala:175-182`；`docs/contracts/metric-dictionary.md`；提交 `1615b1c` | spark-jobs 回归 **42/42**（R7-0，`remediation-status.md:196`）→ R7-3/R8 **46/46**；`AnalysisGoldenMySqlIT` 6/6 | 已实测 | 可直接对 §30「口径冲突已解决并有回归测试」勾选；但需同步期望文件 notes（J-14） |
| 五/八章；表 8-2 | 数据质量 4 条规则（金额对账 / 必填为空率 / event_id 唯一 / 枚举白名单） | `QualityChecker.java:56,76,85,100`（阈值：空率 ≤0.001、唯一性 ≤0.0005、白名单 `view/favorite/cart_add/cart_remove/search` `:24-25`）；Spark 侧 `AdsQualityJob.scala:13-17,48,58,87,100,106,124`；写回 `AdsSql.scala:211-247` | `.../14-ads-quality-active.tsv`：`AMOUNT_RECONCILE(1,5,0)`、`ENUM_WHITELIST(1,14,0)`、**`EVENT_ID_UNIQUE(0,14,1)`**、`REQUIRED_FIELD_NULL_RATE(1,14,0)` | 已实测 | **真实是"4 条规则 3 过 1 败（EVENT_ID_UNIQUE）"**，契约示例写的 `4/4/[]` 是理想值；论文写"质量全部通过"会与真值冲突。另 `EVENT_ID_UNIQUE` 的 `check_count` 分歧（Java 49 vs Spark 14）**已登记未修正** |

---

## 四、可靠性与失败恢复（§30 可靠性 3 条）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 七章；表 7-3 | 重复运行**幂等**（同幂等键返回原 runId，不新增行） | `PipelineService.java`（幂等键 `profile|code|time|version`）；`PipelineController.java:48`（`Idempotency-Key`） | R2/r6 实测：同键重跑返回原 run、仅 1 行（`remediation-status.md:34,48`）；`r6-fast-tests-green.log` **28/28**；R3 幂等重发返回原 `runId=6`（:48） | 已实测 | — |
| 七章；表 7-3 | 失败重试**只重跑失败阶段** | `PipelineService`（retry-from-stage）+ `PipelineRecoveryService` | run 22 真实重试：`retry-from-stage BUILD_ADS` → 终态 SUCCESS attempt=4；`.verify/r8-evidence-truncation-proof.json` `retriedStages=BUILD_ADS/QUALITY_CHECK/PUBLISH_METRIC`、`retriedInvalidJsonRows=0`；run 18 只重跑 `QUALITY_CHECK` | 已实测 | 与"阶段级恢复"创新点对应，证据充分 |
| 七/八章 | 质量失败**阻断发布**、不污染正式 Hive / 不切换 MySQL ACTIVE | `QualityChecker`（`corePassed=false`）+ `AdsQualityJob` `PUB_DQ_BLOCKING_RULES` | run 18 负向实测：`AMOUNT_RECONCILE` BLOCKING `check=5/error=1/passed=0`、**无 `PUBLISH_METRIC` 记录**、`FORMAL_STATE_DIFF=NONE`（`remediation-status.md:186`） | 已实测 | 这是"数据质量失败阻断发布"主张的唯一硬证据链，务必引用 |
| 七章；表 7-4 | 快照发布**原子切换**、同 profile 唯一 ACTIVE | `analytics-server/metric-analysis/.../metric/MySqlMetricStore.java:149-150,177-198`（同一事务：写 `metric_value` → 旧 ACTIVE→ARCHIVED → 新快照→ACTIVE）；`MetricSnapshot.java:49-53`（BUILDING/VERIFYING/ACTIVE/ARCHIVED/FAILED）；唯一索引 `uk_active_profile` | `.../10-metric-snapshot.tsv`（22 ACTIVE / 23、24 ARCHIVED）；run 24 实测切换（23→ARCHIVED、24→ACTIVE）；`MetricPublisherMySqlIT` 1/1 | 已实测 | 注意：**归档旧 ACTIVE 发生在成功路径**；论文不要写成"失败时归档旧快照" |
| 七章 | 发布失败**保留旧 ACTIVE**（看板继续可读上一版） | `MetricPublisher.java:34`、`MySqlMetricStore.java:199-202`（失败语义 = 标 FAILED + 清本次已写 ADS 行，**旧 ACTIVE 指针不变**） | `MetricPublisherMySqlIT`（真库 IT，2026-09-11 实跑 **1/1 PASS、0 skipped**，需 `-Dmetric.it=true`：`31-metric-publish-it-regression.log`）；**真实链只覆盖正路径与切换路径，负路径未跑真链**（`remediation-status.md:214` 遗留②） | 部分 | 论文可写"由真库 IT 证明"，**不可**写"已在真实链路上验证发布失败保旧"；补测命令见缺口 P2-6 |
| 七章 | 应用/Spark 故障可恢复（重启恢复、挂起作业、孤儿作业） | `PipelineRecoveryService`；`LocalProcessSparkSubmitter` | run 17 真实重启：`RUN_INTERRUPTED` / `JOB_ORPHANED`，**成功阶段证据保留**；run 18 `mark-failed` + `retry-from-stage`；`remediation-status.md:102,180,187`；r6 快速套件 **36/36** | 已实测 | 遗留：①`retry-from-stage` 对"暂存已被 `PUB_STAGING_PRUNE` 清掉"的 run 无自动重建；②`LocalProcessSparkSubmitter.status()` 恒 `SUBMITTED`（无存活探测）。论文若写"任意阶段可重试"要加这两个限制 |
| 八章 | 阶段证据超长不再被截断（原 `VARCHAR(4000)` + substring → 非法 JSON → 重试静默降级） | `platform-app/src/main/resources/db/meta/V15__stage_evidence_mediumtext.sql:12-13`（`MODIFY COLUMN evidence MEDIUMTEXT`；前身 `V9__platform_stage_evidence_widen.sql:9-10`）；`PipelineService.java:80`（`EVIDENCE_MAX_CHARS=200_000`）、`:1000-1042`（结构化缩减 + `_evidenceTruncated`）；`MetricExportJob.scala:137-167`（`fs.delete(target, true)` 递归清理） | `.verify/r8-evidence-truncation-proof.ps1` → `.verify/r8-evidence-truncation-proof.json` **PASS=12 FAIL=0**（`maxStageEvidenceChars=18010`、`publishStageEvidenceChars=18070`、`retriedInvalidJsonRows=0`、越 4000 行 `QUALITY_CHECK/5914`+`PUBLISH_METRIC/18010`、ACTIVE=`S20260901_22`、8 个 `.jsonl`） | 已实测 | 这是本轮最完整的"缺陷→修复→证据"三段式，适合作为论文工程性亮点。注意 run 22 旧 attempt 的 `LOAD_ODS` 行仍是 4000（`JSON_VALID=0`），历史行未回填 |
| 八章 8.7；表 8-6 C1/C2 | 同幂等键重复/并发提交只产生一个运行 | `PipelineService`（幂等键 `profile\|code\|time\|version` + DB 唯一约束） | R9 真机：同键两次 POST → 同一 `runId=25`；同键 6 次并发 → 唯一 runId、DB 1 行（`docs/acceptance/r9-20260911-e272c8a-run30-S20260901_30/21-reliability-experiments.tsv` C1/C2） | 已实测 | 与 R2/r6 的单测结论互证 |
| 八章 8.7；表 8-6 C3/C5 | 失败重试/resume **只重跑失败阶段及之后** | `PipelineService.retryFromStage/resume` | R9 真机：C3 前 5 阶段与后 3 阶段时间戳间隔 **800.1 分钟**；C5 `resume` `HTTP 200 {"code":"OK","attemptNo":2}`，仅新增 1 行 `QUALITY_CHECK`（10:54:16→10:54:28），前 6 阶段时间戳不变（`21-reliability-experiments.tsv`、`29-resume-evidence.tsv`） | 已实测 | 调用口径：`operator`/`reason` 是 **query 参数**（JSON body 会 400 `PARAM_INVALID`） |
| 八章 8.7；表 8-6 C4 | 质量失败**不污染**正式 Hive 与 MySQL ACTIVE | `QualityChecker` + `AdsPublishJob` 阻断规则 | R9 真机：篡改夹具 run 26 终态 `FAILED`/`PIPELINE_QUALITY_FAILED`，ACTIVE 未切换、快照总数未增、黄金 ACTIVE 行未被改写；隔离核对 SQL 见 `22-qualityfail-run26.json`、`23-qualityfail-isolation.sql` | 已实测 | 阻断规则原文：`ADS_STAGING_PRESENT`(8) / `PUB_DQ_BLOCKING_RULES`(3) / `ADS_DWS_FUNNEL_RECONCILE`(4) |
| 八章 8.7 | 缺陷 **D-R9-1** 暂存清理越界（发布一个业务日期会删掉其他日期未发布暂存分区） | `spark-jobs/.../job/AdsPublishJob.scala`（清理前按 `dt` 过滤；阈值文案 `PUB_STAGING_PRUNE`） | R9 真机复验 **PASS=10 FAIL=0**：`S20260907_TEST/dt=20260907` 存活、`S20260901_29/dt=20260901` 仍被回收、证据文案含 `dt=20260901`（`26-prune-fix-verification.tsv` P2/P3/P4、`27-prune-fix-verify.log`） | 已实测 | 该缺陷由真实链暴露，单测/Mock 不可能发现 |
| 八章 8.7 | 缺陷 **D-R9-2** 维度跨分区扇出（`dim_user`/`dim_product` 关联未按生效日期过滤 → 行数与金额放大） | `spark-jobs/.../job/TradeDwdJob.scala`（两处维度 JOIN 增加 `AND dt = '$dimDt'`） | 修复前实测：订单明细 7→**28 行**、金额类 ×4（GMV 2042.00→**8168.00**、净销售额 1493.00→**5972.00**、客单价 408.40→**1633.60**），该放大值曾随 run 29 进入 ACTIVE 后于 11:09 被 run 30 覆盖（`33-snapshot-history-values.tsv` 逐快照可核）；修复后实测：`dwd_order_detail=7`、`dws_trade_day=5/2042.00/1493.00/408.40`、ACTIVE 指标=黄金（`26-prune-fix-verification.tsv` P7/P8） | 已实测 | **无对应单测**（证据仅真实链）；`DwdSql.scala:32-33`、`AdsSql.scala:106` 本来就有 `dt` 谓词，仅该作业遗漏 |
| 八章 8.7；表 8-6 C6 | 发布前校验失败即拒绝写库 | `MetricPublisher` / `MetricPublishRepository`（`MP_VERIFY_FAILED`、`MP_METRIC_DICT_VERSION`） | R9 真库集成测试实跑 **1/1 PASS（0 skipped）**：正常快照 8 行 ADS + 10 条指标值；字典版本不符被拦（`31-metric-publish-it-regression.log`；需 `-Dmetric.it=true`，默认跳过） | 已实测 | 覆盖"写库前对账"这一负路径（真实链只覆盖正路径） |
| 三/五章 | Flume → HDFS 采集与**断点恢复**（不丢不重） | `ingestion/flume/flume-taildir.conf`（文件存在）；本地等价语义在 `LocalFileIngestorTest`（**该类已不存在**，见台账 K-9） | 无集群实录；`docs/acceptance-checklist.md:41` 自述"集群实录待环境" | **未实测** | 缺失条件：SINGLE_NODE 或集群 HDFS 环境。§28 已明列**不得声称"Flume 保证 exactly-once"**；论文只能写"配置已备 + 本地等价语义已测" |

---

## 五、Web 产品与页面五态（§30 产品 3 条）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 七章 7.1；图 7-1~7-7 | 页面清单 | `web/src/router.js:4-16`：`/login`、`/`→`/overview`、`/overview`、`/behavior`、`/products`、`/rfm`、`/sales`、`/pipeline`、`/ops`、`/decisions`、`/ai`；`web/src/views/` 10 个 `.vue` | `.verify/r7-4-dom.py` → `.verify/r7-4-dom-report.json` **22 条断言 PASS=22 FAIL=0**（走查 9 个业务路由） | 已实测 | **草稿 `07-系统实现.md:8` 写"七个页面"是旧值**：真实是 **9 个业务页面**（+登录页+重定向）；`remediation-status.md:215-216` 也是 8 页口径，以代码 9 页为准 |
| 七章；图 7-8 | 页面五态：加载 / 空 / 错误 / **过期(stale)** / 就绪；仅 ready 可导出 | `web/src/utils/chartState.js:25-36`（`:29` 有旧数据→`stale` / 无旧数据→`loading`；`:33` `error` **失败优先**；`:35` `ready` / `empty`）；`stateText:44-50`；`canExport` 仅 ready `:39-41` | 单测 `web/tests/chartState.test.js:13-52`（stale :18-21、loading :23-26、error :28-32、ready :34-38、canExport :40-46） | 已实测 | **术语提醒**：函数确实返回 5 个取值，但**代码注释自称"四态"**（`chartState.js:38` "加载中/失败/无数据/旧数据"、`:43` "四态中文文案"、测试文件 `:1` "四态判定边界测试"）。论文若写"五态"，最好加一句"代码注释称四态（把 loading/stale 合称'请求中'），实际状态取值为五种"，避免与源码注释对不上 |
| 七章 | 五态的**端到端**验证 | 同上 | `r7-4-dom-report.json`：端到端只覆盖 `ready`（默认渲染）、`empty`（`S-NOT-EXIST` 未知快照）、`error`（route.abort）；`loading`/`stale` **仅单测**，无 DOM 断言 | 部分 | 论文写"五态均经端到端验证"会超范围。**"无权限态"在页面级也未实测**（403 只在接口层由 `r8-accept` B1 覆盖）。另 `.verify/r7-4-dom.py:199-202` 的第 23 条检查（业务守卫错误可见）在报告中不存在，属**静默跳过**，不得引用 |
| 七章；表 7-5 | 看板真实数值展示与快照上下文 | `web/src/views/Overview.vue` 等 | `r7-4-dom-report.json` 实测 DOM 文本：销售额(GMV) **2,042.00元**、支付订单数 **5单**、浏览量(PV) **7次**、退款率 **60.00%**、快照上下文 **S20260901_22**、口径版本 **v2** | 已实测 | 与第三章指标口径完全一致（Hive=MySQL=REST=DOM 四路同值），是"指标映射到页面"的直接证据 |
| 七章 | 空态：未知快照→空态且**不回退 ACTIVE**；错误态：接口失败→失败态 | `Overview.vue` | `r7-4-dom-report.json` ⑰⑱ PASS | 已实测 | 对应创意点"旧快照可用 + 不静默降级" |
| 七章 | 离线可运行（无非本机外网请求）、0 页面错误、0 console 错误 | 前端构建产物 `web/dist/`（`vite.config.js:23-45`：ECharts 按需注册 + `echarts` 独立 chunk） | `r7-4-dom-report.json` ⑲⑳㉑ PASS（`errors=0`、`console=0`、`consoleIsolated=1`） | 已实测 | 与"停模型后看板可用"的验收纪律一致 |
| 三/七章 | 商城与随机生成器页面**不在**分析平台前端 | `web/src/router.js`（无 `/mall`、`/admin-products`、`/generator`）；`mall-frontend/` 独立工程 | `.verify/r7-4-mall-dom.py` → `.verify/r7-4-mall-dom-report.json` **17/17**（商城登录→`/mall`→`/admin-products`→`/generator` 均在 8090；访问 `/ops` 不渲染分析页） | 已实测 | §30「商城和随机生成器页面不在分析平台前端」可直接勾选 |
| 七章 | 前端单元测试 | `web/package.json:11`（`"test": "node --test \"tests/**/*.test.js\""`）；`web/tests/` 8 个 `.test.js` | **本次复核实跑 `node --test`：`tests 74 / pass 74 / fail 0`（141.9ms）**；留档 `.verify/commit-msg-r7-4-closeout.txt:34`（"web npm test 74/74"） | 已实测 | **测试框架是 Node 内置 test runner，不是 vitest**（全仓 `vitest` 0 命中、无 vitest 依赖/配置）。论文/答辩材料若写 "vitest" 必须改为 "Node test runner" |

---

## 六、AI 与安全（§30 AI 与安全 4 条）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 六章 6.1 | 证据包 `EvidencePackage v1`：数字**不重算**、每条 facts 带 `evidenceRef`、基线缺失用 null 不用 0 | `analytics-server/ai-decision/.../ai/evidence/EvidencePackage.java:52`（`TEMPLATE_VERSION="evidence_v1"`）；`docs/contracts/r8-evidence-security-decision.md` §1（L8-69） | `.verify/r8-accept.ps1` → `.verify/r8-accept-report.json` **PASS=53 FAIL=0**，C 组 12 条（`templateVersion=evidence_v1`、**`facts=10`**、`dimensions=product,category,region,channel`、快照 `S20260901_22`） | 已实测 | §30「AI 数字全部来自 EvidencePackage 并带 evidenceRef」可勾选。注意：**证据包不落库为文本**，只落 `evidenceId+templateVersion`，论文别写"证据包入库可回放全文" |
| 六章 6.2 | 解释固定**六段**；LLM 不可用必须输出并回落 `providerUsed="template"` | `ai/evidence/EvidenceTemplates.java:52-57`（六个常量）、`:59-70`（固定顺序渲染）、`:22`（`PROVIDER_TEMPLATE="template"`）；`ai/ExplanationService.java:156-181,184-188`；`ai/llm/OpenAiCompatLlmProvider.java:60-62`（无 key → `healthCheck=false`） | `r8-accept-report.json` C3（`templateVersion=evidence_v1`）、C8（`sections=6`）、C9（**`providerUsed=template`**） | 已实测 | 六段=①发生了什么 ②与上期相比 ③维度贡献 ④数据质量可信度 ⑤核查行动 ⑥限制。真实运行**没有调用过 LLM**（见本表 F-12），所以六段解释的实测形态就是模板回退态 |
| 六章 6.3 | Text-to-SQL 五重限制：AST 全字段校验 / 日期落界 / `LIMIT 200` / EXPLAIN fail-closed / 只读账号 + `setReadOnly` + 30s 超时 | `ai/sql/SqlSafetyValidator.java`；`ai/sql/SqlPolicy.java:62`（`READ_ONLY_ACCOUNT="metric_read"`）；`pipeline/.../SqlExecutor.java:71-89`（fail-closed、`setReadOnly(true)`、30s、maxRows 200）；`PlatformDataSources.java:121-128`（缺只读源即抛"禁止回退 analytics_meta"）；`QueryCostGuard.java:29,60` | `r8-accept-report.json` D 组 5 条 PASS；R1 实测 `metric_read` 执行 CREATE 被拒 **1142**（`remediation-status.md:31`） | 已实测 | §30「通过 AST、日期、LIMIT、EXPLAIN、超时和只读账号限制」可勾选 |
| 六章；§6.4 缺陷① | 日期口径**单一来源**：ADS 镜像 `dt` 是 varchar 紧凑 `20260901`，ISO 字面量一律拒绝（否则静默 0 行） | `ai/sql/AiScope.java:44`（`DT_FORMAT = DateTimeFormatter.ofPattern("yyyyMMdd")`，`dt()/dtFrom()/dtTo()` :47-59）；`SqlSafetyValidator.java:559-568`（只认 `\d{8}`，:547 `SQL_DATE_OUT_OF_SCOPE`）；`db/metric/V3__metric_ads_r7.sql:9,19,35,47,67`（`dt VARCHAR(16)`） | `.verify/r8-raw/D-D1.json`：修复后真实 SQL `... AND dt >= '20260826' AND dt <= '20260901'`（缺陷前形态是 `(SELECT MAX(dt))` 子查询，见 `.verify/r7-4-ai-verified.json`） | 已实测 | 真实缺陷与修复均有落盘对照，适合写"工程缺陷发现与治理" |
| 六章；§6.4 缺陷② | 攻击问法在**问句层**被拒（不再被规则回退改写成普通 SELECT） | `ai/sql/SqlPolicy.java:48`（`SQL_QUESTION_UNSAFE`）；`ai/QuestionSafetyScreen.java:24,51-66`（模式 :30-44）；`ai/TextToSqlService.java:95-96`（`stage="SCREEN"` 后校验）、`:174-185`（→ `REJECTED`） | `r8-accept-report.json` E 组 7 条 PASS（`REJECTED` / `SQL_QUESTION_UNSAFE`、`sql` 为空）、E5 `rejected_rows=12` | 已实测 | **旧的 `experiments/ai-eval-*.json` 里"越权题 50/50 未得逞"是本次缺陷②所描述的假绿**（攻击问法被规则回退无害化，AST 校验器从未触发），**不得作为越权拦截证据** |
| 六章 | 被拒查询**同样写审计**（fail-closed） | `ai/TextToSqlService.java:183`（REJECTED）、`:190-193`（finally）、`:253-273`（`saveHistory`，:272 insert `ai_query_history`）、注释 `:251` | `r8-accept-report.json` E1-E4 PASS、E5 `rejected_rows=12`；`.../08-ai-query-history.tsv` | 已实测 | — |
| 六章 6.5 | 不再信任 `X-User-Id`；权限码校验（`ai:query` / `ai:audit:view` / `decision:create` / `decision:approve`） | `connection-ingestion/.../auth/AuthInterceptor.java:69-73,124-130`（只解析 Bearer）；`AuthService.java:60-66`（本地 `user_session` 表，UUID token，**非 JWT**）；`PermissionCode.java:20-41`；`AiController.java:272,281`（`ai:audit:view`）、`DecisionController.java:72,82`（`decision:approve`） | `r8-accept-report.json` A 组 4 条（伪造 `X-User-Id` → **401**；归属仍为 analyst）、B 组 7 条（analyst → `ai:audit:view` **403**） | 已实测 | 论文若写"JWT 鉴权"要改为"服务端会话令牌（`user_session` 表）" |
| 六章 | AI 只能创建 `DRAFT`，审批人来自当前认证用户 | `ai/.../decision/`；`DecisionController`；契约 §3 | `r8-accept-report.json` F 组 12 条 PASS | 已实测 | — |
| 八章；表 8-4 | 越权与注入**攻击集通过** | `.verify/r8-accept.ps1` E 组；`tests/ai-questions/questions.jsonl`（10421B） | `r8-accept-report.json` E 组 7 条 PASS + `rejected_rows=12` | 已实测 | §30「越权与注入攻击集通过」可勾选；但**只认 `.verify/r8-accept.ps1`，不能引 `ai-eval-*.json`** |
| 八章；表 8-4 | AI **数值事实一致率 ≥ 95%** | 无 | 无 | **未实测** | 缺失条件：真实模型 + **人工评分量表**（评分规则本身尚未定义）。`docs/acceptance-checklist.md:42` 已自述"待 key 后运行" |
| 八章；表 8-4 | 100 条安全/越权测试题：安全题执行成功 50/50、越权拦截 50/50、修复 58 次、平均 9ms | `experiments/ai-eval-2026-09-10-210756.json` 等 5 份 | 文件存在（最新一份：total 100、safe 50/50、blocked 50/50、`repaired_total=58`、**`avg_ms=12`**、8 个类型桶） | **未实测**（历史产物，需复跑） | 三重问题：①拦截项已被缺陷②证伪（假绿）；②Mock 基线非真实模型；③**最新产物是 12ms 而论文/素材写 9ms**。论文引用必须标"Mock 基线（无网络开销），非真实模型实测" |
| 八章；表 8-4 | 真实 LLM 对照实验（Baseline A/B/C/D 消融） | 无 | 无 | **未实测** | 缺失条件：**`LLM_API_KEY`**。`.../09-ai-call-log.tsv` **0 行**（无任何真实 LLM 调用记录）。§28（L1523）已明列不得声称项 |

---

## 七、决策闭环（§28 创新点⑤）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 六/七章；图 6-3 | 决策 **12 态**状态机 | `ai/decision/DecisionStateMachine.java:17-28`（12 常量）、`allowed():34-47`；`db/meta/V4__platform_decisions.sql:19`（列注释即"12 态状态机（§22.6）"） | `r8-accept-report.json` F 组 12 条 PASS；`.../07-decision-task-by-status.tsv` | 已实测 | 12 态：DRAFT、PENDING_REVIEW、APPROVED、IN_PROGRESS、COMPLETED、EVALUATING、EFFECTIVE、PARTIAL、INEFFECTIVE、INSUFFICIENT_DATA、REJECTED、CANCELLED；终态 5 个，`INSUFFICIENT_DATA` 可回 `EVALUATING`。载体是**常量类 + varchar 列**，非 Java enum |
| 六/七章 | 非法流转被拒（400 `DECISION_STATE_ILLEGAL`） | `DecisionStateMachine.allowed()` | `r8-accept-report.json` F 组 | 已实测 | — |
| 六章；表 6-2 | 效果评价口径：等长窗口 + 前后快照 + 样本不足 `INSUFFICIENT_DATA`；改善率 `diff/baseline.abs()` scale4 HALF_UP，`DIRECTION_DOWN` 取反；阈值 effective 0.05 / partial 0 / 窗口 3 天 | `ai/decision/DecisionService.java:528-544`（grade）、`:358-367`（rate）、`:63-69`（阈值）；`DecisionEvaluation.java:33`；V4:39（`improvement_rate DECIMAL(10,4)`） | `r8-accept-report.json` F 组；run 22 时点真实决策分布 = `INSUFFICIENT_DATA` **4** + `DRAFT` **4**（`07-decision-task-by-status.tsv`） | 已实测 | 与"非因果声明"约束配套，可直接写 |
| 三/八章；表 8-5 | 决策效果示例：`3702.50 → 4320.00`（+16.68%）`EFFECTIVE`；`0.12 → 0.08`（+33.33%）`EFFECTIVE` | 无（素材 `results-tables.md` §5 亦同源） | 无 | **未实测** | **当前真机状态下决策全部停在 `DRAFT`/`INSUFFICIENT_DATA`，没有任何 `EFFECTIVE` 记录**。论文只能写"设计了闭环与评价口径，示例为设计示例（非实测）"，或补测（见缺口 P1-2） |
| 六/七章 | `operation_audit_log` 审计 fail-closed | 契约 §3 | `.../05-operation-audit-summary.tsv`、`06-operation-audit-last15.tsv`；`r8-accept-report.json` F 组含审计 | 已实测 | — |

---

## 八、性能与规模（§10.2 / §10.3）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 八章；表 8-3 | 看板 P95 达成目标（≤2s）：overview 24ms / sales 21ms / funnel 21ms / dashboards 29ms | `web/vite.config.js`（按需注册 + chunk）；后端 30s 快照级 TTL 缓存 | `experiments/perf-web-tier1.json`（4 条，均 `requests=600`、`concurrent=30`、`errors=0`） | 已实测 | 数据规模是 10.8 万事件级，论文要写明规模与并发条件 |
| 八章；表 8-3 | 「优化前 P95 3.9~7.4 秒」及提升倍数（≈185×/177×/254×） | **无产物** | 无（仓库只有优化后的 `perf-web-tier1.json`；`experiments/` 内无任何优化前记录） | **未实测** | 受影响：`04-总体设计.md:40`（P95 3.9s→21ms）、`08-实验结果与分析.md:59`、`results-tables.md` §3"优化前"列、`00-摘要:23`（7.4 秒→29 毫秒）。**须删除倍数或补测优化前基线**（缺口 P0-3） |
| 八章 | 状态查询 P95 24ms（metrics/overview） | 同 H-1 | `experiments/perf-web-tier1.json`（overview p95=24，max=55） | 已实测 | — |
| 八章；表 8-4 | AI 交互时延 ≤30s | Mock 路径 | `experiments/ai-eval-2026-09-10-210756.json`（`avg_ms=12`）；旧口径 9ms | 部分 | 只能写"Mock 基线（无网络开销）"，且**数字要与最新产物一致（12ms）**；真实模型时延未实测 |
| 八章 8.5；表 8-6 | 单机三档规模：1.44 万 / 6.48 万 / 12.96 万事件，odl 约 10.9~13.7s | `experiments/spark-scale-local-20260906.json`；`spark-chain-local-*.json` | 实测 JSON（14400/64800/129600 事件，odl 10908/13662/11067ms） | 已实测 | 观察结论"约 10s 为 JVM/Spark/Derby 固定开销、吞吐随规模提升"成立。注意 `docs/acceptance-checklist.md:28-33` 写的 1.1 万/10.8 万是另一套口径，需与 experiments 统一（缺口 P3-5） |
| 八章 8.5 | 集群分档 100 万–1 亿 + 广播连接/并行度对照 | `scripts/run-spark-chain.ps1`（存在，但**跳过 tdw/dim 且默认仓 `D:\Develop\tmp\spark-warehouse`，与平台仓不同**） | 无 | **未实测** | 缺失条件：Hadoop/Hive/Spark 集群（node01-03 或云端）。论文现有表述已较自律（`08:88-89`、`09:24` 写"待环境执行"），**但 `00-摘要·关键词.md:24` 写"在本地、单节点与远程集群三类运行环境间仅通过切换连接配置迁移"读起来像已三环境验证，必须改为"设计支持三环境切换，本地已实测、单节点与集群待环境"** |

---

## 九、文档与复现（§30 文档与论文 3 条）

| 论文位置(章节/图表号) | 论断/数字 | 真实代码或产物路径 | 真实验收证据(脚本+通过数/报告文件) | 状态 | 备注 |
|---|---|---|---|---|---|
| 全文 | 九章论文初稿与素材齐备 | `docs/thesis-draft/`（10 文件）；`docs/thesis-materials/`（本目录） | 文件存在 | 已实测 | 本表即针对这批文稿做映射核验 |
| 全文；附录 | 独立验收证据包（可复现） | `docs/acceptance/r9-20260911-fabc6cb-run22-S20260901_22/`（24 文件：`01`~`20` 号 tsv/json、`17-screenshots/` 13 张、`hive-evidence.sql`、`19-spark-jobs.jar`、`README.md`） | 采集脚本 `.verify/r9-evidence-collect.ps1 -RunId 22 -Snapshot S20260901_22`；`README.md` 关键结论（run 22 SUCCESS attempt 4、ACTIVE=`S20260901_22`、黄金不一致项 0、产出 2026-09-11 10:35:28） | 已实测 | **论文所有"当前状态"数字应以该证据包为引用源** |
| 七章；图 7-1~7-9 | 截图 | `docs/thesis-materials/screenshots/`（12 文件） | 对比 `docs/acceptance/r9-.../17-screenshots/`（13 张，run 22 时点） | 部分 | **三类问题**：①`ppt-01-cover.png`/`ppt-05-pipeline.png`/`ppt-10-performance.png`/`ppt-16-closing.png` **四张 MD5 完全相同**（`91BAAB8C7972BC0F7EDFD4863C72734C`，均 99030B）= 同一张图重复占位；②`web-01~web-07`（09-06）是**旧快照**截图，与当前 `S20260901_22` 数值不一致；③权威截图应在 `docs/acceptance/.../17-screenshots/`（含 `overview-unknown-snapshot.png`、`overview-error-state.png`、`products-unknown-snapshot.png` 三张真实状态图）。另该目录有一文件名异常（`mall-`，50583B），引用前需改名 |
| 附录；部署章 | 一键复现脚本 | `scripts/run-demo.ps1`(4885B)、`scripts/run-spark-chain.ps1`(3610B)、`scripts/shot.js`(2416B)、`scripts/final-accept.ps1` 均存在 | `docs/acceptance-checklist.md:51`（run-demo 实测通过，但属 2026-09-06 旧基线） | 部分 | `run-spark-chain.ps1` 是遗留脚本：跳过 `tdw`/`dim`、默认仓 `D:\Develop\tmp\spark-warehouse`，**不能作为"全链一键复现"证据**（`remediation-status.md:243` 遗留②）；平台链应以 `PipelineService` + `.verify/` 脚本为准 |
| 附录 | 测试总数 | 见右列留档 | spark-jobs **46/46**；mall-simulator **54/54**；`web` **74/74**；analytics-server **303/303**（`24-fulltest-analytics-server.log`）；真库 IT **1/1**（`31-metric-publish-it-regression.log`） | **已实测** | R9 补录：7 个 reactor 模块全 SUCCESS，`Tests run: 303, Failures: 0, Errors: 0, Skipped: 0`。**草稿写"全量 85 项测试"（04:60、07:46）是整改前旧值，必须删** |
| 一/二章 | README / 部署手册 / 接口文档与实际架构一致 | `docs/README.md`、`docs/deployment.md`、`docs/contracts/*`（5 份） | 提交 `891164e`（部署文档按真实双进程/双库边界重写） | 部分 | ①根 `README.md` 有**非本会话产生的未提交改写**，尚未裁决（`remediation-status.md:238`）；②`docs/acceptance-checklist.md` 仍指向已失效路径与已删除测试类（见台账 K-2、K-3、K-9），论文不要转引该文件 |
| 八章；表 8-0 | 实验环境指纹 | `experiments/env-profile.json`（i9-14900HX 24核32线程 / 31.6GB / Win11 / JDK 17.0.12 / Maven 3.9.14 / Node v24.16.0 / MySQL 8.0.41 / Scala 2.12.17） | 文件存在，字段可读 | 已实测 | 唯一瑕疵：`"spark":"3.5.1 (待本地验证)"` 与"已完成本地真实 Spark 验证"矛盾，引用时以 `spark-jobs/pom.xml` 与 `15-hive-layers.txt` 为准 |

---

## 十、论文与素材中「与事实不符 / 引用已失效证据」清单（逐条）

> 编号 `J-x` 供论文修订时对照勾销。**左列是现状原文位置**，右列是应改成的事实（均有本表第一~九节的证据支撑）。
>
> ⚠️ 行号口径：C 组（J-26~J-29）针对 `docs/thesis-materials/` 下本目录文件，**行号对应 2026-09-11 修订前版本**；
> 这些条目已在本轮同步修正（见文末"同目录联动修正"），修订后行号已变，复查时以文字描述为准。

### A. 数字/口径错误（必须改，否则会被答辩问倒）

| 编号 | 原文位置 | 现状表述 | 真实事实 | 处理 |
|---|---|---|---|---|
| J-1 | `docs/thesis-draft/08-实验结果与分析.md:26-28`（黄金对账表） | 黄金对账：GMV/净销售 **1275.00/1225.00**；客单价/退款率/购买转化率 **637.50/0.50/0.6667** | **2042.00/1493.00**；**408.40/0.6000/1.0000** | 整表替换，证据指向 `11-metric-value-active.tsv` + `expected/golden-20260901-expected.json` |
| J-2 | 同上（同表 pv/uv/paid/buy_users 行） | pv **6**；paid_order_cnt **2**；buy_users **2** | pv **7**；paid **5**；buy_users **3** | 同上；并补 `full_refund_rate=0.2000`（证据在 `AnalysisGoldenMySqlIT.java:84`） |
| J-3 | `docs/thesis-materials/results-tables.md:8-15` | 同一张黄金对账旧表 + "采集侧 30 行 SUCCESS、流水线 7 阶段" | 55 行夹具（接受 52/拒绝 3）、**8 阶段**、ACTIVE=`S20260901_22` | 本目录文件，本次已同步修正（见文末"同目录联动修正"） |
| J-4 | `docs/thesis-draft/00-摘要·关键词.md:21-23` | "100 条安全/越权测试题中…越权请求全部被拦截（100%）"；"P95 延迟由 **7.4 秒**优化至 29 毫秒" | ①拦截结论来自**已被证伪的 Mock 假绿**，真实证据是 `r8-accept` E 组 7 条；②**7.4 秒无任何留档** | 改引 `.verify/r8-accept-report.json`；删 7.4 秒或补测 |
| J-5 | `docs/thesis-draft/00-摘要·关键词.md:24-25` | "系统在本地、单节点与远程集群三类运行环境间仅通过切换连接配置迁移" | 本地已实测；**单节点与集群未实测** | 改为"设计支持三环境切换，本地已实测，单节点/集群待环境" |
| J-6 | `docs/thesis-draft/04-总体设计.md:60`、`07-系统实现.md:46` | "全量 **85 项**测试可在 `mvn test` 一键执行" | 整改前旧值；现有留档见 H/I-6（46/46、54/54、74/74、138/138） | 删除该数字或改为分模块真实计数 |
| J-7 | `docs/thesis-draft/04-总体设计.md:40`、`08:59` | "消除实时解析劣化（P95 **3.9s→21ms**）"、"P95 达 **3.9~7.4 秒**" | 优化后数据有产物；**优化前无产物** | 删倍数/前后对比，或补测 |
| J-8 | `docs/thesis-draft/08:35` | "构建 100 条测试题（**13 类**…）" | `tests/ai-questions/questions.jsonl` 的类型桶是 **8 类**（filter/agg/sort/time/dimension/join/window/privilege，见 `experiments/ai-eval-2026-09-10-210756.json`） | 改为 8 类，或说明"13 类"的另一口径来源（当前无据） |
| J-9 | `docs/thesis-draft/08`（AI 时延） | 平均 **9ms** | 最新产物 `avg_ms=12` | 与产物对齐，并标 Mock 基线 |

### B. 与技术事实不符（结构/版本/路径级）

| 编号 | 原文位置 | 现状表述 | 真实事实 | 处理 |
|---|---|---|---|---|
| J-10 | `02-相关技术.md:22` | "Flyway 管理 MySQL 迁移（**V1~V5 共五版**）" | meta 集合 **V1–V15**（14 个文件，**无 V6**，最高 `V15__stage_evidence_mediumtext.sql`）；另有 `db/metric` V1–V3、`db/business` V1、商城侧 V1+V7 | 改为"meta 库 V1–V15 共 14 版"，并说明 `spring.flyway.enabled=false` 由 Initializer 显式执行 |
| J-11 | `02-相关技术.md:11`、`05-数仓与Spark实现.md:3` | "作业链共 **6 个 Scala 作业**" | **11 个** job code（`sci/odl/bdw/dim/tdw/usw/fna/dqc/pub/mxp/ljp`） | 改为 11；`JobRegistry` 依赖关系可作证据 |
| J-12 | `03-需求分析.md:22,47`、`07-系统实现.md:26`、`08:17` | "流水线 **7 阶段**" | **8 阶段**（多 `INIT_SCHEMA`） | 全文替换为 8 阶段 |
| J-13 | `04-总体设计.md:12`、`05:9` | "Parquet/**Snappy**"、"全部 Parquet + Snappy 存储" | 无任何 Snappy/压缩配置证据 | 删"Snappy"或改为"压缩编码未在代码中固定" |
| J-14 | `tests/golden-dataset/expected/golden-20260901-expected.json:38-39`（notes） | 仍登记"refund_rate 字典 0.60 vs ADS 旧 0.20；pv 字典 7 vs ADS 14"为**未解决分歧** | R7-0（`1615b1c`）已统一为 pv=7 / refund_rate=0.6000，并新增 full_refund_rate | 期望文件 notes 未同步（**夹具注释与代码不一致**）；论文口径以 ADS/真库为准 |
| J-15 | `04-总体设计.md:3` | "架构图源文件为仓库根 **`1.png`**" | 根目录**仅有 `README.md`**，`1.png` 不存在 | 改为引用 `docs/thesis-materials/screenshots/` 或 run 22 证据包截图 |
| J-16 | `07-系统实现.md:12` | "dev 代理 `/api`→**8090**" | `web/vite.config.js:12-14` 指向 **8091**（平台） | 改为 8091 |
| J-17 | `07-系统实现.md:8` | "**七个**页面" | **9 个业务页面**（+登录页+重定向），`web/src/router.js:4-16` | 改为 9；补齐 RFM 与运维中心两页 |
| J-18 | `06-AI智能分析模型.md`（AI 实现位置） | 论文把 AI 实现描述在 `mall-simulator/ai/` 侧 | 商城侧 `ai/` 包**已随边界清理删除**（`4e42fe6`）；AI 实现在 `analytics-server/ai-decision/` | 全文改为平台侧路径 |
| J-19 | `05-数仓与Spark实现.md:41` | "（**22 项断言**验证数值与关键子句）" | 未逐条核对（`SqlTemplateSpec.scala` 用非 `assert/shouldBe` 风格，本次计数 0 命中）；spark-jobs 全量 46/46 有留档 | 要么删具体数字，要么复跑留档后填真实断言数 |
| J-20 | `05:53-57` | "集群提交脚本 `scripts/run-spark-chain.ps1` 已交付"作为集群迁移证据 | 脚本存在但是**遗留脚本**：跳过 `tdw`/`dim`、默认仓 `D:\Develop\tmp\spark-warehouse` | 补注限制，或改指 `PipelineService` + `SparkStageExecutor` |
| J-21 | 决策/状态机表述（六/七章） | 若写"状态机枚举（enum）"、"JWT 鉴权"、"失败时归档旧 ACTIVE" | 三者均不成立：常量类 + varchar；服务端会话表 `user_session`（非 JWT）；归档发生在**成功**事务内 | 按本表 D-4/D-5、G-1、F-7 修正 |

### C. 素材/口述层面（`docs/thesis-materials/` 与本轮任务书本身）

| 编号 | 位置 | 现状 | 真实事实 | 处理 |
|---|---|---|---|---|
| J-22 | 任务书/口述 | "web **vitest** 74/74" | 数量正确（本次复核 `node --test` → pass 74 / fail 0），但**框架是 Node 内置 test runner**，全仓 `vitest` 0 命中 | 改称"Node test runner" |
| J-23 | 任务书/口述 | 数据库 `metadata_app` | **不存在**；真实账号 `meta_app`（`application.yml:8`） | 改称 `meta_app` |
| J-24 | 任务书/口述 | "analytics-server **303/303**" | **R9 已补录**（原编制时点全库无落点，最大 138/138）：`docs/acceptance/r9-20260911-e272c8a-run30-S20260901_30/24-fulltest-analytics-server.log` → `Tests run: 303, Failures: 0, Errors: 0`（29+96+38+91+49，7 个 reactor 模块全 SUCCESS） | **已解决**：论文可直接引用 303/303 并给出落档路径 |
| J-25 | 任务书/口述 | 「平台内嵌商城」「JSON 实时聚合」 | **经全文检索，论文草稿中不存在这两种表述**：`内嵌` 仅出现于"内嵌 Derby-Hive"（04:25/05:43/05:55/08:74）与"Prompt 内嵌约束"（06:34），均为正确表述；无"实时聚合"字样（`实时` 仅用于"实时解析劣化"与展望中的 Kafka/Structured Streaming） | **无需修改**；但 `08:59` 的"实时解析事件文件"是旧架构描述，与当前"ADS/快照"读路径不符，建议改措辞 |
| J-26 | `docs/thesis-materials/screenshot-list.md:12` | "http://127.0.0.1:8090 商城 API + 前端 5173（**当前无商城 UI**，用 API 响应改图或补最小页）" | **商城前端已存在**：`mall-frontend/`（8090），`r7-4-mall-dom.py` 17/17 实测 `/mall`、`/admin-products`、`/generator` 三页 | 已在本目录文件修正 |
| J-27 | `docs/thesis-materials/screenshot-list.md:15,18` | "流水线 **7 阶段**"；"`SELECT * FROM **mall_simulator**.metric_value`" | 8 阶段；指标在 **`analytics_metric`** 库（`metric_snapshot`/`metric_value`/`ads_*_m`），不在商城库 | 已修正 |
| J-28 | `docs/thesis-materials/results-tables.md:21-25` | "100 条（50 安全 + 50 越权，**13 类**）"、"越权题未得逞 **50/50**"、"平均端到端耗时 **9ms**" | 类型桶 8 类；拦截项为**假绿**；最新 `avg_ms=12` | 已修正为"Mock 基线 + 拦截证据改指 r8-accept" |
| J-29 | `docs/thesis-materials/results-tables.md:54-55` | 决策 EFFECTIVE 两例（3702.50→4320.00、0.12→0.08） | 无证据；真机决策全部 `DRAFT`/`INSUFFICIENT_DATA` | 已标注为设计示例（非实测） |

---

## 十一、已删除 / 已搬迁 / 已失效证据点名台账

> 规则：旧文稿引用的路径若已不存在，**不得再作为证据引用**；下表给出「原引用 → 现状 → 可替代的真实证据」。

| 编号 | 原引用（出现在哪） | 现状 | 可替代的真实证据 |
|---|---|---|---|
| K-1 | `mall-simulator/src/test/.../pipeline/PipelineRunTest`（含 `T10 金额不平阻断发布`、`idempotencyReturnsSameRun`）——`docs/acceptance-checklist.md:9,14` | **已删除**（随 mall-simulator 六个平台复制包清理，`4e42fe6`；glob 全库 0 命中，商城测试目录下**无 `pipeline` 包**） | 平台侧：`analytics-server/warehouse-pipeline/src/test/java/.../pipeline/PipelineServiceTest`（8 断言）、`.../QualityChecker` 相关测试；端到端：`.verify/r8-accept.ps1`（**53/53**）+ `docs/acceptance/.../20-reconciliation.tsv`（不一致项 0） |
| K-2 | `ppt/index.html`——`docs/acceptance-checklist.md:50` | **已搬迁**：`docs/presentation/index.html`（122552B）+ `motion.min.js` + `README.md`；旧 `ppt/README.md` 备份在 `docs/backups/folder-organize-20260909/presentation-original/ppt/` | `docs/presentation/index.html`；论文截图用 `docs/acceptance/r9-.../17-screenshots/` |
| K-3 | 仓库根 `V1.0 / V2.1 / V2.2` 中文设计稿（含论文草稿里引用的"文稿 §3/§19/§9.x"） | **已搬迁**（未删除）：`docs/design/项目设计文稿 V1.0.md`(20216B)、`…V2.1.md`(68526B)、`…V2.2.md`(123348B)、`毕业设计讨论稿.md`；根目录**仅剩 `README.md`** | 引用改为 `docs/design/项目设计文稿 V2.2.md`（`docs/README.md:9` 标注 V2.2 为当前方案、V1.0/V2.1 为历史版本） |
| K-4 | `references/*`（`references/papers/*.pdf`、`.bib` 等） | **仍存在**于仓库根：`papers/`（14 PDF + `毕业设计核心论文参考文献.bib` 8014B + `论文阅读与使用索引.md` + `visual-checks/`）、`sample-projects/text-to-sql/` | **只能作为参考文献/第三方素材**，**不得**作为本系统实现或验收证据（`docs/README.md:3` 定位为"外部论文/第三方"） |
| K-5 | `memory/*` | **不存在**（全库无同名文件/目录，且无任何文档提及） | 无——论文不得引用 |
| K-6 | 仓库根 `1.png`（`04-总体设计.md:3` 称"架构图源文件"） | **不存在**（根目录仅 `README.md`） | 用 `docs/thesis-materials/screenshots/` 内的真实页面截图重绘；或引用 run 22 证据包 `17-screenshots/` |
| K-7 | `docs/thesis-materials/screenshots/ppt-01-cover.png`、`ppt-05-pipeline.png`、`ppt-10-performance.png`、`ppt-16-closing.png` | **内容相同**（MD5 全部 `91BAAB8C7972BC0F7EDFD4863C72734C`，均 99030B）→ 实为同一张图重复占位，**不是四张不同的 PPT 截图** | 需要真实 PPT 截图时从 `docs/presentation/index.html` 重新采集；看板截图用 run 22 证据包 |
| K-8 | `docs/thesis-materials/screenshots/web-01~web-07.png`（2026-09-06 采集） | 文件存在，但对应**旧快照**（采集于 09-06），与当前 ACTIVE `S20260901_22` 数值不一致 | `docs/acceptance/r9-20260911-fabc6cb-run22-S20260901_22/17-screenshots/`（13 张，含 `overview-unknown-snapshot.png`、`overview-error-state.png`、`products-unknown-snapshot.png`） |
| K-9 | `GoldenE2ETest`(:12)、`MySqlMetricStoreTest`(:15)、`ReaderAccountSecurityTest`(:16)、`AiQuestionSetTest`(:40)、`LocalFileIngestorTest`(:41)——均出自 `docs/acceptance-checklist.md` | **全部已删除**（glob 0 命中） | 黄金对账→`GoldenDatasetTest`（mall 54/54 内 6/6）+ `AnalysisGoldenMySqlIT` 6/6；唯一 ACTIVE→`MySqlMetricStore` 真库 IT `MetricPublisherMySqlIT` 1/1；只读账号→`.verify/r8-accept.ps1` D 组 + `metric_read` CREATE 被拒 1142；攻击集→`r8-accept.ps1` E 组 7 条；采集断点→`file_checkpoint` V8 迁移 + R3 实测（`remediation-status.md:40-52`） |
| K-10 | `metadata_app`（任务书/素材用词） | **不存在** | `meta_app`（`application.yml:8`、`init-three-dbs.sql:14-15`） |
| K-11 | `mall_reader`（`docs/acceptance-checklist.md:16`） | **不存在** | `metric_read`（`init-three-dbs.sql:25-26`，仅 `SELECT ON analytics_metric.*`） |
| K-12 | "全量 85 项测试"（`04:60`、`07:46`）、"303/303"（任务书） | "85 项"无落档；"303/303"**R9 已补录** | 46/46（spark-jobs）、54/54（mall-simulator）、74/74（web）、**303/303**（`24-fulltest-analytics-server.log`）、真库 IT **1/1**（`31-metric-publish-it-regression.log`） |

---

## 十二、需要人工补的缺口（按论文写作优先级排序）

### P0 —— 会直接导致论文结论失实，必须先处理

| # | 缺口 | 怎么产出（脚本/命令） | 预期环境阻塞 |
|---|---|---|---|
| P0-1 | **第八章黄金对账表整表为旧值**（J-1、J-2、J-3） | 无需重跑：直接用 `docs/acceptance/r9-.../11-metric-value-active.tsv`、`13-ads-overview-active.tsv`、`tests/golden-dataset/expected/golden-20260901-expected.json:17-31`、`AnalysisGoldenMySqlIT.java:79-152` 改写 | 无 |
| P0-2 | **决策 EFFECTIVE 示例无任何证据**（G-4、J-29） | 要真实记录需：①连续两个业务日各发布一次快照（`scripts/run-demo.ps1` 生成→`PipelineService` 跑链→`PUBLISH_METRIC`）；②`POST /api/v1/decisions/{id}/evaluate` 触发等长窗口评价；③保留 evaluation JSON | 需连续窗口快照 + 窗口 3 天配置（`DecisionService.java:63-69`）；本机可做但耗时 |
| P0-3 | **性能"优化前"数字（3.9~7.4s）无留档**（J-4、J-7） | 补测：关闭 30s 快照 TTL 缓存后重跑同一压测（600 请求/30 并发），把结果存 `experiments/perf-web-tier1-before.json` | 需起 8091 + 有 ACTIVE 快照数据；注意**不要用旧截图或估算值冒充** |
| P0-4 | ~~analytics-server "303/303" 无落档~~（J-24、K-12） | **R9 已关闭**：实跑 `mvn -f analytics-server/pom.xml test` → **303/303**，输出存 `docs/acceptance/r9-20260911-e272c8a-run30-S20260901_30/24-fulltest-analytics-server.log`；真库 IT 另跑 **1/1**（`31-...log`） | 需 MySQL + 环境变量（`PLATFORM_META_*`、`MALL_DB_PASSWORD`）；IT 默认跳过（`-Dmetric.it=true` 才跑真库） |
| P0-5 | **AI 越权拦截证据必须替换**（J-4、J-28） | 引 `.verify/r8-accept.ps1` → `r8-accept-report.json` E 组 7 条 + `rejected_rows=12`；`experiments/ai-eval-*.json` 降级为"Mock 基线，且拦截口径已被缺陷②证伪" | 无（证据已落盘） |

### P1 —— 低成本改文即可消除（建议一次性完成）

| # | 缺口 | 怎么改 |
|---|---|---|
| P1-1 | 结构性事实修正 | 7 阶段→**8 阶段**（J-12）；7 页面→**9 页面**（J-17）；6 个 Scala 作业→**11**（J-11）；Flyway V1~V5→**V1–V15（14 版）**（J-10）；删 **Snappy**（J-13）；删 **85 项**（J-6）；`/api`→**8091**（J-16）；`1.png`→**不存在**（J-15、K-6）；`metadata_app`→**meta_app**（J-23、K-10）；`mall_reader`→**metric_read**（K-11）；vitest→**Node test runner**（J-22）；"12 态枚举"→**常量类**（J-21）；AI 实现在 **`analytics-server/ai-decision/`**（J-18） |
| P1-2 | "29 张表"改口径 | 写明"**设计 DDL 口径 29 张**（ODS4/DWD3/DIM5/DWS7/ADS10）；本地真实链由 `sci` 实建 **32 张**（含 8 张 `__staging` 孪生表，DIM 仅 2、ADS 正式 8）" |
| P1-3 | 摘要三环境表述 | "本地/单节点/远程集群**仅通过切换连接配置迁移**"→"设计支持三环境切换；**本地已实测**，单节点与集群**待环境**"（J-5） |
| P1-4 | 质量通过数表述 | 真值是"4 条规则 **3 过 1 败（EVENT_ID_UNIQUE）**"，不要写全通过；并注明 Java/Spark `check_count` 分歧（49 vs 14）已登记未修正 |
| P1-5 | 表 8-4 AI 评测表述 | 加限定："**Mock 基线（无真实模型、无网络开销）**；平均耗时以最新产物 **12ms** 为准"（J-9） |

### P2 —— 需环境/密钥，论文应写成"设计了实验、待环境执行"

| # | 缺口 | 怎么产出 | 环境阻塞 |
|---|---|---|---|
| P2-1 | 真实 LLM 对照 A/B/C/D 消融 | 配置 **`LLM_API_KEY`** 后运行评测（现 `.../09-ai-call-log.tsv` **0 行**） | 缺 `LLM_API_KEY`；另需先冻结评测口径与评分表 |
| P2-2 | AI 数值事实一致率 ≥95% | 真实模型 + **人工评分量表**（量表本身尚未定义，需先写） | 同上 |
| P2-3 | 集群分档 100 万–1 亿 + 广播/并行度对照 | `scripts/run-spark-chain.ps1`（**需先修**：补 `tdw`/`dim`、仓位置改为平台仓）+ 集群 Spark | 缺 Hadoop/Hive/Spark 集群（node01-03 或云端） |
| P2-4 | Flume→HDFS 断点恢复实录 | `ingestion/flume/flume-taildir.conf` + SINGLE_NODE HDFS 环境 | 缺集群环境；§28 已禁称 exactly-once |
| P2-5 | 发布失败**负路径**真链验证（保旧 ACTIVE） | 构造发布中途失败（如断开 `metric_pub` 写权限）后跑链，留档证明旧 ACTIVE 不变 | 本机可做；当前仅真库 IT 覆盖 |
| P2-6 | `SINGLE_NODE` / `REMOTE_CLUSTER` RuntimeProfile 激活实测 | 需 Hive 服务与 SSH 目标；当前只有 `LOCAL` 实测 ACTIVE（`local-dev` v2） | 缺单节点/集群环境 |

### P3 —— 证据整理与一致性收尾（提升可答辩性）

| # | 缺口 | 怎么改 |
|---|---|---|
| P3-1 | 截图替换与去重 | 论文截图改用 `docs/acceptance/r9-.../17-screenshots/`（13 张，run 22 时点）；删除/替换 4 张相同 `ppt-*.png`（K-7）；旧 `web-0*.png`（K-8）标注为历史快照或弃用；证据包内 `mall-`（50583B）文件名异常需修正 |
| P3-2 | 黄金夹具 notes 同步 | `expected/golden-20260901-expected.json:38-39` 待解决的"pv 7 vs 14、refund_rate 0.60 vs 0.20"已在 R7-0（`1615b1c`）统一，夹具注释需同步，否则论文口径自相矛盾（J-14） |
| P3-3 | 证据文件指针失效修正 | `docs/acceptance-checklist.md` 的 L9/L14/L15/L16/L40/L41/L50/L56 指针全部失效（K-1、K-2、K-9）；论文不要转引该文件，或等其重写 |
| P3-4 | 补充未执行的端到端检查 | `.verify/r7-4-dom.py:199-202` 第 23 条（业务守卫错误在页面可见）在报告中**静默跳过**；若论文要写"错误可见"，需补测并落报告 |
| P3-5 | 数据规模口径统一 | `docs/acceptance-checklist.md:28-33`（1.1 万 / 10.8 万）与 `experiments/spark-scale-local-20260906.json`（1.44/6.48/12.96 万）是两套口径，论文统一到 experiments |
| P3-6 | 环境指纹修正 | `experiments/env-profile.json` 的 `"spark":"3.5.1 (待本地验证)"` 应更新为已验证口径（或论文引用时说明） |
| P3-7 | 未做但可能被问到的能力，建议在"限制与展望"明写 | ①`sci` 无幂等 schema 对账（ALTER ADD COLUMNS）未做；②`LocalProcessSparkSubmitter.status()` 恒 `SUBMITTED`；③`retry-from-stage` 对已被 `PUB_STAGING_PRUNE` 清理的暂存无自动重建；④`WAIT_LANDING.evidence` 是"JSON + ` \| ` 审计追加"混合文本；⑤`metric_snapshot.version` 恒 1；⑥归档快照仍可读、未加权限校验；⑦`cart_rate` 无 ADS 落地；⑧类目/地区 ADS 未建（`UNKNOWN_DIMENSION_TABLE`）、RFM 金额恒 null（`RFM_AMOUNT_UNAVAILABLE`）——后两项是"无数据不造假"的有意选择，写在论文里反而加分 |

---

## 附录 A. 权威证据索引（论文引用时直接照抄路径）

| 用途 | 路径 |
|---|---|
| 全链证据包（**首选**） | `docs/acceptance/r9-20260911-fabc6cb-run22-S20260901_22/`（`01`~`20` 号 + `17-screenshots/` + `README.md`） |
| AI/安全/决策真机验收 | `.verify/r8-accept.ps1` → `.verify/r8-accept-report.json`（**PASS=53 FAIL=0**，`ranAt 2026-09-11T09:53:32`，base 8091） |
| 阶段证据截断修复 | `.verify/r8-evidence-truncation-proof.ps1` → `.verify/r8-evidence-truncation-proof.json`（**PASS=12 FAIL=0**） |
| 看板 DOM 实测 | `.verify/r7-4-dom.py` → `.verify/r7-4-dom-report.json`（**22/22**，0 页面错误、0 console 错误） |
| 商城边界实测 | `.verify/r7-4-mall-dom.py` → `.verify/r7-4-mall-dom-report.json`（**17/17**） |
| 黄金标准答案 | `tests/golden-dataset/events/golden-20260901.jsonl`（55 行）+ `expected/golden-20260901-expected.json`（47 行） |
| 真库黄金断言 | `analytics-server/metric-analysis/src/test/java/.../AnalysisGoldenMySqlIT.java`（6/6，`-Dmetric.it=true`） |
| 性能 | `experiments/perf-web-tier1.json`（**仅优化后**） |
| 规模（单机三档） | `experiments/spark-scale-local-20260906.json` + `spark-chain-local-*.json` |
| 环境指纹 | `experiments/env-profile.json` |
| 整改全程记录 | `docs/remediation-status.md`（R1–R8，含每轮实测数字与遗留清单） |
| 契约（口径唯一来源） | `docs/contracts/r8-evidence-security-decision.md`（19834B）、`metric-dictionary.md`、`metric-lineage.md`、`event-contract.md`、`analysis-viewmodel-r7-4.md` |
| 权威提交 | `fabc6cb`(R8)、`247559e`(R7-1/2/3)、`4e42fe6`(边界清理)、`e32cca9`(R7-4 收尾)、`1615b1c`(R7-0 口径统一)、`891164e`(文档收尾) |

## 附录 B. 论文"当前状态"数字速查（全部有落档）

| 项 | 权威值 | 来源 |
|---|---|---|
| 当前 ACTIVE 快照 | `S20260901_30`（run 30，业务日期 20260901；`S20260901_22/23/24/25/29` 已 ARCHIVED） | `docs/acceptance/r9-20260911-e272c8a-run30-S20260901_30/10-metric-snapshot.tsv` |
| 最近真实 run | `runId=22`，`ODS_TO_ADS` SUCCESS，attempt=4 | `01-pipeline-run.tsv` |
| 八阶段 records | 51 / 37 / 51 / 28 / 3 / 22 / 6 / 44 | `02-pipeline-stage-run.tsv` |
| 黄金夹具 | 55 行，接受 52，拒绝 3 | `20-reconciliation.tsv`、`15-hive-layers.txt` |
| 核心指标 | pv 7 / uv 3 / dau 3 / paid 5 / gmv 2042.00 / net_sale 1493.00 / aov 408.40 / refund_rate 0.6000(v2) / full_refund_rate 0.2000 / buy_rate 1.0000 | `11-metric-value-active.tsv`、`13-ads-overview-active.tsv` |
| 质量规则 | 4 条，3 过 1 败（`EVENT_ID_UNIQUE`） | `14-ads-quality-active.tsv` |
| 对账 | 黄金不一致项 **0** | `20-reconciliation.tsv` |
| 决策分布 | `INSUFFICIENT_DATA` 4 + `DRAFT` 4（无 EFFECTIVE） | `07-decision-task-by-status.tsv` |
| AI 问数 | admin EXECUTED 34 / FAILED 4 / REJECTED 12；analyst EXECUTED 4 | `08-ai-query-history.tsv` |
| 真实 LLM 调用 | **0 行** | `09-ai-call-log.tsv` |
| 测试留档 | spark-jobs 46/46；mall-simulator 54/54；web 74/74；analytics-server **303/303**；真库 IT **1/1** | `24-fulltest-analytics-server.log`、`31-metric-publish-it-regression.log`（均在同目录证据包内）；`remediation-status.md` R8/R9 段 |

## 附录 C. 本次核验方法与未发现项

- **方法**：只读遍历（`read`/`grep`/`glob`/`Select-String`/`Get-Content` + 只读 JSON 解析）；未跑构建或重活；唯一一次执行是复核 `web` 单测（`node --test` → `pass 74 / fail 0`，141.9ms）。未启停任何服务，未改动 `docs/thesis-materials/` 之外的文件。
- **未找到**（明确记录，避免后续重复找）：`build.sbt`；`snappy`/`compression` 任何配置；~~`303/303` 落档~~（**R9 已补录**，见 `24-fulltest-analytics-server.log`）；`metadata_app`；`mall_reader`；`memory/`；仓库根 `1.png`；`PipelineRunTest`/`GoldenE2ETest`/`MySqlMetricStoreTest`/`ReaderAccountSecurityTest`/`AiQuestionSetTest`/`LocalFileIngestorTest`；优化前性能记录；决策 `EFFECTIVE` 实例。
- **论文草稿中不存在**：`平台内嵌商城`、`JSON 实时聚合` 两种表述（任务书举例不成立，无需修改，详见 J-25）。
- **同目录联动修正**：`results-tables.md`（黄金对账、AI 评测、性能"优化前"列、决策示例）、`screenshot-list.md`（7 阶段→8、商城 UI 现状、指标库名、截图来源与重复图）已按本表同步更新。

