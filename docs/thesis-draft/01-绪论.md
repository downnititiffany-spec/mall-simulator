# 第一章 绪论（初稿）

## 1.1 研究背景与意义

电商平台每日产生浏览、点击、收藏、加购、下单、支付、退款等海量用户行为日志。
随着数据规模到达百万级以上，传统关系型数据库在分析时效与口径统一上出现明显短板；
同时，业务运营人员大多不具备 Hive SQL 编写能力，数据分析门槛高、口径易漂移。
近年来大语言模型在 Text-to-SQL 任务上展现出潜力，但直接以模型生成 SQL 接入生产存在
三个核心风险：一是字段幻觉（将不存在或口径错误的字段带入 SQL）；二是口径错误
（同一指标在不同语义下被混用，如"销售额"与"净销售额"）；三是安全越权
（未经约束的 SELECT 可访问底层明细，甚至被注入改写语句）。

本课题的目标是设计并实现一套"基于 Spark 大数据平台和智能分析模型的电商用户行为分析系统"，
以数据工程为可信基础、业务应用为主要入口、AI 为横向增强能力：由 Spark/Hive 承担全部可信计算，
由受控 Text-to-SQL 与证据约束解释承担智能问答，并在人工审核下形成可追踪的决策闭环，
最终为普通电商运营人员提供"能看、能问、能做、能复盘"的自助分析体验。

## 1.2 国内外研究现状

> 引用编号对应 `references/papers/毕业设计核心论文参考文献.bib`；正文写作时替换为学校要求的
> 序号格式（GB/T 7714 或模板），标注位置以本节方括号为准。

1. **数仓建模与电商分析**：经典维度建模 [kimball2013warehouse] 与 Inmon 型企业数仓 [inmon2005building]
   为指标口径统一提供工程范式；HDFS [shvachko2010hdfs]、Hive [thusoo2010hive] 与 Spark
   [zaharia2012rdd, armbrust2015sparksql] 构成海量行为日志离线加工的主流开源组合；
   电商场景的用户分层/聚类分析研究 [john2023retailclustering] 说明该方向的持续关注。
   本文按 ODS/DWD/DWS/ADS 四层建模，并以指标字典约束口径（对应中文 NL2SQL 综述
   [liu2022nlsqlsurvey] 指出的口径问题）。

2. **Text-to-SQL**：从序列到序列 [zhong2017seq2sql, xu2017sqlnet] 到大规模数据集驱动
   （WikiSQL [zhong2017wikisql]、Spider [yu2018spider]、BIRD [li2023bird]），
   再到解码约束（PICARD [scholak2021picard]）与分解式上下文学
   （DIN-SQL [pourreza2023dinsql]）；综述见 [deng2022texttosqlsurvey]。
   工程落地侧普遍采用"受约束生成 + 解析校验"的防御式架构，本文的
   "语义层裁剪 + 结构化输出 + 语法树校验 + 至多一次修复"即属于该路线，
   并以 ADS 白名单从架构上杜绝明细越权；数据库安全基线参考 OWASP
   注入防护建议 [owasp_sql_injection]。

3. **可信 AI 分析**：大模型结论的证据约束是降低幻觉风险的关键；本文以执行证据
   （SQL、行数、耗时、口径、快照）与结论结构化输出绑定实现该约束，
   并以规则回退 [apache_flume 等外部依赖按需冗余] 保证模型不可用不阻塞主线
   （此句为设计说明，引用可省略）。

## 1.3 主要研究内容

本文围绕一条"业务发生→可靠采集→分层加工→质量校验→指标发布→固定看板→
智能问答→证据解释→人工决策→效果复盘"的完整链路开展设计与实现，主要研究内容为：

1. 面向电商场景的可复现数据工程：Java 模拟商城与场景化数据生成器、事务 Outbox 与
   断点采集、四层 Hive 数仓与 Scala Spark 作业链、快照版本化发布机制；
2. 面向数仓语义的智能分析模型：业务语义层、受控 Text-to-SQL 流程、四层 SQL 安全校验、
   一次受控修复、证据约束结果解释与结构化输出；
3. 决策闭环与审计：AI 建议→人工审核→锁定基线→效果评价的 12 态决策生命周期（状态集合与
   迁移规则由常量类显式定义），以及查询历史/模型调用/流水线全链路审计；
4. 多层次实验验证：黄金数据对账、受控 Text-to-SQL 攻击集、接口性能压测、Spark 数据规模
   扩展与决策闭环运行状态（含样本不足判定）。

## 1.4 创新点

1. **面向数仓语义的 Text-to-SQL**：以"主题识别→Schema 裁剪→语义+少样本→受约束 JSON 输出"
   减少字段幻觉；指标口径由字典统一提供，模型不猜测。
2. **四层 SQL 安全校验与至多一次修复**：字符串预检、JSqlParser 语法树、ADS 表白名单、
   LIMIT/时间条件资源约束，配合数据库只读账号构成纵深防御；失败反馈一次修复，
   杜绝无限循环；越权与注入问法在生成 SQL 之前即被问句层拒绝（实验见 8.3）。
3. **证据链驱动的智能分析**：结论必须引用真实查询结果的字段与数值，
   推测与事实分离（facts/possibleCauses），页面回显 SQL、行数、耗时与口径，
   并声明前后对比非因果推断。
4. **场景驱动可复现数据工程**：同种子同结果的事件生成、断点不丢不重采集、
   金额对账阻断发布、快照唯一活跃的发布机制，使论文实验完全可复现。
5. **指标快照决策闭环**：AI 只能创建草稿，人工审核锁定基线，执行后按目标方向
   评价效果，形成"发现—解释—行动—复盘"的完整业务循环。

## 1.5 论文组织结构

第一章绪论；第二章相关技术与选型；第三章需求分析；第四章系统总体设计；
第五章数仓与 Spark 设计实现；第六章智能分析模型设计实现；第七章系统实现（页面与接口）；
第八章实验结果与分析；第九章总结与展望。