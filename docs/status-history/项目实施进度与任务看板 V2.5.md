# 项目实施进度与任务看板 V2.5

> 2026-09-14；唯一新执行入口。对应《项目完整实施指导书 V2.5》。
> 状态基线 HEAD 8983616 + 当前未提交工作树；本版新建不代表任务完成。
> 历史编号/证据在 V2.2 看板保持原文；本表替换后续执行队列，不删除历史需求。

## 1. 使用规则

READY=可派发，非已开工；TODO=尚未达到前置；REVIEW=已有实现等待完整验收；DONE_LIMITED=仅限定范围通过；DEFERRED=显式后置；BLOCKED须写具体缺失信息；DONE须证据足够。不得从“有文件/已提交/测试通过”直接推导全功能完成。

以下每行是一个可交付任务；复合项可建立 <ID>-01/02 子任务，不改原范围。表内“依赖”省略 V25- 前缀的编号均指本表；P3-02-* 保留原编号。每行默认 owner=未分配、最近反馈=未开工、下次反馈=开工时填写、结果证据=待交付，不能把 READY 当作正在后台执行。

活跃任务最长30分钟回报，失败立即报；修改本看板前备份。状态与证据就地更新，不在末尾追加长台账。需求实质变化另出版本；完成证据一任务一目录。任务包完整字段见指导书 §10。

## 2. 历史成果保留

| 范围 | 状态 | 证据与限定 |
|---|---|---|
| 三程序拆分基础 | DONE_LIMITED | 旧M1各项历史证据保留；不能替代分析平台异构源闭环 |
| 本地小测试09-14 | DONE_LIMITED | local-readiness-20260914.md：后端566中2失败；Spark111通过；前端80通过 |
| 历史run47链 | DONE_LIMITED | m3-step8-parity-20260912/；不是当前WSL验收 |
| 集群1000条10作业 | DONE_LIMITED | e4-cluster-1000-20260912/README.md；同输入对照与完整导入未闭环 |
| P5夹具实验 | DONE_LIMITED | p5-heterogeneous-source-20260912/IMPL-REPORT.md；已发现92/180不符，不能算P5成功 |
| P3-02草案 | DONE_LIMITED | p3-02-mapping-executor-20260912/README.md；规格盘点，没有生产代码 |

## 3. 可执行任务队列

| ID | 小任务 | 状态 | 依赖 | 允许改动范围 | 完成判据 | 最小测试/证据级别 | 指导书 |
|---|---|---|---|---|---|---|---|
| V25-T01 | 库名字面量门禁 | REVIEW | 无 | platform-common 测试、两处 Spark 注释 | 注释误命中消除；真实 SQL 硬编码负例仍红 | T0/T1 | §9.5 |
| V25-T02 | manifest 冻结夹具 | REVIEW | 无 | platform-app 测试/专用 tests fixtures | 新旧清单分开校验；真实40–43.json不动 | T0/T1 | §9.5 |
| V25-S01 | 写入型 IT 安全 | REVIEW | 无 | metric-analysis IT、测试安全 helper、相关 scripts | 正式库在写前被拒；受限账号；cleanup按runId；扫描其他*Test | T0/T1+隔离IT | §9.4 |
| V25-S02 | 全链测试隔离配置 | IN_PROGRESS | S01 | 测试档案、scripts、测试配置 | server/db/namespace/HDFS/checkpoint/快照全隔离，跑前后证据 | T1 | §6.6/9.4 |
| V25-S03 | IT/脚本同类写入面收口 | DONE_LIMITED | S01 | mall-simulator 测试配置、platform-app IT、spark-jobs SmokeTest、scripts 隔离预检 | R-1/R-3/R-4/R-5/R-6 逐项收口：默认关闭、实例白名单、写前拒绝、无明文口令、skip 不等于 PASS | T0/T1 | §9.4/§6.6 |
| V25-S04 | 隔离脚本与配置收口 | READY | S03；run-demo 项等 L4 | scripts/、analytics 侧凭据口径、主配置 application.yml | 仅限隔离库硬校验、失败统一 exit 5、credref 口径单一所有者、主配置不得默认连 3306 | T0/T1 | §9.4/§6.6 |
| V25-S05 | 存量明文默认口令收口与门禁 | READY | S03；与 U01 同域（U01 管权限语义，本行管"运行期不得有明文默认口令"） | 运行期配置与脚本（`platform-app/src/main/resources/application.yml`、`EnvCredentialService`、`scripts/**`、`warehouse/migrations/**`）；**不得改写 `docs/**` 历史证据内的口令字面量** | 运行期明文默认口令 0 命中；缺凭据即拒绝启动；`git grep` 剩余命中只允许"显式标注的测试夹具/历史证据"白名单 | T0/T1 | §8.4 |
| V25-E3 | 隔离库真链验收（E3） | DONE_LIMITED | S01/S02/S03（隔离前置）；W03（3307 实例） | 只读取证＋仓内 `docs/acceptance/v25-e3-isolated-chain-20260914/`；**不得改提交配置与主代码** | 隔离库建表→三步链路→指标非空→**3306 零写三路自证**；F-88 写侧复测另立 | T2/E3 | §6.5/§9.4 |
| V25-W01 | WSL与宿主只读盘点 | DONE_LIMITED | 无 | 环境清单文档；不改系统 | 版本、资源、发行版、磁盘、现有配置、待确认权限成表 | E1 | §6.2/6.6 |
| V25-W02 | 迁移前源码/worktree备份 | DONE_LIMITED | W01 | 备份目录；主目录只读 | 独有改动与hash逐一登记，不丢未提交/未跟踪文件 | E1 | §6.3 |
| V25-W03 | WSL运行时与兼容矩阵 | DONE_LIMITED | W01/S02 | 获准Linux安装目录、环境scripts | Java17/8、Spark/Hadoop/Metastore/MySQL版本锁定；不升级最新版 | E1+探针 | §6.2 |
| V25-W04 | Linux源码副本构建 | TODO | W02/W03 | 新Linux开发目录 | 独立Git关系、Linux依赖重装、T1；用户确认后唯一主目录 | T1 | §6.3 |
| V25-W05 | 正交档案与SINGLE_NODE修复 | READY | 无；写库验收等S02 | RuntimeProfileSnapshot/JobCommandBuilder/Factory、档案API与迁移 | SINGLE_NODE缺省local[1]；Metastore与JDBC分开；配置矩阵拒冲突 | T0/T1 | §6.4 |
| V25-Q01 | F-88版本化规则 | IN_PROGRESS | S01/S02 | RuleSeverity/DataQualityGate/QualityChecker/Parser/Validator；总控独占 | 现有未提交代码须修版本边界/阈值；未知规则拒绝；完整规则版本记录 | T1 | §7.3.1 |
| V25-Q02 | 质量失败保快照 | TODO | Q01/S02/W03 | PipelineService及发布集成测试；总控独占 | BLOCKING/ERROR失败，原ACTIVE可读不变；WARN有效例 | T2/E3 | §7.3.1/7.4 |
| V25-C01 | P3-02新映射契约冻结 | READY | 无 | contract-specs、docs/contracts、source profile新版本 | D1–D14逐条Schema/样本/差异/验收映射；旧语义不暗改 | T0/T1 | §5.6 |
| V25-C02 | 原因码单一所有者 | TODO | C01 | 中立契约+DDL注释+Java/Scala派生守卫 | 主码/细码/旧码映射一致，历史不回填 | T1 | §5.6 D6/D12 |
| P3-02-a | 纯映射器与dry-run | TODO | C01/C02 | connection-ingestion profile/normalize、API/测试 | 字段/金额/枚举/时间/数组与碰撞正反例；覆盖分母准确 | T1 | §5.7 |
| P3-02-b | 规范化生产接线 | TODO | P3-02-a/S02/Q02 | LocalFileIngestor/IngestionService/manifest/迁移 | raw全保真、accepted规范化、版本冻结、故障重放 | T2/E3 | §5.7 |
| P3-02-c | 两源仓库接线 | TODO | P3-02-b | 源解析、Spark输入/各层、指标作用域 | 相同外部ID不串源；不修改核心模板适配B | T2/E3 | §5.7 |
| V25-I01 | LandingStorage/HDFS接口 | TODO | W03/W05/S02 | connection-ingestion storage/connector、scripts | HDFS URI不走Files；UUID探针；断点在持久化后推进 | T1+真实HDFS | §5.3/6.5 |
| V25-I02 | Flume最小接入 | TODO | I01 | Flume配置、spool桥接、§5.3连接器接口、FlumeLandingConnector、接入编排（`LandingStorage`/`LocalLandingStorage`/`HdfsLandingStorage` 已存在，不必重写） | 完整文件投递、FileChannel恢复、checksum、未关闭文件不读 | T2/E3 | §6.5（§11.4 文字待修订，以本行为准） |
| V25-E01 | WSL黄金55条全链 | TODO | W04/I02/P3-02-b/Q02 | 独立验收scripts与证据 | HDFS→Hive/Spark→MySQL→API；唯一快照、失败与重放 | T2/E3 | §6.6 |
| V25-P01 | 数据源管理页面 | TODO | C01；接口可与映射并行 | web source页面、source API、router总控 | 源列表/状态/连接/启停/激活/dry-run，权限与错误明示 | T1/E5 | §5.5/12 |
| V25-P02 | 按源指标与历史读取 | TODO | P3-02-c | metric-analysis API/MetricStore、web信封 | sourceId/code与snapshot一致；跨源拒读；归档显式入口 | T1/T2/E5 | §7.4 |
| V25-E02 | P5异构源闭环 | TODO | E01/P3-02-c/P02 | 版本化A/B fixture、验收脚本 | 完整异构成功、可选缺失可见、必需缺失拒绝；两源相同ID隔离 | T2/E3 | §5.7 |
| V25-E03 | 普通员工E5 | TODO | E01/P01/P02 | 页面验收脚本/截图/说明 | 不填技术参数；失败有解释和旧结果标签；停商城仍查历史 | E5 | §8.1 |
| V25-D01 | DIM和交易校验补齐 | TODO | C02/Q01 | spark-jobs DIM/交易、warehouse DDL | 日期/地区/指标维、订单状态、订单项/付款/退款三类独立校验 | T1/T2 | §7.1 |
| V25-D02 | ADS指标补齐 | TODO | D01 | Spark DWS/ADS、metric发布 | 分类地区/净销售/复购/RFM字段真实落表发布；稳定排序 | T1/T2 | §7.2 |
| V25-D03 | 异常检测接线 | TODO | D02 | metric-analysis异常计算与结果表/API | ≥14历史点、MAD/zscore边界、版本、快照、页面解释 | T1/T2 | §7.2 |
| V25-D04 | 12项质量规则矩阵 | TODO | D01/D02/Q01 | 规则定义/执行器/负例；无全局降级 | §7.3每项算法输入与oracle、版本、失败保快照 | T1/T2 | §7.3 |
| V25-A01 | 真实AI分析证据链 | TODO | P02/E02；真实凭据待提供 | ai-decision provider/evidence、安全测试 | 只读SQL AST/EXPLAIN/限额；真实调用与mock分开；同快照数值 | T1/受控API/E5 | §8.2 |
| V25-A02 | 决策五模板 | TODO | D02/A01 | ai-decision决策、web页面 | 五模板与四类效果样本、等长窗口、DRAFT/审批/审计 | T1/E5 | §8.3 |
| V25-A03 | 受控AI建仓专项 | TODO | E02/P01/A01/C01 | AIW-001～020原专项文件范围 | 先画像映射，后蓝图/编译/sandbox/审批/回滚；同B人工vsAI实测 | T1/T2/E5 | §8.2/13 |
| V25-U01 | 权限凭据审计补齐 | TODO | S01/P01 | auth/security/audit/前端权限 | data_dev角色、禁默认口令、启动与重试分权、脱敏、跨源拒绝 | T1/E5 | §8.4 |
| V25-R01 | 修复覆盖矩阵 | DONE_LIMITED | 无；随任务持续更新 | 独立audit文档，不改历史证据 | V2.5每条要求映射代码+断言+运行证据；纠正原229条误判 | 文档核验 | §1/12 |
| V25-R02 | 重新冻结269条验收 | TODO | 当前产品任务与R01 | 新冻结验收与README | 逐条复核；新增/替代/延期可追溯，不沿用132/90/42 | E1～E5 | §11 |
| V25-L01 | WSL1000条里程碑 | TODO | E01/E02 | 隔离负载与验收脚本 | 固定seed、批次恢复、内存峰值/耗时；非性能提升宣称 | T3 | §9.2 |
| V25-X01 | 旧M3多节点/YARN/SSH | DEFERRED | 用户重启该里程碑 | 旧M3全部任务保留 | 含55同输入两侧对照、1000链、权限日志；历史10/10不作全验收 | E4 | §6.7 |
| V25-X02 | Doris服务库 | DEFERRED | 当前底座完成 | MetricStore实现/同步/验收 | 同指标定义/快照语义对账 | 后续阶段 | §7.4 |
| V25-X03 | ClickHouse/DataX/Kafka | DEFERRED | 对应扩展立项 | 接口与能力矩阵 | NOT_IMPLEMENTED不可展示可用 | 后续阶段 | §1/5.3 |
| V25-X04 | 论文与答辩正文 | DEFERRED | 项目完成+学校要求 | 不在本轮写正文 | 只允许事实与测量记录 | 后续阶段 | §1.1 |

## 4. 当前工作登记（执行Agent开工时填写）

| ID | Owner | 当前子步骤 | 上次反馈 | 下次反馈 | 提交/dirty指纹 | 测试结果 | 证据链接 | 阻塞或下一动作 |
|---|---|---|---|---|---|---|---|---|
| V25-T01 | 泳道L2(T01+T02) | 根因已定位（两处裸库名在 scaladoc 块注释内）；改扫描器词法排除注释 | 2026-09-14 12:10 | 2026-09-14 12:35 | HEAD f00462c；本人负责文件已可编译 | **四级＝测试通过（E1/E2）**：注释误命中消除＋真实 SQL 负例仍红；红证 12:16:37 `EXIT=1`（判红 `TradeDwdJob.scala:145`/`SurrogateKey.scala:160`）→ 绿证 12:19:11 `EXIT=0`/BUILD SUCCESS/33 tests/0F；全反应堆收集口径 9/0/0 | docs/acceptance/v25-t01-t02-baseline-20260914/ | 待总控复核后置 DONE_LIMITED；**E3 未测** |
| V25-T02 | 泳道L2(T01+T02) | 已证 40–43.json 为运行期新增（19 键含 4 新键）非回填；拆 hermetic 契约测试＋sha256 运行时巡检 | 2026-09-14 12:10 | 2026-09-14 12:35 | HEAD f00462c；本人负责文件已可编译 | **四级＝测试通过（E1/E2）**：契约组 9/0/0＋巡检组 3/0/0＋单方法 1/0/0 三组 `EXIT=0`（正确键 `-Dsurefire.failIfNoSpecifiedTests=false`，旧四条命令确曾 `SKIPPED` 未执行——R01 判定仍成立）；等长字节改写/回填/缺失/目录不在四负例判红；43 行 manifests 快照前后逐行相同、`git status` 空 | docs/acceptance/v25-t01-t02-baseline-20260914/ | 40–43 只报告不判红，禁删真实文件；**E3 未测** |
| V25-S01 | 泳道L1(S01+S02) | 已整改两个写入型 IT：库名只取 TestRunContext、host/账号只走门禁解析链、无正式兜底；cleanup 按 runId 并先列删除目标；新增 TestIsolationGuard（端口白名单 {3307}、禁 3306、实连核对 SELECT DATABASE()/@@server_uuid/@@port/SHOW GRANTS）；负向测试在**建连前**失败 | 2026-09-14 12:30 | 已交（T1 待编译绿后补跑） | HEAD 3fcf90e；净改动＝2 个 IT＋门禁 helper＋测试 | **四级＝测试通过**（T0 20/20 退出码 0；负向 3306 退出码 1 且零连接；正式库内容指纹 452b7223… 与 12/110 行、ACTIVE 指针一致；`v25it-%` 档案 0 行）⇒ **未达限定验收**；**S03 项与真实隔离往返待收口**（L1 泳道 13:0x 中途失败退出，未交报告） | docs/acceptance/v25-s01-it-safety-20260914/ | 由 L5 独立复核 R-1/R-3/R-4/R-6 与脚本项；R-5 已由总控收口（见 §5） |
| V25-S02 | 泳道L1(S01+S02) | **部分交付**：`integration.local.properties.template` 已按 R2/Q9 改为 127.0.0.1:3307＋WSL-V25IT-ISOLATED＋`v25it_<run>_*` 账号计划；Hive namespace/HDFS 根/manifest 根**未做·环境不具备** | 2026-09-14 12:30 | 2026-09-14 13:00 | HEAD 3fcf90e；模板与测试配置 | 未验收（隔离实例不存在，真库往返从未跑通） | docs/acceptance/v25-s01-it-safety-20260914/ | 下一动作：**3307 实例已由 L3 交付且 Windows 可直连**，由 L4 把模板里的占位指纹 `WSL-V25IT-ISOLATED` 换成真 uuid `de8ebbea-aff4-11f1-8037-00155d5dba47` 并跑通一次隔离往返 |
| V25-S03 | 泳道L1(IT安全续) | 收口 L1 扫描出的 R-1/R-3/R-4/R-5/R-6：mall-simulator 测试 yml 直连正式库＋createDatabaseIfNotExist＋Flyway、SourceRegistryMigrationMySqlIT 默认真实元数据库、assumeTrue(false) 假绿、SparkStageExecutorSmokeTest 无门禁且递归删目录、scripts 硬编码 root 口令与直删业务库 outbox | 2026-09-14 12:35 | 2026-09-14 13:00 | HEAD 3fcf90e；只改测试配置/测试类/scripts 预检，**不改正式部署目标** | **总控接管**：L1 泳道 13:0x **中途失败退出**（未交报告）；其遗留改动经核对全模块 `test-compile` **7/7 SUCCESS / exit 0**；R-5 由总控收口（`SparkStageExecutorSmokeTest`→`SparkStageExecutorSmokeIT`，默认套件 **131/0F/0E exit 0**，显式点名 **1E/exit 1** 硬拒）；R-1/R-3/R-4/R-6 由 L5 独立验证中 | docs/acceptance/v25-s01-it-safety-20260914/ | **L5 独立复核已完成（14:1x）**：R-1/R-4＝提交完成✅＋限定验收✅＋完整验收❌（默认关闭红 30 ERROR／120 run·20E 且 **Skipped=0**＝设计意图；绿灯需 3307 ⇒ 未取证）；R-6＝限定验收⚠️部分；R-3＝总控实测✅（L5 未重跑）。**修复**：`scripts/it-prepare-isolation.ps1:28` `$Host` 撞只读自动变量（阻塞级）已修并实测；**遗留**：`run-demo.ps1` DELETE 可达、`smoke-pipeline.ps1` exit 码、analytics 侧 credref 无解析器、主配置未收口 ⇒ 全部移入 **V25-S04**；**下一动作：V25-S04 按单写者串行执行，run-demo 项必须等 L4 E3 跑通后再改** |
| V25-S04 | 泳道待派（总控独占，单写者） | 登记 6 项：①`run-demo.ps1` 加"仅限隔离库"硬校验 ②`smoke-pipeline.ps1:137/138/149/194` `Q()` 失败统一 exit 5 ③analytics 侧 `credref:` 口径收口 ④主配置 `mall-simulator/src/main/resources/application.yml:8-10` 与 generator 同形配置收口 ⑤范围外明文残留 3 处（`start-all.ps1:19`/`accept-p1-baseline.ps1:28`/`accept-three-programs.ps1:9`）⑥既有 NPE 另单 | 2026-09-14 14:3x | 未开工 | 未改任何文件；HEAD 1cb318b（本行占用文件仍未动） | 未开工（n/a） | docs/acceptance/v25-s01-it-safety-20260914/L5-verification-report.md | **硬约束**：不与在跑泳道并发改同一文件；④属"应用默认目标"变更 ⇒ 执行前须总控确认；①②必须等 L4 E3 结束后开工；**⑤已并轨 V25-S05**（本行剩 5 项） |
| V25-S05 | 未分配（总控独占热点配置） | 登记 L6 §6 的存量明文默认口令面：`EnvCredentialService.java:20`、`platform-app/src/main/resources/application.yml`、`SourceRegistryMigrationMySqlIT.java`、`scripts/remediate/split-migrations.ps1`、`warehouse/migrations/init-three-dbs.sql` 等；总控 2026-09-14 实测 `meta_app_pw_2026` **17 命中/16 文件**、`metric_pub_pw_2026` **16/15**、`metric_read_pw_2026` **23/21**（**含 `docs/**` 历史证据与 raw 日志，证据不计入待修面**） | 2026-09-14 15:1x | 未开工 | 未改任何文件；HEAD 1cb318b | 未开工（n/a） | docs/acceptance/v25-stray-consolidation-20260914/README.md §6 | **L6 建议优先级高于散落物归并**；S04 ⑤ 并入本行；改 `application.yml` 默认目标属"应用默认行为"变更 ⇒ 执行前须总控确认；**不得删改历史证据中的字面量**（只登记不改） |
| V25-E3 | 泳道L4（已收官） | 交付 `docs/acceptance/v25-e3-isolated-chain-20260914/`：README 13 节＋`scripts/` 9（8 ps1＋1 只读 SQL）＋`raw/` 24＋`raw/http/` 19 ＝ **53 文件/377 KB**；另出总控核验 `CONTROLLER-VERIFICATION-20260914.md` | 2026-09-14 14:22（交付）／15:2x（总控核验） | 已交 | HEAD 2a10483；L4 自述"未改任何已提交配置"（仅启动期 env 覆盖） | **四级＝提交完成✅／测试通过❌（未运行任何自动化测试；实测 jar 为 `-DskipTests` 产物）／限定验收✅（限定集＝3307／工件 `7BE717A9…7561`／仅 env 覆盖／3 步链路／local[2]）／完整验收❌（5 条缺项＋F-88 写侧未复测）**；3306 零写三路（连接面 3306=0·3307=30；内容面 8 键前后一致；窗口面 23 表 `inwin_*=0`） | docs/acceptance/v25-e3-isolated-chain-20260914/ | **总控已独立复跑 `fingerprint-3306.sql`：8/8 与 after 一致、差异 0 ⇒ 可复现且仍零漂移**；下一动作＝**F-88 写侧复测**（重新构建含 `V19`/`V20` 的工件后**仅在 3307** 重跑链路）；D-5 闸门落 **V25-S04**；见 §5 |
| V25-W01 | 泳道L3(只读盘点) | 已交：71 项已确定、10 项未取证、12 项待用户确认；README 39 KB/277 行＋raw 10 件 | 2026-09-14 12:2x | 已交（本任务无需再反馈） | HEAD 7296fda；只读未改配置（**副作用**：把原 Stopped 的 Ubuntu 启成 Running，未复位） | E1 成表；不声明验收 | docs/acceptance/v25-w01-env-inventory-20260914/ | 下一动作：等用户答「安装与目录／资源上限与重启／sshd」后开 W03；其余问题已按指导书裁决 |
| V25-W02 | 泳道L3(环境) | 主目录与 worktree 只读盘点→独有改动/未跟踪/未推送逐一 hash 登记→备份到授权目录 | 2026-09-14 12:15 | 2026-09-14 12:45 | HEAD 09d7046；主目录只读 | **四级＝限定验收通过；完整验收未通过**：4 快照 5195 文件/189 MB 逐文件 sha256；初版「干净/仅 4 改动」被**自我推翻**（worktree 的 `.git` 存 Windows 路径 ⇒ WSL 内判 prunable ⇒ 静默假阴性），复采 `m3-jdk8fix` **1226**/`.f88-baseline-wt` **1524**，2750/2750 重算 0 失配；密钥正则曾误杀 9 个真实源码文件（`cat-file -p` 返回 0 字节 ⇒ 从未进对象库）已按内容补备 | docs/acceptance/v25-w02-w03-wsl-runtime-20260914/ | 未取证 W-U9（两 worktree detached HEAD 是否已推送，`fetch` 属 git 写操作）；Node/pnpm/Flume 另派 |
| V25-W03 | 泳道L3(环境) | 已获授权：追加 .wslconfig 限额＋重启复位＋按清单安装锁版本运行时＋两条 Java 命令路径兼容矩阵 | 2026-09-14 12:15 | 2026-09-14 12:45 | HEAD 09d7046；WSL 侧安装 | **四级＝限定验收通过；完整验收未通过**：`.wslconfig` memory=8GB/processors=4/swap=4GB 实测生效（Mem 7942 MiB/nproc 4）；Spark **3.5.1**/Hive **3.1.3**/Hadoop **3.3.4**/JDK17 **17.0.12**/JDK8 **1.8.0_351**/MySQL **8.0.41**/Connector-J **8.0.33** 锁定；`ssh.socket`+`ssh.service` 双停用（:22 无监听）；**隔离 MySQL `127.0.0.1:3307` 可用**（uuid `de8ebbea-aff4-11f1-8037-00155d5dba47`）；禁项零越界（未 format/未启 HDFS/HMS/YARN/HiveServer2、未碰宿主 MySQL） | docs/acceptance/v25-w02-w03-wsl-runtime-20260914/ | 未完成：Node/pnpm/Flume 未装（WSL 内 `apt` 完全不可用）；W-U8（Spark↔HMS 3.1.3 连通）未实跑；**`wsl --shutdown` 后 3307 不可用** |
| V25-W04 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-W05 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-Q01 | 总控泳道(Q01独占热点文件) | **当前优先：把 platform-common 恢复到可编译**（12:08 实测 `QualityRuleDefinition.java:84` 找不到 `isKnownSeverity`，已阻塞另两条泳道 E2）；随后做版本边界/未知规则拒绝/读侧兼容版本范围 | 2026-09-14 12:10 | 2026-09-14 12:35 | HEAD 6062434 ＋ Q01 未提交工作树（含新文件 QualityRuleDefinition/QualityRuleCatalog） | 未验收（E1/E2 待跑；主代码当前不可编译） | docs/acceptance/f88-dq-severity-20260912/ | 下一动作：编译绿后一句话回报；凡触库测试等 S01/S02；DDL 草案只入 raw/，迁移号由总控分配 |
| V25-Q02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-C01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-C02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| P3-02-a | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| P3-02-b | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| P3-02-c | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-I01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-I02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-E01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-P01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-P02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-E02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-E03 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-D01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-D02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-D03 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-D04 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-A01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-A02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-A03 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-U01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-R01 | 审计泳道(R01) | v1：139 条要求/27 未取证/零完成率/四硬缺口/两门禁「测什么·不能证明什么」；**v2**：复核 T01/T02 退出码 ⇒ **发现 T02 从未在 Maven 下执行**；旧 229 订正（A104/B117/C8）；决策按四类重分类；ADS 首版三批计划 | 2026-09-14 12:52 | 第二版已交；第三版待 T02/编译时点澄清 | HEAD 3fcf90e；只读（MATRIX/VERIFY-T01-T02/CORRECTION-229/DECISIONS/ADS-FIRST-RELEASE） | **四级＝未验收**（文档级 E1；T02 证据强度＝强推断·探针级，不得记测试通过） | docs/acceptance/v25-r01-coverage-20260914/ | 下一动作：按总控裁决修订；T02 收口由 L2 负责 |
| V25-R02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-L01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-X01 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-X02 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-X03 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |
| V25-X04 | 未分配 | 未开工 | — | 开工时填写 | 待登记 | 未验收 | 待交付 | 见§3依赖 |

## 5. 需求不丢失与待输入

- 本表是实施队列，不是完整功能统计。原269条、旧M1/P1–P5/AIW-001～020必须由R01逐条映射到本版任务。未映射项标REVIEW等待归并，不得自动关闭或删除。
- WSL安装位置/资源/重启权限：W01先只读探测，只有无法确定的权限与偏好向用户询问。
- 真实LLM凭据：A01真实调用前索取安全引用；不要求用户在文档贴密钥。
- 论文/答辩日期与学校必交清单：待用户提供，不阻塞本版工程实现。
- 远程集群参数：X01重新启动前核实；当前不因node01–03未运行阻塞主线。
- 旧工作树清理、历史数据库清理：不在本版默认授权内，先证明独有修改已保存再申请。
- 本地服务运行状态（2026-09-14 12:05 只读实测）：8090/8091/8092 **均无监听**，原 analytics PID 61104 **已不存在**（两个 java 进程非本项目监听端口）⇒ 历史「8091 只读 HTTP 通过」证据仅对当时有效，**当前无在跑服务**；E3/E5 真机验收前须先申请是否启动服务，不自动启动。
- 泳道配额（§9.1）：本轮并发＝**开发泳道3**（L1 S01+S02／L2 T01+T02／Q01）＋**只读与文档泳道2**（W01 环境盘点／R01 覆盖矩阵）；未开启第四个重型任务。
- W01 结论与硬约束（2026-09-14，证据 `docs/acceptance/v25-w01-env-inventory-20260914/`）：**发行版实为 Ubuntu 26.04 LTS（非资料假设的 22.04）**；根盘 VHDX 在 `D:\Develop\wsl\Ubuntu\ext4.vhdx`，WSL 内 `/` 可用 **953 GB**；`.wslconfig` 仅 `networkingMode=mirrored`＋`dnsTunneling`，**未设内存/CPU** ⇒ WSL 取到 15.8 GiB/32 线程而**宿主当时仅余 3.56 GB**；WSL 内已装 OpenJDK **11**、Maven 3.9.12、MySQL **客户端** 8.4.10、Python 3.14，**未装** Hadoop/Spark(apt)/Hive/MySQL 服务端/Node/pnpm；`/opt/jdk-8u351` 与 `/opt/spark-3.3.2-bin-hadoop3` 为本地安装。**三条必须写进 W03/W04 的约束**：**(a)** HDFS/metastore/scratch **必须落 WSL ext4**，禁放 `/mnt/c`（宿主 C 盘仅余 74.8 GB）或 `/mnt/d`（9P 性能与权限语义风险）；**(b)** WSL 内 Hadoop **必须原生安装到 ext4**，**不得复用宿主 `D:\soft\hadoop`**（WSL 内 `command -v hadoop` 命中 `/mnt/d/...` 是宿主 PATH 透传的**假可用**）；**(c)** `.bashrc:123-126` 的 `JAVA_HOME/SPARK_HOME` 因顶部非交互 shell 早退而**在脚本/非交互 shell 中不生效**（实测 `spark-submit` MISSING、`java -version` 为 11）⇒ 所有 WSL 入口脚本必须显式设环境，不得依赖 `.bashrc`。另：宿主 MySQL8.0.41 `datadir` 在 **C 盘**、38 个非系统库合计 698.09 MB（最大 `mall_simulator` 650.94 MB）——**本版不迁移、不清理**。
- 盘点类任务的副作用纪律（新增）：**「只读盘点」不等于「无副作用」**——启动一个已停止的发行版、拉起 sshd、改变服务运行态都属状态变更，必须在任务包里显式授权或显式禁止；本次 W01 因取证需要把原本 Stopped 的 Ubuntu 启成 **Running**（未改配置、未起任何服务、未动 8090/8091/8092），**是否复位待用户答 Q6**。实时状态须每次实测，不得沿用旧结论。
- **用户总授权与定位（2026-09-14 12:20，长期生效，以此为准，替代此前逐项询问）**：**(a) 服务启动**：E3/E5 **可自行启动 8090/8091/8092**（长期授权，不必每次询问）；本地服务默认绑 **127.0.0.1 或 WSL 可达范围**，不为方便暴露公网。**(b) 数据边界**：开发、联调、自动测试**默认使用隔离库**，**默认禁止写正式库**；隔离库内建表/删表/初始化/重复跑测试属正常操作；**涉及正式数据的写入、删除、结构变更须先确认**。**(c) WSL 定位**：WSL2 已安装，作**主要本地开发环境**（IDEA 对接代码、Spring Boot、Vue、构建工具与少量本地服务），**不承担正式 Spark/Hadoop 集群计算**；**禁止在 WSL 内复制一套三节点 Hadoop 集群**；**真正的大数据集群仍是 VMware 三节点**。**(d) WSL 权限与资源**：**项目范围内** sudo 安装开发依赖、改项目目录配置、启本地开发服务**无需逐项询问**；**宿主机级修改**（Windows 全局网络、安全策略、磁盘分区、VMware 网络）**必须先问**；资源定 **4 vCPU＋8 GB RAM＋4 GB swap**（宿主 32 GB，需与 VMware 三节点并行；不足再升 12 GB，不影响架构）。**(e) 升级判据**：只有**架构分叉／数据口径变化／论文范围变化／破坏性操作**四类才上升用户裁决，其余按「最小可实现、可验证、可答辩」自行决定；§6.5 等待输入内容按「能形成完整数据闭环、可演示、可做论文实验的最小指标集合」取舍，不为"看起来企业级"扩表。**(f) 关键顺序**：服务虽可自行启动，但**启动前必须已有隔离库配置**，否则应用会连正式 `analytics_metric`（正踩 F-100/R3）⇒ **E3/E5 的启动门在 S02 隔离配置与 W03 的 WSL 隔离 MySQL 交付之后**。
- 总控裁决（2026-09-14 12:2x，回应 R01 首版 Q1–Q10；按用户「只有架构分叉／数据口径／论文范围／破坏性才上升」判据自行裁定，不再占用用户）：**Q1** 不预建空目录，登记行写**预期交付路径**，交付后按实际目录就地更正（避免"看似已交付"）。**Q2** 过渡期**门禁唯一判据＝版本化 `RuleSeverity.resolve(rules,…)`**；全局 `of()` 仅供未迁移调用点临时兼容且**必须列清单限期迁移**（Q01 负责；R01 已实测 `PipelineService.java:712,748,808`、`MetricPublishValidator.java:201,219,225` 六处仍在用）。**Q3** 允许在编译红时先交文档；该红已由 Q01 于 12:2x 修复（`-pl platform-common -am -DskipTests compile` BUILD SUCCESS）⇒ R01「E1 不成立」**按 12:08 时点有效、现须更正**。**Q4** 任务文字修订：§11.4「实现 FlumeLandingConnector/HdfsLandingStorage」与实测不符 ⇒ **以看板 V25-I02 修订后文字为准**（真实缺口＝§5.3 连接器接口＋Flume 连接器＋接入编排），指导书文字待下版修订。**Q5** 基线**不写死 HEAD**，改为「引用时点实测 HEAD＋时点」表述（写死会在每次提交后立即过期）。**Q6** §5.7 新字段**先并入 `ingestion-manifest.v1` 作可选扩展、不自动升 major、不改 `contract-specs/VERSION`**，是否升 major 留待 V25-C01 契约冻结时定。**Q7** 不为 AIW-001～020 拆 20 个任务行（看板是执行队列非功能清单）⇒ 在专项文件内逐条登记 Owner/判据/证据位，看板 V25-A03 行加索引指针，专项文件原门槛按此口径解释。**Q8** §10 是**任务包格式**（发给执行方的完整字段），**不要求看板单表齐备**；看板列只是状态视图，任务包全文随派发留存。**Q9** 269 条逐条归并**由 V25-R02 独占**，R01 只交「未映射·待归并」桶。**Q10** **授权只读 `information_schema`**：须带 schema 限定、行数用 `COUNT(*)`（**禁用 `TABLE_ROWS` 估计值**）、每条带查询时点、**不写库**。
- 用户授权（2026-09-14 12:15，就 W01 的 12 问中真正需人决定的 4 项；**其中资源与 WSL 定位两项已被 12:20 总授权覆盖，其余仍以此为准**）：**(1) WSL 安装与目录＝全部授权**：按 §6.2 在 WSL 内联网安装 Hadoop 3.3.4／Spark 3.5.1(for Hadoop 3.3)／JDK17(Linux)／Hive+Metastore／MySQL 服务端隔离实例／Node+pnpm／Flume；数据目录 `/data/{hdfs,metastore,spark-scratch}`、代码 `/home/asus/projects/GraduationProject`；**全部落 WSL ext4**，**不覆盖既有配置**。**(2) 资源与重启＝允许追加限额并重启复位**：`.wslconfig` **只追加** `memory=6GB`、`processors=2`**（2026-09-14 12:20 总授权已改为 `memory=8GB`、`processors=4`、`swap=4GB`）**（不覆盖既有 `networkingMode/dnsTunneling`），随后 `wsl --shutdown` 生效并把 Ubuntu 复位为 Stopped；执行前须实测确认无其他工作依赖 WSL。**(3) sshd＝关闭并禁止自启**（mirrored 网络下宿主 22 亦可达，按 §6.6「只绑必要接口」处理），并记录为状态变更。**(4) MySQL＝默认不动**：不迁移 C 盘 datadir、不 drop/TRUNCATE 任何库、不清理任何历史库；测试与真实链路一律用隔离库/隔离实例。其余 W01 问题就地按指导书裁决：**Q5 源码位置**＝按 §6.3 六步迁到 WSL 副本，**切换唯一主目录仍须用户确认（W04 门）**；**Q7**＝不做开机自启（§6.6「不修改系统自启」），按需启动；**Q8**＝不迁移；**Q9**＝WSL 内建**隔离 MySQL 实例**（避免与宿主 3306 生产实例混用）；**Q10**＝不清理；**Q11**＝不启动 Docker；**Q4**＝WSL 需 JDK17（后端）与 JDK8（Hadoop/Hive/Spark 侧）两条显式路径，由 W03 兼容矩阵实测锁定。
- 四级标记的表达约定（2026-09-14 12:3x 起）：**§3 状态列仍用 V2.5 §2.2 词汇**（TODO/READY/IN_PROGRESS/…），**四级判定写进 §4 的「测试结果」列**（形如「四级＝测试通过」），两者不得混用；未取证项一律显式写「未验收／未取证」。
- V25-S01 结果与新增风险面（2026-09-14 12:30，证据 `docs/acceptance/v25-s01-it-safety-20260914/`）：**缺陷本体已消除**——两个写入型 IT 原先 URL 写死 `127.0.0.1:3306/analytics_metric` ＋ 正式写账号 `metric_pub` ＋ cleanup 用可复用 `runtime_profile_id` 批量删，**加一个 `-Dmetric.it=true` 即可删生产快照**；现将库名绑定 `TestRunContext`、host/账号只走门禁解析链，**无任何正式目标兜底**。**最强负向证据**：给完整隔离档案但 `mysql.host=127.0.0.1:3306` 时在 `@BeforeAll` 即抛 `IsolationViolationException`，栈为 `rejectForbiddenInstancePort:596 ← assertJdbcTargetAllowed:635 ← verifyBeforeWrite:483`，**全程无 `Communications link failure`/`Access denied`** ⇒ 拒绝发生在**建立连接之前**，非「先连正式库试写再回滚」。**零写入取证用内容指纹而非行数**（符合用户 R3 要求）：`metric_snapshot=12`／`metric_value=110`／ACTIVE `S20260901_47 v12`／全表内容指纹 `452b7223a4a2b9dd0df7f3c883cdb74b` 均一致，且 `runtime_profile WHERE profile_code LIKE 'v25it-%'` = **0 行**。**新增风险面 R-1～R-6（L1 只报不改，本版开 V25-S03 收口）**：**R-1（高危）** `mall-simulator/src/test/resources/application-test.yml:3-4`＝宿主 3306＋`root`＋`createDatabaseIfNotExist=true`＋Flyway enabled；**R-3** `SourceRegistryMigrationMySqlIT.java:55` 默认值即正式 `analytics_meta`，加 `-Dp1.it=true` 会在正式库跑 Flyway DDL；**R-4** `GeneratorMetaStoreTest.java:58` 用 `assumeTrue(false,…)`＝「skip 后 PASS」；**R-5** `SparkStageExecutorSmokeTest.java:32,39,65` 无任何门禁且真实 `spark-submit`＋递归删目录；**R-6** `scripts/run-demo.ps1:80`（`mysql -uroot … DELETE FROM mall_simulator.event_outbox`）与 `scripts/smoke-pipeline.ps1:39-42`（硬编码 root/123456）。另 `AnalysisGoldenMySqlIT.java:80-81` 经核**全类无写语句**（纯只读，只报不改），但用 `meta_app` 连正式 `analytics_meta` 且文件内**硬编码明文口令**，改 `credentialsRef` 归文件所有者 Q01。
- 构建槽纪律补强（2026-09-14 12:3x，第二次实例）：**Q01 泳道两次使共享主代码不可编译**（第一次 `QualityRuleDefinition.java:84` 缺 `isKnownSeverity`；第二次 `MetricPublishValidator.java:48` 找不到 `QualityRuleCatalog` ＋ `RuleSeverityTest.java:129-207` 类型错误 14+ 处），**第二次卡住了 L1 的 T1**。规则补强：**收工前必须确认 `platform-common` 与 `metric-analysis` 均可编译**；做不到就先回报「当前编译状态＋预计恢复时间」，不得让工作树停在不可编译状态过夜；他人因此拿不到退出码时，一律记「等编译绿」而非失败。
- **勘误（2026-09-14 12:3x，针对上一条 Q01 事故记录）**：上条中「`MetricPublishValidator.java:48` 找不到 `QualityRuleCatalog`」一节，经**总控独立 grep 核验**为**编辑中间态**，非最终缺陷：该文件 `:4` 即有 `import com.graduation.analytics.metric.QualityRuleCatalog;`，全类各路径均改收 `QualityRuleCatalog.FrozenRules`，`@Deprecated` 兼容重载仅 `:288-304`；**`PipelineService.java` 与 `MetricPublishValidator.java` 内已无任何 `RuleSeverity.of(` 残留**（`PipelineService` 走 `:346 rulesFor(run)`，用于 `:372/:677/:733/:765/:807`）。**「`RuleSeverityTest.java:129-207` 14+ 处类型错误」一节属实且已修**（根因＝删除会静默丢弃首参的 `blocks(字面 severity, code, passed)` 重载后测试未跟进）。Q01 实测：`-pl platform-common -am -DskipTests compile` exit 0、`-pl metric-analysis -am -DskipTests compile` exit 0、`-pl metric-analysis -am -Dtest=MetricPublishValidatorSeverityTest test` **Tests run 10 / Failures 0 / Errors 0 / exit 0**（Q01 自报，总控未独立复跑，避免抢构建槽）。**结论：编译事故在时间窗内成立（L1 取样有效、纪律登记保留），但对 Q01 最终代码的指控不成立**，两个方向都据实记录。
- **新阻塞（2026-09-14 12:3x，owner＝L2 泳道）**：`analytics-server/platform-app/src/test/java/com/graduation/analytics/ingestion/IngestionManifestRuntimePatrolTest.java`（**未跟踪 `??`，12:28 仍在编辑**）在 `:134/:135` 报 **`未报告的异常错误 java.io.IOException`** ⇒ **platform-app 的 `test-compile` 整体失败**。后果：Q01 依总控要求写的**跨模块回归护栏** `platform-app/.../guard/RuleSeverityPathConsistencyTest.java`（唯一能同时加载 `DataQualityGate` 与 `MetricPublishValidator` 的位置）**无法编译、无法执行 ⇒ 不构成任何验收证据**，R01 首版 #1 缺口（同一规则码两条路径不同严重度）**尚未拿到护栏证据**。已要求 L2 立即修复并给出 `-pl platform-app -am test-compile` 退出码。
- **R01 第二版登记与三个推翻性发现（2026-09-14 12:52，证据 `docs/acceptance/v25-r01-coverage-20260914/`）**：**① T02 从未在 Maven 下真正执行过**——`-pl platform-app -am` 会把 `-Dtest=` 施加到上游 `platform-common`，四条命令（final-04a/04b/10a/10b）全部死于 surefire `No tests matching pattern`，platform-app 被 **SKIPPED**；且所传 `-DfailIfNoSpecifiedTests=false` **不是 surefire 的键**（正确键 `-Dsurefire.failIfNoSpecifiedTests=false`）⇒ **T02 证据只到探针级（PROBE-OK），强度＝强推断，不得记「测试通过」**；final-13 另被 warehouse-pipeline 的 1F/1E 挡住。**② `spark-jobs` 出现假绿风险**：`EXIT=0` 但 **`Tests run: 0`**，与 09-14 记录的「111 通过/15 套件」**互斥** ⇒ 该基线**降级为「矛盾·未取证」，澄清前不得引用为 E2 证据**（07 处真实测试数由对应泳道复测）。**③ E1「绿」必须带时点**：12:19:17 platform-common 绿（79 tests/0F/EXIT=0）→ **12:20:53 又因他泳道新建的未跟踪 `TestIsolationGuard*` 转红** → 12:3x Q01 实测两模块绿。另：T01 有 Maven 级**红→绿**双证据（12:16:37 `EXIT=1` → 12:19:11 `EXIT=0`/BUILD SUCCESS/33 tests/0F，负例仍红 ⇒ 非空跑）；旧 229 订正为 A=104/B=117/C=8（「缺 README」成立并定位到 `COVERAGE.md` 第 3 行自指断链；「指针错位」**不能按路径断链复现**，真实所指应为语义错位·未取证；「覆盖不足」部分成立：104 行无路径型指针）。
- **总控只读跨库普查（2026-09-14 12:4x，root 只读 SELECT，仅 `information_schema`，零写；用于裁决 E-03）**：实例指纹 `port=3306 / host=dahaishui / server_uuid=85191145-1491-11f0-b4e2-60cf84d55629`。**① `analytics_*` 共 9 个库**：`analytics_meta`(+`_p103`/`_p105`/`_p105it`/`_v17probe`)＝**22 表**，`analytics_metric`(+`_p103`/`_p105`)＝**11 表**，`analytics_verify_m3_parity`＝10 表。**② §5.2 五个对象在全部 9 个 schema 中均不存在**（`source_connector`/`schema_mapping`/`source_manifest`/`quality_rule_definition`/`source_profile` 零命中；仅 `source_registry` 在 5 个 meta 库存在，`data_quality_result` 在 5 个 meta 库存在）⇒ R01「可见范围内 0 命中」**升级为跨库结论**，R01 的自我更正方向正确。**③ `metric_snapshot` 出现在 9 个库且结构分叉**：meta 族 **12 列**（索引 PRIMARY(id)+`uk_snapshot_id`+`idx_snapshot_status`），metric 族 **15 列**且**独有 `uk_active_profile(runtime_profile_id, active_flag)`** ⇒ **ACTIVE 权威库＝`analytics_metric`（与 `application.yml:15` 一致），`analytics_meta` 内是 12 列历史副本** ⇒ **R01 的 E-03「权威库分叉」经取证消解为「历史重复表＋schema 漂移」，不上升用户**。**④ ADS 实名只有 `ads_*_m` 8 张**（active_trend/behavior_funnel/data_quality/hot_product/operation_overview/product_conversion/sale_trend/user_profile，存在于 metric 族 4 库），**无后缀名在任何 schema 都不存在** ⇒ R01 新缺口 #12（DDL 文字 `ads_user_profile`/`ads_data_quality` vs 代码 `_m`）**成立，以库内 `_m` 为准**（代码与库一致），DDL 文字待 C01 冻结时订正。**⑤ 总控裁决 D-10**：§7.4「按发布作用域唯一」**用现有 `uk_active_profile(runtime_profile_id, active_flag)` 满足，不新增 `source_id` 列**（首版最小可实现；§7.4 字面「至少 source_id+runtime_profile_id」的措辞待 C01 按等价列改写，不因此开迁移）。**⑥ 只上升两条到用户**（命中「论文范围变化」）：漏斗维度粒度（E-01）、是否含 AI 建仓章节（E-02）；其余 R01 的 D-01～D-12 按上列裁决留档。
- **普查口径的双向更正（2026-09-14 13:5x，总控自查＋R01 v3 交叉）**：总控 12:4x 那次「§5.2 五个对象在全部 9 个 schema 中均不存在」的**依据不成立**——查询加了 `analytics%` 前缀过滤，而前缀过滤**不能证明"实例内不存在"**（这是本项目同一类错误的**第二次**，第一次是 R01，总控自己也犯了，一并登记）。已用**全实例无过滤普查**（319 张非系统表）重立：**§5.2 的八个名字**（`source_connector`/`schema_mapping`/`source_manifest`/`source_profile`/`quality_rule_definition`/`source_instance`/`ai_modeling_session`/`source_profile_draft`，指导书 line 310–312/501/520/586–587）**全实例 0 命中** ⇒ **结论保留、依据更换**（仅 `source_registry` 存在于 5 个 meta 库）。反向也须更正：R01 v3「三条表述同时作废」**过头**——我查的名字属 **§5.2**，R01 举证的 `generator_meta` 那 5 张属 **§4.2**（line 265/267），**两组不同**，其「漏了 `generator_meta`」不适用于我查询的名字。**§4.2 五表经总控 `COUNT(*)` 独立复核与 R01 数字逐项一致**：`generator_target` 422／`generation_plan` 430／`generation_run` 184／`generation_artifact` 551／`generation_event_stat` 947，Flyway DDL 见 `synthetic-data-generator/src/main/resources/db/generator/V1__generator_meta.sql:12,30,50,74,92`，代码调用点 7+ 处（`GeneratorMetaStore.java`、`GenerationRunService.java:531` 等）⇒ **§4.2 对象存在，列级一致性仍未取证**；「生成器元数据未建表」类表述**从未写入论文口径，无需撤回**。
- **R01 v3 送达与其自身两处翻案（2026-09-14 13:0x，证据 `docs/acceptance/v25-r01-coverage-20260914/`）**：**E-03（权威库分叉）已消解**——`analytics-server/platform-app/src/main/resources/db/metric/V1__metric_store.sql:3` 自述 `analytics_meta` 内由 `db/meta/V2__platform_pipeline_quality.sql` 建的 `metric_snapshot`/`metric_value` 自 R7 起为**弃用副本**（`db/meta/V13__metric_definition_r7.sql:19-20` 重复该措辞）⇒ 与总控普查互为独立证据，**不上报用户**。R01 另自我更正两处：`active_flag` 语义＝ACTIVE 行 `1`、非 ACTIVE 行 **NULL**（12 行中 1 行为 1、11 行为 NULL），但不变量「每档案至多一行 ACTIVE」实测 `multi_active=0`、「不得无 ACTIVE」`without_active=0`，**规模仅 1 个档案/12 行 ⇒ 并发原子性未取证**；权威侧独有 **`definition_version varchar(16) NOT NULL`** ⇒ §7.16 的 `definitionVersion` **确实存在**（v2 基于 12 列副本得出的「不存在」结论**作废**）。**新增强制口径**：`metric_snapshot` 实际出现在 **11** 个 schema（含 `mall_simulator`/`mall_simulator_test`），同名表存在 **12/15/11** 三种列结构 ⇒ **任何表级结论必须带 schema 名**（MATRIX #16/#17），并在 C01 加启动自检（已登记、未实现）。**D-10 冻结**：§7.4「按发布作用域唯一」用现有 **`uk_active_profile(runtime_profile_id, active_flag)`** 满足，**不新增 `source_id` 列、不开迁移**；#12 冻结：ADS 八张表在 `analytics_metric` 内**实名带 `_m`**（`ads_user_profile_m`/`ads_data_quality_m` 等），**以库与代码为准**，DDL 文字待 C01 订正。
- **V25-S03 进度与总控接管（2026-09-14 13:5x）**：**L1 泳道中途失败退出**（无报告、无完整证据）；其遗留改动经总控核对：全模块 `test-compile` **7/7 SUCCESS / exit 0**。**R-5 由总控自行收口**：`SparkStageExecutorSmokeTest.java` 以 `git mv` 改为 **`SparkStageExecutorSmokeIT.java`**（类名与 javadoc 同步）——surefire 默认 includes 只含 `*Test` 一族，改名后该例**退出默认套件**，既落地 R-5「默认关闭、未开开关则硬拒」，又不让默认反应堆因「未开开关」永久 1 error（与仓库既有 `*MySqlIT` 一族同一约定）。实测：`-ol warehouse-pipeline -am test`＝**131 tests / 0F / 0E / exit 0 / BUILD SUCCESS**（原 132 含该真实 Spark 冒烟 ⇒ **覆盖率位移恰好 1 例**，已写入类注释）；显式 `-Dtest=SparkStageExecutorSmokeIT`（不带开关）＝**Tests run 1 / Errors 1**，异常为 `TestIsolationGuard$MissingConfigurationException: [SparkStageExecutorSmokeIT.realSparkOdlLoadsGoldenDataset] …未提供 -Dv25.spark.it=true…`，**exit 1**，surefire 报告 `13:55:22` 生成。**过程纠错（登记）**：该硬拒的**第一次取样不成立**——不带 `-am` 时 Maven 很可能在依赖解析阶段就失败，surefire 报告根本没生成，我当时据 `exit 1` 下的结论属**未经证据的推断**；已用带 `-am` 的重跑替换，全量日志 `docs/acceptance/v25-s01-it-safety-20260914/raw/l4-controller-spark-it-refusal.log`。R-1/R-3/R-4/R-6 与脚本项由新泳道 **L5** 独立验证（只允许追加证据、不得改写上一泳道已写内容）。
- **隔离环境已可用，E3 真链路线定为「宿主机跑应用＋WSL 隔离库」（2026-09-14 13:5x，L3 交付＋总控实测）**：WSL 内独立 MySQL **8.0.41 / `127.0.0.1:3307` / `@@server_uuid=de8ebbea-aff4-11f1-8037-00155d5dba47` / `@@datadir=/data/mysql-isolated/data/`**；**Windows 已实测可直连**（`Test-NetConnection 127.0.0.1 3307 = True`；`mysql.exe -h127.0.0.1 -P3307` 返回 `3307/dahaishui`）⇒ 不必把整套应用搬进 WSL。**硬提醒**：隔离实例 `@@hostname` 与宿主**同名**（`dahaishui`）⇒ 门禁指纹**只能用端口/uuid**，模板里 `serverFingerprint=WSL-V25IT-ISOLATED` 是**占位符，必须替换为真 uuid**，否则指纹核对形同虚设。三个隔离库基本为空（`analytics_meta` 0 表／`analytics_metric` 1 表／`mall_simulator` 0 表）⇒ 首启由 Flyway 在隔离库建表（**`V19`/`V20` 因此会先在隔离库落地**）。**`wsl --shutdown` 后该实例不可用**；启动脚本 `docs/acceptance/v25-w02-w03-wsl-runtime-20260914/raw/scripts/start-isolated-mysql-3307.sh` 幂等可复用。**仍未完成**：Node/pnpm/Flume 未装（WSL 内 `apt` 完全不可用 ⇒ 拟由宿主下载官方 tarball 落 ext4）；W-U8（Spark↔HMS 3.1.3 连通）未实跑；W-U9（两 worktree 的 detached HEAD 是否已推送）未取证。
- **服务与自启实测（2026-09-14 13:5x，总控）**：**8090/8091/8092 均无监听**（仅宿主 MySQL 3306 在听）；**无平台自启计划任务**（`Get-ScheduledTask` 命中的 `RunPlatformExperienceHelper*` 为 Windows 自带项）⇒「`V19`/`V20` 一旦入库，下一次以正式库启动应用即会执行 DDL」这条耦合**当前没有自启放大器**，但仍登记为硬约束：**在用户确认正式库结构变更前，任何泳道不得对 3306 启动 `platform-app`**（本就默认禁止连正式库，此处显式化）。
- **L2 交卷（T01/T02＝测试通过 E1/E2）与三处口径澄清（2026-09-14 13:0x）**：① `IngestionManifestRuntimePatrolTest` 的 test-compile 阻塞**已修**（`missingDirectoryIsAViolation` 补 `throws IOException`），全模块 `test-compile` exit 0；② T02 用**正确键** `-Dsurefire.failIfNoSpecifiedTests=false` 重跑得 **9/0/0**、**3/0/0**、**1/0/0** 三组 `EXIT=0`（Maven 真实执行、N>0）⇒ T02 由「强推断·探针级」升为**测试通过（E1/E2）**，但 R01 对**旧四条命令**「从未执行 T02」的判定**仍然成立**（两组事实并存，均已登记）；③ `spark-jobs` 的「111 对 0」**不是假绿而是插件分工**：**111** 出自 `scalatest-maven-plugin:2.2.0`（`Suites: completed 15`），**0** 出自 `maven-surefire-plugin`（把 ScalaTest 套件当 JUnit 类却跑不到方法）；报告 XML 独立复算 17 份/tests=111/F=0/E=0 ⇒ 09-14 记的「111/15 套件」基线**恢复为已取证**，引用时必须注明「111 出自 scalatest 插件」；④ `PipelineServiceTest` 15 例 10F 属 **Q01 编辑窗口中间态**（其后 Q01 自测 616 run/0F/1E、总控 13:54 实测 131/0F/0E）⇒ **不作为持久红登记**。
- **Q01 四项裁决（2026-09-14 13:5x，总控）**：**待裁-8＝批准**，迁移号定 **`V19`＋`V20`**（meta 侧最高 `V18`；一表一改拆两个文件：`db/meta/V19__quality_rule_definition.sql` 建表并播种、`db/meta/V20__data_quality_result_rule_version.sql` 增列）。**待裁-9＝批准** `data_quality_result` 增 `rule_version`/`effective_severity`/`compat_policy_version`/`rule_fingerprint` 四列：**允许 NULL、NULL 默认、严禁回填猜测值**，历史行留 NULL 并注明「版本化引入前记录，不得解读为 WARN/PASS」；**只许写迁移文件，不得对 3306 执行**。**待裁-10＝批准**把 13 个既有未登记码按**改造前档位**登记为 version=1，条件：逐码给出 `rule_code → 档位 → 依据（表/run/时点）`；**若其中任何码语义上属 PK/required/amount 而会被登记为 WARN，必须先报总控**（§7.3.1 line 522 明令不得降级）。**待裁-11＝上升用户**：新口径下 run 24/47 及**当前唯一 ACTIVE 快照** `S20260901_47` 的质量判定由 PASS 翻为 **FAIL**（`EVENT_ID_UNIQUE` 实测 `error_rate=0.020408` 对阈值 `0.0005`＝约 **40 倍**，两行 `passed=0`；`PUB_DQ_EVENT_ID_UNIQUE` `0.071429`＝约 **143 倍**）⇒ **页面质量卡会变红**，这是收紧过松契约的必然结果；**不得**改阈值、**不得**对 3306 跑 `AnalysisGoldenMySqlIT`、**不得**把期望值翻回 PASS，等用户裁决。**`V19`/`V20` 存在不等于「版本化已闭合」**——持久化载体虽补齐，跨 run 闭环与并发发布原子性**仍未取证**。
- **Forensics 勘误已落（2026-09-14 13:0x，`docs/acceptance/b13-db-forensics-20260912/README.md` §12 勘误 R1–R4）**：**R1 撤销**全文所有 `AUTO_INCREMENT`/`max(id)` 指纹级论证——「`AUTO_INCREMENT > MAX(id)+1`」**既不充分也不必要**（有删除却因同语句多行插入而不变；无删除却因 `innodb_autoinc_lock_mode=2`、多行插入回滚、innodb 重启后重算而变），且以自身反证坐实：**同一字段 09-12 读 25、当时 `MAX(id)=27`，09-14 读 28**，「无删除」却不守恒；删除结论只能由**覆盖完整且作用域正确的 binlog ＋ 多来源交叉验证**得出。**R2 撤销**「9 行早于窗口 26 小时固化于磁盘」措辞（该 dump mtime `2026-09-11 20:11:08` 落在 **W2** 窗口内）。窗口重定义 W1/W2/H1/W3/H2/P1；结论改述为**三项交叉验证**：binlog 双向核对 W3/W2 上 INSERT/UPDATE/DELETE 三项**全 0** 且 `000131` 单次解析 904,814 行/80,600 事件/诊断 0，W1 的 334 命中作**阳性对照**；两份独立留存件（09-12 22:00:17 与 22:43:34）逐值一致；原先的只读 HTTP 单点读数**已降级为不可复现**。新增 **26 行证据强度分级表**（已证 20／强推断 4／未取证 2）；§4.3-U7 由「未取证」**升级为「已证」**（`000125` 实证 `analytics_meta.metric_snapshot` 建于 260907 16:04:26、17:15:57 被 in-place ALTER 改写 `CREATE_TIME`、此后恰好 9 次写入对应现存 9 行，全文 `TRUNCATE=0`）。
- **新登记口令与工具链缺陷（只登记未改，2026-09-14 13:0x）**：`EnvCredentialService.java:20` 含明文默认口令 `metric_read_pw_2026`（L3 仅登记未改）⇒ **移交 I01/E01 处置**；WSL 内 `apt` 完全不可用（挂起 6 分钟后 `archives/partial` 恒 0 字节）⇒ Node/pnpm/Flume 安装须改走**宿主下载官方 tarball 再落 ext4**。
- **用户裁决：接受质量卡翻红，并重建合格快照替换 ACTIVE（2026-09-14 14:0x，待裁-11 收口）**：用户选 **A** —— **接受 run 24/47 与 `S20260901_47` 判 FAIL**（契约收紧的正确结果，**不改阈值、不降级、不静默放行**），并在**隔离库**重跑一条干净链、产出一个**合格快照**作为演示/答辩用的 ACTIVE；旧快照翻红的原因如实写进论文。⇒ Q01 的期望值翻转**按 FAIL 定稿**；后续由 E3 泳道（L4）在隔离库完成「干净链→合格快照」，**正式库 `S20260901_47` 是否切换 ACTIVE 属正式数据变更，须另行确认**（当前仍原样不动：12 行/110 行/指纹 `452b7223a4a2b9dd0df7f3c883cdb74b`）。
- **用户硬约束：产物不得离仓（2026-09-14 14:0x，最高优先级，覆盖此前一切相反约定）**：用户明令「**不要把文件到处创建**」。实测仓外散落 **约 329 MB / 6700 文件**，逐项如下：① **git worktree 3 个**——`D:\Develop_code\_gp_headcheck`（**今日 13:57:36 由 L5 违规创建并主动自报**，detached `f00462c`，2070 文件/92.8 MB，已核**无 dump/备份/新明文口令**）、`D:\Develop_code\GraduationProject-wt\m3-jdk8fix`（`f26be26`，1526 文件）、`D:\Develop\GraduationProject\.f88-baseline-wt`（`dba4381`，1783 文件；其父目录 `D:\Develop\GraduationProject` **并非 git 仓库**，纯容器目录）；② **正式库数据转储** `D:\p103-e3`（94.3 MB/562 文件：`real-meta-before.sql`、`real-meta-after(-det).sql`、`real-metric-*.sql`、`real-meta-run4/run5-*.sql`、`base.zip`、`classes/`）与 `D:\p105-e3`（36.2 MB/382 文件：`analytics_meta_p105.sql`、`analytics_metric_p105.sql`、`dump-*.err`、`e3-p105*.ps1`）；③ **冷备份** `D:\Develop_code\graduation-lane-backup`（3.0 MB/182 文件，**被已入库文档引用**：`docs/acceptance/ct-batch-20260912/PLAN.md:112` 与 `lane/PROGRESS.md:16` 明写「契约文件写前各留**离仓**冷备份」，另有 `docs/acceptance/p1-03-source-registry-api-20260911/e3-acceptance.ps1:7` 与 `p1-05-manifest-source-20260911/raw/*` 引用 p103/p105 路径）；④ 杂项 `D:\Develop_code\.verify`（1 文件）。**根因登记为总控失职**：此前从未设「产物不得离仓」约束，早期泳道把「隔离」误解为「搬到仓外」，并把「离仓冷备份」当作防篡改特性写进 PLAN ⇒ **正确做法是隔离靠 gitignore／命名空间／端口／uuid，不靠搬出项目**；上述「离仓冷备份」约定**按本裁决作废**（原文不改写，后续以带日期勘误追加）。**已立即生效**：唯一落盘根＝仓库根；临时文件只准 `$env:TEMP` 与仓库内 gitignored 目录；**严禁** `git worktree` 到仓外、严禁自建 `D:\*` 工作目录；在跑泳道已收到最高优先级硬约束并被要求自报仓外创建物（L5 已自报并停用，其仓外 worktree 保留待批）。**处置模式＝用户裁决 A（冻结新增＋分类归并，删除前逐项报批）**：先归并可用证据/冷备份进仓内 gitignored 归档并做 sha256 一致性校验，再逐 worktree 核清「未推送/独有内容」（**已知 F1：`m3-jdk8fix` 一类 worktree 中 9 个真实源码文件的改动从未进入对象库 ⇒ 无法从 git 恢复，核清前一律不得列入可删**），最后提交待删清单等用户逐项点头；**有独有内容的目录一律不删**。归档清单由只读泳道 **L6** 产出（`docs/acceptance/v25-stray-consolidation-20260914/`）。**当前状态：未删除、未迁移任何一项**；今日（除 L5 那次外）`D:\` 根与 `D:\Develop_code` 下**无新建目录**。
- **L5 独立复核完成（2026-09-14 14:1x）**：报告 `docs/acceptance/v25-s01-it-safety-20260914/L5-verification-report.md`（＋README §10、`external-write-surfaces.md` §5、`raw/l5-*.log` 16 个；**前序章节一字未改**，原末行 L284 完好）。**四级状态**：R-1 mall／R-4 generator＝**提交完成 ✅ ＋ 限定验收 ✅ ＋ 完整验收 ❌**（"测试通过"栏＝默认关闭红 30 ERROR／120 run·20E **且 `Skipped=0`**，属设计意图，绿灯需 3307 ⇒ **未取证**）；R-6 脚本＝提交完成 ✅／限定验收 ⚠️ 部分；R-3＝总控实测 ✅（L5 **未重跑**）；R-5＝超任务包，未取证。**核心主张独立复现成立、非假绿**：默认关闭时两模块套件**硬失败且 `Skipped=0`**（`assumeTrue(false)` 假绿已彻底移除）；显式指向宿主 3306 时在**任何连接建立之前**被拒（`MallIsolationGuard.java:133 assertUrlAllowed`／生成器侧 0.043 s）；反证搜索 `Communications link failure`／`Access denied`／`Connection refused`／`HikariPool-`／`Unknown database` **0 命中**；**加强判据**——即使把 3306 塞进 `-Dmall.it.mysql.host`（守卫显示 `allowedPorts=[3306]`）该端口仍被**无条件**拒，配置不能洗白正式端口。**时点可信性**：取证期间 HEAD 由 `f00462c` 前移至 `fd3cee5`，L5 用被测两棵树＋两个 pom 的**内容哈希二次快照（190 行逐行一致）**证明结论不受 HEAD 前移影响。
- **L5 发现的阻塞级缺陷已由总控修复并实测（`scripts/it-prepare-isolation.ps1`，属 R4 授权范围）**：`:28` 的 `[string]$Host` 撞 PowerShell **只读自动变量** `$Host` ⇒ 参数绑定阶段即失败（`-File` exit 1、**`-Command` 静默 exit 0**）⇒ **3307 隔离环境准备的唯一入口不可用**，这才是 S02「真库往返从未跑通」的真正卡点（不只是"等 W03"）。修复内容：①参数改 `$DbHost` 并保留 `[Alias('Host','HostName')]` 兼容旧写法；②新增**必须显式给出**的 `-AllowRootOnIsolated`（root 仅用于 3307 上建空库/建账号，**应用与测试连接一律不用 root**），未给出时 `-Confirm` 也 **exit 2 且先于任何落盘/连接**；③凭据文件从**仓外** `%USERPROFILE%\.graduation` 改到**仓内**模块工作目录（守卫 CWD 候选路径），已被 `.gitignore:82 credref-*.properties`、`:83 *.local.properties` 覆盖；④**修掉 L5 未覆盖的第二隐患**——脚本原写 `mall.password=`/`generator.password=` **双键**，而两守卫 `requireCredential` 只读**单键 `password`**（`mall-simulator/src/test/java/com/graduation/itguard/IsolationGuard.java:169`、`synthetic-data-generator/src/test/java/com/graduation/itguard/IsolationGuard.java:169`）⇒ 即便跑通也取不到口令；现改为每侧一个文件、单键 `password`，并把档案落点与"Spring 不读档案、数据源须走环境变量/系统属性"写进脚本输出。**实测**：`-File -DryRun`／`-File` 带 `-Host` 别名／`-Command` 三种形式 **exit 0 且打印清单**；`-Command` 缺参 → **exit 1 ＋ 报错**（静默失败模式消失）；`-Port 3306`→**exit 2**；非法 runId→**exit 2**；`-Confirm` 未给 `-AllowRootOnIsolated`→**exit 2**；仓库外 `.graduation` **未创建**、无 `credref-*` 文件产生。**调用约定**：一律用 `-File`（`-Command` 的退出码语义由调用方决定，不作为门禁依赖）。
- **总控对 L5 建议 A–J 的裁决（2026-09-14 14:2x）**：**A 立即修**＝总控已完成（见上，不另派泳道）；**B 3307 上是否用 root**＝允许但**只由 `-AllowRootOnIsolated` 显式开启**，脚本自述由"绝不用 root"更正为"应用/测试连接不用 root；仅 3307 建库/授权例外"；**C `GeneratorContractParityTest.java:86` NPE**＝**既有缺陷、另开单**（L5 用 blob 三方比对证明：`git status --porcelain` 为空、`git hash-object` 工作区＝`rev-parse HEAD:`＝`rev-parse f00462c:`＝`f43859f1…`／`1e4f2d85…`）——它使 `synthetic-data-generator` 默认套件**在 S03 之前就是红的（20E 中第 20 个）**⇒ 该树**不存在全绿基线**；**D 占位符口径**＝隔离档案只归门禁读，Spring 只认环境变量/系统属性；**E `run-demo.ps1` 加"仅限隔离库"硬校验**＝要加，但**排在 L4 E3 结束之后**（禁止边跑边改）；**F `smoke-pipeline.ps1` `Q()` 失败统一 exit 5**＝要改（现以 `[long]` 强转崩 `ErrorRecord` 而 exit 1）；**G 主配置收口**＝独立任务 V25-S04；**H 范围外明文残留 3 处**＝并入 S04；**I `_gp_headcheck`**＝进"待逐项批准删除"清单；**J HEAD 时点**＝固化为纪律：证据必须记 `git rev-parse HEAD` ＋ 被测文件内容哈希。
- **总控新登记的第三隐患（L5 未覆盖）：analytics 侧没有 `credref:` 口令解析器**：`git grep -i credref -- analytics-server` 仅命中 `SourceRegistryMigrationMySqlIT:136` 的 `credentialsRef` **引用名字段**（守卫用它做"引用必须含本次 runId"的形状校验），**没有任何口令解析实现**；而 `analytics-server/integration.local.properties.template:44,46` 明确要求 `mysql.publish.password=credref:...`／`mysql.read.password=credref:...` ⇒ **该模板当前不可实施**（会拿字面量当口令）。**裁决**：E3 及本轮一切 analytics 侧连接**用环境变量/系统属性直接提供口令，禁用 `credref:`**；**S04 必须收口为二者之一**（实现对称解析器 或 改模板为环境变量），不得两处并存。对照：mall／generator 两侧**已有**解析器（`IsolationGuard.requireCredential:148-179`）。
- **Q01 自我勘误：待裁-10 的"13 个未登记码"实为 5 个（用估计冒充测量的实例，已按纪律登记）**：`analytics_meta.data_quality_result` 全表 **467 行**、`GROUP BY rule_code` 实测**仅 15 个不同码**，其中不在 `RuleSeverity.REGISTERED`（33 码）内的是 **5 个**：`ADS_STAGING_SNAPSHOT_ISOLATION`(目录 WARN·FIXED)、`PUB_FORMAL_PARTITION_MATCH`／`MXP_SNAPSHOT_PINNED`／`MXP_EXPORT_ROWS`／`MXP_EXPORT_COMPLETE`（均 BLOCKING）。`q01-version-scope.md` §2.3 那份"约 13 个"清单**自相矛盾**（声明"不在 REGISTERED 里"却列了 8 个**恰在** REGISTERED 中的码，并用 `MXP_*`／`PUB_DQ_*` 通配符冒充枚举）；§2.3 原文**不改写**、以新证据文件为准，取证输出已改名 `q01-dq-result-all-codes-grouped.txt`。**红线检查通过**：唯一登记 WARN 的 `ADS_STAGING_SNAPSHOT_ISOLATION` 语义为"历史暂存快照存在本身"，**不属**必需/PK/金额 ⇒ 未触发"必需项降 WARN 即停止"；四个行数/快照类码（含金额相关对账）**全部 BLOCKING**。**总控裁决该码的档位冲突**：库内 19 行 `passed=0` 是**版本化引入前**的既有记录（当字面档位 `ERROR`），按目录 `WARN+FIXED` **不阻断** ⇒ **不发新 version**，但必须：①V20 的 NULL 语义注释已声明"无版本信息、不得解读为 WARN/PASS"（Q01 已做）；②由 **Q02** 补一条"WARN 有效例"测试，显式证明该码判红**不阻断发布**（§7.3.1 的 WARN 路径），避免"看起来降级了却无人验证"。
- **Q01 交付与未闭合项（V19/V20）**：`db/meta/V19__quality_rule_definition.sql`（建表＋**35 行** version=1 种子，逐字段由 `QualityRuleCatalog.DEFAULT` 脚本导出，导出指纹 `6bc272d7…` 与运行期 E2 日志实测值相同 ⇒ 无第二份手抄口径；取 35 行而非"库里出现过的 15 码"是因为未登记码会**停止发布**，登记必须先于首次产出）＋`db/meta/V20__data_quality_result_rule_version.sql`（4 列全 `NULL` 可空、**无 DEFAULT、不回填**）；门禁 `QualityRuleVersionMigrationScriptTest` 6 条（含"种子与 Java 目录逐字段零漂移"防第二所有者）；全模块 `test-compile` **7/7 SUCCESS／exit 0**，本泳道 6 个门禁类 **32 run／0F／0E／exit 0**（首轮踩到"门禁自伤"：V20 注释含"不设 DEFAULT"被全文判违规，修法是改钉列定义结构而非放宽 → 1F→6/6 green）。**未闭合（不得声称已闭合）**：①**V19/V20 从未被任何进程应用**（正面证据：`information_schema` 四列＝0；`flyway_schema_history` 最高 **18**；8091 所用 jar 构建于 **13:58:40**，早于本次落盘，`jar tf` 不含 V19/V20）；②**写侧未接入**这 4 列；③两脚本 SQL **未在 MySQL 上执行验证**（静态检查不能替代执行验证；`EXPLAIN` 预检已写但本轮未跑）。
- **前向效应与新增硬约束：启动前检查必须拒 3306**：8091 正在运行（**PID 62984，13:59:37 起**，jar 构建于 13:58:40）⇒ **下次重启该 jar（或其重建版）会由 Flyway 直接对目标库应用 V19/V20，属不可自动回退的前向效应**（回退语句已写在两脚本头部）。经实测判定**本次未越界**：`Get-NetTCPConnection -OwningProcess 62984` 显示其**全部 31 条已建立连接都指向 `127.0.0.1:3307`**、**指向 3306 的为 0 条**（3306 上 4 条连接属 DataGrip PID 55804）；3307 上出现 **runId 化隔离库** `analytics_meta_v25it_20260914_1358_l4e3`（21 表）与 `analytics_metric_v25it_20260914_1358_l4e3`（11 表），共享的 `analytics_meta`(0 表)/`analytics_metric`(1 表) **未被触碰**；3307 上亦**无 V19/V20 痕迹**（与 13:58:40 的 jar 一致）。启动者＝`docs/acceptance/v25-e3-isolated-chain-20260914/scripts/10-start-app-isolated.ps1`（**仓内**，L4）。⇒ **新增硬约束（永久）**：任何泳道启动 `platform-app` **必须显式给出隔离数据源覆盖**，并通过"目标必须是 3307 且库名含本次 runId"的**启动前检查**；**指向 3306 一律拒绝启动**（该检查属 R4 授权的"启动前检查"，由 V25-S04 落地为脚本/应用双重门）。
- **E3 隔离实链进展（L4，进行中，仅登记不判结论）**：已落盘**全部在仓内**：`docs/acceptance/v25-e3-isolated-chain-20260914/`（`scripts/00-create-isolation|10-start-app-isolated|20-run-chain-isolated|21-dump-isolated-evidence|30-stop-and-postcheck.ps1`、`fingerprint-3306.sql`；`raw/01-create-isolation.txt`、`02-isolated-account-connectivity`、`03-isolated-flyway-and-seeds`、`04-mirror-runtime-profile-from-3306(.sql)`、`10-app-8091-console.log`、`20-chain-console.log`／`20-chain-summary.json`、`21-isolated-meta-evidence.txt`、**`baseline-3306-before.txt`**、`http/00-login.json`…`http/03b-metrics-quality.json`）。**待 L4 交报告后判定**；其中 `04-mirror-runtime-profile-from-3306` 是对正式库的**只读**镜像取证（允许），`baseline-3306-before.txt` 应为"零写"自证的一半，**必须配 after 指纹与行数对照**才成立。
- **L6 只读核查交付与三点更正（2026-09-14 14:5x，证据 `docs/acceptance/v25-stray-consolidation-20260914/`）**：总规模 **329.9 MB／6719 文件（7 项）**，raw 47 件可复算；快照 `fd3cee5`，核查期间 HEAD 前移至 `1cb318b`，L6 在新 HEAD 下复验结论不变。**三点更正**：①**推翻「m3-jdk8fix 可安全删除」**——其 `.verify/` 有 24 个文件（11 个不在 git 里），`final-positive-control.log`(2653 B) 与 `final-effective-scalac-args.log`(140080 B) **被已入库 `spark-jobs/pom.xml` 明文点名为生效证据**，而仓内 `.verify/` 因 `.gitignore:61` 被整体忽略、**这两个文件在仓内根本不存在** ⇒ 删掉即灭失已入库源码点名的证据；②**更正 F1「9 个真实源码文件改动从未进入对象库」为工具假阴性**——成因是用 **WSL 的 git 查 Windows 路径 worktree**（worktree 的 `.git` 是内容为 `gitdir: D:/...` 的**文件**，WSL git 解析不了而返回空）；Windows 侧三个 worktree `git status` 全部 exit 0、所有已修改 blob 均可解析 ⇒「核清未跟踪/已修改文件前不得列入可删」**该规则已满足**；③**S1/S2 明文口令不是新增泄漏**（总控 2026-09-14 复测 `meta_app_pw_2026` 17 命中/16 文件等，见 V25-S05）。**总控独立复验**：7 项文件数与 L6 的 `sha256-all` 清单**逐项相等（7/7 OK）**⇒ 快照后无新增散落。
- **用户逐项批准与处置执行（2026-09-14 15:0x，用户答 4 问全选 A）**：批准＝①S1/S2 先归并到**仓内 gitignored** 归档再删原件 ②W2 的 `.verify/` 归并到 `docs/acceptance/m3-jdk8fix-evidence-20260914/verify/` **并同步改 `pom.xml` 注释指向仓内** ③S3 归并**进仓（tracked）**＋**追加第二条日期化勘误**改引用路径后再删原件 ④W1/W3/S4 批准删除但**先做删除前复验＋sha256 归档**。**执行结果（全部落仓内）**：归并 `m3 verify` 24 文件、`graduation-lane-backup` 182 文件（tracked）、`strays-20260914/{p103-e3,p105-e3}` **944 文件/约 130 MB（gitignored）**——`.gitignore:53 backups/` 实测命中（`check-ignore` exit 0），归并后 `git status` 中 `backups` 相关条目 **0 个** ⇒ 正式库转储**未进版本库**；**逐字节校验 5/5 BYTE-IDENTICAL**（24/24、182/182、562/562、382/382、S4 单文件同哈希 `301DE7D5…`）；**随后删除 6 项原件**：`_gp_headcheck`、`.f88-baseline-wt`（`git worktree remove --force` exit 0 ＋ `prune`）、`D:\p103-e3`、`D:\p105-e3`、`graduation-lane-backup`、`.verify` ⇒ 逐个 `exists=False`；`git worktree list` **现仅剩主仓与 `m3-jdk8fix`**。**保留未删**：`D:\Develop_code\GraduationProject-wt\m3-jdk8fix`（问题②只批准归并＋改注释，未批准删 worktree）⇒ 其独有内容已全部入仓，**现为「可删候选」，待逐项批准**；空父容器一并保留。**过程纠错（登记）**：首轮比对报 4 项 `MISMATCH` 是**我自己的比对脚本 bug**（dest 传相对路径致 `Substring` 截错），**不是复制失败**（当时文件数已逐项相等）；按"校验不全绿一律不删"纪律**首轮未删任何项**，改用绝对根重算后 5/5 全绿才删。
- **引用链收口与新增纪律（2026-09-14 15:0x）**：①`spark-jobs/pom.xml` 注释 ② 两条证据路径**改指仓内** `docs/acceptance/m3-jdk8fix-evidence-20260914/verify/…`（**修掉 L6 缺陷⑤-1「已提交 pom 注释引用了永远无法入库的路径」**）；②`ct-batch-20260912/PLAN.md` §8.1 与 `lane/PROGRESS.md` 追加**第二条日期化勘误**：冷备份新位置＝`docs/acceptance/graduation-lane-backup-20260912/`（原文一字未改，以文末勘误为准）；③新增 `docs/acceptance/v25-stray-consolidation-20260914/EXECUTION-20260914.md`（批准范围／删除前复验／命令／逐字节校验／结果／残留／未取证）与 `docs/backups/strays-20260914/README-DO-NOT-COMMIT.md`（**含正式库真实数据 PII／会话／审计／bcrypt 哈希与明文口令，禁止提交外发**）。**W2 独有性复核**：`spark-jobs/pom.xml` 与主仓**逐字节相同**（sha256 `148180B477FEEE94…C6ED520E`），与 L6「3 blob 同主仓 HEAD、第 4 个为旧变体」一致。**新增永久纪律**：**(a) 外部产物巡检**——每次收尾实测「`D:\` 根／`D:\Develop_code` 是否出现新目录」并与此前清单比对（本轮已执行：**无新建**）；**(b) 删除三件套**——任何删除前必须「归并入仓 ＋ 逐字节校验全绿 ＋ 文件数清单比对」，缺一不删；**(c) 泳道交付模板增列「新建文件全路径」**，防止再出现"文件建到仓外而总控不知情"。**L6 §6 其余既有缺陷登记不改**：395 dangling blob＋22 dangling tree、16 个合法 0 字节 tracked blob、831 处索引/工作树体积不一致（约 **91% 为 `core.autocrlf=true` CRLF 假象**）、主仓 395 条脏污；**⑤-7「`_gp_headcheck` 于冻结裁决后 13:57:36 由 L5 新建且被 Maven 构建运行过（`target/classes` 13:57:44–47、`logs/generator.log` 13:58:13）」证明"冻结令未被所有通道遵守** ⇒ 已随本轮删除消除，纪律 (a) 即防复发。**新开 V25-S05**（存量明文默认口令收口，见 §3/§4；L6 建议其优先级高于散落物归并）。
- **E3 隔离真链（L4）交付与总控独立核验（2026-09-14 14:22 交付／15:2x 核验，证据 `docs/acceptance/v25-e3-isolated-chain-20260914/` ＋ `CONTROLLER-VERIFICATION-20260914.md`）**：交付 **53 文件/377 KB**（README 13 节＋`scripts/` 9＋`raw/` 24＋`raw/http/` 19）。**四级状态**＝提交完成✅／**测试通过❌（本轮未运行任何自动化测试，被实测 jar 是 `-DskipTests` 产物）**／限定验收✅（限定集＝3307 实例、工件 SHA256 `7BE717A9…7561`、仅启动期 env 覆盖、3 步链路、单机 local[2]）／**完整验收❌**（5 条缺项＋F-88 写侧未复测）。**实测要点**：在 `127.0.0.1:3307`（uuid `de8ebbea…`、datadir `/data/mysql-isolated/data/`）Flyway 建表至 meta **v18**／metric **v3**；链路 1（`runId=1`）271 s 后 **FAILED**＝`PIPELINE_QUALITY_FAILED`，根因做成**文件级取证**（fixture `golden-r73-clean-20260910.jsonl` 内 `golden-evt-008` 重复；`EVENT_ID_UNIQUE` `0.010989 > 0.0005`）⇒ **门禁正确拦截、不计缺陷**；链路 2（`runId=2`）291 s **SUCCESS**：8 阶段全绿、**10 个真 Spark 作业**（`spark_job_run` id 8–17、`submitter_type=local-process`）、`/metrics/overview` **10 个指标非空**、`S20260901_2` 成为隔离库 ACTIVE。**3306 零写三路自证**：连接面（3306=0／3307=30；日志中 "3306" 仅 1 次系脚本自身提示语）＋内容面（8 键 before/after 逐项一致）＋窗口面（两库 **23 张带时间戳表**在 `[13:50,14:20]` 内 `inwin_*=0`；`v25it-` 标识在 3306 全为 0）。**总控独立复算**：重跑 L4 的 `scripts/fingerprint-3306.sql`，`Compare-Object` **8/8 一致、差异 0** ⇒ 脚本可复现、且**至核验时点 3306 仍零漂移**。**L4 独立结论（高价值）**：**Flyway「有能力」且默认就会写 3306**（`application.yml:7/15/19` 默认值直指 3306；`MetaFlywayInitializer.java:23-31` 在 `@PostConstruct` 直接 `migrate()`）⇒ 本轮零写入**全靠启动期覆盖、无任何技术闸门** ⇒ **D-5 批准加闸门**（落 V25-S04「脚本＋应用双层、端口不在白名单即拒绝启动」；`V19/V20` 已在树内，误启动一次即对 3306 施加不可逆前向迁移）。**L4 缺陷登记**：D-1 规则版本/指纹不落库（F-88 核心）／D-2 目录为代码常量／D-3 `severity` 一列承担两种语义／D-4 两个异常类全仓无 `throw`（死代码）／D-5 无 3306 闸门／D-6·D-7·D-8 为本泳道脚手架缺陷（已自述自修）。**总控对 L4 §11.2 七项裁决**：①F-88 复测**立为下一任务**（须用含 `V19`/`V20` 的新工件、**仅在 3307**）②`V19`/`V20` 总控已批准（待裁-8/9/10）③D-5 闸门**批准做**④D-4 并入 D04/Q02 域⑤**不在本泳道补跑 `mvn test`**（部分 IT 历史直连 3306，须先有门禁）⇒「测试通过」保持❌⑥`D:\p103-e3`／`D:\p105-e3` **已按用户批准归并入仓并删除原件**（L4 因纪律未读取内容故不知情）⑦旁证指纹 `452b7223…`：全盘**仅 4 处文档命中、无任何脚本**；L4 所述「没有留下任何 SQL 或算法说明」**精确化为**「无 SQL／列集／分隔符／NULL 处理／作用域，仅有一句『排序后拼接再 MD5』描述」；总控另穷举 **90 个常规变体（15＋10 全列 × 3 分隔符 × 2 种行哈希 × 3 种聚合 × 5 种组合）无一命中** ⇒ **跨泳道互证不可得，改以 L4 入库脚本为新基准**（`metric_snapshot_fp=c224e4e9…`／`metric_value_fp=a1a07311…`）。**遗留（登记不清理）**：`%LOCALAPPDATA%\Temp\spark-<uuid>` 186 个＝`spark-submit`/Derby **自建**的 OS 临时目录，在用户授权范围内。
- **⛔ 看板冻结与待裁（2026-09-14 14:26 发现，总控登记）**：仓库中出现**两份从未提交、也未跟踪的新权威文档**：`docs/毕业设计指导书 V2.6.md`（24 874 B，mtime 14:26）与 `docs/design/项目设计文档 V2.3.md`（97 328 B，mtime 14:26）——二者非本会话总控所建，自称「并行开发工作」产物（`项目设计文档 V2.3.md:119` 明确「代码当前未提交改动由其他开发工作产生，不记为本文作者完成」），并已引用 L5（K-01～K-05）与 L4（14:15:09 停机）证据 ⇒ 作者当时已读到本会话泳道的证据。其 `:12-24` 宣布新治理规则：**只维护两个权威核心文档（指导书＋设计文档）、进度内置、旧文件一律只读、不再另建看板**，并称旧指导书/旧设计稿/独立看板/契约/日志/论文稿「不再成为第三个事实源」。**总控未获用户对 V2.6／设计V2.3 的批准，故本行是本看板的最后一次登记**（本行以上为本看板在位期间的事实记录，一律不改写）。待用户裁决：接受新双核心链（则本看板自此只读固化，L4 等后续登记改在 `项目设计文档 V2.3` 或其线性后继版）／退回草案（则需说明这两份未跟踪文档如何处置）。**在此之前不得提交这两份文档，也不得据其结论改动代码。**
- **本轮未取证（显式标注，不得当作已闭合）**：①三项「有独有内容」只做**机械哈希判定**，未做人工价值判读；②**未展开 jar/zip 内部**（`base.zip` 271 条目）；③**未连 3306 核对转储与现库一致性**（越界，且用户禁止写库）；④**未做全盘扫描**（范围仅限用户已识别的 7 项 ＋ `git worktree list` 注册项；如需全盘清点另开通道）；⑤未测 `git worktree prune` 的全部副作用（仅确认 `worktree list` 正确）；⑥`graduation-lane-backup/retired/` 内文件是否仍被运行期代码引用（仅做仓内文本引用统计）。**四级状态**：本通道上限＝**限定验收**（归并／校验／删除／引用收口均有实测证据），**未达完整验收**。

