# 项目完整实施指导书 V2.3

> 文档性质：项目剩余工作的唯一执行规格，不是论文草稿。
> 版本日期：2026-09-12（V2.2 的迭代版；相对 V2.2 的唯一实质变化＝新增 §4.1.1）
> 初始核查基线：Git 提交 `34f37a8`；`docs/v2-completeness-audit.md` 的 269 条核查结果。
> 当前实施基线：Git 提交 `3b7e9b2`（M1-4 的 D10/D12/S5 收口后）；最新状态以配套看板为准。
> 配套看板：`docs/项目实施进度与任务看板 V2.2.md`（看板版本号不变：本次迭代只补契约条款，未改变需求范围；见看板 §5.1 裁决 D-049）。
> AI 专项设计：`docs/superpowers/specs/2026-09-11-ai-assisted-warehouse-onboarding-design-v1.0.md`。
> AI 专项计划：`docs/superpowers/plans/2026-09-11-ai-assisted-warehouse-onboarding-implementation-v1.0.md`。
> 事实记录：旧 `docs/开发过程事实与决策记录.md` 只作历史材料；后续按独立 ADR/证据文件记录，不在本指导书中代写论文。
> 历史文档：V2.0、V2.1、V2.2 与未编号旧看板保留追溯（V2.2 自 2026-09-12 起转历史，其正文逐字未改）；凡与本文件冲突，以 V2.3 为准。

---

## 1. 当前结论与本版目标

当前项目不是“从零开始”，也没有全部完成。冻结基线为：269 条可判定要求中，132 条完成、90 条部分完成、42 条未做、5 条不适用；42 条未做可归并为 25 组功能缺口。主体链路已经能从事件进入 Landing，经 Hive/Spark 形成 ODS、DWD、DWS、ADS，并发布到指标库供页面、AI 和决策读取，但仍存在“有表无数据、有算法未接线、有接口无页面”和证据不足的问题。

V2.3 沿用 V2.2 的阶段判断：不推翻已完成链路，重点完成五件事：

1. 将现有“两程序”改成三个可独立构建、部署、停止和替换的程序：分析平台、参考商城、模拟数据生成器。
2. 建立真正可更换商城的数据源连接器与字段映射层，清除 `mock-mall` 等硬编码。
3. 把剩余 25 组缺口拆成开发 Agent 可直接执行、可单独验收的小任务。
4. 使用分层测试、固定小数据和并行泳道提高开发速度；大规模数据只在里程碑运行。
5. 在确定性商城无关化基础上，增加“受控 AI 数仓设计器”：AI 生成可审查草案，规则引擎负责校验，编译器只生成受控作业，人工审批后才能发布。

### 1.1 当前阶段不做的事

- 不撰写论文正文、摘要、结论、答辩稿，不为了论文包装未完成能力。
- 可以记录真实的架构决策、缺陷根因、修复过程、前后数据和测试证据。
- 未在相同环境、相同数据和相同口径下测量的“提升百分比”不得填写。
- ClickHouse 只保留接口和能力矩阵，状态为 `DEFERRED`；第一阶段不实现。
- 100 万条数据不进入日常回归，只在最终性能里程碑执行。

## 2. 文档与完成状态规则

### 2.1 权威顺序

1. 已冻结的事件、指标、证据、安全等契约文件。
2. 本指导书 V2.3。
3. `项目实施进度与任务看板 V2.2.md` 中已确认的任务状态和证据。
4. 本版链接的专项设计与实施书。
5. V2.1、V2.0、整改指导书、旧看板、历史计划和交接文档。

若实现与契约冲突，先提交契约变更决策，再修改实现；开发 Agent 不得私自改变事件语义、金额口径、状态机或表字段。

### 2.1.1 文档版本规则

- 已编号文稿不得覆盖或在末尾持续追加新阶段内容；每次需求、架构或执行基线发生实质变化，先备份当前文件，再生成一份结构完整的新版本。
- `V2.x` 表示当前“产品化、三程序解耦、商城无关化、受控 AI 接入”建设阶段内的迭代。V2 系列全部出口判据完成，且核心架构、主数据模型或建设阶段发生大变化时，建立 `V3.0`。
- 指导书和进度看板版本必须一致。日常执行只更新对应看板的状态单元格和证据字段，不以自由文本在文末堆叠新需求；范围改变时建立下一版本看板。
- `docs/README.md` 只把一个版本标为当前权威入口。旧版、旧看板和旧专项设计保持只读，供追溯与差异比较。
- 开发事实采用“一项决策/一次验收一个文件”的 ADR/证据方式记录。论文正文、摘要、结论和效果数字继续 `DEFERRED`，只有真实测量结果可进入事实材料。

### 2.2 执行状态

执行任务只允许使用 `TODO / READY / IN_PROGRESS / BLOCKED / REVIEW / DONE_LIMITED / DONE / REGRESSED / DEFERRED / N/A`。代码、表、接口或页面单独存在不等于完成；普通功能至少需要 E2 自动化证据，数据链需要 E3 本地真实链证据，集群能力需要 E4，普通员工可用的页面路径需要 E5。

### 2.3 反馈要求

- 开工前在看板登记任务 ID、Owner、允许修改范围和下次反馈时间。
- 最长 30 分钟更新一次；失败、阻塞或范围变化立即更新。
- 代码写完只能进入 `REVIEW`；通过目标测试并附证据后才能标 `DONE`。
- 开发 Agent 只执行分配的任务 ID，不得自行设计新模块或顺手改其他模块。
- 总控负责契约、热点文件、合并、跨模块测试与最终状态确认。

## 3. 三程序架构裁决

### 3.1 最终边界

| 程序 | 建议名称/端口 | 唯一职责 | 明确禁止 |
|---|---|---|---|
| 分析平台 | `analytics-platform` / 8091 | 数据源配置、采集编排、标准化、Landing、Hive 数仓、Spark 作业、质量、指标发布、看板、AI、决策、运维 | 商城业务增删改；随机造订单；依赖商城数据库或 Java 包 |
| 参考商城 | `reference-mall` / 8090 | 用户、商品、分类、库存、购物车、订单、支付、退款、Outbox 事件 | 分析任务、Spark/Hive、生成场景、直接写分析平台 |
| 模拟数据生成器 | `synthetic-data-generator` / 8092 或 CLI | 固定 seed 的场景生成、负载控制、脏数据策略、运行报告 | 直接操作商城数据库；依赖商城内部 Service/Mapper；写平台业务库；触发平台流水线 |

另外建立中立的 `contract-specs`，保存版本化 JSON Schema、OpenAPI 和清单 Schema。三个程序可以分别根据契约生成或校验数据，但不得通过共享 Java 实体、Mapper 或 Maven 业务依赖耦合。

### 3.2 唯一正常业务路径

```text
模拟数据生成器 --公开 HTTP API--> 参考商城 --Outbox/事件文件--> 数据源连接器
真实商城 -----------------------------------------------> 数据源连接器
数据源连接器 -> HDFS/本地 Landing -> 标准化 -> Hive ODS/DWD/DWS/ADS
             -> 质量门 -> MySQL ACTIVE 指标快照 -> 页面/AI/决策
```

这条路径表达两个关键事实：

- 参考商城只是可替换的数据提供者和演示样例，不是分析平台的一部分。
- 模拟数据生成器只是测试客户端。没有生成器、没有参考商城时，分析平台仍必须能连接其他商城并读取历史 ACTIVE 快照。

### 3.3 生成器的两种模式

#### A. `MALL_API` 模式

用于完整演示。生成器通过 `MallTargetAdapter` 调用商城公开接口完成注册、浏览、加购、下单、支付、取消、退款等行为。禁止调用商城内部类、直接连接商城数据库或修改库存表。

脏数据不得在此模式中绕过商城校验直接写库；需要验证平台隔离能力时，使用 B 模式。

#### B. `CANONICAL_EVENT_FILE` 模式

用于采集、质量、数仓和性能测试。生成器直接输出符合指定契约版本的 JSONL、manifest 和可选脏样本，写入独立测试目录。该模式必须显示 `synthetic=true`，不能被描述为商城端到端测试，也不得直接写 ODS/DWD/ADS/指标库或自动触发流水线。

### 3.4 独立验收

1. 三个程序有独立构建产物、配置、端口、日志和启动命令。
2. 商城工程中不存在 `generator` 包、`GeneratorController` 或生成器页面。
3. 生成器工程不引用商城实体、Mapper、Service、数据源配置，也不能访问商城 JDBC。
4. 分析平台不引用商城/生成器 Java 包、商城表或 `mall.*` 配置键。
5. 分别停止三者：其余程序不会因类加载或数据库依赖崩溃；停止数据源时平台明确显示采集不可用，但历史指标仍可读。
6. 可以用第二个 `MallTargetAdapter` 或一份不同字段的夹具证明平台不是为 `mock-mall` 写死。

## 4. 模拟数据生成器详细设计

### 4.1 核心接口

```java
interface MallTargetAdapter {
    TargetCheckResult test(TargetConfig config);
    TargetCapabilities capabilities();
    ProductPage listProducts(ProductQuery query);
    ExternalUser createSyntheticUser(UserCommand command);
    void emitBehavior(BehaviorCommand command);
    ExternalOrder createOrder(OrderCommand command);
    ExternalOrder pay(PayCommand command);
    ExternalOrder cancel(CancelCommand command);
    ExternalRefund refund(RefundCommand command);
}

interface EventSink {
    void write(CanonicalEvent event);
    Optional<Artifact> rotateIfNeeded();
    void flush();
    Manifest closeAndBuildManifest();
}
```

第一版实现 `ReferenceMallHttpAdapter` 与 `JsonlEventSink`。扩展其他商城时新增 Adapter，不修改场景引擎。

### 4.1.1 核心接口的追认与补全（2026-09-12，M1-9 契约先行）

> **为什么有本节**：§4.1 只给出九个方法签名——没有 `TargetConfig` 的字段定义、没有任何 DTO 的字段定义，
> 也没有约定"谁拥有商城路由"。S4a/S4b 在这些空白处做了实现侧选择，并逐条登记为偏离
> （`docs/acceptance/m1-4-s4b-mall-api-20260911/README.md` §6 的 D1–D12）。M1-9
> 「第二 `MallTargetAdapter`，证明未写死 `mock-mall`」开工前必须先补契约（看板 M1-9 必办项 ①、决策记录 D-041），
> 否则"新增一家商城"就会在契约未定义的区域内继续扩张。本节把其中经复核确认合理的选择**追认为契约**，
> 并补齐实现侧必须遵守的硬约束；**§4.1 正文保持逐字不变**。
> 逐条取证（`path:line`）、变更清单与未取证清单见 `docs/acceptance/m1-9-contract-first-20260912/README.md`。

**条款性质标注**：【追认】= 与当前实现一致，本节只是把既有事实写成契约；【新增要求】= 当前实现**尚未满足**，
排在 M1-9 ② 交付，**不得因为写进契约就当作已完成**。

#### 4.1.1.1 接口签名（以本节为准；§4.1 的骨架写法按本节读）

```java
interface MallTargetAdapter {
    /** 本实现负责的 adapter_type：注册表的唯一查找键，比较不区分大小写；取值集合不冻结。 */
    String adapterType();

    /** 对目标做一次真实检查（可联网），返回实测能力判定与过程说明。 */
    TargetCheckResult test(TargetConfig config);

    /** 按当前配置**声明**本适配器支持的能力（不联网）。默认实现"什么都没声明"。 */
    default TargetCapabilities capabilities(TargetConfig config) { return TargetCapabilities.none(); }

    /** 本适配器在各操作上真实使用的 HTTP 方法与路径（供运行流水如实记录）；无 HTTP 语义的操作不给条目。 */
    default Map<String, TargetRoute> operationRoutes(TargetConfig config) { return Map.of(); }

    default ProductPage listProducts(TargetConfig config, ProductQuery query) { throw unsupported("listProducts"); }

    default ExternalUser createSyntheticUser(TargetConfig config, UserCommand command) { throw unsupported("createSyntheticUser"); }

    default void emitBehavior(TargetConfig config, BehaviorCommand command) { throw unsupported("emitBehavior"); }

    default ExternalOrder createOrder(TargetConfig config, OrderCommand command) { throw unsupported("createOrder"); }

    default ExternalOrder pay(TargetConfig config, PayCommand command) { throw unsupported("pay"); }

    default ExternalOrder cancel(TargetConfig config, CancelCommand command) { throw unsupported("cancel"); }

    default ExternalRefund refund(TargetConfig config, RefundCommand command) { throw unsupported("refund"); }

    private MallOperationException unsupported(String operation) {
        return new MallOperationException(operation, "本适配器未实现：" + operation);
    }
}

/** 一次操作的 HTTP 方法与路径模板（如 `/api/v1/mall/orders/{orderId}/pay`）；不含 base_url 与凭据。 */
record TargetRoute(String method, String path) { }

interface EventSink extends AutoCloseable {
    void write(CanonicalEvent event);

    Optional<Artifact> rotateIfNeeded();

    void flush();

    ArtifactManifest closeAndBuildManifest();

    @Override
    default void close() { closeAndBuildManifest(); }
}
```

`TargetConfig`（契约定义，字段名以此为准）：

| 字段 | 类型 | 来自 `generator_target`（§4.2） | 语义 |
|---|---|---|---|
| `id` | `long` | `id` | 目标 ID；一次运行内冻结 |
| `adapterType` | `String` | `adapter_type` | 适配器查找键（见下 1） |
| `baseUrl` | `String` | `base_url` | 商城公开接口基地址；文件模式实现把它当输出目录 |
| `credentialRef` | `String` | `credential_ref` | 凭据**引用**（环境变量名/别名）；值绝不进配置、日志、流水与异常 |
| `configJson` | `String` | `config_json` | 适配器私有配置 JSON（如 `behavior_path` / `reset_path`）；内部结构由适配器自述 |

【追认】`generator_target` 其余五列（`name` / `config_version` / `status` / `test_environment` / `capabilities`）
**不进入** `TargetConfig`，也不参与任何适配器判定：能力判定一律取自 `capabilities(config)`（声明）与
`test(config)`（实测），运营登记的 `capabilities` 列只是台账。

#### 4.1.1.2 实现硬约束

1. **无状态 + 目标作首参 + 唯一登记处**【追认】：适配器实例不持有目标，目标一律作首参传入；`MallTargetAdapterRegistry` 是 `adapter_type → 实现` 的唯一登记处，一个类型只允许一个所有者；未注册与重复注册都必须响亮失败并列出已注册类型。禁止"每个目标一份适配器实例"这种第二所有者。
2. **跨目标状态不得进实例**【新增要求】：目录缓存、外部 ID 映射一类跨目标状态属于运行期/仓储，不得放进适配器实例字段。
3. **做不到就响亮失败**【追认】：未实现或不支持的操作抛 `MallOperationException`（携带操作名），不得返回 `null` 或空对象冒充成功；引擎侧按"整类降级 + 记缺口"处理，绝不把跳过写成成功。
4. **凭据纪律**【追认】：`credentialRef` 只是引用名；取不到凭据时抛异常，日志、流水与异常信息里只允许出现引用**名**（如 `GENERATOR_TARGET_TOKEN`），绝不出现值；契约文本本身也只写引用名。
5. **能力三态**【追认】：判定只有 `SUPPORTED` / `ABSENT` / `UNDETERMINED`；缺键、未知、探测不到一律 `UNDETERMINED`，**不得**把 `UNDETERMINED` 说成 `SUPPORTED`。`capabilities` 是声明（不联网），`test` 是实测；运行预检以声明为准并强制 `product` / `user` / `order` 三项。
6. **路由所有权**【新增要求】：运行流水与运行报告里的 `http_method` / `route` **必须取自 `operationRoutes(config)`**；引擎、报告与页面不得内置任何商城的路由字面量。适配器对自报路由的真实性负责（它就是实际发起请求的一方）；未声明路由的操作只写占位说明。理由：流水里的路由是"我们到底请求了什么"的证据，换一家商城却继续写参考商城的路径等于在证据里说谎（与 D12 同类问题）。
7. **新增一家商城 = 新增一个 Adapter + 登记处加一行**【新增要求】：不得为此修改场景引擎、事件契约、表结构或页面；引擎里不得出现"哪家商城"的分支（`adapter_type` 只用于选实现，不用于业务分支）。
8. **只经公开接口**【追认】：`MALL_API` 模式只允许经商城**公开 REST 接口**；不得直连商城数据库，不得依赖商城 Java 包、业务表名或 `mall.*` 配置键。
9. **运行期单一配置实例**【追认】：一次运行只解析一次 `TargetConfig`，并贯穿探测、预检、派发与流水；同一运行内不得出现第二份目标配置。
10. **金额与精度**【新增要求】：不同商城的金额标度可能不同（元/分），适配器负责换算到与参考商城同口径后再交给引擎；换算系数必须是适配器内的显式常量并有单测覆盖；金额一律 `BigDecimal`，禁止 `double`；对账与校验一律用整数分比较。规范事件的金额口径以已冻结的事件契约与参考商城实现为准，本指导书不另立单位。
11. **契约纪律**【新增要求】：`TargetRoute`、`operationRoutes` 以及 §4.1.1.3 之外的新增字段/方法，必须先改本节再实现（§2.1）。`contract-specs/openapi/generator-api.v1.yaml` 现在把这一区域标为 `x-unspecified`，待 M1-5 收口时按本节同步（含 `TargetCheckResult` 的空对象声明与 `adapter_type` 取值集合）。

#### 4.1.1.3 DTO 最小字段表【追认】

字段名与类型以此表为准；**在已列字段之外新增字段，必须先改本节再实现**。构造校验（非空、范围、枚举白名单）同在契约内。

| 类型 | 字段 | 关键约束 |
|---|---|---|
| `TargetCheckResult` | `targetId`(long)、`reachable`(boolean)、`detail`(String)、`capabilities`(Map) | `detail` 只写过程说明，不含凭据值 |
| `TargetCapabilities` | `verdicts`(Map)、`declared`(boolean) | 缺键 ⇒ `UNDETERMINED`；`none()` = 空表 + `declared=false` |
| `MallCapability` | `product`、`user`、`order`、`refund`、`behavior`、`reset_state`、`admin` | 生成器侧实现词汇，不是跨程序契约；新增能力名须先改本节 |
| `CapabilityVerdict` | `SUPPORTED` / `ABSENT` / `UNDETERMINED` | 见 4.1.1.2 之 5 |
| `ProductQuery` | `categoryId`(Long)、`keyword`(String)、`offset`(int)、`limit`(int) | `offset ≥ 0`；`limit ∈ [1,500]` |
| `ProductPage` | `products`(List)、`total`(int) | `total` 不小于 `products.size()` |
| `ExternalProduct` | `productId`、`name`、`categoryId`(Long)、`price`(BigDecimal)、`status`(String) | `price ≥ 0`；`onSale()` 只认商城原样返回的 `on_sale` |
| `UserCommand` | `ageGroup`、`cityLevel`、`memberLevel`(String) | 取值受契约枚举白名单约束 |
| `ExternalUser` | `userId`、`memberLevel`(String) | — |
| `BehaviorCommand` | `userId`、`productId`、`sessionId`、`behaviorType`、`channel`(String) | 均非空 |
| `OrderCommand` | `userId`(String)、`items`(List) | 非空；`Item` = (`productId`, `quantity ≥ 1`) |
| `ExternalOrder` | `orderId`、`userId`、`status`(String)、`totalAmount`(BigDecimal)、`itemCount`(int) | `status` 是商城原样文本；读不到记 `UNKNOWN`，未返回记 `null` / `-1` |
| `PayCommand` | `orderId`、`userId`(String) | — |
| `CancelCommand` | `orderId`、`userId`、`reason`(String) | `reason` 必填、去空白后非空 |
| `RefundCommand` | `orderId`、`userId`、`amount`(BigDecimal)、`reason`(String) | `amount > 0`；`reason` 必填 |
| `ExternalRefund` | `refundId`、`orderId`、`status`(String)、`amount`(BigDecimal) | `status` 是商城原样文本；读不到记 `UNKNOWN` |
| `MallOperationException` | `operation`(String) + 消息 | 只承载操作名与脱敏说明 |

`EventSink` 另追认两点【追认】：① 返回类型名是 `ArtifactManifest`（§4.1 骨架里的 `Manifest` 与 `java.util.jar.Manifest` 同名，只作 Java 类名调整，语义与字段不变）；② 追加 `extends AutoCloseable` 与 `default close()`（关闭即产出清单，便于 try-with-resources）。

---

### 4.2 数据结构

| 对象/表 | 关键字段 | 约束与用途 |
|---|---|---|
| `generator_target` | id、name、adapter_type、base_url、credential_ref、config_json、config_version、status、test_environment、capabilities | 凭据只存引用；每次运行冻结版本 |
| `generation_plan` | plan_id、mode、target_id、scenario、seed、start/end、event_count、rate_per_second、dirty_profile、output_uri、version | 运行只引用不可变版本 |
| `generation_run` | run_id、plan_version、target_version、status、started/finished、success_count、failed_count、checksum、error、cancel_requested | 异步运行，可查询和取消 |
| `generation_artifact` | run_id、uri、checksum、bytes、record_count、min/max_event_time、schema_version | 用于可复现和对账 |
| `generation_event_stat` | run_id、event_type、count、amount | 验证各事件类型和金额分布 |

状态机：`PENDING -> RUNNING -> SUCCESS/FAILED/CANCELLED`。相同 `plan_version + seed + time_window` 应可复现；运行中修改计划不会影响已启动任务。

### 4.3 场景和算法

- 商品抽样：类别先按目标权重抽样，类别内采用 Zipf/长尾分布，避免所有商品热度相同。
- 用户分层：新客、活跃、沉睡、高价值、价格敏感等分群使用可配置比例。
- 时间分布：工作日/周末、小时段和活动峰值使用分段权重；不能简单均匀随机。
- 行为链：浏览→收藏/加购→下单→支付→退款使用条件概率，且订单必须引用已存在用户与商品。
- 金额：使用 `BigDecimal`；商品价格、数量、优惠、实付和退款遵守商城公式。
- 库存：MALL_API 模式只通过商城接口变化；失败必须记入运行报告，不能绕过业务规则。
- 可复现：所有随机源由一个根 seed 派生；并行线程也使用确定性子 seed。
- 异常样本：仅在文件模式注入缺字段、未知枚举、重复 eventId、非法金额、迟到事件和非法状态跳转，并在 manifest 中记录期望隔离数。

### 4.4 对外 API

- `POST /api/v1/generation-runs`：按 plan/version 异步启动，返回 runId。
- `GET /api/v1/generation-runs/{id}`：状态、进度、计数、失败摘要。
- `POST /api/v1/generation-runs/{id}/cancel`：幂等取消。
- `GET /api/v1/generation-runs/{id}/artifacts`：文件、checksum、清单。
- `GET /api/v1/scenarios`：场景定义和输入约束。
- `POST/PUT/GET /api/v1/targets` 与 `POST /targets/{id}/test`：目标商城配置和连通性检查。

### 4.5 页面

生成器拥有自己的简洁页面或 CLI，不再出现在 `mall-frontend`：目标商城、场景计划、运行列表、实时进度、失败明细、产物下载。商城页面只面向购物者和商城管理者；分析平台页面只面向分析与运营人员。

## 5. 可更换商城的数据源与连接器

### 5.1 平台统一标识

外部商城不能自行决定平台主键。平台创建 `source_instance_id`，它从 Landing manifest 传播到 ODS、DWD、DWS、ADS、指标快照和页面筛选。跨商城去重键为 `(source_instance_id, event_id)`，不能只按 `event_id` 去重。

第一阶段可限制“一次流水线只处理一个 source instance”，但任何表、Java/Scala 常量、配置键和页面不得写死 `mock-mall`。

### 5.2 元数据结构

| 表 | 关键字段 | 说明 |
|---|---|---|
| `source_instance` | id、tenant_id、code、name、business_type、timezone、currency、status、created_at | 表示一个具体商城/租户 |
| `source_connector` | id、source_instance_id、type、config_json、credential_ref、version、status、capability_json、last_test_result | 表示采集方式和版本 |
| `schema_mapping` | id、source_schema_version、target_contract_version、event_type_map、field_mapping_json、enum_mapping_json、timezone、amount_unit、transform_policy、status、version | 把源字段映射为平台契约 |
| `source_manifest` | batch_id、source_instance_id、connector_version、mapping_version、uri、checksum、record_count、min/max_event_time、event_type_counts、contract_version | 批次溯源与对账 |
| `ingestion_batch_file` | batch_id、file_uri、start/end_offset、checksum、accepted、quarantined、parse_errors | 文件级证据 |
| `file_checkpoint` | connector_id、file_identity、offset、updated_at | 断点必须绑定连接器 |

### 5.3 连接器接口

```java
interface SourceConnector {
    ConnectorType type();
    CapabilitySet capabilities();
    ConnectorTestResult test(ConnectorContext context);
    DiscoveryResult discover(Checkpoint checkpoint);
    FetchResult fetch(SourceObject object, LandingStorage storage);
    Checkpoint commitAfterSuccess(FetchResult result);
}
```

首期：`LocalFileConnector` 和 `FlumeLandingConnector`。Flume 已负责投递到 Landing 时，连接器不重复搬运数据，而是管理配置、心跳、目标 URI、批次发现和 manifest。DataX/Kafka 只登记类型、配置 Schema 和 `NOT_IMPLEMENTED/DEFERRED` 能力，不能显示为可用。

### 5.4 标准化与映射

采集只负责可靠移动和断点；`EventNormalizer` 负责源事件到 canonical event 的转换：事件类型映射、JSONPath 字段提取、枚举映射、时区转换、金额单位转换和允许的纯函数变换。禁止在映射表达式中执行任意脚本或 SQL。

未知源版本、没有 ACTIVE mapping、必填字段缺失和不允许的转换全部进入 quarantine，并保存 source instance、mapping version、reason code 和原始位置。映射激活必须先用样本 dry-run，并输出接受/隔离/错误计数。

映射的最终真相仍是版本化配置和审核记录，不是大模型对话。P1–P5 先证明人工/配置驱动接入异构源；其后 AI 可以读取字段元数据和脱敏样本，生成 `SourceProfileDraft`、字段映射建议、置信度和待确认问题。低置信度、金额、退款、用户身份、时区等业务语义必须由人确认，AI 草案不能直接变为 ACTIVE mapping。

### 5.5 数据源页面

普通员工在分析平台中看到“数据源”，而不是填写 `runtimeProfileId`。页面包括：商城名称、连接器类型、状态、最后心跳、最近批次、映射版本、测试连接、启停和查看错误。运行环境档案属于管理员配置；数据源选择使用可读名称。

## 6. 集群、HDFS、Hive 与 Spark 运行方案

### 6.1 不绑定 node01–03，运行能力必须正交配置

node01–03 是一个可选运行环境，不是代码常量。不能再仅凭 `LOCAL / SINGLE_NODE / REMOTE_CLUSTER` 推断所有行为，因为“平台部署在哪里”“Landing 存在哪里”“Spark 由谁提交”“Spark 使用哪种集群管理器”是四个相互独立的维度。例如，平台部署在 node01 时可以由本地进程提交 YARN；平台部署在 Windows 时才需要先 SSH 到提交网关。

`RuntimeProfile` 至少显式区分：

- `platformPlacement`：WINDOWS_LOCAL / LINUX_LOCAL / CLUSTER_GATEWAY，仅用于说明部署位置。
- `landingStoreType`：LOCAL / HDFS。
- `sparkSubmitterType`：LOCAL_PROCESS / SSH。
- `clusterManager`：LOCAL / YARN / STANDALONE。
- `hiveMetastoreUris`：`thrift://host:9083`，供 Spark/Hive catalog 使用。
- `hiveJdbcUrl`：`jdbc:hive2://host:10000/...`，只供 HiveServer2 连通与查询；不得填入 `hive.metastore.uris`。
- `sshCredentialRef`、`hiveCredentialRef`、`hadoopConfRef`：职责分开的凭据/配置引用。
- `remoteLogRoot`、`jobJarUri` 和 jar SHA-256。

旧 `profileType` 可保留为展示标签，但不能继续作为提交方式和存储方式的唯一判断条件。每个档案激活前运行真实环境检查。

### 6.2 推荐集群数据流

```text
参考商城 Outbox/事件文件
    ├─ 商城在 node01：Flume Taildir/Spooling + FileChannel -> HDFS Sink
    └─ 商城在本机/Windows：SFTP/HTTP 网关/明确共享目录 -> node01 spool
                                                        -> Flume -> HDFS
HDFS /graduation/landing/{source_instance_id}/dt=yyyyMMdd/hour=HH/
    -> 平台发现 manifest -> SSH 提交 spark-submit 到 YARN/standalone
    -> Hive warehouse -> ADS -> MySQL 指标快照 -> 页面/AI/决策
```

Flume 不能跨机器直接 tail 一个不存在于 agent 主机的本地文件。商城若不在集群节点上，必须明确传输到 node01 spool 的协议；开发 Agent 不得凭空假设 Windows 路径可被 Linux Flume 读取。

### 6.3 RuntimeProfile 关键字段

- `profileType`：LOCAL/SINGLE_NODE/REMOTE_CLUSTER。
- `landingType` 与 `landingUri`：LOCAL_FILE/HDFS，以及完整 URI。
- `hdfsDefaultFs`、`warehouseUri`、`hiveJdbcUrl` 或 `metastoreUri`。
- `sparkMaster`、`deployMode`、`queue`、`sparkSubmitPath`、`jobJarUri`。
- `submitHost`、`sshPort`、`sshUser`、`sshCredentialRef`。
- `kerberosEnabled`、principal 与 keytab 的凭据引用。
- `metricStoreType` 与 metric read/publish 数据源引用。
- `configVersion`、`status`、`lastCheckAt`、`lastCheckReport`。

敏感值不写数据库明文、YAML、命令日志或页面；只保存环境变量、密钥文件或密钥服务引用。

### 6.4 集群预检顺序

1. 主机名、IP、端口、DNS、时间同步和 Java/Spark/Hadoop/Hive 版本。
2. HDFS 临时文件创建、写入、读取、删除。
3. Hive 临时库/表创建、写入、查询、删除。
4. 提交最小 Spark count 作业，取得 externalJobId、最终状态和日志。
5. Flume 投递一个小文件到目标 HDFS 目录，校验 checksum。
6. 用 55 条黄金数据跑集群小链。
7. 用固定 seed 的 1,000 条数据跑集群链。
8. 对同一输入比较本地与集群的核心指标同值。

任何一步失败都必须返回“检查项、目标、结果、错误摘要和修复建议”，不能只返回激活失败。

### 6.4.1 当前代码在接入集群前必须先修的缺口

以下均为当前实读代码结论，不能把“类已存在”写成“集群已支持”：

1. 生产采集仍有把 `landingUri` 转成 `java.nio.Path` 并使用 `Files.*` 的路径；`hdfs://` 会被误当成本地路径。必须让生产入口实际经过 `LandingStorage`/连接器抽象。
2. `HdfsLandingStorage` 的健康检查不得只列根目录，必须在授权的 `<base>/_health/<uuid>` 中完成 create/write/read/rename/delete，且只清理自己创建的探针。
3. 现有 Flume 配置使用 `%{eventType}` 路径，但 Taildir 没有产生该 header；首版改为按 source + 采集时间写 raw，并启用本地采集时间。业务 `event_type/event_time` 由 ODS 解析后分区。
4. SSH 提交目前可能等待远端 spark-submit 完成并返回伪造 `ssh-*` ID。目标协议是 1–2 秒返回 launcher reference，随后提取真实 YARN `application_...`；伪 ID 不能冒充 externalJobId。
5. 远端命令只允许调用固定 wrapper 和白名单参数，禁止直接拼接 UI 输入；SSH 必须校验 known_hosts，禁止 `StrictHostKeyChecking=no`。
6. 第一版远程执行优先 `master=yarn, deployMode=client`，便于从 node01 网关收集 driver 日志；确认 YARN 日志聚合后再支持 cluster deploy。
7. 版本化 jar 放到 `/apps/graduation/spark-jobs/<gitCommit>/...jar` 并保存 SHA-256，不能只引用可变的 `latest`。
8. Flume 是 at-least-once；平台只能通过 checkpoint、manifest 和 `(source_instance_id,event_id)` 去重提供幂等结果，不得宣称 exactly-once。

### 6.5 需要用户提供的信息

以下内容登记为 `USER-INPUT` 阻塞项，不得由 Agent 猜测：

- node01、node02、node03 的主机名/IP和 NameNode、DataNode、ResourceManager、NodeManager、Hive、Spark 等角色。
- HDFS NameNode URI/端口、允许写入的根目录、warehouse 路径和服务用户权限。
- Spark 是 YARN client、YARN cluster 还是 standalone，队列名称和提交命令路径。
- HiveServer2 JDBC 或 Metastore URI、数据库名前缀。
- SSH 主机、端口、用户、密钥/凭据引用、是否经过跳板机。
- Hadoop/Spark/Hive/Java 版本与安装目录；作业 jar 位于提交节点还是 HDFS。
- 是否启用 Kerberos，以及 principal/keytab 的安全提供方式。
- Flume agent 运行位置，商城事件到 agent spool 的传输方式和监听目录。
- 平台、商城、MySQL 与集群之间实际开放的端口。

## 7. 数仓、算法与剩余实现规格

### 7.1 DIM 与交易 DWD

- `dim_date`：由可配置日期区间生成日期、周、月、季度、工作日和节假日标志；生成任务幂等。
- `dim_region`：版本化地区字典，至少覆盖来源商城实际地区；未知值落 `-1/unknown` 并计数。
- `dim_metric`：由 MySQL `metric_definition` 同步，记录 metric_code、名称、单位、聚合方式和口径版本。
- 订单状态机：Java 与 Scala 共享同一份中立契约；非法转换写 `dwd_reject_record`，不能静默接受。
- 订单行校验：`SUM(quantity * unit_price - line_discount) = order_amount`，误差绝对值小于 0.01。
- 退款：按 `refund_id + event_time + ingest_time + event_id` 取最新；累计退款不得超过实付。
- 去重排序：同一业务键按 event_time、ingest_time、event_id 确定唯一结果，保证重跑稳定。

### 7.2 DWS/ADS 与算法

- 漏斗增加渠道和分类粒度。若后阶段人数大于前阶段，保留真实数值并输出“非严格同 cohort 漏斗”说明，禁止用 `min` 截断伪造单调性。
- 新增 `ads_category_sale` 与 `ads_region_sale`；地区指标至少有 sale_amount、net_sale_amount、order_count、buyer_count。
- `ads_sale_trend` 增加 net_sale_amount；repeat_rate 必须实际落表并发布。
- RFM：宽表保存 recency_days、frequency、monetary_amount、R/F/M score、segment、rule_version。M 的时间范围、退款口径和评分方向必须冻结；小样本标为“仅演示”。
- 排行和 NTILE 增加稳定次序键，避免同值重跑漂移。
- 异常检测：至少 14 个历史点才计算；第一版可采用滚动中位数/MAD 或滚动均值/z-score。结果表保存 metric_code、source_instance_id、window、actual、baseline、deviation、threshold、algorithm_version、snapshot_id。算法类必须接入生产指标链，不允许只有孤立类。

### 7.3 质量规则

建立 `quality_rule_definition` 和版本/生效区间，严重度行为如下：BLOCKING/ERROR 阻断发布，WARN/INFO 持久化并展示但不阻断。至少分别实现和验收：

1. Landing JSON 可解析率。
2. ODS event_id 非空和 schema version 支持。
3. DWD 行为字段/枚举合法。
4. 订单行金额公式误差 < 0.01。
5. 退款额 ≤ 实付额。
6. 非法状态跳转。
7. DWS 与 DWD 金额对账。
8. ADS 的 GMV ≥ 净销售 ≥ 0。
9. ADS 的 UV ≤ PV。
10. 支付用户 ≤ 浏览用户；不满足时必须给出口径说明。
11. `ingest_time - event_time` P95 和迟到率。
12. 发布行数一致性与抽样 checksum。

每条 BLOCKING 规则必须有一条“构造失败→流水线失败→不产生新 ACTIVE→旧 ACTIVE 可读”的负向验收。

### 7.4 指标存储

第一版 Hive 全程存在：ODS/DWD/DWS/ADS 均在 Hive。MySQL 不是数仓替代品，而是将常用 ADS 指标发布为 ACTIVE 快照的低延迟服务库。页面、AI 和决策只通过 MetricStore 读取同一快照。

建立 `MetricStoreFactory` 按 `metric_store_type` 选择实现：MySQL 为默认并完成；Hive 为管理员显式降级查询路径，不由系统按数据量随机切换；Doris 为第二阶段首选；ClickHouse 仅接口和能力矩阵，标记 DEFERRED。

普通用户只能读 ACTIVE 快照。必须提供当前 ACTIVE 元信息接口，包含 snapshotId、sourceInstanceId、businessDate、version、createdAt 和口径版本。

## 8. 页面、AI、决策和安全

### 8.1 面向普通员工的页面

必须补齐：数据源管理、流水线 stage/job/externalJobId/log/error、总览环比、行为渠道/分类构成、商品库存覆盖天数、销售分类/地区、RFM 金额、决策截止日期、表格分页和稳定排序。页面用业务名称，不暴露 runtimeProfileId、Hive 表名或 Spark 参数。

### 8.2 AI 的产品定位

AI 不是独立页面或单一 Text-to-SQL 功能，而是横向辅助三个业务环节：

1. **分析辅助**：自然语言问数、异常解释、指标归因候选与经营摘要，全部引用同一 ACTIVE snapshot 的证据包。
2. **决策辅助**：基于固定模板生成 DRAFT 建议，补充适用前提、风险、观察窗口和目标指标；人负责审批和执行。
3. **新商城接入与数仓设计辅助**：读取数据库元数据、OpenAPI、JSON/CSV Schema 和少量脱敏样本，提出字段映射、分层表、粒度、主键、质量规则、指标与工作流草案。

现有 `LlmProvider`、`TextToSqlService`、`SemanticCatalog`、`SqlSafetyValidator` 和 `EvidencePackage` 是可复用基础，但“类已存在”不代表真实 LLM、动态语义目录或 AI 建模工作台已经完成。开发 Agent 必须按看板证据如实登记。

### 8.2.1 受控 AI 数仓设计器

AI 接入新商城时不得直接创建库表或执行任意 SQL。平台固定通用核心域：用户、商品、行为、订单、支付、退款；只有不能映射到核心域的信息才进入源扩展区。完整流程如下：

```text
源连接/样本/元数据
  -> 确定性数据画像与脱敏
  -> AI 生成 SourceProfileDraft + WarehouseBlueprint + WorkflowDraft
  -> JSON Schema/命名/必填字段/SQL AST/血缘 DAG/资源预算校验
  -> 人工确认低置信度映射和业务口径
  -> 受控编译器生成 DDL、转换 SQL 和固定类型作业 DAG
  -> sandbox 命名空间用 20–100 条数据 dry-run
  -> 对账、质量检查和证据包
  -> data_dev/admin 审批
  -> 不可变版本发布；失败或回归时回滚到 previous_release
```

状态机为：

```text
DISCOVERING -> DRAFT -> NEEDS_CONFIRMATION -> VALIDATING
-> SANDBOX_RUNNING -> REVIEW -> APPROVED -> DEPLOYED
```

失败终态/旁路状态为 `VALIDATION_FAILED / SANDBOX_FAILED / REJECTED / ROLLED_BACK`。只有 `APPROVED` 能进入部署；部署动作必须二次校验 blueprint checksum、审批人、目标 namespace 和前一 release。

### 8.2.2 AI 建模数据结构

| 对象/表 | 核心字段 | 用途与约束 |
|---|---|---|
| `ai_modeling_session` | session_no、source_id、source_profile_version、status、provider、model、prompt_version、input_fingerprint、created_by、started/finished、error | 一次可追溯建模会话；相同输入指纹可识别重复请求 |
| `source_profile_draft` | session_id、version、draft_json、confidence_summary、questions_json、status、checksum | 源画像草案；不得覆盖历史版本 |
| `mapping_review_item` | draft_id、source_path、target_field、inferred_type、confidence、rationale、status、reviewer、comment | 逐字段人工确认；低置信度默认待确认 |
| `warehouse_blueprint` | blueprint_no、version、canonical_model_version、source_id、blueprint_json、status、checksum、approved_by/at | ODS/DIM/DWD/DWS/ADS 的结构化蓝图 |
| `workflow_definition` | blueprint_id、workflow_code、version、nodes_json、edges_json、status、checksum | 只允许受控节点类型的 DAG |
| `modeling_validation_result` | blueprint_id、check_code、severity、status、message、evidence_json | 规则校验和 sandbox 证据 |
| `workflow_release` | release_no、target_namespace、artifact_uri/checksum、status、deployed_by/at、previous_release_id、rollback_at | 不可变发布与回滚指针 |

结构化中间对象至少包括 `SourceProfileDraft`、`WarehouseBlueprint`、`TableBlueprint`、`ColumnMapping`、`QualityRuleDraft`、`MetricDraft` 和 `WorkflowNodeDraft`。工作流节点白名单只允许 `DISCOVER_SOURCE / LANDING_VALIDATE / ODS_LOAD / DIM_BUILD / DWD_BUILD / DWS_AGG / ADS_BUILD / QUALITY_CHECK / PUBLISH_METRIC`，禁止 AI 生成任意 shell 节点。

### 8.2.3 AI 与规则系统的责任边界

AI 可以提出：类型与主键候选、字段/枚举映射、事实表和维表、粒度、SCD 类型、分区、代理键、质量规则、指标、血缘、资源估算和回滚建议。AI 必须显式提出它无法确定的问题，例如“订单金额是标价还是实付”“退款是否冲减 GMV”“订单状态含义”“匿名用户如何合并”“源时区是什么”。

AI 不得决定或执行：销售/退款/复购公式、破坏性重建、低置信度强制映射、任意 SQL/shell、绕过质量门和审批、直接发布经营动作。确定性系统负责 JSON Schema、命名白名单、字段完整性、类型兼容、SQL AST、无环 DAG、扫描量/资源上限、敏感字段脱敏和危险 DDL 拦截。

### 8.2.4 AI 调用、安全与一致性

- 模板、Mock LLM、真实 LLM 三种路径共享结构化输出 Schema、超时、有限重试、熔断、每日预算和审计。
- 真实调用记录 traceId、provider/model、prompt_version、输入输出 token、耗时、状态、错误类型和费用估算；traceId 与 input_fingerprint 用于幂等，避免重复计费。
- 样本在发给外部模型前做字段级脱敏和数量上限；列名、注释、样本值都按不可信输入处理，防止 prompt injection。
- Text-to-SQL 继续经过语义目录、AST 白名单、EXPLAIN、扫描/行数/时间限制和只读身份；建模 SQL 只能由受控编译器从已验证蓝图生成。
- Hive、MySQL、API、DOM、AI 必须在同一 snapshotId 下进行五处同值断言；AI 无证据时输出“不足”，不得估算或补零。

### 8.2.5 面向非专业用户的建模向导

页面采用七步向导：选择数据源与样本 → 查看画像 → 审核 AI 字段映射与置信度 → 查看分层表/粒度/血缘蓝图 → 执行规则校验和 sandbox → 查看差异、对账和风险 → 审批、部署或回滚。普通员工可以使用解释和默认值完成草案，但只有 `data_dev/admin` 可以批准部署。

### 8.3 决策

完成五个模板：流量下降、漏斗流失、商品转化、库存风险、用户召回。每个模板包含发现、量化证据、适用前提、建议动作、目标指标、观察窗口、截止日期和风险。效果评估使用等长前后窗口聚合，不得只比较两个端点快照。

必须生成并保存 EFFECTIVE、PARTIAL、INEFFECTIVE、INSUFFICIENT_DATA 四类可达样本。人工草稿来源为 `human`；AI 草稿来源记录模型和证据版本。

### 8.4 安全与审计

- 增加 data_dev 测试角色；前端路由和后端 API 双层鉴权。
- 删除可用默认口令；生产可关闭种子账号并要求首次登录改密。
- Hive/SSH/LLM 凭据统一使用 credential_ref，日志脱敏 password、token、Authorization、key。
- 分开“启动流水线”和“重试”权限。
- 审计环境激活、流水线启动/重试、质量强制处理、快照激活和归档快照读取。

## 9. 并行开发和测试提速

### 9.1 并行泳道

在四个并发槽位下采用“一总控 + 三开发泳道”：

- A：三程序拆分、连接器、集群。
- B：数仓、ADS、质量、指标发布。
- C：页面、AI、决策、安全。
- 总控：冻结契约、分任务、控制热点文件、集成和验收。

`PipelineService`、迁移版本号、统一事件契约、前端 router、主配置属于热点文件，同一时间只允许一个 Owner 修改。任何跨泳道字段变化先由总控登记契约任务，其他 Agent 等待契约冻结。

### 9.2 测试分层

| 层级 | 内容 | 执行时机 |
|---|---|---|
| T0 | 受影响类的单测、静态检查、编译 | 每个小任务提交前 |
| T1 | 当前 Maven/Vue/Scala 模块测试，使用内存或 20–100 条数据 | 每个泳道交付前 |
| T2 | 55 条黄金链，真实本地 MySQL/Spark/Hive | 一批并行任务合并后一次 |
| T3 | 固定 seed 1,000 条集群链 | 连接器/集群里程碑一次 |
| T4 | 10 万/100 万条性能 | 性能里程碑/最终验收，不做日常回归 |

不删除已有测试，只把重测试移动到里程碑门。以下变化必须运行 T2：事件契约、分区、金额口径、流水线阶段、发布事务、质量阻断。纯页面样式或单一 DTO 变化不应反复启动全 Spark 链。

开发阶段优先使用固定 seed、覆盖所有事件类型和失败分支的小数据；“缩数据量”不能缩业务场景。每个失败只复跑受影响层，修复后再过当前里程碑门。

### 9.3 AI 建模评测与提速

AIW 日常开发不反复运行大集群。T0/T1 使用三个固定小源：参考商城 A、字段/枚举/ID 均不同的异构源 B、包含歧义和恶意内容的源 C；每源只取 20–100 条脱敏记录。稳定评测集统计字段映射 precision/recall、必填字段召回、枚举覆盖、蓝图规则通过率、编译首过率、sandbox 成功率、人工修正项数、耗时、token 和估算费用。

AI 接入的有效性必须用同一异构源 B 做 A/B 对照：P5 的人工配置接入是基线，AI 辅助接入是实验组，环境、数据和验收脚本相同。没有实测前不得填写“效率提升百分比”。安全集必须覆盖样本/列名 prompt injection、SQL/shell 注入、路径穿越、PII 泄漏、循环 DAG、破坏性 DDL、超预算、超时和模型不可用；模型不可用时允许保存人工草案，但不得伪造 AI 成功。

## 10. 开发 Agent 的任务包格式

总控派发任务必须包含以下内容，缺一不可：

```text
任务 ID / 标题：
目标与用户价值：
当前证据与缺陷：
允许修改的模块和文件：
禁止修改的模块和文件：
输入契约与数据结构：
输出契约/API/页面：
算法与边界条件：
依赖任务：
完成定义：
T0/T1 最小测试：
里程碑测试与证据路径：
下次反馈时间：
```

Agent 遇到规格缺失时标记 BLOCKED 并列出唯一需要决策的问题，不得自行发明字段或改变业务口径。

## 11. 实施顺序

1. M1 冻结三程序边界、契约和独立构建；这是后续并行的前提。
2. M2 建立 source instance、connector 和 mapping；同时收集集群参数。
3. M3 先跑集群预检和 55 条链，再接 1,000 条链。
4. P5 先用异构源 B 完成一次完全确定性的、配置驱动接入，作为 AI 接入的人工基线；不得用 AI 掩盖连接器、映射和语义注册表尚未完成的问题。
5. M2-AIW 分两段实施：先完成“AI 源画像与映射助手”，再完成“受控数仓蓝图、工作流编译、sandbox、审批、发布与回滚”。
6. B 泳道并行补 DIM/ADS/质量/指标；C 泳道并行补页面、AI 分析、决策和安全，但热点文件仍由总控串行控制。
7. 每个合并批次只跑一次 T2；跨集群里程碑跑一次 T3；AI 日常评测只跑 20–100 条固定集。
8. 完成证据复核队列后，重新执行 269 条核查，生成新的冻结快照。
9. 项目功能和证据完成后，才统一撰写论文和答辩材料。

具体任务、状态、依赖和证据要求全部以配套进度看板为执行入口。

## 12. 2026-09-11 M1-12 后的执行裁决

### 12.1 已确认的当前事实

- `M1-1/2/6/7/8/10` 已完成。
- `M1-12` 已以 E2+E3 证明代码闭环：checkpoint 键由 `LocalFileIngestor.checkpointKey/canonical` 唯一铸造，状态计数与当前 events 目录同量纲；真实读数由 101 归一为 51，且无新数据时没有发生重读。
- 数据库内 50 行含 `\.\` 的历史 checkpoint 副本仍保留。它们当前不会放大读数或继续更新，清除属于不可逆数据变更；不把它作为 P1–P5 的前置条件，也不授权开发 Agent 擅自删除。
- `M1-5` 契约目录处于 REVIEW；`M1-11` 本地提交器进程态处于 REVIEW；`M1-4` 的 S1、S2、S3a、S3b 已完成，页面与 MALL_API 收尾未完成。
- 平台 8091、参考商城 8090、生成器 8092 已有三服务独立运行证据；当前最新真实链路为 run 39、快照 `S20260901_39`。

### 12.2 下一主线选择

下一主线选择 **A：分析平台商城无关化 P1–P5**。商城功能与美化不进入当前关键路径；C 类互不依赖的小收尾可以并行，但不得占用公共契约、数据库迁移号、`PipelineService` 或 P1–P5 热点文件。

理由不是商城“不重要”，而是本项目的产品主体是分析工具。参考商城只需提供稳定、可审计的数据源；等平台能用异构源证明“可替换”后，再决定是否接入一个成熟开源商城。任何开源商城都通过 Adapter/Profile 接入，禁止为了适配它修改平台核心 SQL、事件常量或表结构。

### 12.3 P1–P5 五项默认决策

1. **P5 先用纯文件异构 fixture。** 它便宜、可复现、能验证字段/枚举/UUID/多余字段差异。接真实开源商城是 P5 后的加分项，不作为当前主线依赖。
2. **ODS v2 允许对演示数据重建。** 重建前必须备份对应 warehouse 目录、记录表数/行数/checksum，并由显式的 `INIT_SCHEMA` 审计步骤执行；不得静默 DROP，也不得影响其他 source namespace。
3. **新增 `source_registry`，不折叠进 `runtime_profile`。** source 表示长期业务身份；profile 表示可变化的运行参数，两者生命周期不同。
4. **P4 先做单源切换器。** 同屏跨源比较推迟到 P5 之后；第一版 API 未传 source 时返回当前激活源，并在响应中显式带 sourceCode。
5. **数据源管理不能永久只有 API。** P1/P2 可先交付 API 以解锁后端；P4 出口必须有最小管理员页面，至少支持列表、查看状态、测试连接、启停、选择当前源和查看画像校验结果。可视化拖拽字段映射后续再做。

### 12.4 契约严格度与 synthetic 标识裁决

- `canonical-event.v1.schema.json` 表示目标严格契约；当前旧夹具和旧校验器登记为 `mock-mall-legacy-v1` 兼容来源，不能把 26/55 校验通过伪写成契约已完全冻结。
- P1 不改变黄金指标；P2/P3 由新生成器重建符合严格契约的 golden-55，再逐步移除兼容规则。兼容规则必须按 source profile/version 生效，不能成为全局放宽。
- `synthetic=true` 放在 generation run、artifact manifest 和验收元数据中，不加入 8 字段事件信封。真实与模拟事件保持相同业务契约，避免下游根据 synthetic 分叉业务逻辑。

### 12.5 `newFileCount` 口径裁决

接受现实现的业务口径：“当前目录中至少含一条可采集完整记录的文件数”，而不是“最近新创建的文件数”。文档和页面统一显示为“可采集文件数”。API 为兼容可暂时保留 `newFileCount`，但需在 DTO/OpenAPI 标记 deprecated，并新增语义准确的 `consumableFileCount`；过渡期两者同值，前端只读新字段。

### 12.6 P1–P5 的入口与出口

P1–P5 的文件级步骤、迁移结构、算法、失败态和测试门见 `docs/superpowers/plans/2026-09-11-mall-agnostic-platform-implementation.md`。每个阶段必须独立提交、可回滚、在看板更新后才允许进入下一阶段。

## 13. V2.2 新增裁决：AI 辅助多商城数仓接入

### 13.1 采用方案

采用“受控 AI 数仓设计器”，不采用“让大模型直接建库、任意生成并执行 SQL”。第一阶段只做源画像、字段/枚举映射建议、置信度和待确认问题；第二阶段再做 ODS/DIM/DWD/DWS/ADS 蓝图、受控 DAG、sandbox、审批、不可变发布和回滚。

### 13.2 与商城无关化主线的关系

- P1–P3 是硬前置：必须先有 source registry、每源 namespace、ODS 保真、代理键和语义注册表，AI 才有稳定目标契约。
- P4 提供按源指标和数据源管理 API/页面，可承载建模向导入口。
- P5 先完成异构源 B 的人工配置接入，形成可比较基线。
- AIW 不阻塞 P1-01，不修改参考商城和生成器边界；参考商城、开源商城和文件夹具都只是可替换数据源。

### 13.3 完成定义

1. 源 B 在不修改平台核心 Java/Scala/SQL 模板的条件下，由 AI 产生结构化草案。
2. 草案经过确定性规则校验；所有低置信度和业务语义问题可在页面逐项确认。
3. 编译器只接受白名单节点和转换，输出物包含 blueprint/version/checksum/lineage。
4. sandbox 小链完成对账、质量和安全检查；失败不能影响 ACTIVE 数仓/指标快照。
5. 未审批不能部署；已部署版本可回滚；所有调用、修改、审批和发布有当前用户审计。
6. 人工基线与 AI 辅助组使用相同源 B、相同数据、相同机器和相同验收脚本，输出真实时间、修正次数、成功率和费用，不预填结论。

详细接口、表结构、校验器、任务编号和验收方法见本版链接的 AI 专项设计与实施书。
