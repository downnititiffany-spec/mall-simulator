# 项目设计文档 V2.4

> 当前版本：V2.4  
> 上一版本：V2.3（《项目设计文档 V2.3》）  
> 更新时间：2026-09-14（Asia/Shanghai）  
> 文档定位：记录系统应当如何实现的正式设计（架构、模块、数据库、数仓、数据流、接口、Spark、AI、部署、测试设计）。**动态进度不再由本文件维护**，见状态文件。  
> 范围依据：[毕业设计指导书 V2.7](../毕业设计指导书%20V2.7.md)。范围由指导书决定，本文件不能擅自缩减或增加必交内容。  
> 状态文件：[PROJECT_STATUS.md](../PROJECT_STATUS.md)（当前阶段、已完成／进行中／待实现、阻塞、已知缺陷、测试与验收状态、关键证据、工作记录）。  
> 权限：本文件与指导书**仅总控可写**；代码 Agent 只读，禁止修改、覆盖或新建下一版本。代码实现与本设计冲突时不得改本文件“适配代码”，必须报告总控裁决。  
> 本版只做治理调整：剥离“当前项目状态”职责，保留稳定实现基线；不改变技术方案与既有验收事实。

## 1. 使用约定

### 1.1 三文件治理与写入权限

项目采用三文件治理：指导书（项目决策权威）、本文件（正式设计权威）、`docs/PROJECT_STATUS.md`（实际开发状态报告）。三者职责互不重叠：

| 文件 | 唯一职责 | 写入权限 |
|---|---|---|
| 毕业设计指导书 V2.7 | 目标、范围、阶段规划、技术选择、验收原则、项目级裁决、重大架构决策 | **仅总控**；代码 Agent 只读 |
| 本文件（项目设计文档 V2.4） | 系统架构、模块、数据库、数仓、数据流、接口、Spark、AI、部署、测试设计与正式实现方案 | **仅总控**；代码 Agent 只读 |
| `docs/PROJECT_STATUS.md` | 当前代码 commit、当前阶段、已完成／进行中／待实现、当前阻塞、已知缺陷、测试与验收状态、关键证据、工作记录、建议、待总控裁决 | **代码 Agent 可持续修改**（不设版本号，历史由 Git 提供） |

旧契约说明、看板、专项计划、日志、论文稿和 README 只作参考，内容不因本文生成而被修改。正式设计变化必须由总控生成本文件下一个线性版本；日常进度、Bug、测试与验收状态只写 PROJECT_STATUS，不再升本文件版本。代码实现与本设计冲突时，不得修改本文件“适配代码”，必须报告总控裁决。旧版本只读，禁止追加更正使读者反复寻找“最后一句”。

本文件中：

- **现有实现**：代码或迁移存在；不自动代表部署和验收。
- **历史限定通过**：有特定环境/输入/时点的运行证据。
- **目标/待实现**：开发规格，不是已存在的类、表或API承诺。
- **待裁决**：不能可靠确定最终业务选择，见指导书G-01～G-07。
- **未取证**：不能据此判通过或不存在。

字段和算法在本文给出设计约束，代码落点供实现者导航。现有机器制品的字段/枚举与本文有差异时，先冻结迁移设计和兼容性，不能无声改旧数据。证据ID、任务ID继续保留用于追踪，但不要求继续修改旧专项文档。

### 1.2 文档导航

| 章节 | 内容 |
|---|---|
| 2 | 实现基线（阶段、正式架构基线、正式问题 K 编号定义）；动态状态见 PROJECT_STATUS.md |
| 3–5 | 总体架构、模块、环境、部署和服务 |
| 6–8 | 三程序、生成器、来源映射、Flume与Landing |
| 9–12 | 数仓、Spark、指标、质量与发布 |
| 13–15 | AI、受控建仓、决策与员工页面 |
| 16–18 | API、安全、测试与恢复 |
| 19 | 小任务队列、依赖和Agent交付规则 |
| 20 | 资料归并、冲突、覆盖与一致性 |
| 末尾 | 版本变更记录 |

## 2. 实现基线

本文件只维护**稳定的实现基线**；动态状态（每轮测试结果、临时阻塞、Bug 进展、工作日志、Agent 待办、每次 commit 状态）自 V2.4 起由 [`docs/PROJECT_STATUS.md`](../PROJECT_STATUS.md) 维护，本文件不再重复登记。PROJECT_STATUS.md 由代码 Agent 按指导书 V2.7 建立；建立之前，本版所剥离内容的最后已知口径以《项目设计文档 V2.3》§2 原文为准（历史只读，不修改）。

### 2.1 当前实现阶段

不是从零设计，也不是全项目完成。已有历史真实Spark链及指标发布、三程序拆分、源登记和页面；目前在修复“有实现未接线、有数据但证据口径不一致、测试默认依赖正式库、规格已改但旧文档未同步”的问题。

当前过渡验证路线为宿主应用连接WSL隔离MySQL3307。WSL软件安装已有成果，但完整HDFS/HMS/Spark互通、Linux源码主目录切换尚未完成。远程集群历史成果保留，不能替代当前改动的回归。

### 2.2 正式架构基线

| 项 | 基线 | 详见 |
|---|---|---|
| 三程序 | analytics-server + web（平台）／mall-simulator + mall-frontend（参考商城）／synthetic-data-generator（独立生成器），分树、分进程、独立配置与失败 | §6–8 |
| 平台模块 | analytics-server 聚合 6 个子模块；仓库无根 POM，各模块独立构建目标为自身 `pom.xml` | §3–5 |
| 数据库 | 平台 `analytics_meta` / `analytics_metric`；商城 `mall_simulator`；生成器 `generator_meta`；Hive 按源 namespace 分层 | §9 |
| 数仓分层 | Landing → ODS → DIM+DWD → DWS → ADS（按源级逻辑前缀命名）→ 发布制品 | §9–12 |
| 端口 | 平台 8091、商城 8090、生成器 8092；开发前端 5173/5174；隔离 MySQL 3307；宿主 3306 为正式库，禁写且非默认测试目标 | §5、§17 |
| 技术栈 | Java17/Boot3.2.5、Spark3.5.1/Scala2.12、MySQL8、Hadoop3.3.4、Hive3.1.3 | §5 |
| 文档基线 | 指导书 **V2.7**；状态文件 `docs/PROJECT_STATUS.md` | 指导书 §1、§12 |

### 2.3 正式问题编号（K 编号）

`K-xx` 为正式设计/治理问题编号，只能由总控在指导书或本文件中定义；代码 Agent 在开发过程中发现的实现问题使用 `DEV-xxx`，不得占用 K 编号（指导书 V2.7 §12）。下表是本文件对 K 编号**问题定义与下一动作**的唯一来源；每项的**当前状态**（进行中/已闭合/证据链接）由 `docs/PROJECT_STATUS.md` 维护，不在本文件逐轮改写。

| ID | 问题 | 下一动作/禁止事项 |
|---|---|---|
| K-01 | 隔离准备脚本声明PowerShell只读自动变量Host，参数绑定失败；某调用方式退出0但未执行 | 改任务专用变量名、测试真实入口和退出码；本轮仅记规格不修码 |
| K-02 | 商城/生成器数据库型测试仍以默认Test被选中，禁用时硬拒导致默认套件大量ERROR | 区分默认纯测试与显式IT；默认不启动外部服务，显式缺配置硬拒；不靠skip后全绿 |
| K-03 | GeneratorContractParityTest仍读取source_system.const，而当前Schema刻意无const，产生NPE | 按可替换来源契约修断言；不得重新写死mock-mall来修测试 |
| K-04 | run-demo清理分支参数化后仍可能触及正式outbox；凭据兜底在部分脚本有残留 | 一律禁止在正式库运行清理；按实例指纹和runId根收口，不以手工Confirm代替隔离 |
| K-05 | smoke脚本数据库失败后把ErrorRecord转long，错误原因与脚本缺陷混淆 | 非零数据库退出立即结构化失败，不继续聚合数字 |
| K-06 | SINGLE_NODE仅LOCAL被isLocal识别，默认master可能走yarn | 正交manager/submitter配置；缺省local[1]；无YARN时禁止构造yarn命令 |
| K-07 | F88版本化已有源码不等于生效；V19/V20首启自动迁移可能触正式库 | 所有DataSource启动前隔离校验；正式库启动/DDL另行确认 |
| K-08 | Spark“111通过”与后续“退出0但Tests run0”证据冲突 | 读取实际ScalaTest报告、套件/用例数及时间，未澄清前不引用全绿 |
| K-09 | P5历史92/180与预期不符；没有完整确定性映射执行器 | 先冻结14条映射规则，再分a/b/c交付 |
| K-10 | 现有快照唯一键只按profile；source动态绑定可能使旧快照归属含混 | 对同profile换源构造反例；G-03未决前不声称多源历史隔离 |
| K-11 | Hive DDL逻辑名与MySQL镜像_m被旧审计混作同表 | 明确引擎/namespace/实体名三元组，不能全局把Hive表也改成_m |
| K-12 | 未提交文件仍在变化，局部编译绿不代表当前全仓绿 | 每次引用附时点、commit和dirty文件指纹；不抢共享构建槽 |
| K-13 | 旧B-13失数据警报已撤回；不同schema同名表结构不同 | 不进行“重建基准”或恢复数据；全部表级结论带schema |
| K-14 | WSL非交互shell不继承.bashrc预期环境，Windows工具可能被PATH透传 | 每个入口显式JDK/SPARK/HADOOP路径，不以which命中/mnt/d视为Linux安装 |

K-01～K-05主要依据09-14 13:56–14:02的L5独立报告，本轮静态核对来源路径。修复后应交新证据，由总控决定是否升版本；不能在未复测时直接删掉问题。历史验收报告中出现的临时编号 K-10～K-13（指 MySQL 8 隔离门禁、隔离指纹配置、IT 入口缺失、迁移覆盖缺口）**不是本表编号**，其勘误映射见指导书 V2.7 §12。

### 2.4 动态状态的迁移去向

自 V2.4 起，以下内容不再由本文件维护，改由 `docs/PROJECT_STATUS.md` 承担；本版剥离时的原文完整保留在《项目设计文档 V2.3》§2（历史只读，不修改）：

| 原 V2.3 栏目 | 内容性质 | 现归属 |
|---|---|---|
| §2.2 已完成或历史限定通过、§2.2.1 隔离实链证据 | 完成判定与证据 | PROJECT_STATUS「已完成／关键证据」 |
| §2.3 进行中、§2.4 待实现 | 进度 | PROJECT_STATUS「正在进行／待实现」 |
| §2.6 当前可运行功能与在线状态 | 运行期状态 | PROJECT_STATUS「当前可运行功能」 |
| §2.7 本版本代码/设计变更 | 每轮变更 | PROJECT_STATUS「最近工作记录」 |
| §2.8 下一步计划 | 动态计划 | PROJECT_STATUS「下一步建议」；任务细目仍见本文件 §19 |

唯一例外：**§2.3 的 K 编号定义留在本文件**（编号所有权），其状态归 PROJECT_STATUS。

## 3. 总体架构和代码结构

### 3.1 分层

```text
业务生产面：商城 <-HTTP- 独立生成器
交换边界：业务日志 / 标准事件 / manifest（版本化）
数据面：Landing -> ODS -> DIM+DWD -> DWS -> ADS -> 发布制品
控制面：SourceRegistry + RuntimeProfile + PipelineService + 质量/发布/恢复
服务面：MySQL MetricStore -> REST -> 分析Vue页面、AI、决策
```

控制面不代算业务指标；指标算法在Spark/版本化指标层，不使用Java“假计算器”替代真实ADS。页面和AI不直读商城业务库。

### 3.2 平台六个模块

| Maven模块 | 现有职责/代码锚点 | 输入→输出 | 待补重点 |
|---|---|---|---|
| platform-common | 公共DTO/错误码、WarehouseNamespace、MetricStore契约、质量规则公共类型 | 类型/契约→共享约束 | 跨模块同规则一致，不引入商城业务类 |
| connection-ingestion | RuntimeProfile、SourceRegistry、LocalFileIngestor、LandingStorage | 源配置/文件→批次/manifest | 通用Connector、确定性Mapper、Flume编排 |
| warehouse-pipeline | PipelineService、JobSubmitter、JobCommandBuilder、质量门 | 批次/档案→异步Spark阶段 | 配置冻结、恢复、SINGLE_NODE、质量版本 |
| metric-analysis | MySqlMetricStore、MetricPublisher、专题分析 | 发布制品→指标快照/页面DTO | 完整ADS、作用域、原子发布、窗口聚合 |
| ai-decision | TextToSqlService、EvidenceBuilder/ExplanationService、DecisionService | 同快照证据→解释/草稿/评价 | 真模型、模板、评价正样本、AIW |
| platform-app | Controller、鉴权、装配、Flyway、静态前端 | HTTP→应用服务 | 数据源/运行中心页面与安全启动 |

analytics-server父POM不算第七业务模块。spark-jobs是独立Scala构建；web为分析前端；mall-frontend为商城前端；synthetic-data-generator含自身API/静态管理入口。增加模块要说明必要性，禁止只建空目录当作交付。

### 3.3 依赖纪律

平台不得依赖mall-simulator或generator的实现jar；生成器不得依赖MallBusinessService。交换协议用JSON/HTTP，不跨库查询。公共契约不含某商城数据库实体。每个客户端有连接/请求超时、有限重试、幂等与错误映射。

## 4. 技术栈和运行版本

| 内容 | 当前代码/安装依据 | 使用约束 |
|---|---|---|
| Java后端 | Java17、SpringBoot3.2.5、MyBatisPlus3.5.7 | 不新增Python服务作为首版依赖 |
| Spark计算 | Spark3.5.1，POM Scala2.12.19 | 发行包Scala补丁差异记录；旧JDK8编译证据需按jar确认 |
| Linux侧JVM | JDK17 17.0.12；JDK8 1.8.0_351安装记录 | 后端与Hadoop/Hive/Spark显式选择，不继承错误JAVA_HOME |
| Hadoop/Hive | 3.3.4/3.1.3安装记录 | 安装≠Metastore互通；无默认format命令 |
| MySQL | 隔离8.0.41，Connector-J8.0.33安装记录 | 3307隔离；宿主3306保留，不清理迁移 |
| 前端 | Vue3/Vite5/ECharts5，package锁文件 | Node/pnpm的WSL安装未完成；不复用Windows node_modules |
| SQL安全 | JSqlParser4.9现有实现 | 解析失败拒绝，不回退“仅EXPLAIN即可执行” |
| 采集 | LocalFile现有；Flume待完整集成 | Flume版本安装验证后冻结，不宣称当前已运行 |
| 调度 | 应用异步执行器+阶段状态 | 可按需定时，首版不依赖DolphinScheduler |
| 模型 | Java HTTP可替换Provider | 模型名/endpoint/密钥引用分离；真实调用未完整验收 |

不再统计“用了多少软件所以创新性足够”或编造技术工作量百分比。Hadoop、Spark、Hive分别承担存储、计算、数仓元数据；AI只增强而不替代工程底座。

## 5. 环境、部署、端口和服务

### 5.1 环境矩阵

| 环境 | 当前作用 | 数据/提交方式 | 不能宣称 |
|---|---|---|---|
| Windows LOCAL | 现有开发与历史小链，过渡应用执行 | 本地Spark/文件及旧本地Metastore；可连WSL3307 | 完整HDFS集群验收 |
| WSL2开发 | 已批准主开发方向、Linux依赖/隔离实例 | ext4源码/数据；4CPU/8GB/4GBswap | 已完成源码迁移或多节点容错 |
| WSL单节点数据兼容验证 | 可按需用1NN/1DN、HMS、local[1]小样本 | HDFS replication=1；不要求YARN/SSH | 正式集群吞吐或物理高可用 |
| REMOTE_CLUSTER | VMware或以后其他服务器/云端 | HDFS+Hive+Spark，SSH/YARN等显式配置 | 绑定node01–03才可工作 |

完整WSL数据链是否首版强制验收见指导书G-01。不得再次在WSL模拟三台虚拟节点。

### 5.2 目录和迁移

批准的Linux源码候选为/home/asus/projects/GraduationProject，数据在/data/hdfs、/data/metastore、/data/spark-scratch；隔离MySQL数据已有/data/mysql-isolated/data记录。先验证真实用户/权限和既有目录，不能覆盖。

迁移顺序：读取Windows主树及每个worktree状态→逐文件备份和hash→确认未提交/未跟踪/未推送独有内容→建立独立Linux副本→安装Linux构建依赖→小测试→用户确认后切唯一主目录。Windows绝对路径的.git worktree关联不能当作Linux可用Git关系；不得盲目move原项目、删除旧树或拷贝target/node_modules充当跨系统构建。

### 5.3 端口表

| 服务 | 端口 | 状态与边界 |
|---|---:|---|
| 分析平台SpringBoot | 8091 | application.yml现有值；构建后可承载分析静态页面 |
| 参考商城SpringBoot | 8090 | 独立业务服务/商城静态页面 |
| 生成器 | 8092 | 独立造数API与管理入口 |
| 分析Vite开发服务 | 5173 | proxy→8091 |
| 商城Vite开发服务 | 5174 | proxy→8090 |
| 宿主MySQL | 3306 | 正式/历史数据实例；测试默认禁止写 |
| WSL隔离MySQL | 3307 | 已交付安装/连通记录；使用server_uuid鉴别 |
| Hive Metastore | 9083候选 | 待实际配置/连通锁定，不等于Hive JDBC |
| HiveServer2 | 10000候选 | 仅需要Hive JDBC路径时按需启动 |
| HDFS RPC/UI | 由core-site/hdfs-site明确读取 | 8020/9000不能混用猜测，预检记录真实值 |
| Spark UI | 运行时分配，常见4040起 | 非长期必需端口，不能作为作业成功判据 |
| SSH | 不为WSL本地链启用 | ssh.socket/service有停用记录；远程档案另配 |

所有本地服务绑定127.0.0.1或经确认的WSL可达范围，不因便利开放公网。启动前确认端口不被其他项目占用；变更端口必须在下一设计版本同步前端代理、客户端和测试。

### 5.4 RuntimeProfile具体约束

现有快照字段含id、sourceId、version、type、landingUri、hiveJdbcUrl、sparkMaster、deployMode、yarnQueue、sshHost/Port/User、sparkSubmitPath、sparkJobJarUri、metricStoreType、credentialRef、timezone。

目标补齐正交维度：platformPlacement、landingStoreType、warehouseUri、hiveMetastoreUri、sparkSubmitterType、clusterManager、JDK/config目录引用、jarSHA256、每类凭据引用。hiveMetastoreUri=thrift元数据服务，不是hiveJdbcUrl。不能用type标签猜所有行为。SINGLE_NODE缺省local[1]，不自动yarn；远程master/deployMode/队列按manager验证。

一次run冻结：sourceId、sourceCode、source profile版本/hash、warehousePrefix解析值、runtime版本、jar指纹、输入manifest、指标/规则版本。现有快照只冻结sourceId而可能再次查可变prefix，须补一致性验证，不能只靠注释宣称完全冻结。

### 5.5 启动和停止

顺序：验证隔离档案与真实实例指纹→启动3307/必要HMS→预检只读/写入账号范围→隔离Flyway→平台/商城/生成器按需启动→前端→小数据链。集群不可达不影响已有MySQL快照读取。停止生产者先停止生成器，再等待商城Outbox可追踪落地；停止流水线应记录取消或恢复信息，不杀进程后写SUCCESS。

本轮不安装、启动、停机或改配置。后续按用户已有项目范围授权执行，正式数据变更和宿主全局网络等需单独确认。

## 6. 独立商城与数据生成器

### 6.1 商城业务模型

| 实体 | 最小字段 | 规则 |
|---|---|---|
| 商品/分类 | productId、categoryId、name、price、cost、stock、status、version | 价格用decimal；cost缺失时不算利润；库存更新做乐观锁/事务 |
| 用户 | userId、registeredAt、region、memberLevel | 只生成模拟标识和非敏感画像，不需要真实电话/身份证 |
| 订单头 | orderId、userId、status、orderAmount、paidAmount、createdAt、paidAt | 状态按中立契约流转；取消不能再正常支付 |
| 订单行 | orderId、productId、quantity、unitPrice、lineDiscount | 数量正整数；金额精确计算 |
| 支付/退款 | paymentId/refundId、orderId、amount、status、eventTime | 模拟支付；退款成功才冲减，累计不超过实付 |
| event_outbox | eventId、eventType、aggregateId、payload、createdAt、publishStatus | 与业务变更同事务写入，发布至少一次，下游幂等 |

商城负责“按自身规则完成一笔交易”；平台只消费其提供的数据。不能要求每个外部商城改成参考商城数据库结构。公开商城API失败、库存不足、重复请求应给业务码，由MallTargetAdapter翻译生成器结果，平台不复制商城错误码全集。

### 6.2 生成器五类元数据

现有generator_meta中五表已有记录，不得再写“生成器元数据表未建”。目标列级一致性仍需检查。

| 表/结构 | 核心内容 | 约束 |
|---|---|---|
| generator_target | 目标URL、adapterCode、credentialRef、能力、状态 | 不存平台DB连接；目标停用时不发请求 |
| generation_plan | planId、version、mode、seed、timeWindow、rate、用户/商品/场景参数 | 启动冻结plan版本；不可用能力提前拒绝 |
| generation_run | runId、planVersion、status、startedAt、counts、failure | STOPPING需停止新派发并结算在途 |
| generation_artifact | runId、URI、checksum、recordCount、contractVersion | 文件模式制品可重放；与采集manifest不同 |
| generation_event_stat | runId、eventType、accepted/rejected/count等 | API成功数、事件数、订单数不同量纲，不能相互代替 |

目标接口职责：MallTargetAdapter负责discover/probe、catalog读取及用户/购物车/下单/支付/退款操作；PlanCompiler验证场景和能力；ScenarioRegistry提供版本化场景；GenerationRunService调度；FileModeGenerationEngine生成文件。现有具体签名沿实现制品校验，不因本章补充语义而擅改调用方。

每次请求使用稳定业务幂等键（runId+virtualUser+logicalActionIndex），只重试可重试错误；超时结果不明时先查订单状态或使用商城幂等API，不能当作“未下单”重复扣库存。外部商城不具备某能力则报告UNSUPPORTED，不伪造成功。

### 6.3 商品与用户分布算法

- 商品目录演示目标可用8–12个一级分类、每类5–15个二级分类、500–5000商品；不是每次测试必须造这么多。黄金测试只保留覆盖分类/价格/库存边界的小集合。
- 分类价格用截断对数正态分布，在配置的min/max内采样；金额转BigDecimal并按约定保留两位。成本可按价格乘区间系数生成，但不得据此声称真实毛利。
- 商品受欢迎程度使用Zipf权重，指数候选0.8–1.2，配置版本固定。分类偏好、价格敏感度、时段活跃权重共同决定选择概率。
- 行为采用有状态转移：浏览→收藏/加购→下单→支付→可选退款；不把事件类型独立随机抽取导致支付先于订单。
- 根seed拆成用户/商品/场景/时间子流，使线程数变化不会改变文件模式的逻辑样本；固定序列化和排序后才可声称字节复现。
- MALL_API受服务器分配ID、并发、真实时钟和库存影响；同seed不保证返回字节相同。应固定初始状态与逻辑时钟，并比较规范化事件/经营汇总，记录非确定性来源。

### 6.4 场景、执行和UI

保留normal、promotion、weekend_growth、sales_decline、refund_rise、price_increase、hot_product、stock_shortage、new_product_cold_start、old_user_churn、new_user_growth等现有场景方向。每个场景记录作用窗口、受影响商品/人群、概率或价格参数变化、限速和expectedEffect。expectedEffect只供独立验收oracle，不发送给AI作为“答案”。

生成器页面：目标管理→计划参数/能力检查→场景预览→启动/停止→成功失败/事件统计→制品下载。数据清理需独立隔离检查，不和“启动计划”绑定。文件模式可加入缺失、非法枚举、重复、乱序、迟到、金额错误和恶意文本；MALL_API模式不得绕过商城业务规则硬写脏订单。

### 6.5 独立性验收

商城关闭时生成器报告目标不可用，不写商城数据库；平台仍读旧指标。生成器关闭时商城仍可人工下单。第二MallTargetAdapter至少验证协议/字段差异；第二分析来源夹具不是第二商城HTTP适配器，两者不能互相替代。

## 7. 来源登记和确定性字段映射

### 7.1 身份唯一所有者

source_registry为源登记：id、source_code、display_name、ingest_mode、profile_path、timezone、currency、status、profile_version、warehouse_prefix及时间字段。source_code创建后不改；当前FILE实现，JDBC/HTTP预留且不显示可用。

读层兼容DRAFT/ACTIVE/PAUSED/DISABLED，现有写层DRAFT/ACTIVE/PAUSED；当前选择来源由激活runtime_profile.source_id表达，不新增另一套is_current。warehouse_prefix归源登记，非空且合法；runtime_profile旧prefix列只断读不默认删除。源画像只管语义，不存密钥。

外部event/order/user/product/refund标识按原始字符串保存，不能去掉U/P前缀、前导零或强转Long。去重和代理键作用域包括sourceId；HASH64使用已冻结的统一契约和测试向量，不另造hash实现，null必须定型，碰撞检测与反查策略需保留。

### 7.2 映射版本与存储载体

现有源画像是analytics-server/source-profiles中的版本文件。历史资料同时规划schema_mapping/source_profile等表，但不得既写文件又写DB形成两个owner。首版继续文件为不可变画像内容，DB登记引用与版本；未来确需数据库编辑发布时由新设计版本给迁移、唯一内容owner及回滚。本文中的MappingProfile等是逻辑结构，不代表已经建表。

MappingProfile最小字段：profileVersion、sourceCode、contractVersion、checksum、eventTypeMappings、按事件类型分组的fieldMappings、enumSemantics、timePolicy、amountPolicy、required策略。运行manifest记录版本+完整checksum。新语法先冻结再实现；不得就地改变旧profile语义。

### 7.3 映射执行器14条规则

1. **契约先行**：先把当前事件Markdown与JSON Schema差异收敛为明确语义。现有canonical-event.v1是机器投影，source_system非固定mock-mall。扩展字段可先做兼容可选项，是否升主版本由破坏性判断，不只为改文档升级。
2. **映射方向**：sourcePath→targetPath；按eventType分组，显式区分envelope与payload。同组两个来源写同一目标时profile无效；不同事件组可复用字段名。
3. **保留扩展**：@keep按原始叶子名称保留扩展，撞canonical字段或其他扩展则拒绝。原始完整JSON另存，不能把“保留部分扩展”当成原文保真。
4. **时间**：timePolicy.field指源字段，先按有序formats逐个严格匹配，第一个成功格式生效；epoch秒/毫秒必须声明。使用IANA时区，DST跳空/重叠拒绝并待确认。保留原时间文本和规范化值。
5. **缺失**：必填缺键/null/trim空白均EMPTY_FIELD，detail区分MISSING/NULL/BLANK。可选缺失可省略并计数；显式null只在nullable允许，否则按已声明OMIT策略计数；不隐式补0。
6. **错误码**：保留EMPTY_FIELD、DUPLICATE_EVENT、BAD_ENUM、BAD_AMOUNT、FUTURE_TIME、BAD_TIME_FORMAT、UNSUPPORTED_SCHEMA_VERSION；新增PROFILE_INVALID、PROFILE_VERSION、JSON_PARSE_ERROR、UNKNOWN_EVENT_TYPE、TYPE_MISMATCH、ENUM_UNRESOLVED、TIME_AMBIGUOUS_LOCAL。别名仅兼容翻译，不能重写历史错误行。
7. **金额**：从JSON数字原词或字符串解析BigDecimal。声明FEN/YUAN，目标元字段FEN÷100；分应为整数；两位金额使用精确舍入规则，不接受隐式double和任意倍率。负值按字段契约拒绝。
8. **原始/标准化隔离**：raw记录全部输入；canonical只保存通过转换的数据，单独checksum。eventId来自稳定业务标识，不用随机UUID或整行hash冒充原事件ID。缺稳定ID应隔离并问源方。
9. **路径和数组**：只允许对象路径和固定数组下标，不支持任意递归、通配执行或脚本。ITEM_MAP保持元素顺序和数量；任一必填订单项失败则整事件隔离，禁止只留下成功几项改变订单金额。
10. **dry-run**：最多100条受控脱敏样本，sampleRef只能引用平台批准的位置，不任意URL/本机路径。输出转换预览、行数、覆盖率、原因和版本；不写正式数据/checkpoint/激活指针。
11. **三步交付**：a纯映射/预览；b采集/双manifest/重放；c两源数仓/指标。不得一口气重写所有层后再验证。
12. **枚举**：声明null表示观察到但语义未决，保留warning；关键字段未决禁止激活。可选nullable可按契约保留null并警告。完全没登记的值为BAD_ENUM，不用“未知=成功”吞掉。
13. **统计**：requiredCoverage=成功必填位置/总必填位置；enumCoverage按观察到的字段路径+原始枚举去重计算有效映射比例；分母0为null。reasonCounts按违例计可大于隔离行数；系统异常不冒充坏数据。
14. **身份与发布**：预览与激活比对profile内容hash；运行冻结同一版本。先修真实测试入口再用其验收，不删旧manifests来制造绿色测试。

### 7.4 映射接口目标

| API（待实施/冻结） | 输入 | 输出/副作用 |
|---|---|---|
| POST /api/v1/sources/{sourceId}/mappings/dry-run | profile候选版本、sampleRef、limit≤100 | reportId、统计、转换预览、错误；无正式写入 |
| GET /api/v1/sources/{sourceId}/mappings/dry-runs/{reportId} | 授权sourceId/reportId | 同版本预览与对账 |
| POST /api/v1/sources/{sourceId}/mappings/{version}/activate | checksum、已通过预览引用、幂等键 | 激活已验证不可变版本，留审计 |

上述路径为目标，不是已有API声明。开工先对现有SourceRegistryController/OpenAPI做重名与参数核对；输入无效400，版本变化冲突409，权限不足403。不能让开发Agent自己发明另一套端点。

## 8. 采集、Flume和Landing

### 8.1 数据流

正常业务路径：生成器HTTP→商城事务+Outbox→滚动日志→完成文件→Flume或LocalFile采集→原始Landing→版本映射→canonical/隔离→manifest→Spark。人工下单同一路径。文件夹具从独立受控导入口进入，标明fixture来源。

Flume不“监听任意商城网页”；必须有受支持日志、导出、消息或同步来源。接开源商城先确认可读数据边界和许可证/脱敏，再做适配。

### 8.2 Flume首版链路

首个完整集成目标采用Spooling Directory Source + File Channel + HDFS Sink：

1. 生产者写临时文件；写完flush/close后原子改名或移动到spool，文件名全局不复用。
2. File Channel的checkpoint/data目录分离并持久化，异常重启可恢复。
3. Sink写source级Landing原始区，按采集日期/小时组织。未提供event_type header时不能使用它拼路径。
4. Sink in-use临时文件不参与manifest；只枚举已完成文件。
5. Flume至少一次传输，平台必须去重、记录文件offset/hash和批次计数；不承诺端到端exactly-once。
6. Windows商城→WSL spool需要明确文件传输/交接方式，不能默认Linux能无代价直接读任何Windows目录。

现有LOCAL残行等待和Taildir式断点语义继续适用其既有实现；不得把Flume目标Spooldir与运行中的文件尾读混写成同一种配置。Taildir可作为后续单独连接器。每个源至少显示接收时间、积压、已采断点、隔离数、最新批次和错误，不探测生产者就不能断言“商城在线”。

### 8.3 连接器与存储接口

| 抽象 | 输入/返回 | 实现注意 |
|---|---|---|
| SourceConnector（目标） | source+冻结配置→probe/发现文件/采集批次 | FILE/Flume先做；DataX/HTTP等未实现明确拒绝 |
| LandingStorage（已有） | namespace、exists/list/open/stat、writeManifest、healthCheck | local/HDFS职责一致，不传hdfs URI给java.nio.Files |
| HdfsLandingStorage（已有） | Hadoop FileSystem API | 不是待新建类；需验证依赖打包、路径越界、客户端关闭、写清单覆盖策略和实例权限 |
| Mapper（目标） | raw+mapping→canonical或quarantine | 纯转换与持久化分离，供dry-run与真实采集共用 |
| BatchCompiler（目标接线） | 完成文件+统计→immutable manifest | 原文/标准化分别引用hash，完成后提交checkpoint |

HDFS客户端依赖若provided，Boot运行时需真实提供对应jar；仅设置环境变量不能保证Java classpath已加载。HDFS探针应只访问获授权根而不是要求列整个“/”。

### 8.4 目录、批次与checkpoint

目标逻辑目录：source/raw/ingestDate/batch；source/canonical/mappingVersion/batch；source/quarantine/batch；source/manifests/batch.json。实际前缀来自档案，source目录名必须经过安全编码和白名单。输出使用临时文件+完成标识，已有批次号冲突但hash不同应报错，不能覆盖。

manifest包含batchId、sourceId/sourceCode、contractVersion、profileVersion/hash、raw/canonical URI与各自checksum、时间窗口、总/接收/隔离/重复数、文件长度和offset、创建者/时间。文件生产制品清单与平台采集清单独立；新可选字段兼容旧ingestion-manifest.v1，必填/行为变化需显式升级。

LocalFileIngestor.checkpointKey/canonical是路径规范化唯一owner；读取历史键也用同规则。checkpointFiles表示当前events目录已建断点的文件数，pendingFiles为同次扫描目录文件总数；newFileCount现定义为仍有可采完整行的文件数，不是刚出现的文件数。残行等换行后再消费。库中历史“\\.\\”副本不在本轮删除范围。

无新数据返回noNewData=true和零新增记录，但这不证明上游持续在线。成功提交水位采用左闭右开窗口；失败不推进，重试同批次幂等。并发采集需要source级租约或唯一批次约束，避免两worker读同一offset各发布一份。

## 9. 数仓设计

### 9.1 层次、命名和保真

Landing不是Hive层；ODS保留来源与追溯，DWD清洗明细，DWS聚合主题，ADS应用指标，DIM独立提供一致维度。实际库名由source_registry.warehouse_prefix派生prefix_ods/prefix_dim/prefix_dwd/prefix_dws/prefix_ads。默认旧源常见dw_*只是实例，不是所有商城的固定库名。

ODS必须能追踪到原始字节及转换后事件。raw_json应说明保存的是源原文还是canonical文本；两个概念不可混用。保留source/event/ingest时间、schema/mapping版本、batch、文件路径和offset，支持重新映射和补算。

### 9.2 层内逻辑表和主要字段

| 层/逻辑表 | 粒度/字段 | 算法和约束 |
|---|---|---|
| ODS ods_behavior_event | 事件；source_id、event_id、event_type、user/product原ID、event_time、ingest_time、payload/raw引用 | 只做可解析/契约分流；原文不丢 |
| ODS ods_trade_event | 订单/支付/退款事件；订单/支付/退款ID、金额、状态、items | 金额原词保留；版本支持可追溯 |
| ODS ods_user_event | 用户变更/注册事件 | 原始ID不转数字 |
| ODS ods_product_event | 商品/价格/库存事件 | 缺cost不伪造利润 |
| DIM dim_user/dim_product | source+业务键；surrogate、属性、版本/生效时间 | 确定性最新状态；SCD2只有明确需要才启用 |
| DIM dim_date/dim_region/dim_metric | 日期、地区、指标编码 | 日期区间幂等；地区未知成员；指标字典同步，不只建空表 |
| DWD dwd_user_behavior_detail | 有效行为事件、源身份、代理键、行为类型、时间/日期 | source+eventId去重，合法枚举、稳定排序 |
| DWD dwd_order_detail | source+orderId、user_key、状态、实付、累计退款、有效标志、时间 | 顺序重放/退款最新状态、订单项金额核对 |
| DWD dwd_reject_record | source/batch/event、reasonCode/detail、原文引用、规则版本 | 系统错误与业务坏数据分开 |
| DWS dws_user_behavior_day | source+user+dt，PV/收藏/加购/活跃 | 日行为聚合 |
| DWS dws_behavior_funnel_day | source+dt；分类/渠道粒度待G-04 | 去重用户集合，不min截断 |
| DWS dws_product_behavior_day | source+product+dt，pv/fav/cart/buy | 区分支付用户和支付件数 |
| DWS dws_product_sale_day | source+product+dt，金额/件数/退款 | 订单行分摊口径冻结 |
| DWS dws_trade_day | source+dt，支付订单/金额/退款/用户 | 支付发生与归属口径一致 |
| DWS dws_user_trade_period | source+user+观察窗口，R/F/M/复购 | 窗口不重复累计 |
| DWS dws_region_sale_day | source+region+dt，sale/net/order/buyer | 未匹配地区保留unknown |
| ADS | 下节10个逻辑专题 | 每行带snapshot/定义版本/业务日期，发布可追溯 |

历史DDL声明ODS4/DWD3/DIM5/DWS7/ADS10，不代表29张都有正确数据。已知DIM仅user/product有日常产出、分类/地区ADS缺生产链，需逐项补证。目录schema与数据库真实表形状必须对照，不靠文件名推理。

### 9.3 ADS与MySQL镜像

| Hive逻辑ADS | MySQL目标镜像 | 当前边界 |
|---|---|---|
| ads_operation_overview | ads_operation_overview_m | 历史已发布 |
| ads_behavior_funnel | ads_behavior_funnel_m | 历史已发布，分类/渠道粒度缺口 |
| ads_active_trend | ads_active_trend_m | 历史已发布 |
| ads_hot_product | ads_hot_product_m | 历史已发布 |
| ads_product_conversion | ads_product_conversion_m | 历史已发布 |
| ads_sale_trend | ads_sale_trend_m | 历史已发布，net_sale等字段需补 |
| ads_user_profile | ads_user_profile_m | 历史已发布，RFM完整性需补 |
| ads_data_quality | ads_data_quality_m | 历史已发布，规则版本与实时结果待接齐 |
| ads_category_sale | 待发布规格冻结镜像 | 规划有DDL，不宣称已有实产 |
| ads_region_sale | 待发布规格冻结镜像 | 同上 |

以上是逻辑对应，不断言Hive物理表一定无后缀；Spark实际登记与Hive DESCRIBE需逐表验证。MySQL analytics_metric中8张_m是已证实体，不应因为HiveDDL文字不同而批量改名。表标识统一为engine+catalog/namespace+table。

### 9.4 交易和维度算法

订单事件按业务标识分组，以event_time、ingest_time、event_id确定稳定顺序；状态机非法跃迁隔离，不直接“取最后一行”绕过业务。去重同序键仍冲突时用冻结输入位置做稳定仲裁并记冲突，不依赖分区执行顺序。

订单行核对SUM(quantity×unit_price−line_discount)=order_amount，绝对误差<0.01。付款金额与订单金额另检；退款按source+refundId取最新成功状态再按订单累计，累计≤实付。不得重复重放累加退款两次。

代理键由统一HASH64契约输出BIGINT，空键显式CAST(NULL AS BIGINT)。插入采用列名/类型对齐并验证运行时schema，不能只改一个NULL表达式就认定根因已消除。旧user_key STRING→BIGINT事故表明SELECT顺序、表schema、实际jar指纹必须一起取证。

### 9.5 存储与增量

目标使用列式Parquet/受支持压缩、日分区dt=yyyyMMdd，ODS按需要小时分区；实际codec要从产物检查，不能仅引用历史README“全Snappy”。Spark local也能访问HDFS，文件存储和执行manager是两个配置轴。

迟到数据按事件归属日进入可追踪补算计划；水位成功后推进。重跑写run/snapshot专属临时分区，验证后发布，旧快照保留。分区裁剪、小维表受阈值广播、复用中间结果、必要的小文件合并都应先测后用，不能无条件cache或repartition(1)。

## 10. Spark任务、异步调度和恢复

### 10.1 实际作业注册

JobRegistry位于spark-jobs/src/main/scala/com/graduation/analytics/job/JobRegistry.scala。现有11个注册码，其中ljp是局部验证，不是正式十作业链的一环。

| code | 类 | 输入→输出 | 前置 |
|---|---|---|---|
| sci | LocalSchemaInitJobInstance | 配置/DDL→库表自举 | 预检；仅允许批准namespace |
| odl | EventOdsLoadJob | manifest/事件→4类ODS | 输入冻结 |
| bdw | BehaviorDwdJob | ODS→行为DWD | odl |
| dim | DimensionBuildJob | ODS→用户/商品DIM等 | odl |
| tdw | TradeDwdJob | 交易ODS+DIM→订单DWD | odl、dim |
| usw | UserProductDwsJob | 行为/订单DWD→DWS | bdw、tdw |
| fna | FunnelAdsJob | DWS→核心ADS staging | usw |
| dqc | AdsQualityJob | ADS/上下游→质量结果 | fna |
| pub | AdsPublishJob | 验证staging→Hive发布分区/指针 | dqc |
| mxp | MetricExportJob | 已发布Hive ADS→导出制品 | pub |
| ljp | LocalJsonParquetJob | JSON→Parquet局部实验 | 独立，不代表全链 |

注册表当前hasCycle=false是固定返回，扩展DAG时必须用Kahn拓扑排序或DFS检测真实环，不能沿用假定。合法DAG的拓扑排序遇同优先级节点使用固定次序，避免重跑顺序漂移。

### 10.2 平台8阶段与Spark任务映射

| 平台阶段 | 行为 | 完成证据 |
|---|---|---|
| WAIT_LANDING | 定位本run的已完成批次/manifest | 输入hash、记录数、源和版本 |
| INIT_SCHEMA | sci/受控schema校验 | 目标namespace、DDL版本、列型 |
| LOAD_ODS | odl | 各主题输入/输出/隔离 |
| BUILD_DWD | bdw、dim、tdw按依赖 | 键类型、状态/金额对账 |
| BUILD_DWS | usw | 主题聚合与去重计数 |
| BUILD_ADS | fna | staging表/快照、各表行数 |
| QUALITY_CHECK | dqc+版本化平台门禁 | 规则版本与失败详情 |
| PUBLISH_METRIC | pub→mxp→MySQL staging/验证/切ACTIVE | Hive制品hash、MySQL发布事务、旧指针处理 |

8阶段不是8个Spark作业，不能拿阶段数作执行数。具体命令由JobCommandBuilder和提交器生成，不向员工显示shell作为操作步骤。

### 10.3 JobSubmitter与运行数据

现有LocalProcess/Ssh提交器负责submit/poll/cancel/log引用，不计算业务。提交返回任务ID不等于完成；externalJobId是进程标识或集群应用ID，需记录kind/host/startedAt以防PID复用。

pipeline_run最少记录runId、idempotencyKey/requestHash、sourceId、runtimeProfileId/version、businessWindow、inputVersion、outputSnapshotId、status、attempt、timestamps。pipeline_stage_run记录stageCode、状态、开始结束、输入/输出引用、错误、重试。spark_job_run记录jobCode、externalJobId、artifactHash、exitCode、logsURI、计数和应用终态。

目标异步流程：校验请求/权限→事务创建run和幂等约束→返回taskId→有界执行器领取→按DAG提交→持续更新阶段→质量与发布→终态。不能在HTTP请求线程串行等待整条Spark链。应用重启后根据数据库运行状态和外部作业状态恢复，不能所有RUNNING直接改成功。

### 10.4 幂等、恢复和调度

- 同幂等键同请求返回原taskId；同键不同输入返回冲突，不覆盖原run。
- retry可跳过成功阶段的前提是输出仍存在、source/profile/输入/规则/代码指纹仍匹配；否则从首个失效阶段重算。
- 临时输出用run/attempt/snapshot隔离；重试不覆盖他人分区；清理只针对本run可证明所有的临时根。
- 网络瞬态可有限重试（默认上限2次目标）；schema、参数、权限、业务质量失败不能无条件重试。
- 取消要停止后续派发、请求cancel并等待外部状态；无法确认记UNKNOWN/需人工检查，不写CANCELLED成功。
- 定时器只触发同一应用服务；默认开发按需手动启动。启用小时/日任务时固定时区和左闭右开窗口，租约/唯一键避免重复调度。迟到补算单独任务，不直接改已发布快照。
- 运行中心展示最近反馈、阶段耗时、日志尾部和失败建议；长期无心跳标“待核实”，不自动删除现场。

## 11. 指标体系、算法和业务专题

### 11.1 统一指标结构

MetricDefinition：metricCode、名称、单位、公式、数据粒度、时间字段、过滤条件、分子/分母、zeroPolicy、direction、definitionVersion、lineage。MetricValue：source作用域、snapshotId、businessDate/窗口、dimensionKey、metricCode、decimalValue、definitionVersion、qualityStatus、warnings。

中文名/别名用于交互，不产生另一公式。v1/v2退款定义需显式版本；sale_amount等内部列名与gmv展示编码使用唯一映射。空分母为null，显示“无可计算数据”，不能0%或无穷。UV等distinct指标不能简单累加每日值计算整月UV。

### 11.2 核心公式

| 指标 | 计算约束 | 输出用途 |
|---|---|---|
| PV | 有效view事件数 | 流量 |
| UV | 观察窗口浏览用户按source+user去重 | 流量/转化分母 |
| DAU | 当日有效行为用户去重 | 活跃趋势 |
| 收藏/加购 | 对应行为事件数，用户转化时另算去重用户数 | 行为 |
| 支付订单数 | 有效支付订单按source+order去重 | 销售 |
| GMV | 有效支付订单paid_amount之和，不直接扣退款 | 销售规模 |
| 净销售 | 同口径支付金额−成功退款金额 | 真实收入方向，退款归属期需冻结 |
| 客单价 | GMV/支付订单数 | 订单价值 |
| refund_rate v2 | 有成功退款的支付订单数/支付订单数 | 退款风险；不是退款事件条数比 |
| full_refund_rate | 全额退款支付订单数/支付订单数 | 全额退款风险 |
| buy_rate/cart_rate | 支付/加购去重用户数÷浏览去重用户数 | 标注独立集合或严格cohort |
| repeat_rate | 有效购买≥2次用户/支付用户；完全退款不算有效复购订单 | 声明观察期和变体 |
| product_heat | 1·ln(1+PV)+2·ln(1+收藏)+3·ln(1+加购)+5·ln(1+支付件数) | 版本化业务权重，不称学习模型 |
| RFM | R距最后有效购买天数；F观察期有效订单数；M观察期金额 | M精确退款口径见G-05 |

黄金55条历史标准值：PV7、UV3、DAU3、支付订单5、GMV2042.00、净销售1493.00、客单价408.40、退款率0.6000、全退款率0.2000、购买转化1.0000。只针对该输入和对应定义版本，不可套到30条旧黄金或其他批次。

### 11.3 漏斗

首版当前存在日粒度宽松漏斗view/intent/order/pay，各步独立去重。后一步可能大于前一步（例如用户此前浏览，本期支付），该情况不是自动造假或质量失败；保留真实数值，标注“非严格同cohort”。禁止min截断。历史提案“各步必须单调”不能用于宽松模式验收。

严格顺序漏斗是明确另外一种定义：以同source+user+session或时间窗口为cohort，排序后找按顺序到达步骤，才可检验单调。分类/渠道粒度见G-04；未决不输出假的分类下钻。总用户不等于各分类distinct之和，不能以此对账。

### 11.4 RFM、留存和复购

R低更好，F/M高更好；五分位评分，按高/低组合得到8类。NTILE的同值加稳定源/用户ID排序保证可复现，但同值用户可能被拆分，必须展示评分规则；是否改为同值同分属于口径变化。记录R/F/M原值、score、segment、窗口、rule_version，不仅存标签。小样本明确“仅演示”。

留存用注册或首次行为cohort，D+N活跃人数/基期cohort人数；观察窗口尚未覆盖D+N时为数据不足，不判0。复购区分支付复购/有效复购，取消不计、全退是否剔除按当前有效口径。M不明时保留null+警告，不能把缺列当0。

KMeans增强实验可对R/F/M做log1p及标准化，固定seed、k候选和训练窗口，使用轮廓系数比较；不是首版分层算法必要依赖。

### 11.5 商品、分类、地区和库存

商品排行按热度/销量/金额并有稳定次序键。分类/地区金额求和必须包含unknown，不丢未匹配维度；分类数不必等于全部商品目录分类数（无销售分类可为0/不出现，页面规则明确）。地区、商品等distinct买家不能相加当总买家。

ABC可按累计金额比例分层（阈值作为版本配置，不嵌业务常量多处）；用于库存/商品优先级建议，不声称预测。库存覆盖=可用库存/近N天平均净售出件数；分母0返回不可计算；周转需要平均库存成本，无cost时不可计算。缺库存流就显示“缺少库存数据”，不可从订单数虚构库存。

### 11.6 异常与贡献

至少14个可比历史点；默认可采用滚动均值z-score（|z|≥2.5为候选）或经版本选择的中位数/MAD，不能同一规则无记录随机切算法。标准差/MAD为0用批准绝对阈值；缺窗口不生成有置信度的统计异常。IQR留作后续对照，不把旧7点方案同时视为当前默认。

异常结构：metricCode、sourceId、window、actual、baseline、deviation、threshold、algorithmVersion、snapshotId、evidenceRefs、limitations。维度贡献为当前−基准，同口径总变化为0时不算占比；正负变化抵消可使贡献占比超过100%，不强行截断。只说明关联和核查方向，不做未经实验的因果结论。

## 12. 数据库、质量门和指标发布

### 12.1 四个应用库及边界

| 逻辑schema | owner | 主要对象 |
|---|---|---|
| analytics_meta | 分析平台控制面 | runtime_profile、source_registry、采集/checkpoint、pipeline/stage/spark_job_run、质量、用户、AI/决策/审计 |
| analytics_metric | 指标服务 | metric_snapshot、metric_value、8张ads_*_m、Flyway历史 |
| mall_simulator | 商城 | 商品/分类/用户/订单/支付退款/Outbox |
| generator_meta | 独立生成器 | target/plan/run/artifact/event_stat五表 |

同名schema在3306和3307是不同实例。测试运行使用runId隔离命名，实际server_uuid+schema为身份，不能只看库名包含test。Hive Metastore的MySQL库独立于analytics_meta，名称由安装配置登记，不能混用。

### 12.2 平台关键对象

| 对象 | 字段/键/实现要求 |
|---|---|
| runtime_profile | 环境类型/连接、source_id、版本、激活状态、credential_ref；不再拥有warehouse_prefix |
| source_registry | §7.1字段；source_code唯一、prefix必填，激活前画像校验 |
| ingestion_batch/file_checkpoint | batch/source/版本/offset/hash/status；路径键唯一owner，不随扫描口径改变重复采集 |
| pipeline_run/stage/spark_job_run | §10.3；idempotency约束、externalJobId与日志关联 |
| data_quality_result | run/stage/rule、actual/threshold/passed；现有severity曾写有效档位（§2.2.1/E3）；目标分开声明/有效severity与版本/compatPolicy/fingerprint |
| quality_rule_definition | rule_code、version、source_scope、stage、severity、severity_mode、threshold_json、enabled、effective_from/to、checksum | 
| metric_definition | 指标编码/公式/粒度/版本；已知在元数据侧管理，不误写指标库已有此表 |
| metric_snapshot | id/snapshot_id、runtime_profile_id、business_time、status、version、definition_version、failure_reason、active_flag等 | 
| metric_value | snapshot/metricCode/dimensions/value；每快照键唯一，禁止跨快照混算 |
| ai_query_history/ai_call_log | 用户、source/snapshot、状态、模型/prompt、耗时/token/error、脱敏审计 |
| decision_task/evaluation | §14状态、证据引用、责任人、窗口、目标、结果和评价版本 |
| operation_audit_log | 主体/动作/目标/结果/trace/time；敏感正文hash而非密钥 |

表格为最小语义规格，不代表每列都已落库。现有V19/V20工作树迁移：V19建定义表并种子规则；V20给结果新增可空rule_version、effective_severity、compat_policy_version、rule_fingerprint，不回填历史猜测值。字段入库≠写侧接入：新run仍写null应被检测，不允许宣称版本闭合。不得修改历史已应用Flyway文件或在正式库试迁移。

### 12.3 质量12项

1. Landing JSON可解析率及隔离数量。
2. ODS event_id非空与schema版本支持。
3. 行为必填、字段类型与枚举合法。
4. 订单行金额公式误差<0.01。
5. 成功累计退款≤实付。
6. 状态转换合法。
7. DWD→DWS金额对账。
8. 同归属口径ADS GMV≥净销售≥0。
9. 同过滤条件UV≤PV。
10. 支付/浏览用户比及cohort解释：宽松口径异常不一概作为阻断规则。
11. 采集延迟P95和迟到率，明确未来时间/负延迟处理。
12. 发布表数、行数、必填和checksum一致性。

每条规则记录作用域、阈值、版本、阶段、实际值、passed、原始/生效严重度。付款vs订单、订单项公式、DWD/DWS对账三者独立，不能用一个AMOUNT_RECONCILE覆盖。

### 12.4 F88最终门禁语义

先冻结规则集→计算actual并判断threshold→得到有效severity→汇总门禁。ERROR/BLOCKING失败阻断，WARN/INFO持久化可见但不阻断。未知ruleCode不能照来路WARN放行；应拒绝发布并标未登记。

原始重复事件只有确定性去重已证且重复率不超过批准阈值时可观察。EVENT_ID_UNIQUE历史dupRateMax=0.0005不能为了黄金测试通过而改大。超阈值升阻断，测试fixture需正/负分开；如果业务要改变阈值先正式裁决。

历史兼容限定source/rule/version或明确批次范围，并带compatPolicyVersion。不改变历史原severity，不无条件读时重算为最新等级；历史缺版本记录UNKNOWN/需解释，不能默认为WARN/PASS。历史staging存在不等于当前混快照，后者必须阻断。

### 12.5 发布顺序与恢复

```text
Spark ADS暂存 -> 数据质量通过 -> Hive不可变发布制品
-> mxp导出+manifest/checksum -> MySQL BUILDING/staging
-> 表形/行数/金额/定义版本/内容验证
-> 单数据库事务：旧ACTIVE归档，新快照ACTIVE
-> API/页面/AI固定读取新snapshotId
```

Hive和MySQL没有宣称分布式原子事务。MySQL ACTIVE是在线发布权威；Hive已经准备好但导入失败时维持旧MySQL指针，记录待重试/对账制品。MySQL不能先切ACTIVE再导入，避免空快照暴露。

现有analytics_metric.metric_snapshot的uk_active_profile(runtime_profile_id,active_flag)保证每profile最多一个active_flag=1；非ACTIVE为NULL允许历史多行。首次没有快照合法，不能把“无ACTIVE”一概当损坏；成功链必须能建立一个。G-03未决前，此键不自动证明source隔离；读取归档需真实源归属，不能按当前profile推断历史源。

目标故障验收：导出不完整、checksum错、DB中断、质量阻断、发布并发分别触发后，新ACTIVE不存在，旧ACTIVE值/hash保持，失败run可定位。归档显式读取按权限审计；指定不存在snapshot不偷偷回最新。

### 12.6 MetricStore

MySqlMetricStore默认；Hive降级仅管理员显式，固定source/date/definition/snapshot，使用只读账号；DorisMetricStore第二阶段；ClickHouse接口声明NOT_IMPLEMENTED。HDFS不能直接替代MySQL JDBC接口，需要Hive/Spark查询或发布适配。

Store能力描述包括supportsSnapshot、supportsDimensions、maxRows、queryTimeout、availability，不仅一个枚举。管理员切换先健康/权限/同值预检，新Store缺数据不能静默展示空0。AI与固定页面从同一语义目录、快照和权限范围读取。

## 13. AI智能分析和受控建仓

### 13.1 横向能力而非孤立聊天页

AI分别辅助来源理解、字段映射、指标问数、异常解释、报告草稿和决策建议。确定性系统负责画像统计、指标数值、权限和执行。LLM不能自己算金额后替换MetricStore结果，不能把预测猜想写成事实。

LlmProvider输入为用例、已脱敏上下文、结构化Schema、promptVersion和预算；返回结果显式携带providerUsed/model/token/耗时/错误。当前通过“文本是否变化”推断providerUsed的历史方式不可靠，需改为真实调用链记录。Mock、template、llm三种证据分开，模型超时后模板成功不记真实模型成功。

### 13.2 EvidencePackage

最小结构：evidenceId、templateVersion、sourceId、snapshotId、definitionVersion、generatedAt、period、comparisonPeriod、facts、comparisons、dimensions、anomalies、dataQuality、lineage、warnings。

每条事实有metricCode、value、unit、evidenceRef；比较由程序计算delta/deltaRate；不存在基期为null+NO_COMPARISON_PERIOD。缺分类/地区表为空数组+UNKNOWN_DIMENSION_TABLE，不让LLM猜比例。证据历史复现需要保存可回读的快照与模板版本及证据hash；现有只存evidenceId不能自动证明历史可重建，补可验证持久化/引用设计，不在不同存储中维护两份可变事实。

固定六段模板：发生什么、上期比较、维度贡献、质量可信度、核查/行动、限制。LLM只改写解释。输出数值守卫对照证据引用，发现新编数字拒绝该输出并回模板。AI反馈记录有用/无用/原因、用户与证据版本，用于质量评测，不让用户评价直接改变指标公式。

### 13.3 Text-to-SQL实际首版边界

当前R8更严格的规则优先，不能恢复旧设计“3表/CTE/1000行”的宽松上限：

- 语义目录仅包含已发布MySQL ADS及真实列，当前5张登记，不代表8张全部可问；扩充前验schema和权限。
- 单SELECT、单表；禁止JOIN、UNION、子查询、CTE、DDL/DML、SELECT *；递归校验列与函数白名单。
- 强制授权source作用域、允许snapshot和日期范围；MySQL ADS日期字符串为yyyyMMdd，不能输出ISO字面量静默0行。
- 扫描最多90天，LIMIT上限200，查询超时30秒；成本默认预估50万行阈值，EXPLAIN失败/空计划/不可解析也拒绝。
- 已有函数名单SUM/AVG/COUNT/MIN/MAX/ROUND/ABS/COALESCE/IFNULL/FLOOR/CEIL/DATE/CAST，增加函数需验证。
- 问句筛与SQL AST是两道独立防线；危险问句不能被回退层偷偷变成正常SELECT后记“攻击测试通过”。
- 只读DataSource+数据库SELECT权限+setReadOnly+timeout/maxRows同时生效，审计拒绝动作。
- 最多一次受控修复，修复后重新走全校验；不能反复修到绕过规则。

用户问的是不支持关联的复杂问题时应明确能力限制，可由固定专题API回答；不为提高AI通过率放开安全限制。

### 13.4 模型治理

凭据只通过credentialRef/受保护配置注入，文档不写真密钥。限制请求字节、输出token、单次/每日预算、并发、重试和熔断。requestFingerprint包含source/snapshot/问题/模型/promptVersion；provider不支持幂等时，超时可能已计费，记录不确定性，不承诺traceId可防所有重复扣费。

样本列名、注释和内容都是不可信数据；先PII脱敏后再发模型，提示中明确其不是指令。不能给模型SSH、数据库写账号或任意本地目录。真实模型试验需正样本、拒绝样本、超时和非法JSON，不是只检查Provider类存在。

### 13.5 AI建仓流程和状态

目标流程：采样/元数据→确定性画像与脱敏→SourceProfileDraft→人工审核关键语义→WarehouseBlueprint/WorkflowDraft→Schema/命名/键/类型/AST/DAG/预算校验→受控编译→sandbox→证据审核→部署不可变Release→必要时回滚。

状态：DISCOVERING→DRAFT→NEEDS_CONFIRMATION→VALIDATING→SANDBOX_RUNNING→REVIEW→APPROVED→DEPLOYED；失败/旁路VALIDATION_FAILED、SANDBOX_FAILED、REJECTED、ROLLED_BACK。审批绑定checksum和目标namespace，修改草案使旧审批失效。普通员工可理解向导和提出草案，data_dev/admin才能完成技术审核/部署权限动作；权限细分在落地时校验，不让操作者伪造reviewer。

核心对象：SourceProfileDraft、WarehouseBlueprint、TableBlueprint、ColumnMapping、QualityRuleDraft、MetricDraft、WorkflowNodeDraft。固定用户/商品/行为/订单/支付/退款骨架；源扩展字段另区，AI不自由创造新的GMV定义。

### 13.6 AI建仓七表（全部目标，不声明已落库）

| 表 | 核心字段/约束 |
|---|---|
| ai_modeling_session | session_no、source_id、source_profile_version、status、provider/model/prompt、input_fingerprint、created_by、started/finished/error |
| source_profile_draft | session_id、version、draft_json、confidence_summary、questions_json、status、checksum；版本只增 |
| mapping_review_item | draft_id、source_path、target_field、inferred_type、confidence、rationale、status、reviewer/comment |
| warehouse_blueprint | blueprint_no/version、canonical_model_version、source_id、blueprint_json、status/checksum、approved_by/at |
| workflow_definition | blueprint_id、workflow_code/version、nodes_json、edges_json、status/checksum |
| modeling_validation_result | blueprint_id、check_code、severity、status、message、evidence_json |
| workflow_release | release_no、target_namespace、artifact_uri/checksum、status、deployed_by/at、previous_release_id、rollback_at |

迁移编号由集成人分配，既有V19/V20不能被AIW复用。SourceProfileDraft与普通source-profile是审核草案和已发布版本的关系，不再产生另一个可变语义owner。

九种节点白名单：DISCOVER_SOURCE、LANDING_VALIDATE、ODS_LOAD、DIM_BUILD、DWD_BUILD、DWS_AGG、ADS_BUILD、QUALITY_CHECK、PUBLISH_METRIC。PUBLISH必须依赖QUALITY；禁止任意shell节点，AI编译器使用模板+参数化标识符+二次AST，不执行LLM自由SQL。

### 13.7 AIW-001～020内置任务细目

全部为TODO/未完整实现。每项开工在本文件下一版本登记负责人、允许文件、输入版本、证据与下次反馈；不再修改旧专项计划维持第三份状态。

| ID | 交付物与实现要点 | 最小验收 |
|---|---|---|
| AIW-001 | 五类结构Schema；additionalProperties=false、大小/枚举上限、九节点 | 合法/缺字段/危险节点/额外字段，Schema指纹 |
| AIW-002 | 七表迁移与repository、版本追加、乐观锁、审批checksum | 隔离库迁移/唯一键/旧审批失效 |
| AIW-003 | SourceMetadataSampler，FILE/CSV/OpenAPI/只读metadata能力声明；受控采样 | 空源、引号、超时、越界、相同seed样本hash |
| AIW-004 | SourceProfiler/TypeInferencer/PII，空值/唯一率/类型和键候选 | 无损类型顺序、decimal/timezone、脱敏正负样本 |
| AIW-005 | A参考/B异构/C歧义恶意三源20–100条truth | 固定checksum；expected问题/映射/拒绝 |
| AIW-006 | Provider/Prompt/StructuredParser，MODEL_OUTPUT_INVALID失败关闭 | 合法/截断/拒答/注入输出，不执行代码块 |
| AIW-007 | 画像统计+AI建议合成草案；再生成version+1 | 输入变化新指纹、幂等、模型失败人工入口 |
| AIW-008 | 低置信度问题队列；金额/身份/退款/时区强制审核 | 问题稳定去重排序，答案变更旧审批失效 |
| AIW-009 | 审核API与最小映射页，状态与reviewer来自会话 | 403、并发冲突、关键项未决409 |
| AIW-010 | 固定canonical模型生成分层蓝图、grain/keys/lineage | A/B可用，C缺语义必须问，禁止跨源表 |
| AIW-011 | BlueprintValidator，Kahn/DFS、类型/命名/资源/PII校验 | 每错误码正负例，稳定问题顺序 |
| AIW-012 | 受控DDL/SQL编译制品、参数化namespace、hash | 同蓝图字节一致，危险标识/模板注入拒绝 |
| AIW-013 | 固定DAG编译，超时/重试/资源/幂等参数 | 缺质量、环、孤儿、重复输出、失败恢复 |
| AIW-014 | 独立sandbox namespace/临时根，异步执行 | 成功/取消/重试/失败，旧ACTIVE不变 |
| AIW-015 | ModelingEvidencePackage：层数/金额/隔离/规则/资源/版本 | 故意缺证据与金额差不准通过 |
| AIW-016 | 审核与部署RBAC/审计 | 越权/伪造用户/过期checksum/重复审批 |
| AIW-017 | 不可变Release、previous指针、受控回滚 | 中断不切指针、重复部署、无previous；不删旧namespace |
| AIW-018 | 七步向导：源、画像、映射、蓝图、校验、sandbox、release | 刷新恢复、键盘焦点、长字段/分页、角色差异 |
| AIW-019 | 全链攻击和故障集 | prompt注入、PII、任意SQL/shell、路径越界、预算爆炸、旧快照保全 |
| AIW-020 | 同源同机同版本人工/P5与AI辅助A/B | 人工操作时长、修正数、P/R、编译/成功率、token/费用，不编造提升 |

画像唯一率=distinct(nonNull)/nonNullCount；候选关系依据类型兼容和包含率，不能因字段同名断言外键。模型confidence不是已校准概率，关键语义再高分也需人工确认。sandbox名称必须通过namespace合法性规则，不机械拼接超长draftId突破24字符前缀限制。

## 14. 决策、报告和效果评价

### 14.1 五个模板

| 模板 | 输入证据 | 建议行动/限制 |
|---|---|---|
| 流量下降 | PV/UV/DAU趋势、渠道差异、质量与采集延迟 | 先核数据时效再核活动/入口；不能直接归因广告 |
| 漏斗流失 | 同口径view/intent/order/pay及基期 | 核购物车/下单/支付体验；宽松漏斗标非同cohort |
| 商品转化 | 商品浏览/支付、价格/退款、分类基准 | 内容/定价/人群核查；不是自动调价 |
| 库存风险 | 当前库存、净销量、覆盖天数、退款 | 补货/限售建议；没有库存输入就明确不足 |
| 用户召回 | RFM、活跃/复购、允许触达人群 | 运营召回计划；不直接发送营销消息 |

DecisionDraft字段：templateCode/version、source、evidenceId/snapshotId/definitionVersion、发现、量化依据、适用前提、action、owner、targetMetric、direction、targetChange、baselineWindow、observeWindow、dueDate、risk。AI来源强制DRAFT，source=ai并保存模型/prompt版本；人工source=human。

### 14.2 状态和权限

DRAFT→PENDING_REVIEW→APPROVED→IN_PROGRESS→COMPLETED→EVALUATING→EFFECTIVE/PARTIAL/INEFFECTIVE/INSUFFICIENT_DATA。拒审REJECTED，取消CANCELLED，需理由。当前保留PENDING_REVIEW，不另改PENDING_APPROVAL制造旧数据迁移。

登录主体决定创建人、审批人、执行人，不接受请求体伪造身份。提交时核完整行动/负责人/目标/窗口/证据；不齐拒绝。非法流转也留审计，不能只有成功日志。审批不等于已经执行商业动作。

### 14.3 效果算法

目标为等长、完整、同来源、同口径的前后窗口聚合，不是只取两个端点快照。加和型指标按相同周期汇总或周期均值；比例型指标先汇总分子分母再相除，不能平均每天的比率。baseline=0时相对改善不可计算，显示绝对差并按批准目标规则评价。

改善方向=(actual−baseline)/abs(baseline)，越低越好取反；达到目标EFFECTIVE、达到部分阈值PARTIAL、无改善INEFFECTIVE；缺日/缺样本/缺口径/数据异常为INSUFFICIENT_DATA。阈值随模板版本，不凭模型自由下结论。至少构造四类可达样本，使用不同真实窗口并独立重算oracle。前后对比只说明变化，不声称行动造成变化。

### 14.4 业务报告

经营日报/周报、订单专题、商品异常、用户运营、决策效果六类模板是产品报告，不等同毕业论文。数字/图表由程序生成；AI只写摘要、建议和限制。reportInstance保存source/snapshot/definition/template/prompt版本、创建者、状态、artifact引用与checksum。未实现的模板标待实现，不用一张摘要页宣称六类齐全。

## 15. 前端设计和普通员工体验

| 页面/功能区 | 数据结构与交互 | 边界/状态 |
|---|---|---|
| 经营概览 | 作用域筛选、核心指标卡、趋势、更新时间/快照、质量提示 | 切源重新解析权限/快照，不保留旧源图表误导 |
| 销售分析 | sale/net/退款/订单/客单价、分类地区下钻 | 缺维度明确空态，区间distinct需正确聚合 |
| 行为/漏斗 | 每步人数、定义、转化率、比较期 | 非同cohort说明，粒度未实现不显示可用筛选 |
| 商品分析 | 热度/销量/转化/退款、稳定排行、分页 | 成本库存缺失就不算利润/覆盖 |
| 用户/RFM | R/F/M原值与score、8群体、观察期、复购 | 小样本仅演示；M缺失null |
| AI分析区 | 当前证据摘要、模板/真实模型标签、引用、限制、反馈 | 普通员工默认折叠SQL，高级权限才看 |
| 决策中心 | 草稿、审批、责任人、截止日、窗口、结果 | 不能从DRAFT直接执行 |
| 运行/质量中心 | taskId、阶段、日志引用、计数、规则阈值/版本/失败 | 技术细节只给授权角色；普通员工看更新时间 |
| 数据源/环境 | 登记、映射预览、激活、连接预检、版本 | 当前完整页面待实现；先API不等于面向员工完成 |
| AI建仓向导 | §13七步 | 独立出口未实现 |

前端web/src/views按功能组织，统一API客户端处理401/403、traceId、取消过时请求。组件接ViewModel，不拼接Hive字段或在前端重算业务指标。每页有加载/正常/无数据/失败四态，表格分页/排序、键盘焦点和图表可读说明；禁只隐藏菜单而无路由鉴权。

数据源停止后不展示“实时更新正常”；显示最后成功数据时间和积压，不需要员工认识NameNode。错误对业务用户显示简明原因和可做动作，技术用户附source/run/stage/rule/trace，不泄露命令、凭据、堆栈。

## 16. 接口设计和错误约定

### 16.1 共通协议

根路径/api/v1；响应code/message/data/traceId；分页page/size/sort；写操作幂等请求头按实际能力明确。长任务返回taskId与可查询状态，不能保持HTTP直到Spark结束。时间API用ISO8601带时区，内部ADS字符串dt按yyyyMMdd转换；金额精确字符串或严格decimal，不用JS浮点做汇总。

既有接口不为文档统一而强行改名。新增目标接口先审OpenAPI/Controller差异，兼容旧客户端，列迁移/废弃窗口。

### 16.2 现有平台接口族（源码/历史API资料导航，非本轮HTTP复测）

| 方法/路径 | 输入/结果要点 |
|---|---|
| POST auth/login、auth/logout；GET auth/me | 平台独立登录会话、token/当前角色；不列可用示例口令 |
| GET dashboards/overview | snapshot/businessTime/updatedAt/definition/quality/data/warnings |
| GET analysis/sales、products、funnel、users、rfm | 日期/TopN/snapshot筛选，返回专题ViewModel |
| GET metrics/overview、snapshots、health、quality | 同快照指标、历史、健康、质量明细 |
| POST ingestion/runs；GET ingestion/status、batches | 批次/无新数据标志、文件/断点统计 |
| POST pipeline-runs；GET pipeline-runs及/{id} | 创建、列表、阶段明细；Idempotency-Key |
| POST pipeline-runs/{id}/retry | 失败重试，验证输入/输出冻结 |
| POST admin/pipeline-runs/{id}/resume、mark-failed、retry-from-stage | 受控恢复和审计，不能任意成功化 |
| GET admin/pipeline-runs/recovery-report | 待恢复现场和处理建议 |
| runtime-profiles相关GET/POST/PUT及test/activate/disable | 环境配置/预检/版本，真实详细路径以Controller绑定校核 |
| GET sources、sources/{id}；POST sources及/{id}/test、activate、pause；PUT sources/{id} | 已有SourceRegistryController；current归属由返回视图/激活档案表达，不虚构/current端点；映射dry-run为新增目标 |
| POST ai/explanations、ai/analyses、ai/queries | 证据解释/问数；返回query.status与provider |
| GET ai/history/my、ai/audit/history、ai/audit/calls | 个人历史与管理员审计分别授权 |
| decisions创建/列表及/{id}/submit、approve、reject、start、complete、cancel、evaluate、evaluations | §14状态机，主体来自登录 |
| admin/users列表/新增及toggle/reset-password | user:manage，参数错误不伪装500 |
| GET health | 存活，不证明数据链可用 |

旧缺口/spark-jobs/{id}/logs、/ingestion/runs/{id}、/metrics/snapshots/active、/ai/queries/{id}应按“用户是否能获取所需信息”核对已有等价端点，再决定新增，不因旧目录出现名字就重复建API。API清单冻结任务负责每个公开端点的完整字段/权限/副作用，与本设计下一版本同步。

### 16.3 其他两个程序

商城8090：auth、mall/users、mall/products及详情、mall/cart/items、mall/orders及pay/cancel/refunds、mall/refunds/{id}/complete、mall/outbox/status/publish、admin/products/price/stock/status、商城admin/users。平台不挂载这些业务端点。

生成器8092：目标配置/探针、场景列表、计划创建/查看、run创建/状态/停止、制品和统计；现有generator-api.v1制品字段需与实现对账。商城上的generator路由已退役，不再在商城内恢复。

### 16.4 新接口对象与错误

AIW目标会话创建→草案生成→review-items→validate→sandbox→approve/reject→deploy/rollback；输入源/草案版本/hash，返回taskId、结构化问题和状态。未经批准不能执行deploy；与普通AI问数接口分开权限。

常见错误：401 UNAUTHORIZED、403 FORBIDDEN_PERMISSION、400 PARAM_INVALID、409版本/幂等冲突、415 UNSUPPORTED_MEDIA_TYPE；AI拒绝可能HTTP200但query.status=REJECTED，不能把HTTP200当执行通过。流水线业务失败必须看run/stage状态和errorCode；retryable只表示在条件变化后可重试，不自动无穷重试。

SQL_PARSE_ERROR/SQL_COST_TOO_HIGH/SQL_DATE_OUT_OF_SCOPE/SQL_QUESTION_UNSAFE为拒绝；UNKNOWN_SNAPSHOT/UNKNOWN_DIMENSION_TABLE/RFM_AMOUNT_UNAVAILABLE为明确数据不足；schema/映射错误见§7；质量失败保旧快照。所有新错误码统一owner，不复制到Java/Scala/页面多处无校验常量。

## 17. 安全、权限、数据留存和异常边界

### 17.1 身份与角色

四角色admin/data_dev/analyst/operator。现有RolePermissions中operator有pipeline:run/ops:log:view/decision:approve等权限，不能在文档声称其当前完全无技术操作。目标把启动和重试权限分开、技术细节按职责收敛；改变既有角色权限需先列迁移矩阵再实施。data_dev角色存在不等于已有可登录测试账号。

Bearer token由服务端校验，不能接受X-User-Id伪造。商城mall_token与平台analytics_token分开，跨端口不共享身份。所有来源/快照/决策/review对象有授权检查，不能只在菜单隐藏入口。AI继承调用用户权限，不获得额外管理员身份。

### 17.2 隔离测试门禁

1. 默认纯测试不连外部服务；真实写IT显式开关启用。缺配置在建立连接前拒绝，不猜3306或root。
2. 对连接串解析host/port/database，验证允许实例/端口/真实server_uuid，拒绝同名正式库与错误代理目标。hostname相同不能区分Windows/WSL。
3. 连接后只读检查SELECT DATABASE、server_uuid、port及授权；确认全部DataSource（元数据、发布、读取、商城、生成器、Metastore）均按计划隔离。
4. 账号权限最小，日常测试不用root。初始化隔离实例需要DDL管理时必须独立bootstrap身份，不沿用业务账号；root不能因“隔离”而进入应用默认配置。
5. runId拥有数据库记录、HDFS根、namespace、checkpoint、导出/staging根；cleanup先列精确目标，只清本run资源，不按可复用runtimeProfileId批量删所有快照。
6. 不仅库名后缀test；3306实例上新建test库也不能绕过现有实例隔离纪律。
7. 跑前/后记录行数、ACTIVE、关键表内容指纹和必要binlog证据。行数不变不是绝对零写证明；曾经回滚或等量替换需内容/日志辅证。
8. V19/V20等Flyway只在批准隔离库验证；首启正式应用可能自动DDL，必须视为数据库结构变更，不能当“仅启动服务”。

### 17.3 路径/命令/秘密

相对路径须resolve后在授权根内，拒绝..逃逸、UNC/任意URL、符号链接逃逸；HDFS同样校验scheme/authority/namespace。Spark参数白名单拒绝未知项，使用参数数组而非拼接shell，防source名称或jar路径注入。

secret只存credentialRef，日志脱敏password/token/Authorization/key。不能输出账户默认密码作交付教程。角色、接口、网络三层防护；WSL mirrored不表示自动安全，服务只绑定必要接口。默认不开公网、自启、SSH；远程访问单独配置。

### 17.4 保留与清理

历史设计的保留候选为商城日志7天、Landing30天、ODS180天、DWD/DWS/ADS365天、MySQL快照90天、审计180天。它们是待资源和重放需求确认的策略参数，不是本轮已启用的自动删除计划。

清理前排除当前ACTIVE、决策/报告引用、在途run和待恢复批次；生成影响清单，验证备份、namespace与权限，再按授权删除本任务资源。不得宽泛递归删除工作区、HDFS根、旧worktree；不能因为历史checkpoint副本“无害”就顺手删。清理失败不影响当前读，审计必须留存。

### 17.5 异常设计

外部连接错→结构化错误和有限重试；配置/Schema/权限错→停止并指明字段；质量失败→失败保旧；模型不可用→template；源停止→最后更新时间和积压；进程状态不明→UNKNOWN并请求恢复检查；数据库访问失败→不将错误对象强转指标0/long；审计敏感写入失败→拒绝该动作。

每次失败保存stage/rule/trace/源/版本和脱敏日志引用。技术详情不直接暴露给普通员工。根因必须以反例验证，像NULL定型必要但不足的修复只能记录“类型卫生改善”，不能算已定位原故障。

## 18. 测试、验收与开发提速

### 18.1 分级测试

| 等级 | 内容 | 规模与触发 |
|---|---|---|
| T0 | 静态/编译/Schema/纯函数/快速单测 | 每小变更；不启动数据库/Spark |
| T1 | 改动模块单测、契约、Mock Provider、前端组件 | 每任务；20–100样本，不全仓无差别重跑 |
| T2/E3 | 隔离真实数据库+Spark最小链和关键失败恢复 | 模块合并/口径修改/发布前；55条黄金 |
| 里程碑链 | 跨源/Flume/真实模型/页面用户旅程 | 约1000条足够覆盖；一个重型任务槽 |
| 性能专项 | 10万/百万等分档、资源/吞吐/延迟比较 | 只有性能相关任务或里程碑；不每日重放 |
| E5 | 员工页面、权限、空态、同快照数字、决策 | 真实隔离入口与截图/DOM，不能静态页面代替 |

E1/E2等旧证据编号保留作为来源标签，具体是否编译/测试/真链以报告声明，不能把不同文档同名E2混为一种强度。

缩规模不删金额、ID串源、重复、迟到、失败保旧、路径越界、SQL注入、角色越权等必要语义。恢复负例可用可控故障点避免每次杀真实大集群；里程碑再用真实外部失败验证。

### 18.2 测试执行要求

- Maven反应堆指定测试可能在上游无匹配时中断；需要准确的surefire配置，核实目标模块实际执行。正确相关键为surefire.failIfNoSpecifiedTests，不能只看命令退出。
- ScalaTest记录suite/test实际数，不以Maven摘要0或exit0单独结案。
- 外部测试使用显式IT分类：默认不运行不是“该IT PASS”；点名执行却缺安全开关必须失败。
- 测试用独立fixtures，不删除/修改landing内40–43等真实运行manifest让测试通过。
- 编译/target共享的两个Agent不得并跑clean/test/package。共享代码变更中间态不作为别人缺陷最终结论，需记录时点并通知构建消费者。
- 小改只重建受影响模块；缓存依赖和稳定jar，但Spark校验必须确认执行的确是新jar的SHA256和JDK版本。
- 页面改动先组件/接口Mock，合并后一次真实E5；不每改一个按钮就跑十个Spark作业。

### 18.3 关键测试矩阵

| 范围 | 正向 | 负向 |
|---|---|---|
| 采集 | 完整文件/追加完整行/重启续采/新源 | 半行/轮转/重复路径/坏JSON/目录不可达 |
| 映射 | 异构字段/精确金额/时区/数组 | 缺ID/未决枚举/类型损失/同目标冲突/恶意路径 |
| 交易 | 创建支付部分退全退/跨日支付 | 非法状态/超额退/订单项少一项/重复退款 |
| 数仓 | 同源同输入重跑/两源同ID | 键类型错/DDL列序错/跨namespace/缺DIM |
| 发布 | 完整制品→原子ACTIVE | checksum/列型/行数不符/DB中断/并发/混快照 |
| AI问数 | 白名单统计及结果oracle | 注入/多语句/越权/日期错/超成本/非法JSON |
| 决策 | 四评价结果/完整审批 | 假用户/跳状态/缺窗口/零基线/同一快照冒充前后 |
| AIW | A/B蓝图、人工审核、sandbox | 低置信度强激活/未知节点/循环/危险DDL/旧审批 |
| 部署 | local档案/隔离3307/新namespace | SINGLE_NODE默认yarn/错误UUID/全部DataSource未隔离 |

### 18.4 证据与反馈

最终报告至少包含：任务/需求ID、代码commit及dirty指纹、工具/JDK/数据库版本、输入与jar/config hash、命令/退出码、实际测试数、断言结果、source/namespace/snapshot、未测范围和下一步。禁止把锁着的写入中日志作为最终结案证据。论文数据仅取完成且可复跑的实验，不自动引用target中随时被覆盖的报告。

执行任务每20–30分钟给进度，重大失败立即报告：已完成什么、正在做哪一步、实际结果、是否阻塞、下一次反馈。一个任务完了立刻登记，不等待十几个小时后汇总。不再要求向旧巨型日志追加十几行“以此为准”；最终结论进入设计文档后继版。

性能记录必须同硬件、同输入、同算法版本、同缓存条件测量。减少运行次数时标“单次探针”，不能当稳定P95或显著提升。跨机器本地与集群耗时不直接相除称算法优化。

## 19. 内置执行队列和Agent工作包

### 19.1 状态规则

TODO前置未达；READY可以派发；IN_PROGRESS有实际执行；REVIEW已有产出待验；DONE_LIMITED限定验收；DONE完整判据与证据齐；BLOCKED写具体条件；DEFERRED后置。状态不继承旧勾选，旧任务ID保留。以下是取样状态，不表示Agent此刻仍在后台工作。

### 19.2 现行小任务

| ID | 任务/交付 | 状态 | 前置 | 最小完成判据 |
|---|---|---|---|---|
| V25-T01 | 库名字面量门禁 | REVIEW | 无 | 注释误命中消除；真实 SQL 硬编码负例仍红 |
| V25-T02 | manifest 冻结夹具 | REVIEW | 无 | 新旧清单分开校验；真实40–43.json不动 |
| V25-S01 | 写入型 IT 安全 | REVIEW | 无 | 正式库在写前被拒；受限账号；cleanup按runId；扫描其他*Test |
| V25-S02 | 全链测试隔离配置 | IN_PROGRESS | S01 | server/db/namespace/HDFS/checkpoint/快照全隔离，跑前后证据 |
| V25-S03 | IT/脚本同类写入面收口 | IN_PROGRESS | S01 | R-1/R-3/R-4/R-5/R-6 逐项收口：默认关闭、实例白名单、写前拒绝、无明文口令、skip 不等于 PASS；包括K-01～K-05，显式IT硬拒不代表默认套件可用 |
| V25-W01 | WSL与宿主只读盘点 | DONE_LIMITED | 无 | 版本、资源、发行版、磁盘、现有配置、待确认权限成表 |
| V25-W02 | 迁移前源码/worktree备份 | DONE_LIMITED | W01 | 独有改动与hash逐一登记，不丢未提交/未跟踪文件 |
| V25-W03 | WSL运行时与兼容矩阵 | DONE_LIMITED | W01/S02 | Java17/8、Spark/Hadoop/Metastore/MySQL版本锁定；不升级最新版 |
| V25-W04 | Linux源码副本构建 | TODO | W02/W03 | 独立Git关系、Linux依赖重装、T1；用户确认后唯一主目录 |
| V25-W05 | 正交档案与SINGLE_NODE修复 | READY | 无；写库验收等S02 | SINGLE_NODE缺省local[1]；Metastore与JDBC分开；配置矩阵拒冲突 |
| V25-Q01 | F-88版本化规则 | IN_PROGRESS | S01/S02 | 现有未提交代码须修版本边界/阈值；未知规则拒绝；完整规则版本记录；包含V19/V20的新jar和写侧字段必须重新隔离验收 |
| V25-Q02 | 质量失败保快照 | TODO | Q01/S02/W03 | BLOCKING/ERROR失败，原ACTIVE可读不变；WARN有效例 |
| V25-C01 | P3-02新映射契约冻结 | READY | 无 | D1–D14逐条Schema/样本/差异/验收映射；旧语义不暗改 |
| V25-C02 | 原因码单一所有者 | TODO | C01 | 主码/细码/旧码映射一致，历史不回填 |
| P3-02-a | 纯映射器与dry-run | TODO | C01/C02 | 字段/金额/枚举/时间/数组与碰撞正反例；覆盖分母准确 |
| P3-02-b | 规范化生产接线 | TODO | P3-02-a/S02/Q02 | raw全保真、accepted规范化、版本冻结、故障重放 |
| P3-02-c | 两源仓库接线 | TODO | P3-02-b | 相同外部ID不串源；不修改核心模板适配B |
| V25-I01 | LandingStorage/HDFS接口 | TODO | W03/W05/S02 | HDFS URI不走Files；UUID探针；断点在持久化后推进 |
| V25-I02 | Flume最小接入 | TODO | I01 | 完整文件投递、FileChannel恢复、checksum、未关闭文件不读 |
| V25-E01 | WSL黄金55条全链 | TODO（验收范围待G-01） | W04/I02/P3-02-b/Q02/G-01 | WSL HDFS/HMS/Spark黄金55条；不能用宿主local[2]+3307的1000条替代 |
| V25-P01 | 数据源管理页面 | TODO | C01；接口可与映射并行 | 源列表/状态/连接/启停/激活/dry-run，权限与错误明示 |
| V25-P02 | 按源指标与历史读取 | TODO | P3-02-c / G-03 | sourceId/code与snapshot一致；跨源拒读；归档显式入口 |
| V25-E02 | P5异构源闭环 | TODO | E01/P3-02-c/P02 | 完整异构成功、可选缺失可见、必需缺失拒绝；两源相同ID隔离 |
| V25-E03 | 普通员工E5 | TODO | E01/P01/P02 | 不填技术参数；失败有解释和旧结果标签；停商城仍查历史 |
| V25-D01 | DIM和交易校验补齐 | TODO | C02/Q01 | 日期/地区/指标维、订单状态、订单项/付款/退款三类独立校验 |
| V25-D02 | ADS指标补齐 | TODO | D01 | 分类地区/净销售/复购/RFM字段真实落表发布；稳定排序；RFM口径G-05，漏斗粒度G-04 |
| V25-D03 | 异常检测接线 | TODO | D02 | ≥14历史点、MAD/zscore边界、版本、快照、页面解释 |
| V25-D04 | 12项质量规则矩阵 | TODO | D01/D02/Q01 | §7.3每项算法输入与oracle、版本、失败保快照 |
| V25-A01 | 真实AI分析证据链 | TODO | P02/E02；真实凭据待提供 | 只读SQL AST/EXPLAIN/限额；真实调用与mock分开；同快照数值 |
| V25-A02 | 决策五模板 | TODO | D02/A01 | 五模板与四类效果样本、等长窗口、DRAFT/审批/审计 |
| V25-A03 | 受控AI建仓专项 | TODO | E02/P01/A01/C01 | 本文件§13.7二十条任务逐项交付；人工/P5先行，G-02决定论文必交时点 |
| V25-U01 | 权限凭据审计补齐 | TODO | S01/P01 | data_dev角色、禁默认口令、启动与重试分权、脱敏、跨源拒绝 |
| V25-R01 | 修复覆盖矩阵 | DONE_LIMITED | 无；随任务持续更新 | 较晚更正已限定交付；原269条仍由R02逐项归并 |
| V25-R02 | 重新冻结269条验收 | TODO | 当前产品任务与R01 | 逐条映射到本设计的验收队列；新增/替代/延期可追溯，不另建权威看板/README |
| V25-L01 | WSL1000条里程碑 | TODO（WSL数据栈前置未达） | E01/E02 | WSL固定1000条、恢复/资源记录；现有宿主1000条仅为可复用限定证据 |
| V25-X01 | 旧M3多节点/YARN/SSH | DEFERRED | 用户重启该里程碑 | 含55同输入两侧对照、1000链、权限日志；历史10/10不作全验收 |
| V25-X02 | Doris服务库 | DEFERRED | 当前底座完成 | 同指标定义/快照语义对账 |
| V25-X03 | ClickHouse/DataX/Kafka | DEFERRED | 对应扩展立项 | NOT_IMPLEMENTED不可展示可用 |
| V25-X04 | 论文与答辩正文 | DEFERRED | 项目完成+学校要求 | 只允许事实与测量记录 |
| D23-DOC01 | 两核心文档收敛与内置进度 | 本版交付 | 本轮用户要求 | 仅新增两文档，旧原件不修改，一致性与链接检查 |
| D23-SAFE01 | 应用首启隔离门禁 | TODO | S01/S02 | 所有DataSource在Flyway前验证真实目标；未授权正式实例禁止启动写迁移 |
| D23-FREEZE01 | 在途run来源/namespace与历史快照冻结核对 | TODO | C01/P02/G-03 | 修改源或prefix不能改变已创建run与历史快照归属；反例先于迁移 |

前39项沿用旧V25编号和原职责；新增D23条目明确区分，禁止把原V25-L01（WSL1000条）借用为文档任务。已有宿主1000条E3归§2限定成果，不替代V25-E01/L01的WSL出口。
### 19.3 旧缺口保留映射

原25组未做和20组部分项不再作为“当前状态”原样搬运，但有效责任全部留在这里：

| 原缺口组 | 当前承接 |
|---|---|
| Connector/连接器配置/接口 | I01/I02/C01；不重复新建已有HDFS类 |
| 采集正常/坏行/残行/轮转/追加/对账 | T02/I02/E01/L01，已修checkpoint不回退 |
| DIM三空表、订单状态/金额/退款 | D01/Q02 |
| ADS分类地区/净销售/复购/语义目录 | D02/A01；发布原子性由Q02和P02验收 |
| RFM金额、稳定排行、漏斗维度 | D02/G04/G05 |
| 异常算法孤岛/质量迟到率/规则版本 | D03/D04/Q01 |
| 未知作业参数/日志/状态/重启恢复 | W05/E01/L01；E03为员工页面验收 |
| AI反馈/真实调用/熔断预算/数值守卫 | A01/§13 |
| 五决策模板/正样本/截止日 | A02/E03 |
| data_dev账号/默认口令/路由/审计动作 | S03/U01/§17 |
| 缺少四类查询端点 | C01/P01/E03先查等价能力，不机械新增重复API |
| MySQL/Hive存储选择 | P02，Doris/ClickHouse保持后置 |
| 中型数据/性能基线 | E01/里程碑测试，性能专项不阻塞日常 |
| 旧元数据库同名指标副本 | K13：保留只读，不删不迁，读写权威analytics_metric |
| PipelineService过大/重复脱敏审计 | 单一owner逐任务提取职责；不能并发重写主服务 |
| 原论文证据薄弱项15组 | R01/R02，逐项标时点/版本/范围；不复活旧真假混合统计 |

### 19.4 每个Agent任务包必须完整

字段：任务ID、目标、指导书/设计版本、状态/owner、输入/接口版本、允许修改文件、明确禁止项、算法/数据结构、依赖、正负测试、运行环境/隔离范围、证据产物、风险/回退、下一反馈时间。不是让执行者根据一个标题猜实现。

交付模板：代码和设计变化→实际测试/退出码/数量→剩余边界→证据位置→下一步。若修改数据库/接口/数据流/模块/Spark/AI/端口/部署，必须交设计文档后继版；范围也变则同时交指导书后继版。

并行建议：①契约/纯映射，②前端ViewModel与页面，③独立生成器，④只读审计。共享schema由集成人先冻结，DDL编号和PipelineService/质量发布热点只一个owner。合并后再跑一次联合小链；重型作业单槽，不以开更多Agent换取共享目录冲突。

## 20. 资料收敛、原件保护和一致性核对

### 20.1 材料范围与读取方式

本轮对docs、references、experiments、contract-specs、warehouse、tests六个材料目录进行包含被忽略文件的目录级盘点，取样3838个文件（docs3268、references311、experiments13、contract-specs9、warehouse7、tests230），包括16个PDF路径。这是**文件数，不是已完成需求数或论文篇数**；运行期间其他Agent仍在新增资料，数字只对应本次取样。

关键资料按当前决定和实现逐项核对；旧大日志按裁决、勘误与相关需求定位阅读，原始输出按证据关联抽查。PDF通过既有论文阅读索引及项目资料位置纳入参考范围，本轮没有逐页重新精读全部PDF，也不据此声称完成文献综述。没有把第三方样例的功能当成本项目实现。

| 材料组 | 收敛位置/处理 |
|---|---|
| 设计讨论稿与V1.0/V2.1/V2.2 | 产品/模块/数仓/算法/报告/安全等吸收进本文件；旧内嵌生成器、FastAPI、固定集群等从现行正文移除 |
| 整改指导书V1.0、实施指导书V2.0–V2.5 | 目标/阶段入新指导书；具体规格入本文件；旧要求不得因改标题丢失 |
| V2.2/V2.5及旧看板、开发事实日志 | 最终有效决定入指导书，状态和任务入§2/19；不继续维护旧第三份看板 |
| 商城无关化P1–P5专项 | source身份/prefix/画像/映射/二源验收入§6–9/19 |
| AIW设计与实施专项 | §13完整保留七表、九节点、状态机、20细目和安全/评测 |
| contracts、contract-specs | 事件/指标/R8/namespace/生成器交换协议有效规则入本文件；旧文件不是更高裁决层 |
| deployment、compatibility、api-overview、warehouse | 环境/服务/接口和逻辑物理映射核对入§4/5/9/16；旧说明不能当新验收 |
| v2-completeness-audit、guideline-v24-coverage、R01后续卷 | 历史统计限定、25组缺口/20组部分项承接、待归并R02；不宣称原269逐项复验完成 |
| W01/W02/W03、T01/T02、S01/S03、E3/F88 | §2当前状态与限制；只接受对应时点的工件/日志事实 |
| thesis-draft、thesis-materials、presentation | 保持原样；有效研究问题/实验纪律入指导书，正文和PPT暂停 |
| references/papers与sample-projects | 理论/实现参考；不复制其成果为自己成果，不改变既有文件 |
| experiments与tests | 历史实验、golden与攻击集按输入/版本区分；旧链或Mock不能冒充当前结果 |
| 根README、脚本、临时记录、日志、运行目录 | 不改动；脚本风险列K项，不在本轮执行修复/清理 |

这次完成的是**设计与决策的收敛以及当前状态的有界汇总**，不是“所有代码通过验收”、不是“3838文件全部逐字审核”、也不是“269项已重新逐条判完”。后者明确保留为R02，不能以文档长度替代审计。

### 20.2 主要证据导航（仅参考，不是新权威文档）

- [旧设计V2.2](基于Spark大数据平台和智能分析模型的电商用户行为分析系统设计与实现——项目设计文稿%20V2.2.md)
- [旧实施指导书V2.5](../项目完整实施指导书%20V2.5.md)
- [旧V2.5看板](../项目实施进度与任务看板%20V2.5.md)
- [历史269项审计](../v2-completeness-audit.md)
- [R01裁决与勘误材料](../acceptance/v25-r01-coverage-20260914/DECISIONS.md)
- [S03独立复核L5](../acceptance/v25-s01-it-safety-20260914/L5-verification-report.md)
- [隔离E3报告](../acceptance/v25-e3-isolated-chain-20260914/README.md)
- [隔离E3原始run2摘要](../acceptance/v25-e3-isolated-chain-20260914/raw/22-chain2-summary.json)
- [WSL安装与备份证据](../acceptance/v25-w02-w03-wsl-runtime-20260914/README.md)
- [旧AIW细项参考](../superpowers/plans/2026-09-11-ai-assisted-warehouse-onboarding-implementation-v1.0.md)
- [原指标字典](../contracts/metric-dictionary.md)
- [R8原接口/安全契约](../contracts/r8-evidence-security-decision.md)
- [论文阅读索引](../../references/papers/论文阅读与使用索引.md)

引用旧材料时应连同本文对其限制一起读。例如E3报告开头11作业误写已经在§2纠正；旧R8的端点快照效果评价已被§14等长窗口要求取代；旧字典的ACHIEVED类状态已被现有12态替代。

### 20.3 原件保护

本轮只新建两份核心Markdown。未覆盖、删除、移动、重命名既有文档、代码、PDF、README或看板。既有V2.2/V2.5原稿原位保留作为完整前版本，符合不覆盖历史和修改前保留原文的要求。本次不建立第三份进度/发布/索引文档。

开始保护校验时记录的关键原件SHA256如下（文件名不改）：

| 原件 | SHA256 |
|---|---|
| docs/项目完整实施指导书 V2.5.md | 33A2C5B3234B1C724C6A9C9C0492B2480D43EFA392D2DB72C3F4902805E55F6F |
| docs/design/基于Spark大数据平台和智能分析模型的电商用户行为分析系统设计与实现——项目设计文稿 V2.2.md | 0A6D0BCC240D3F4D53D0AA977200D810DE0325AEEA30F2BA61ED04FF8CADF353 |
| docs/README.md | 06016FFF7964227C2BC9F01B8B6E01169E495532A76E688823F8C8251716BCB6 |

其它Agent并行改动造成的hash变化应归其任务，不得声称由本轮整理产生，也不能为“保持干净”而撤回用户改动。本轮输出范围只允许治理文档：新建《毕业设计指导书 V2.7》与《项目设计文档 V2.4》（以及其后由代码 Agent 按 V2.7 建立的 `docs/PROJECT_STATUS.md`）；既有文件一律原地不动。

### 20.4 文档一致性与治理边界结论

| 检查 | 结果 |
|---|---|
| 技术栈 | 与指导书§5一致；版本“安装/配置/待验证”分开 |
| 阶段范围 | 核心与二源首版；Doris第二阶段；ClickHouse延后；AIW独立出口 |
| 三程序与模块 | 六平台模块、独立商城/生成器；无直接商城内部类依赖 |
| 库名与数仓 | 四应用库；Hive按源prefix五层命名；MySQL镜像_m与Hive逻辑名不混同 |
| 端口 | 平台8091/商城8090/生成器8092/前端5173与5174/隔离3307一致 |
| 当前状态 | 动态状态不在本文件维护，见 `docs/PROJECT_STATUS.md`（本版剥离时点的最后已知口径：1000条隔离链限定通过；F88/新迁移/全量回归/多源仍未完整完成） |
| 已推翻方案 | 没有把FastAPI、商城内造数、固定node地址、随机存储路由、AI任意执行作为现行设计 |
| 论文范围 | 当前只存事实和测量；不写正文；AIW/漏斗毕业必交取舍待裁决 |
| 未决项 | 指导书G-01～G-07仍有效，任务依赖明确，不凭开发Agent猜测填空 |
| 旧资料保护 | 采用三文件治理；历史文件原地冻结（其中多份被验收脚本运行期读取），旧索引权威标注不更改，效力由新版开头明确取代 |

### 20.5 下次版本如何更新

任何较大代码轮结束，由总控复制本文件内容到唯一后继《项目设计文档 V2.5》，同步受影响的实现章节，最后写版本记录。不得只在末尾追加一句“本版已完成”而保留正文过期实现。§2 只维护稳定实现基线；每轮测试、临时阻塞、Bug、工作日志、Agent 待办与每次 commit 状态写入 `docs/PROJECT_STATUS.md`，不写回本文件。若范围变，另生成《毕业设计指导书 V2.8》，两文档头部写兼容对方版本。历史原件仍不修改，且禁止为整理目录而移动或改名。

## 版本变更记录

| 版本 | 上一版本 | 日期 | 变更 |
|---|---|---|---|
| V2.4 | V2.3 | 2026-09-14 | 治理调整：将“当前项目状态”职责从设计文档剥离，改为稳定「实现基线」（实现阶段、正式架构基线、正式问题 K 编号定义）；动态进度/测试/阻塞/Bug/工作记录迁至 `docs/PROJECT_STATUS.md`；确立三文件治理与写入权限（本文件与指导书仅总控可写，代码 Agent 只读）；登记 `K-xx`/`DEV-xxx` 编号命名空间与历史 K-10～K-13→DEV-001～DEV-004 勘误映射；明确历史文件原地冻结、禁止为整理目录移动或改名。不改变技术方案、端口、库名与既有验收事实。 |
| V2.3 | V2.2 | 2026-09-14 | 原项目设计稿整体重整为《项目设计文档》；与指导书V2.6配套。吸收三程序独立、多商城身份/映射、Flume、源级数仓前缀、11注册码与10作业链、版本化质量/快照发布、安全问数/五决策/AIW20任务；更新WSL开发与3307隔离状态；内置唯一进度与任务队列；保留证据局限和待裁决，不修改任何原件。 |
| V2.2（历史） | V2.1 | 原文件日期为准 | 原总体设计基线。本版保留其有效模块和算法，后续已推翻的内嵌/固定环境/旧权限与口径不继续作为当前方案。 |
