# 项目实施进度与任务看板

> 执行入口：本文件是 V2.1 §11 末句指定的**唯一任务执行入口**；任务状态、依赖与证据以本文件为准。
> 权威顺序（V2.1 §2.1）：① 已冻结契约 → ② `项目完整实施指导书 V2.1.md` → ③ 本看板 → ④ V2.0/整改指导书/历史文档。
> 事实与决策过程记在 `开发过程事实与决策记录.md`；完成度总账在 `v2-completeness-audit.md`（269 条冻结快照）。
> 基线：Git `34f37a8`（2026-09-11）· 冻结核查：132 完成 / 90 部分 / 42 未做（25 组）/ 5 不适用。
> 看板维护人：总控（当前会话）。**最长 30 分钟更新一次**；失败、阻塞或范围变化立即更新（V2.1 §2.3）。

---

## 1. 状态与证据口径

### 1.1 任务状态（V2.1 §2.2，只允许以下取值）

`TODO / READY / IN_PROGRESS / BLOCKED / REVIEW / DONE_LIMITED / DONE / REGRESSED / DEFERRED / N/A`

- 代码、表、接口、页面**单独存在不算完成**。
- 代码写完只能进 `REVIEW`；**通过目标测试并附证据**才可标 `DONE`。
- 部分达成（例如算法类存在但未接入生产链）只能标 `DONE_LIMITED`，并在证据列写清缺哪一半。

### 1.2 证据分级（E1–E5）

| 级别 | 含义 | 最小可接受证据 |
|---|---|---|
| E1 | 编译/静态检查 | `mvn -o -DskipTests compile` 或 `package` 的 SUCCESS 输出 |
| E2 | 模块自动化测试 | 模块测试用例全绿 + 用例名（单测/集成测试，内存或 20–100 条数据） |
| E3 | **本地真实链** | 真实 MySQL/Spark/Hive 上的 55 条黄金链：runId、快照 ID、指标值、对账输出 |
| E4 | 集群链 | 固定 seed 1,000 条集群链 + 本地/集群同值比较 |
| E5 | 普通员工可用页面路径 | 页面路径 + DOM/接口证据（不经 `runtimeProfileId`、不暴露 Hive 表名） |

> E1 为本看板补充定义（V2.1 §2.2 只显式给出 E2–E5）。**未达 E3 的数据链一律不得写"链路完成"。**

### 1.3 日常测试策略（V2.1 §9.2 + 本项目当前提速要求）

| 层级 | 内容 | 本阶段执行时机 |
|---|---|---|
| T0 | 受影响类单测 + 编译 | 每个小任务提交前 |
| T1 | 当前模块/反应堆测试（内存或 20–100 条数据） | 每个泳道交付前 |
| T2 | 55 条黄金链（真实本地 MySQL/Spark/Hive） | **一批并行任务合并后跑一次**（不逐任务跑） |
| T3 | 固定 seed 1,000 条集群链 | 连接器/集群里程碑一次；**当前 BLOCKED（缺 USER-INPUT）** |
| T4 | 10 万/100 万条性能 | 性能里程碑/最终验收；**不做日常回归** |

环境事实（影响执行方式，勿再踩）：本机 `mvn -o -pl <模块> test` 会因 surefire fork 的 classpath 绝对路径问题失败
（`SurefireBooterForkException`），**必须跑全反应堆** `mvn -o test -f analytics-server/pom.xml`。

---

## 2. 里程碑

| 里程碑 | 目标 | 出口判据 | 状态 |
|---|---|---|---|
| **M1** | 冻结三程序边界、契约与独立构建 | V2.1 §3.4 六条独立验收全过；三程序各自可构建、可启停、可替换 | `IN_PROGRESS` |
| M2 | source instance / connector / mapping 落地 | V2.1 §5.2 六张元数据表 + `SourceConnector` 接口 + `EventNormalizer` dry-run | `TODO` |
| M3 | 集群预检 + 55 条链 → 1,000 条链 | V2.1 §6.4 八步预检报告 + E3/E4 同值比较 | `BLOCKED`（缺 §6.5 USER-INPUT） |
| M4 | 剩余缺口收口 + 269 条复查 | 25 组缺口清零或显式 `DEFERRED`；重新生成冻结快照 | `TODO` |

泳道（V2.1 §9.1）：**A** 三程序拆分/连接器/集群 · **B** 数仓/ADS/质量/指标发布 · **C** 页面/AI/决策/安全 · **总控** 契约冻结、热点文件、集成与验收。

### 2.1 热点文件（同一时间只允许一个 Owner 修改，V2.1 §9.1）

| 热点 | 位置 | 冻结责任 |
|---|---|---|
| `PipelineService` | `analytics-server/warehouse-pipeline/.../pipeline/PipelineService.java` | 总控 |
| 迁移版本号 | `platform-app/src/main/resources/db/**` | 总控 |
| 统一事件契约 | `docs/contracts/**` + `contract-specs/**`（M1-5 新建） | 总控 |
| 平台主配置 | `platform-app/src/main/resources/application.yml` | 总控 |
| 前端 router | `web/src/router.js`、`mall-frontend/src/router.js` | 总控 |

---

## 3. M1 任务表（当前里程碑）

| ID | 任务 | 泳道 | 状态 | 允许修改范围 | 证据 |
|---|---|---|---|---|---|
| **M1-1** | 平台去除 `mall.*` 配置键：采集状态总览的 Landing 根改为**只**取自 ACTIVE `RuntimeProfile.landingUri` | A | `DONE` | `connection-ingestion/.../ingestion/IngestionService.java`、`.../runtime/RuntimeProfileService.java`、`RuntimeProfileServiceImpl.java` | **E1+E2+E3**：全反应堆 `mvn -o test -f analytics-server/pom.xml` → BUILD SUCCESS / 41 类 315 用例 0 失败；真实进程 `GET /api/v1/ingestion/status`（`profileState=ACTIVE`、`runtimeProfileId=1`、`eventsDir=D:\...\landing\events`、`pendingFiles=47` 与修复前同值）；`grep "mall\.[a-z]" analytics-server` → 0 命中 |
| **M1-2** | 平台边界守卫（源码级负向证据）：Java/配置不得出现商城包、商城库表、`mall.*` 键 | A | `DONE` | 新增 `platform-app/src/test/java/.../boundary/PlatformMallBoundarySourcePolicyTest.java` | **E2**：3 用例全绿（Java 扫描 0 违规 / 配置扫描 0 违规 / SQL 仅存在于未被加载的 `db/business/`） |
| **M1-3** | 删除死文件 `db/business/V1__init_mall.sql`（9637 字节，未被任何 Flyway location 加载） | A | `BLOCKED` | 同上文件 | 等待删除确认（破坏性操作需 scoped confirmation，见事实记录 D-004） |
| **M1-4** | 新建独立程序 `synthetic-data-generator`（8092 或 CLI）：`MallTargetAdapter` + `EventSink` 两接口、`JsonlEventSink`（`CANONICAL_EVENT_FILE`）、`ReferenceMallHttpAdapter`（`MALL_API`）、`generator_target` 等 5 张表、§4.4 六个 API、独立页面/CLI | A | `IN_PROGRESS`（S1+S2 完成、S3a 完成、S3b 进行中） | 新建目录 `synthetic-data-generator/**`、`contract-specs/**`（只读消费） | 待开工；**先建后删**。抽取测绘已完成（只读）：现有生成器 1421 行 = 纯逻辑 690 + 脏写入 130 + 深度耦合 529（`SimulationEngine` 480 行注入 3 Mapper/3 Service/5 `outbox` 类/4 `MallDtos`）；5 张表全仓 0 处 DDL；§4.4 六组端点仅 `GET /scenarios` 存在。**两处能力真空（需裁决，见 B-04）**：商城无行为事件端点（`emitBehavior` 不可实现）、无库存重置端点（同 seed 可复现依赖直连 Mapper，被 §4.3 禁止）。任务包见 §3.1。**S1 证据（2026-09-11 12:53，E1+E2+E3 局部）**：`synthetic-data-generator/` 单模块 Maven 工程落盘（`pom.xml` 无商城/平台构件依赖、`GeneratorApplication`、`application.yml` 端口 8092 + 自有库 `generator_meta` + 自有日志与产物目录、`db/generator/V1__generator_meta.sql` 五表）；`mvn -o test -f synthetic-data-generator/pom.xml` → **BUILD SUCCESS，4/4 绿**（`GeneratorBoundarySourcePolicyTest`：Java 源码 0 违规 / 配置 0 违规 / 迁移只建自有五表 / 构建不依赖商城与平台）；`mvn -o -DskipTests package` → 可执行 jar 25 984 556 B；真实 `java -jar` 启动 → Tomcat 8092 + `Started GeneratorApplication in 3.71 s`，Flyway 在真实 MySQL 建库建表（`generator_meta` 6 表 = 五契约表 + `flyway_schema_history`，InnoDB/utf8mb4）；`Stop-Process` 后 8092 释放、平台 8091 仍 HTTP 200（§3.4-5 局部证据）。**S2 证据（2026-09-11 13:03，E1+E2）**：`core` 12 类（mall 侧纯逻辑逐字节迁移，仅 `GenerationResult` 3 行改动 = `DirtyDataInjector.DirtySample` → 顶层 `core.DirtySample`）+ `contract` 9 类（事件模型 / 12 类载荷 / 格式与枚举校验 / `JsonlEventSink` + 产线制品清单）+ 5 个内核单测 + 2 个契约测试；`mvn -o test -f synthetic-data-generator/pom.xml` → **BUILD SUCCESS / 39 用例 0 失败 0 跳过**，其中 `GeneratorContractParityTest` 7 条把信封 8 字段、12 类枚举顺序、金额与时间正则、7 组枚举逐项与 `canonical-event.v1.schema.json` 对齐（含 7 个负例：`behavior_type="purchase"`、`channel="web"`、`age_group="45-54"`、`change_type="restock"`、3 位小数金额、无时区时间、`synthetic=false` 清单），`JsonlEventSinkTest` 5 条覆盖 55 行制品（逐行 8 字段 / 清单 `bytes` 与文件实际大小一致 / SHA-256 重算一致 / 轮转 25→3 文件 10+10+5 / 两次运行同校验和 / `landing` 路径守卫）。**本片修掉两处真实缺陷**：轮转后的制品不写清单（只在最终关账写）、`Artifact` 缺 `min/max event_time` 导致 §4.2 对账列无法落库。**S3a 证据（同日，E1+E2+局部 E3）**：`meta` 2 类（`RunStatus` 状态机 + `GeneratorMetaStore` 五表读写），`RunStatusTest` 4 条（合法链、`PENDING→CANCELLED` 受控扩展、非法迁移含 `SUCCESS→FAILED` 全拒、`fromDb` 严格解析），`GeneratorMetaStoreTest` **5 条在真实 MySQL `generator_meta` 上执行、0 跳过**（目标 CRUD 与 `config_version` 自增、计划版本 1/2 追加且旧版本仍可读、运行生命周期单向 + 终态保护 + `started_at` 由库侧写入、取消幂等第二次返回 false、制品 `INSERT IGNORE` 去重、毫秒时间往返一致、事件统计 upsert）。**S2/S3a 未做到（不得当完成）**：引擎实现与 §4.4 端点尚未落地（S3b）、脏数据注入器未迁移、引擎级汇总断言（GMV/复购/退款）当前无测试覆盖（待 S3b 补回）；详见 `docs/backups/m1-4-s2-20260911-1303/MANIFEST.md` §4 |
| **M1-5** | 中立契约目录 `contract-specs`（版本化 JSON Schema / OpenAPI / 清单 Schema） | A | `REVIEW` | 新建 `contract-specs/**`；`platform-common` 新增契约对账测试 | **E1+E2**：6 个文件落盘（`README.md` 20298 B、`VERSION`=`contract-specs 1.0.0`、`canonical-event.v1.schema.json` 32516 B、`ingestion-manifest.v1.schema.json` 9202 B、`generation-artifact-manifest.v1.schema.json` 6696 B、`openapi/generator-api.v1.yaml` 24863 B）；3 个 JSON 通过 draft 2020-12 `check_schema`，真实 `landing/manifests/30.json` 按清单 schema 校验 **VALID**，OpenAPI 7 路径 / 11 schema / 17 个本地 `$ref` 全部可解析；防漂移测试 `CanonicalEventSchemaParityTest` **5/5 绿**（信封 8 字段、12 类枚举、`schema_version.const`、`source_system.const`、金额正则、payload 路由全覆盖）。**未 DONE 的原因**：payload 级严格度与在跑校验器存在实测分歧（见 B-06），全部未冻结处已标 `x-contract-status: DRAFT` / `x-unspecified`，待裁决后升版 |
| **M1-6** | 平台自有异常类改名 `MallBizException` → `PlatformBizException`（89 处 / 14 文件，命名遗留） | A | `TODO` | `platform-common/.../common/`、各调用点 | 单独立项：涉及 14 文件属热点冲突面，不与 M1-1 混做 |
| **M1-7** | 商城侧移除 `generator` 包 / `GeneratorController` / `Generator.vue` / 已构建产物（V2.1 §3.4-2） | A | `TODO` | `mall-simulator/**`、`mall-frontend/**` | **依赖 M1-4 验收通过**；删除范围含 `mall-frontend/src/views/Generator.vue` 与 `static/assets/Generator-*.js` |
| **M1-8** | 三程序独立启停验收（V2.1 §3.4-1/5）：独立构建产物、配置、端口、日志、启动命令；停止任一不影响其余；无数据源时平台明示"采集不可用"且历史指标仍可读 | A | `TODO` | `scripts/**` | 依赖 M1-4/M1-7；产出一份 E5 级验收记录 |
| **M1-9** | 第二 `MallTargetAdapter` 或不同字段夹具，证明未写死 `mock-mall`（V2.1 §3.4-6） | A | `TODO` | 生成器测试 + 夹具目录 | 依赖 M1-4 |
| **M1-10** | Landing URI 解析收敛为唯一实现 `LandingUri`（`IngestionService` / `PipelineService` / `LocalLandingStorage` 三份重复实现合并） | A | `DONE` | `platform-common/.../common/LandingUri.java`（新增）、`platform-common/pom.xml`、`IngestionService`、`PipelineService`、`LocalLandingStorage` | **E1+E2+E3**：`LandingUriTest` 4 用例 + `IngestionServiceStatusTest` + 真实 `POST /api/v1/ingestion/runs`（产出 `manifestPath=file:///D:/...`，正是旧实现会崩的形态）；全反应堆 **41 类 / 315 用例 0 失败** |
| **M1-11** | `JobSubmitter` 缺进程存活检测：`LocalProcessSparkSubmitter.status()` **恒返回 `SUBMITTED`**（丢弃 `Process` 句柄），`cancel()` 无句柄可用 → ① 作业进程死亡不可被感知，阶段只能烧满 `job-timeout-ms`（默认 **15 分钟**）；② 取消/重试无法回收 Spark driver，可能留下僵尸进程争抢 Derby/warehouse 锁 | A | `TODO`（本次仅登记，不在 M1-1 批次内改） | 待定（若改：`connection-ingestion/.../runtime/submit/LocalProcessSparkSubmitter.java` + `JobSubmitter` 契约 + `SshSparkSubmitter`） | 证据（2026-09-11 12:30–12:41 真实观测）：run 31 `BUILD_DWD-tdw` 的 driver 卡在 Spark 本地依赖拉取，12:37 手工终止该 JVM 后 `pipeline-1` 线程仍在 `SparkStageExecutor.executeJob:182` 轮询，run 31 保持 `RUNNING` 直到 15 分钟超时；`jstack 32484` 可见其为 `TIMED_WAITING` 轮询而非 `waitFor` |

---

### 3.1 M1-4 任务包（V2.1 §10 格式，开工前登记）

```text
任务 ID / 标题：M1-4 新建独立程序 synthetic-data-generator（8092 + CLI）
目标与用户价值：把"造数据"从商城中彻底摘出，成为可独立构建/启停的第三个程序；平台与商城可各自替换或停用而不影响它（V2.1 §3.1）。
当前证据与缺陷：只读抽取测绘（2026-09-11 12:45 完成）——现有生成器 1421 行 = 纯逻辑 690（DistributionKit/BehaviorChain/HourlyTraffic/ScenarioCode/ExpectedEffect/GenerationFactors/ScenarioRegistry/ScenarioStrategy/GeneratorConfig/GeneratorDtos/GenerationResult）+ 脏数据写入 130（DirtyDataInjector，配置键耦合 mall.landing.path）+ 必须重写 529（SimulationEngine 480 注入 3 Mapper/3 Service/5 outbox 类/4 MallDtos；GeneratorRunService 72 同步无状态）；5 张表零 DDL；§4.4 六组端点仅 GET /scenarios 存在；两处能力真空见 B-04。
允许修改的模块和文件：新建 synthetic-data-generator/**；scripts/**（仅 M1-8 阶段加第三进程与 -GeneratorOnly）；contract-specs/** 只读。
禁止修改的模块和文件：mall-simulator/**、mall-frontend/**（M1-7 才删）；analytics-server/**（仅允许新增边界守卫测试）。
输入契约与数据结构：contract-specs/schemas/canonical-event.v1.schema.json、ingestion-manifest.v1.schema.json、generation-artifact-manifest.v1.schema.json、openapi/generator-api.v1.yaml、V2.1 §4.2 五表字段。
输出契约/API/页面：§4.4 六组端点 + 独立页面或 CLI + 五张表 + 产物 manifest（须标 synthetic=true）。
算法与边界条件：纯逻辑类原样搬迁；单一根 seed 派生子 seed；脏数据只在 CANONICAL_EVENT_FILE 生效并写入期望隔离数；MALL_API 不得绕过商城校验；文件模式产物写入独立目录，绝不写平台 landing/manifests。
依赖任务：M1-5（契约，IN_PROGRESS 中，先冻结再消费）。
完成定义：V2.1 §3.4-1/2/3/6 四项独立验收 + T0/T1 全绿 + T2 小链一次通过。
T0/T1 最小测试：模块独立构建；纯逻辑类原样搬迁后其 5 个既有单测改为不依赖商城上下文；JsonlEventSink 小样本（55 行量级）+ manifest 对账；状态机 PENDING→RUNNING→SUCCESS/FAILED/CANCELLED 单测；生成器侧边界守卫（禁 com.graduation.mall.*、禁 mall_simulator、禁 8091 调用）。
里程碑测试与证据路径：T2 小链（生成器产文件 → 人工放入 landing → 平台采集 → 流水线小链）；证据写《开发过程事实与决策记录》§4/§6。
切片顺序（每片独立可验证）：S1 骨架/配置/端口/日志/库与迁移 → S2 纯逻辑搬迁 + JsonlEventSink + 产物 manifest → S3 运行模型与 §4.4 API 骨架 + 状态机 → S4 ReferenceMallHttpAdapter（依赖 B-04 裁决）→ S5 页面或 CLI → S6 第二适配器/夹具（M1-9）。
切片进度：**S1 已完成**（2026-09-11 12:53）——单模块工程 + 端口 8092 + 自有库 `generator_meta`（五表 DDL 已真实迁移）+ 边界守卫 4/4 绿 + 真实启停实测；**S2 已完成**（2026-09-11 13:03）——`core` 12 类逐字节迁移 + `contract` 9 类契约发射层 + 7 个测试类，39 用例全绿；**S3a 已完成**（同日）——状态机 + 五表读写，真实 MySQL 集成测试 5 条 0 跳过；**S3b 主体已完成**（2026-09-11 13:17，证据见 §3.2）——`FileModeGenerationEngine` + §4.4 全部 9 个操作 + 运行服务 + 运行报告 + CLI（`plan-append`/`run-start`），52 用例全绿 + 真实 jar 跑出 1000 条契约事件并由外部工具独立核对；**S4/S5 页面/S6 待开工**（S4 依赖 B-04）。S2/S3a/S3b 不依赖 B-04/B-05/B-06 任何裁决。
下次反馈时间：每片结束即更新本表状态与证据，间隔不超过 30 分钟。
```

---

## 3.2 M1-4 S3b 证据（2026-09-11 13:17）

**E2（模块自动化，实测输出）**：`mvn -o test -f synthetic-data-generator/pom.xml` → `Tests run: 52, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。分布：boundary 4 / contract 7+5 / core 14 / engine 9 / meta 9 / web 4，0 跳过。其中 engine 9 条与 web 4 条为本切片新增（引擎纯逻辑 7 组：确定性、契约合法性、清单配对、引用完整性、金额一致性、事件预算、脏样本与取消；API 4 条：文件模式全链、未实现模式显式 501、场景目录、目标 CRUD 与真实探测）。

**E3（真实小规模链，非测试工具核对）**：`java -jar synthetic-data-generator-0.1.0-SNAPSHOT.jar`（26 129 801 B）走 CLI 两跳，`plan_id=cli-smoke version=1 mode=CANONICAL_EVENT_FILE scenario=normal seed=20260911 event_count=1000`：

- `run_id=cli-smoke-v1-20260911-131739-ddf7`，状态 `SUCCESS`，`success_count=1000`，`failed_count=0`（§2.2 状态机 + 五表真实落库）。
- `events-0001.jsonl`：`bytes=363848`、`record_count=1000`、`min_event_time=2026-09-01T00:07:41+08:00`、`max_event_time=2026-09-30T21:06:34+08:00`、`synthetic=true`。
- **外部独立核对**（PowerShell `Get-FileHash` + 逐行 JSON 解析，与程序内校验和互不相关）：文件 SHA-256 `5397907a6016936805ffab694e9799b25154251849da7ec933c18064bebb0257` **等于**清单 `checksum`；`bytes_actual=363848` 与清单一致；`lines=1000` 与 `record_count` 一致；1000 行**全部**含 8 个信封字段（`envelope_field_violations=0`）；`event_type` 出现 10 种：behavior 365 / stock_reserved 138 / order_created 138 / stock_released 83 / user_registered 83 / order_cancelled 83 / order_paid 54 / product_created 50 / refund_completed 3 / refund_created 3（`product_updated`、`stock_changed` 为 0 符合 `normal` 场景无改价无缺货的预期）。
- `dirty-samples.jsonl`：10 行（`light` = 1% × 1000），**主事件流仍为 1000 行**，证明脏样本未混入主流（D-018）；`run-report.json` 已生成。

**E3+ 跨程序边界（生成器 → 平台采集，2026-09-11 13:20）**：把生成器的契约严格制品 `events-0001.jsonl`（1000 行、SHA-256 `5397907a…0257`）原样放入 `landing/events/gen-s3b-1000-20260911.jsonl`，经 `POST /api/v1/ingestion/runs` 触发一轮真实采集 →

- **batch 31**：`recordCount=1000`、`quarantineCount=0`、`errorCount=0`、`fileCount=1`、`acceptedBytes=363848`，耗时 **0.1 秒**。
- 落盘 `landing/accepted/31/gen-s3b-1000-20260911.jsonl` 与源文件**逐字节相同**（363 848 B / 1000 行 / SHA-256 前 16 位 `5397907a60169368` 一致）；`landing/manifests/31.json` 记 `schemaVersions=["1.0"]`、`status=READY`、`batchId` 为**数字** 31（1–5 号的字符串分歧是历史遗留）。
- **对照价值**：同一平台对商城模拟器 legacy 路径的 55 行制品隔离 **4 行**，对生成器的契约严格输出隔离 **0 行**——这是"契约 → 实现"一致性的直接实测证据。
- **口径澄清（避免误读）**：`GET /api/v1/ingestion/status` 的 `pendingFiles=49 / pendingBytes=385.4 MB` 是 landing 目录**全量**统计，不是未消费积压；消费位置由 checkpoint 记录，实测一轮只新增这 1000 行。

**已知限制（不掩饰）**：① `web` 4 条走真实 MySQL `generator_meta`，不清理自己插入的 `it-*` 目标与计划行，重复运行会累积（曾因此撞到 `uk_target_name` 唯一键而报 500，已修为 409 并把改名的测试值改为每运行唯一）；② 引擎侧未覆盖 `rate_per_second`（文件模式刻意惰性）、除 `normal`/`refund_rise` 外的场景分布形状、`DONE_LIMITED` 清单 `min/max` 与真实极值比对、取消中断时的引用完整性；③ `GET /artifacts` 因冻结契约无 `kind` 字段而无法区分清单行，调用方需按 `uri` 后缀过滤后再累加 `record_count`（D-016）；④ §3.4 六项中的"独立构建/启动"为 M1-8 范围，本切片不算通过。

---

## 4. 剩余 25 组缺口 → 泳道/里程碑映射

来源：`v2-completeness-audit.md` §7.2（42 条未做归并为 25 组）。此处只做**分派**，明细见该文件。

| 组 | 缺口 | 泳道 | 归属 | 状态 |
|---|---|---|---|---|
| 1 | 连接器插件体系（`SourceConnector`/`source_connector` 表/`/connectors`） | A | M2 | `TODO` |
| 2 | 采集侧 6 类验收用例（正常/重复/坏行/半行/追加/轮转 + 对账） | A | M2 | `TODO` |
| 3 | DIM `dim_date`/`dim_region`/`dim_metric`（有表无数据） | B | M2 | `TODO` |
| 4 | 订单状态机非法跳转进 reject | B | M2 | `TODO` |
| 5 | ADS 缺口：`ads_sale_trend.net_sale_amount`、`ads_category_sale`、`ads_region_sale` | B | M2 | `TODO` |
| 6 | `repeat_rate` 落表并发布 | B | M2 | `TODO` |
| 7 | 异常规则权重/阈值版本化 | B | M2 | `TODO` |
| 8 | RFM 金额列可读（宽表 `monetary_amount` + 页面列） | B+C | M2 | `TODO` |
| 9 | 异常检测算法接生产链（≥14 历史点） | B | M2 | `TODO` |
| 10 | 作业未知参数校验报错 | B | M2 | `TODO` |
| 11 | 质量含迟到率 `ingest_time - event_time` P95 | B | M2 | `TODO` |
| 12 | AI 反馈可写可读 + `ai_call_log` 留痕 | C | M4 | `TODO` |
| 13 | LLM 治理（超时/有限重试/熔断/每日预算/`traceId` 防重扣）+ 真实 LLM 端到端 | C | M4 | `TODO` |
| 14 | 决策 EFFECTIVE 正样本（四类可达） | C | M4 | `TODO` |
| 15 | 五类决策模板（流量下降/漏斗流失/商品转化/库存风险/用户召回） | C | M4 | `TODO` |
| 16 | `data_dev` 测试角色账号 | C | M4 | `TODO` |
| 17 | 删除可用默认口令（凭据走 `credential_ref`） | C | M4 | `TODO` |
| 18 | 审计覆盖环境激活/流水线启动重试/质量强制/快照激活/归档快照读取 | C | M4 | `TODO` |
| 19 | 接口缺口：`/spark-jobs/{id}/logs`、`/ingestion/runs/{id}`、`/metrics/snapshots/active`、`/ai/queries/{id}` | C | M2 | `TODO` |
| 20 | `quality_rule_definition` + 版本/生效区间 | B | M2 | `TODO` |
| 21 | 12 条质量规则逐条实现 + BLOCKING 负向验收（V2.1 §7.3） | B | M2/M4 | `TODO` |
| 22 | 交易层 3 公式（行金额、退款取最新、净销售） | B | M2 | `TODO` |
| 23 | `MetricStoreFactory` 按 `metric_store_type` 选择（MySQL 默认；Hive 显式降级；Doris 二期；ClickHouse `DEFERRED`） | B | M2 | `TODO` |
| 24 | 中型数据集（1,000 条固定 seed；10 万/100 万仅里程碑） | 总控 | M3/M4 | `TODO` |
| 25 | 路由级角色拦截（前端路由 + 后端 API 双层） | C | M4 | `TODO` |

> V2.1 §8/§7 新增要求（漏斗渠道与分类粒度、`min` 截断禁止、稳定次序键、库存覆盖天数、总览环比、
> 表格分页稳定排序、五处同值含 AI、语义目录补活跃/RFM/质量并版本化）并入上表对应任务，不单列。

---

## 5. 阻塞项

| ID | 阻塞内容 | 类型 | 状态 |
|---|---|---|---|
| B-01 | M1-3 删除 `db/business/V1__init_mall.sql` | 破坏性操作需确认 | `BLOCKED`（等待确认） |
| B-02 | 集群参数：node01–03 主机名/IP 与角色、HDFS URI 与可写根、warehouse 路径、Spark 提交模式与队列、Hive JDBC/Metastore、SSH 用户与凭据、Kerberos、Flume agent 位置与 spool 传输方式、开放端口 | `USER-INPUT` | `BLOCKED`（不得猜测，V2.1 §6.5） |
| B-03 | 真实 LLM 接入所需的 `LLM_BASE_URL`/`LLM_API_KEY`/模型名与预算额度 | `USER-INPUT` | `BLOCKED` |
| B-04 | M1-4 的 `MALL_API` 模式能力真空：① 商城无任何行为事件端点 → `emitBehavior` 无法用公开 API 实现（现状靠 `outboxService.append` 直写 outbox）；② 无库存重置端点 → 同 seed 可复现依赖 `SimulationEngine:110–122` 直连 Mapper，而 V2.1 §4.3 明令 `MALL_API` 只能经商城接口改库存 | `USER-INPUT`（规格未定，不得自行发明） | `BLOCKED`（待裁决：给商城加公开端点 / 生成器如实声明能力位并把可复现性断言限定在文件模式 / 混合先降级后补） |
| B-05 | M1-4 未定项：生成器独立库与账号（新建 `synthetic_generator` vs 复用已建未用的 `mall_business`）、生成器 API 鉴权模型（无鉴权 / 复用商城账号 / 自建 token）。V2.1 只规定"独立构建产物、配置、端口、日志、启动命令"，未规定库名与鉴权 | `USER-INPUT` | `BLOCKED`（不阻断切片 S1–S3：引擎搬迁、`JsonlEventSink`、运行状态机与 §4.4 API 骨架与适配器无关） |
| B-06 | M1-5 契约冻结的两个必须裁决项：① **必填字段执行强度**——按 `EventContractValidator` 复刻回放夹具 55 行得「接受 51 / 隔离 4」，与批次 30 清单完全一致；而新契约 schema 的 payload 必填集校验同文件得「26 通过 / 29 不通过」，即采集层现今接受的 51 行里约 25 行按契约属脏数据（缺 `status`/`created_at`/`paid_at`/`cancelled_at`/`completed_at`、`items` 写成转义 JSON 字符串、`channel="web"`、`change_type="restock"`）。补齐校验器会让黄金数据大批转隔离、**改动既有指标口径**，故不可由 AI 自行决定；② **`synthetic=true` 的载体**（制品清单 / 事件信封 / 运行行）规格未指明 | `USER-INPUT`（规格未定 + 影响黄金口径） | `BLOCKED`（M1-5 停在 `REVIEW`；契约文件已用 `DRAFT` 标注，未伪装成已冻结） |

---

## 6. 变更日志

| 时间 | 变更 |
|---|---|
| 2026-09-11 12:xx | 看板建立（基线 `34f37a8`）；登记 M1-1…M1-9、25 组缺口分派、B-01…B-03 阻塞项。M1-1/M1-2 完成（E1+E2，证据见任务表）。 |
| 2026-09-11 12:30 | 合并批次 1（M1-1 + M1-2 + M1-10）：全反应堆 **41 类 / 315 用例 0 失败**（platform-common 首次有测试类 4 用例）。M1-10（Landing URI 三份重复实现 → `LandingUri` 唯一实现）登记并 `DONE`，反熵台账 AE-05、决策 D-008、事实 D-009（启动脚本调用方式）记入《开发过程事实与决策记录》。真实证据：采集 batchId **29**（869k 全量回填，`manifestPath=file:///D:/...`）+ batchId **30**（55 行夹具 51/4）+ 流水线 run **31**（E3 小链）。 |
| 2026-09-11 12:35 | 提速口径落定：T2 只跑**小规模黄金链**（55 行夹具 → 采集 → 流水线 → 快照），不跑 869k 全量链；1,000,000 行只在最终性能里程碑（T4）执行（用户明确要求）。 |
| 2026-09-11 12:42 | 批次 1 已备份（`docs/backups/m1-10-20260911-1237/`，含 MANIFEST 与回滚命令）并提交 `8dc7010`。M1-5 登记为 `IN_PROGRESS`（仅新建 `contract-specs/**` + `platform-common` 契约对账测试，不触碰其他程序源码）。M1-4 的抽取依赖测绘已并行派出（只读分析，禁止改文件）。 |
| 2026-09-11 12:41 | T2 实测（run 31）暴露**环境级缺陷 M1-11**：`LocalProcessSparkSubmitter.status()` 恒 `SUBMITTED`，Spark driver 死亡不被检测，阶段烧满 15 分钟超时；同时观测到 Spark 本地模式依赖拉取（Netty `fetchFile`）偶发卡死（`jstack` 证据入《开发过程事实与决策记录》§6）。M1-11 登记为 `TODO`，不改 M1-1 批次范围。 |
| 2026-09-11 12:47 | run 31 终态**核实**（此前为超时预测）：`FAILED`@12:46:12，950 秒，`RUN_JOB_FAILED` / `BUILD_DWD 作业失败: tdw: 未找到 JobResult 日志`，阶段 `BUILD_DWD` `FAILED` 925 秒；同时确证 `WAIT_LANDING`(51) / `INIT_SCHEMA`(37) / `LOAD_ODS`(51，21 分区，`dt=20260901`，`odsQuarantinedRecords=4`) 全绿——M1-10 的落地根解析在真实链路中已被执行。事实记录 S-02 已按实测改写。 |
| 2026-09-11 12:55 | **M1-5 交付并停在 `REVIEW`**：`contract-specs/` 6 文件（版本 `contract-specs 1.0.0`）；3 个 JSON 过 draft 2020-12 `check_schema`，真实 `landing/manifests/30.json` 校验 VALID，OpenAPI 7 路径 17 个 `$ref` 全部可解析；`CanonicalEventSchemaParityTest` **5/5 绿**（`mvn -o test -f analytics-server/pom.xml -Dtest=CanonicalEventSchemaParityTest`）。**同时发现契约↔实现真实分歧**并登记 B-06：校验器复刻回放得 51/4 与批次 30 一致，而按契约 schema 同文件 26/55 通过 → 约 25 行"被采集层接受但按契约属脏数据"，属会改动黄金口径的裁决项，不由 AI 自定。M1-4 抽取测绘回报并写入 §3.1 任务包（含 B-04 两处能力真空、B-05 两项未定）。 |
| 2026-09-11 13:02 | 批次 2 已提交 `15c488b`（M1-5），备份 `docs/backups/m1-5-20260911-1300/`（含 MANIFEST 与回滚命令）。批次级测试记录：全反应堆 `mvn -o test -f analytics-server/pom.xml` → **BUILD SUCCESS，55.7 秒，43 份 surefire 报告 / 321 用例 / 0 失败**（上一批次 315 用例；新增 5 个契约对应用例 + 1 个测试类）。 |
| 2026-09-11 13:03 | **M1-4 S2 完成 + S3a 完成**：`synthetic-data-generator` 新增 4 个主代码包（`core` 12 / `contract` 9 / `engine` 4 / `meta` 2 类）+ 9 个测试类；`mvn -o test -f synthetic-data-generator/pom.xml` → **BUILD SUCCESS / 39 用例 0 失败 0 跳过**（S1 时 4 用例）。修掉两处真实缺陷：轮转制品不写清单、`Artifact` 缺 min/max event_time。契约一致性测试把 8 个信封字段、12 类事件顺序、金额/时间正则、7 组枚举与冻结 schema 逐项对齐并含 7 个负例。备份 `docs/backups/m1-4-s2-20260911-1303/`（37 文件 + `FILES.txt` 逐文件 SHA-256 + MANIFEST 含 8 条"未做到"清单）。决策 D-013（`Manifest` → `ArtifactManifest` 命名）、D-014（`PENDING→CANCELLED` 受控扩展）记入《开发过程事实与决策记录》。S3b（引擎 + §4.4 端点）已开工。 |
| 2026-09-11 12:53 | **M1-4 开工，S1 完成**（第三个独立程序骨架落地）：`synthetic-data-generator/` 单模块 Maven 工程（`com.graduation:synthetic-data-generator:0.1.0-SNAPSHOT`，parent `spring-boot-starter-parent:3.2.5`，包 `com.graduation.generator`）；`application.yml` 端口 **8092** + 自有库 `generator_meta`（库名为配置项，B-05 裁决只改一行）+ 自有日志 `./logs/generator.log` 与产物 `./generator-output`；`db/generator/V1__generator_meta.sql` 建 §4.2 五表。**实测**：`mvn -o test -f synthetic-data-generator/pom.xml` → BUILD SUCCESS / 2.9 秒 / **4 用例 0 失败**；`package` → 可执行 jar 25 984 556 B；`java -jar` 真实启动 → Flyway 在真实 MySQL 建库并迁移（`Current version: << Empty Schema >>` → `Successfully applied 1 migration`），Tomcat 8092，`Started GeneratorApplication in 3.71 s`；`information_schema` 核实 6 表（`generator_target` 12 列 / `generation_plan` 14 / `generation_run` 16 / `generation_artifact` 11 / `generation_event_stat` 5 + `flyway_schema_history` 10，全部 InnoDB+utf8mb4）；停止生成器后 8092 释放而 **8091 平台仍 HTTP 200**（§3.4-5 局部）。看板 §3.1 已补切片进度；本片不依赖 B-04/B-05/B-06 任何裁决。 |
